---
phase: 21-portfolio-websearch-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/meeting/schemas.ts
  - src/meeting/schemas.test.ts
  - src/scripts/validate-portfolio-research.ts
  - src/scripts/generate-report.test.ts
autonomous: true
requirements: [PORT-02]

must_haves:
  truths:
    - "webSearchResultSchema accepts both canonical and commonly-invented alias field names and transforms them to the canonical WebSearchResult shape (D-12)"
    - "The Step 3-P fallback JSON shape passes schema validation without throwing"
    - "validate-portfolio-research.ts validates every tmp/portfolio-research/{symbol}.json against the schema and exits non-zero if any file fails (D-12 verification target)"
    - "Daily Report loaders (loadWebSearchResults / loadReevalResults) are structurally isolated from tmp/portfolio-research/ — their output is unaffected by files present there (Pitfall 1, PORT-02 separation)"
  artifacts:
    - path: "src/meeting/schemas.ts"
      provides: "Hardened webSearchResultSchema (passthrough().transform() alias hardening)"
      contains: "rawWebSearchResultSchema"
    - path: "src/meeting/schemas.test.ts"
      provides: "Unit tests for alias-transform, default-fill, passthrough, fallback JSON"
      contains: "webSearchResultSchema"
    - path: "src/scripts/validate-portfolio-research.ts"
      provides: "Standalone verification script for the 12 saved portfolio-research files"
      contains: "portfolio-research"
    - path: "src/scripts/generate-report.test.ts"
      provides: "Directory-isolation test proving Daily Report loaders never read portfolio-research"
      contains: "portfolio-research"
  key_links:
    - from: "src/meeting/schemas.ts"
      to: "src/meeting/types.ts WebSearchResult"
      via: "transform output shape"
      pattern: "researchSummary.*positiveFindings.*negativeFindings.*keyArticles.*researchedAt"
    - from: "src/scripts/validate-portfolio-research.ts"
      to: "webSearchResultSchema"
      via: "parse each file"
      pattern: "webSearchResultSchema|validateWebSearchResult"
---

<objective>
本フェーズの TS/スキーマ層を整備する。エージェント生成 JSON を信頼する前の検証境界（zod alias-transform 硬化）と、保存された12ファイルのスキーマ適合確認スクリプト、および Daily Report ローダーの構造的隔離を証明するテストを作る。

Purpose: PORT-02 の「分離保存」を検証で保証し、Pitfall 8（フィールド名発明インシデント）の再発を12倍のプロンプト表面積に対して構造的に防ぐ。invest.md（Plan 02）が書き出す JSON のファイル契約を、この層が定義・検証する。
Output: 硬化された webSearchResultSchema + ユニットテスト、validate-portfolio-research.ts、generate-report の隔離テスト。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-portfolio-websearch-research/21-CONTEXT.md
@.planning/phases/21-portfolio-websearch-research/21-PATTERNS.md

<interfaces>
<!-- 実装対象と参照契約。executor はコードベース探索不要でこれらを直接使うこと。 -->

現行の strict スキーマ（硬化対象、src/meeting/schemas.ts:112-124）:
```typescript
export const webSearchResultSchema = z.object({
  ticker: z.string(),
  researchSummary: z.string(),
  positiveFindings: z.array(z.string()),
  negativeFindings: z.array(z.string()),
  keyArticles: z.array(z.object({ title: z.string(), summary: z.string() })),
  researchedAt: z.string(),
});
export function validateWebSearchResult(data: unknown): WebSearchResult {
  return webSearchResultSchema.parse(data) as WebSearchResult;
}
```

alias-transform 前例（src/meeting/schemas.ts:150-189、holdingEvaluationSchema / portfolioAnalysisSchema）:
```typescript
const rawHoldingSchema = z.object({
  symbol: z.string(),
  decision: decisionEnum.optional(),
  action: decisionEnum.optional(),   // alias for "decision"
  rationale: z.string().optional(),
  reason: z.string().optional(),     // alias for "rationale"
}).passthrough();
export const holdingEvaluationSchema = rawHoldingSchema.transform((raw) => ({
  symbol: raw.symbol,
  decision: raw.decision ?? raw.action ?? "保持",
  rationale: raw.rationale ?? raw.reason ?? "",
}));
```

正準ターゲット型（src/meeting/types.ts:86-96）: WebSearchResult = { ticker, researchSummary, positiveFindings[], negativeFindings[], keyArticles[{title,summary}], researchedAt }

Daily Report ローダー（src/scripts/generate-report.ts:24-66、触らない・隔離対象）:
- loadWebSearchResults() は join(TMP_DIR, "websearch") をハードコード
- loadReevalResults() は join(TMP_DIR, "reeval") をハードコード
- どちらも tmp/portfolio-research/ を一切参照しない

検証スクリプト前例（src/scripts/validate-meeting.ts、全23行、mirror 対象）:
- readFile → JSON.parse → validate → console.log → catch で process.exit(1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webSearchResultSchema を alias-transform で硬化しユニットテストを追加</name>
  <files>src/meeting/schemas.ts, src/meeting/schemas.test.ts</files>
  <read_first>
    - src/meeting/schemas.ts（現行 webSearchResultSchema 112-124 と前例 holdingEvaluationSchema/portfolioAnalysisSchema 150-189 の実装形）
    - src/meeting/schemas.test.ts（既存 describe("validateRawNewsCuration") 32-69 のテスト三点構成: 正常系/欠落耐性/未知フィールド許容）
    - src/meeting/types.ts（WebSearchResult 正準型 86-96）
    - .planning/phases/21-portfolio-websearch-research/21-PATTERNS.md（Pattern 4 と推奨 alias 一覧）
  </read_first>
  <behavior>
    - 正常系: 正準フィールドのみの入力が throw せず通過し、6フィールドが正準形で返る
    - エイリアス受理: summary → researchSummary、findings/positives → positiveFindings、negatives/concerns → negativeFindings、articles → keyArticles、timestamp/date → researchedAt に解決される
    - 欠落耐性: ticker 以外の必須フィールド省略時、文字列は ""、配列は [] にデフォルト補完される
    - 未知フィールド許容: 追加の未知トップレベルフィールドを含んでも throw しない（passthrough）
    - フォールバックJSON: {ticker:"EE", researchSummary:"リサーチ失敗", positiveFindings:[], negativeFindings:[], keyArticles:[], researchedAt:"..."} が throw せず通過する
    - 後方互換: 既存の候補銘柄ローダー（loadWebSearchResults）が使う validateWebSearchResult のシグネチャと戻り型（WebSearchResult）は不変
  </behavior>
  <action>
    src/meeting/schemas.ts:112-124 の strict な webSearchResultSchema を、holdingEvaluationSchema/portfolioAnalysisSchema（150-189）と同一の `passthrough().transform()` 2層構成へリファクタする（D-12）。

    (1) `rawWebSearchResultSchema` を新設: `ticker: z.string()` のみ必須。それ以外は全て `.optional()` とし、正準フィールドと以下のエイリアスを併記した上で `.passthrough()` を付ける。エイリアス: `summary`(→researchSummary)、`findings`/`positives`(→positiveFindings)、`negatives`/`concerns`(→negativeFindings)、`articles`(→keyArticles、要素は {title,summary})、`timestamp`/`date`(→researchedAt)。
    (2) `export const webSearchResultSchema = rawWebSearchResultSchema.transform((raw) => ({ ... }))` として正準形を組み立てる。null 合体で解決: researchSummary = raw.researchSummary ?? raw.summary ?? ""、positiveFindings = raw.positiveFindings ?? raw.positives ?? raw.findings ?? []、negativeFindings = raw.negativeFindings ?? raw.negatives ?? raw.concerns ?? []、keyArticles = raw.keyArticles ?? raw.articles ?? []、researchedAt = raw.researchedAt ?? raw.timestamp ?? raw.date ?? ""、ticker = raw.ticker。
    (3) `validateWebSearchResult`(140-142) は変更不要（`webSearchResultSchema.parse(data) as WebSearchResult` のまま）。

    src/meeting/schemas.test.ts: import 行に `webSearchResultSchema, validateWebSearchResult` を追加し、`describe("webSearchResultSchema")` ブロックを新設。<behavior> の6ケースを it() として実装（正常系・各エイリアスペア受理・欠落デフォルト補完・未知フィールド passthrough・フォールバックJSON通過）。既存 validateRawNewsCuration テストの三点構成を踏襲する。

    注意: この変更は既存の候補銘柄 tmp/websearch/{ticker}.json と本フェーズの tmp/portfolio-research/{symbol}.json の両方の同一 WebSearchResult 形状を保護する共有バックポート。フェンスコードは書かず、上記識別子で実装すること。
  </action>
  <verify>
    <automated>npx vitest run src/meeting/schemas.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/meeting/schemas.test.ts -t "webSearchResult"` が全 green
    - `grep -c 'rawWebSearchResultSchema' src/meeting/schemas.ts` が 1 以上
    - `grep -c 'passthrough' src/meeting/schemas.ts` が硬化前より 1 以上増加（webSearchResultSchema にも付与された）
    - `npm test` フルスイート（15+テストファイル）が全 green（後方互換: 既存 generate-report テストが壊れない）
    - フォールバックJSON検証テスト（researchSummary:"リサーチ失敗"）が throw せず通過することを assert している
  </acceptance_criteria>
  <done>正準入力・全エイリアス・欠落・未知フィールド・フォールバックJSON の各テストが green、フルスイート green</done>
</task>

<task type="auto">
  <name>Task 2: validate-portfolio-research.ts 新設 と generate-report 隔離テスト追加</name>
  <files>src/scripts/validate-portfolio-research.ts, src/scripts/generate-report.test.ts</files>
  <read_first>
    - src/scripts/validate-meeting.ts（全23行、mirror 対象の検証スクリプト構造）
    - src/scripts/generate-report.ts（loadWebSearchResults / loadReevalResults 24-66 の readdir+per-file try/catch イディオムと websearch/reeval ハードコードパス）
    - src/scripts/generate-report.test.ts（先頭の vi.mock("node:fs/promises") ハーネス 1-9 と既存テスト構造）
    - .planning/phases/21-portfolio-websearch-research/21-PATTERNS.md（validate-portfolio-research.ts の adapt 指針と隔離境界 DO-NOT-TOUCH リスト）
  </read_first>
  <action>
    (1) src/scripts/validate-portfolio-research.ts を新規作成し、validate-meeting.ts の構造を mirror する。import に `readdir, readFile`（node:fs/promises）と `join`（node:path）、`webSearchResultSchema` または `validateWebSearchResult`（../meeting/schemas.js）を用いる。`TMP_DIR = join(import.meta.dirname, "../../tmp")`。`validate()` 関数で `readdir(join(TMP_DIR, "portfolio-research"))` → `.json` で filter → 各ファイルを readFile+JSON.parse → `webSearchResultSchema.parse()`（または validateWebSearchResult）。symbol ごとに pass/fail を console.log。1件でも失敗したら process.exit(1)。末尾は validate-meeting.ts と同じ `validate().catch((error) => { console.error("Validation failed:", error); process.exit(1); });`。

    (2) src/scripts/generate-report.test.ts に構造的隔離テスト（Pitfall 1 / PORT-02 分離保証）を追加する。node:fs/promises の readdir モックを使い、loadWebSearchResults / loadReevalResults を実行した際、readdir が `websearch` / `reeval` を含むパスでのみ呼ばれ、`portfolio-research` を含むパスでは決して呼ばれないことを assert する（例: `expect(readdir).toHaveBeenCalledWith(expect.stringContaining("websearch"))` かつ `expect(readdir).not.toHaveBeenCalledWith(expect.stringContaining("portfolio-research"))`）。generate-report.ts 本体には一切変更を加えないこと（隔離は構造的に成立、Pitfall 1）。

    注意: loadWebSearchResults / loadReevalResults 自体と generate-report.ts のプロダクションコードは絶対に触らない。tmp/portfolio-research/ を Daily Report ローダーに配線しないこと（Phase 22 の領域）。フェンスコードは action に書かない。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/generate-report.test.ts` が全 green（隔離テスト含む）
    - 隔離テストが `not.toHaveBeenCalledWith(expect.stringContaining("portfolio-research"))` を assert している（grep で確認可能）
    - `grep -c 'portfolio-research' src/scripts/validate-portfolio-research.ts` が 1 以上
    - `git diff --stat src/scripts/generate-report.ts` が空（プロダクションローダー無変更、隔離は構造的に成立）
    - validate-portfolio-research.ts が webSearchResultSchema または validateWebSearchResult を import している
  </acceptance_criteria>
  <done>検証スクリプトが存在し webSearchResultSchema で12ファイルを検証、隔離テスト green、generate-report.ts 本体無変更</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent-authored JSON → TS trust | エージェントが生成した tmp/portfolio-research/{symbol}.json は zod で検証されるまで信頼しない |
| portfolio-research dir → Daily Report loaders | 本フェーズが書くディレクトリは Daily Report ローダーの信頼境界の外側（構造的隔離） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | webSearchResultSchema (agent-authored JSON) | mitigate | zod `.parse()` が構造違反（型・欠落必須）で throw。alias-transform（D-12）が「フィールド名を発明した silently-wrong shape」を正準形に吸収し、データ欠落を防ぐ。ユニットテストで6ケース検証（Task 1） |
| T-21-06 | Tampering | tmp/portfolio-research → Daily Report | mitigate | generate-report.ts に0行追加（構造的隔離）。隔離テストで readdir が portfolio-research を呼ばないことを assert（Task 2、Pitfall 1） |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | 本フェーズは新規パッケージインストールなし（zod ^4.3.6 / vitest ^4.0.18 は既存依存、package.json 確認済み）。RESEARCH.md「no new dependencies」。供給網リスク増分なし |
</threat_model>

<verification>
- `npm test` フルスイート green（本フェーズが変更する2ファイル + 後方互換の全ファイル）
- `npx vitest run src/meeting/schemas.test.ts src/scripts/generate-report.test.ts` が本フェーズの直接対象を green
- `git diff --stat src/scripts/generate-report.ts` が空（隔離の構造的保証）
</verification>

<success_criteria>
- webSearchResultSchema が passthrough().transform() で硬化され、正準+エイリアス+欠落+未知フィールド+フォールバックJSON の全ケースがテストで green（D-12）
- validate-portfolio-research.ts が tmp/portfolio-research/ の全ファイルをスキーマ検証し、1件でも失敗で非0終了（D-12 検証ツール）
- generate-report.ts 本体は無変更で、隔離テストが Daily Report ローダーの portfolio-research 非参照を証明（PORT-02 分離保証 / Pitfall 1）
</success_criteria>

<output>
Create `.planning/phases/21-portfolio-websearch-research/21-01-SUMMARY.md` when done
</output>
