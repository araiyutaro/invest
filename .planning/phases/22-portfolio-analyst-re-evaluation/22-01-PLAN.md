---
phase: 22-portfolio-analyst-re-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/meeting/types.ts
  - src/meeting/schemas.ts
  - src/meeting/schemas.test.ts
autonomous: true
requirements: [PORT-04, PORT-05]
must_haves:
  truths:
    - "urgent フィールドが4表記（urgent/urgency/isUrgent/urgentFlag）から boolean へ正規化され、欠落時 false になる（D-10）"
    - "LLM が decisionChanged/previousDecision を出力してもスキーマ transform で strip される（D-11）"
    - "HoldingEvaluation 型が urgent（必須）・previousDecision?・decisionChanged? を持つ（D-08）"
  artifacts:
    - path: "src/meeting/types.ts"
      provides: "HoldingEvaluation に urgent/previousDecision/decisionChanged を追加"
      contains: "readonly urgent: boolean"
    - path: "src/meeting/schemas.ts"
      provides: "rawHoldingSchema の urgent alias 吸収 + default false、explicit object literal で strip 維持"
      contains: "urgent"
    - path: "src/meeting/schemas.test.ts"
      provides: "holdingEvaluationSchema の alias/default/strip テスト"
      contains: "holdingEvaluationSchema"
  key_links:
    - from: "src/meeting/schemas.ts"
      to: "src/meeting/types.ts"
      via: "holdingEvaluationSchema output shape が HoldingEvaluation を満たす"
      pattern: "urgent"
---

<objective>
LLM 出力契約の型・スキーマ層を拡張する。HoldingEvaluation に `urgent`（LLM出力・省略時 false）と `previousDecision?` / `decisionChanged?`（TS付与・LLMからは受け取らない）を追加し、`holdingEvaluationSchema` の alias-transform を硬化して `urgent` の表記揺れ4種を吸収し、LLM が誤って出力した `decisionChanged` / `previousDecision` を explicit-object-literal 方式で strip する。

Purpose: PORT-04（urgent フラグ付与）と PORT-05（LLM自己申告を信用しない — strip）の機械可読な土台を、既存の rawHoldingSchema alias-transform 規約の自己拡張として提供する。後続の decision-diff（22-03）・レンダラー配線（22-04）はこの型を前提にする。
Output: 拡張された types.ts / schemas.ts と holdingEvaluationSchema の新規テストブロック。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-CONTEXT.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-VALIDATION.md

<interfaces>
現状の契約（22-PATTERNS.md / 直接 read で確認済み、executor はこれをそのまま拡張する）:

src/meeting/types.ts (108-124行) — HoldingEvaluation:
  readonly symbol / nameJa / decision("保持"|"買増"|"一部売却"|"全売却") / rationale / riskNote?

src/meeting/schemas.ts (191-215行) — rawHoldingSchema.passthrough() + holdingEvaluationSchema.transform():
  既存 alias 吸収は decision??action, rationale??reason, riskNote??(keyMetric/riskLevel)。
  transform は必ず explicit object literal を return する（`...raw` スプレッド禁止）。

decisionEnum = z.enum(["保持","買増","一部売却","全売却"]) は schemas.ts 191行に既存。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HoldingEvaluation 型に urgent / previousDecision / decisionChanged を追加</name>
  <files>src/meeting/types.ts</files>
  <read_first>
    - src/meeting/types.ts（HoldingEvaluation 110-116行の現状、PortfolioAnalysis の holdings 型参照）
    - .planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md（"src/meeting/types.ts (MODIFY)" セクション 175-203行 — 拡張形の確定案）
  </read_first>
  <action>
    HoldingEvaluation インターフェースに3フィールドを追加する（22-PATTERNS.md の推奨形を採用、ラッパー型は作らずフィールド直接追加）:
    (1) `readonly urgent: boolean;` — LLM出力（alias-transform 正規化済み・省略時 false）。コメントで由来を明記。
    (2) `readonly previousDecision?: "保持" | "買増" | "一部売却" | "全売却";` — TS付与、raw LLM output には決して存在しない旨をコメントで明記。
    (3) `readonly decisionChanged?: boolean;` — TS付与。`undefined = 比較不能`、`false = 変化なし` を区別する旨（D-14）をコメントで明記。
    decision enum リテラルは既存 HoldingEvaluation.decision と同じ4値を使う。readonly 修飾は既存フィールドと揃える。PortfolioAnalysis 型は変更しない（holdings は HoldingEvaluation の配列のまま、新フィールドを自動的に含む）。DecisionDiffResult のような別 export interface は作らない（22-03 は ReadonlyArray<HoldingEvaluation> を返す）。
  </action>
  <verify>
    <automated>grep -q "readonly urgent: boolean" src/meeting/types.ts && grep -q "readonly previousDecision" src/meeting/types.ts && grep -q "readonly decisionChanged" src/meeting/types.ts && npx vitest run src/meeting/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "readonly urgent: boolean" src/meeting/types.ts` が一致する（urgent は必須・非 optional）。
    - `previousDecision?` と `decisionChanged?` は optional（`?` 付き）で宣言されている。
    - previousDecision の enum リテラルが既存 decision の4値（保持/買増/一部売却/全売却）と完全一致する。
    - `npx vitest run src/meeting/schemas.test.ts` が既存テストを壊さず green のまま（型拡張のみで実行時挙動不変）。
  </acceptance_criteria>
  <done>HoldingEvaluation が urgent（必須 boolean）・previousDecision?・decisionChanged? を持つ。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: holdingEvaluationSchema の urgent alias 硬化 + strip テスト（test-first）</name>
  <files>src/meeting/schemas.ts, src/meeting/schemas.test.ts</files>
  <read_first>
    - src/meeting/schemas.ts（rawHoldingSchema / holdingEvaluationSchema 191-215行、rawWebSearchResultSchema 141-167行の3-way alias 前例、rawPortfolioSchema/portfolioAnalysisSchema 218-232行）
    - src/meeting/schemas.test.ts（`describe("webSearchResultSchema", ...)` の alias 受理テスト様式 305-456行。holdingEvaluationSchema の既存テストは無い）
    - .planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md（"src/meeting/schemas.ts (MODIFY)" 101-171行 と "src/meeting/schemas.test.ts (MODIFY)" 526-558行）
  </read_first>
  <behavior>
    - urgent 省略時: holdingEvaluationSchema.parse({symbol,nameJa,decision,rationale}) の結果 urgent === false（D-08 default）
    - alias 受理: 入力 `urgency: true` / `isUrgent: true` / `urgentFlag: true` / `urgent: true` の4表記いずれでも結果 urgent === true（D-10、Pitfall 8）
    - strip: 入力に `decisionChanged: true` / `previousDecision: "買増"` を含めても、結果オブジェクトに decisionChanged / previousDecision プロパティが存在しない（D-11、`not.toHaveProperty`）
  </behavior>
  <action>
    先にテストを書く（RED）: src/meeting/schemas.test.ts に新規 `describe("holdingEvaluationSchema", ...)` ブロックを追加し、上記 behavior の全ケース（default false、4 alias 各1件、strip 1件）を webSearchResultSchema テストと同じ `.parse()` + `expect().toBe()` / `.not.toHaveProperty()` 様式で書く。holdingEvaluationSchema が未 export の場合は schemas.ts から export する（既存 export 状態を read_first で確認）。
    次に実装（GREEN）: src/meeting/schemas.ts の rawHoldingSchema に `urgent`, `urgency`, `isUrgent`, `urgentFlag` を全て `z.boolean().optional()` で追加し、holdingEvaluationSchema.transform() の return object literal に `urgent: raw.urgent ?? raw.urgency ?? raw.isUrgent ?? raw.urgentFlag ?? false` を1行追加する。transform の return は既存どおり explicit object literal を維持し、`...raw` スプレッドは絶対に導入しない（decisionChanged/previousDecision の strip はこの literal に含めないことで自動達成 — 追加 strip コード不要）。riskNote の条件付きスプレッド行は既存のまま残す。
  </action>
  <verify>
    <automated>npx vitest run src/meeting/schemas.test.ts -t holdingEvaluationSchema</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/meeting/schemas.test.ts` が全て green（新規 describe 含む、既存 webSearchResultSchema 等を壊さない）。
    - urgent 省略ケースで `result.urgent === false`。
    - urgency / isUrgent / urgentFlag / urgent の4入力すべてで `result.urgent === true`。
    - decisionChanged/previousDecision を入力に含めたケースで `expect(result).not.toHaveProperty("decisionChanged")` かつ `not.toHaveProperty("previousDecision")` が通る。
    - `grep -c "\.\.\.raw" src/meeting/schemas.ts` が holdingEvaluationSchema.transform 内で増えていない（explicit literal 維持。grep で transform ブロックに `...raw` が無いことを目視確認）。
  </acceptance_criteria>
  <done>urgent が4表記+default で正規化され、LLM出力の decisionChanged/previousDecision がスキーマ層で strip される。テストが green。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portfolio-analyst (LLM) → tmp/portfolio-analysis.json → schema | LLM が生成した未検証 JSON がスキーマ境界を越えて型付きドメインへ入る |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | holdingEvaluationSchema.transform() | mitigate | explicit object literal を return（`...raw` 禁止）することで LLM が偽装した decisionChanged/previousDecision を strip する（D-11、PORT-05 の中核統制）。Task 2 の strip テストで検証 |
| T-22-02 | Denial of Service | urgent フィールド欠落での throw | mitigate | `z.boolean().optional()` + `?? false` default で欠落時も throw せず（zero-throw 規約、Pitfall 8）。default テストで検証 |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | 本フェーズは新規パッケージを一切導入しない（22-RESEARCH.md「Package Legitimacy Audit: N/A」）。install タスクなし |
</threat_model>

<verification>
- `npx vitest run src/meeting/schemas.test.ts` green（新規 holdingEvaluationSchema ブロック + 既存全テスト）
- types.ts に3フィールドが grep で確認できる
- schemas.ts transform に `...raw` スプレッドが無い
</verification>

<success_criteria>
- HoldingEvaluation 型が urgent（必須）・previousDecision?・decisionChanged? を持つ
- urgent が4 alias + default false で正規化される
- LLM出力の decisionChanged/previousDecision が strip される
- 既存テストスイートが green のまま
</success_criteria>

<output>
Create `.planning/phases/22-portfolio-analyst-re-evaluation/22-01-SUMMARY.md` when done
</output>
