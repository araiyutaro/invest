---
phase: 22-portfolio-analyst-re-evaluation
plan: 04
type: execute
wave: 3
depends_on: ["22-01", "22-03"]
files_modified:
  - src/scripts/report-data-loaders.ts
  - src/scripts/report-data-loaders.test.ts
  - src/scripts/generate-report.ts
  - src/scripts/generate-report.test.ts
  - src/scripts/generate-portfolio-report.ts
autonomous: true
requirements: [PORT-05, UI-07]
must_haves:
  truths:
    - "loadPrevPortfolioAnalysis が tmp/prev-portfolio-analysis.json を portfolioAnalysisSchema でパースし、失敗時は console.warn して null を返す（D-15）"
    - "generate-report.ts main() が prev を並列ロードし、attachDecisionChanges で enriched な PortfolioAnalysis を組み立ててレンダラーへ渡す（D-13 の収束点配線）"
    - "loadWebSearchResults / loadReevalResults の per-file catch が console.warn でログするようになる（Pitfall 7 負債回収、D-15）"
    - "urgent 銘柄カードに赤『⚠ 緊急』バッジ、decisionChanged===true 銘柄にアンバー『判断変更: X → Y』バッジが表示される（D-16/D-17）"
    - "border-left の decision 色はバッジ有無に関わらず維持される（D-18）"
    - "decisionChanged===undefined ではバッジが描画されない（false と同じく無、D-14）"
    - "リサーチ内容のカード直接表示・ニュースなしカードのデエンファシスは行わない（D-19/D-20 の非採用決定を維持）"
    - "バッジは formatHoldingEvaluationsHtml の純関数拡張 + ユニットテストで実装され、フォールバックパス（portfolioAnalysis === null）に影響しない（D-21）"
  artifacts:
    - path: "src/scripts/report-data-loaders.ts"
      provides: "loadPrevPortfolioAnalysis ローダー"
      exports: ["loadPrevPortfolioAnalysis"]
    - path: "src/scripts/generate-report.ts"
      provides: "prev 並列ロード + attachDecisionChanges 配線 + Pitfall7 backfill"
      contains: "loadPrevPortfolioAnalysis"
    - path: "src/scripts/generate-portfolio-report.ts"
      provides: "formatUrgentBadgeHtml / formatDecisionChangedBadgeHtml"
      contains: "⚠ 緊急"
  key_links:
    - from: "src/scripts/generate-report.ts main()"
      to: "src/portfolio/decision-diff.ts attachDecisionChanges"
      via: "enrichedPortfolioAnalysis 組み立て"
      pattern: "attachDecisionChanges"
    - from: "src/scripts/generate-portfolio-report.ts formatHoldingEvaluationsHtml"
      to: "h.urgent / h.decisionChanged / h.previousDecision"
      via: "バッジ描画（escapeHtml 経由）"
      pattern: "formatUrgentBadgeHtml|formatDecisionChangedBadgeHtml"
---

<objective>
レポート生成層を配線し、決定論的差分をレポートに反映する: (1) `loadPrevPortfolioAnalysis()` ローダー新設（console.warn、D-15）、(2) generate-report.ts の Promise.all に prev ロードを追加し `attachDecisionChanges` で enriched な PortfolioAnalysis を組み立ててレンダラーへ渡す配線 + `loadWebSearchResults`/`loadReevalResults` の per-file catch への console.warn backfill（Pitfall 7 負債回収）、(3) `formatHoldingEvaluationsHtml` へ緊急バッジ（赤『⚠ 緊急』）と判断変化バッジ（アンバー『判断変更: {前日} → {当日}』）を追加（border-left decision 色は維持、D-18）。

Purpose: PORT-05（TS 側差分の実配線）と UI-07（赤/アンバーの視覚強調 + 変化バッジ）を、既存の loader / Promise.all fan-out / 純関数レンダラー / 社名一致バッジ規約の自己拡張として満たす。
Output: 拡張された loader・main() 配線・レンダラー2バッジと各テスト。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-UI-SPEC.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-01-SUMMARY.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-03-SUMMARY.md

<interfaces>
既存契約（直接 read / grep で確認済み）:
- report-data-loaders.ts 76-84行 loadPortfolioAnalysis: try/readFile(join(TMP_DIR,...))/portfolioAnalysisSchema.parse/catch→console.error→null。portfolioAnalysisSchema は既に import 済み。新 loader は同型だが console.warn を使う（D-15 が severity を区別）。
- generate-report.ts 96-115行: 9項目 Promise.all（loadWebSearchResults/loadReevalResults/loadRound1..3/loadPortfolioAnalysis/loadMarketData/loadNewsPool/loadHoldingNews）→ resolvePortfolioHoldingNews → generatePortfolioReportHtml(meetingResult, portfolioAnalysis, resolvedHoldingNews)。
- generate-report.ts 24-66行: loadWebSearchResults/loadReevalResults の per-file inner catch が「catch { return null; }」で無言（Pitfall 7 負債）。外側の readdir catch は対象外。
- generate-portfolio-report.ts 47-69行 formatHoldingEvaluationsHtml: decisionColor→border-left、riskNote アンバー、newsHtml。<h4> は 60行付近。社名一致バッジ 28-30行がピル様式の前例。escapeHtml は report-utils.ts。
- generate-report.test.ts: validPortfolioAnalysis fixture 288行、Portfolio Report describe 320行〜、fsMock.readFile mockImplementation でパスマッチ、Test 39 が portfolio-research 隔離アサーション。

確定コード案は 22-PATTERNS.md（loader 207-239行 / wiring 243-329行 / badges 332-392行 / tests 562-654行）と 22-UI-SPEC.md 104-137行（バッジ確定実装・6視覚状態）にある。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loadPrevPortfolioAnalysis ローダー新設 + テスト（console.warn, D-15）</name>
  <files>src/scripts/report-data-loaders.ts, src/scripts/report-data-loaders.test.ts</files>
  <read_first>
    - src/scripts/report-data-loaders.ts（loadPortfolioAnalysis 76-84行、loadHoldingNews 104-112行、TMP_DIR / portfolioAnalysisSchema の import）
    - src/scripts/report-data-loaders.test.ts（`vi.mock("node:fs/promises")` セットアップ、`describe("loadHoldingNews", ...)` 44-69行の mockResolvedValueOnce/mockRejectedValueOnce 様式）
    - .planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md 207-239行 と 562-593行
  </read_first>
  <behavior>
    - 正常系: readFile が有効 JSON を返すと portfolioAnalysisSchema.parse 結果を返す（result?.holdings[0]?.symbol が取れる）
    - ENOENT: readFile が reject すると throw せず null を返し、console.warn が呼ばれる（D-15/Pitfall 7）
    - パース失敗: 不正 JSON でも throw せず null を返し console.warn が呼ばれる
  </behavior>
  <action>
    先にテスト（RED）: report-data-loaders.test.ts に新規 `describe("loadPrevPortfolioAnalysis", ...)` を追加し、上記3 behavior を loadHoldingNews テストと同じ mock 様式で書く。console.warn は `vi.spyOn(console, "warn").mockImplementation(() => {})` でスパイして `toHaveBeenCalled` を検証、末尾で mockRestore。
    次に実装（GREEN）: report-data-loaders.ts に `loadPrevPortfolioAnalysis(): Promise<PortfolioAnalysis | null>` を loadPortfolioAnalysis の兄弟として追加。`readFile(join(TMP_DIR, "prev-portfolio-analysis.json"), "utf-8")` → `portfolioAnalysisSchema.parse(JSON.parse(raw) as unknown) as PortfolioAnalysis`、catch で `console.warn("Prev portfolio analysis load failed (D-15/Pitfall 7):", ...)` して null 返却。console.error ではなく console.warn を使う（前日データ欠損は想定内のエッジ条件、D-15 が severity を区別）。新スキーマは不要（portfolioAnalysisSchema を再利用、既に import 済み）。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/report-data-loaders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/report-data-loaders.test.ts` が全て green（新規 describe + 既存6テスト）。
    - `grep -q "loadPrevPortfolioAnalysis" src/scripts/report-data-loaders.ts` が一致し export されている。
    - loader の catch が `console.warn`（`console.error` ではない）を呼ぶ（`grep "console.warn" src/scripts/report-data-loaders.ts` が新 loader 内に一致）。
    - ENOENT / パース失敗の両ケースで null 返却かつ warn スパイが呼ばれるテストが通る。
  </acceptance_criteria>
  <done>loadPrevPortfolioAnalysis が prev-portfolio-analysis.json を fail-soft + console.warn で読み込み、テスト green。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate-report.ts 配線（prev ロード + attachDecisionChanges）+ Pitfall 7 backfill + Test 39 拡張</name>
  <files>src/scripts/generate-report.ts, src/scripts/generate-report.test.ts</files>
  <read_first>
    - src/scripts/generate-report.ts（Promise.all 96-115行、loadWebSearchResults/loadReevalResults 24-66行 の inner catch、import 群）
    - src/scripts/generate-report.test.ts（Test 39 の fsMock.readFile mockImplementation、warn 検証様式）
    - src/portfolio/decision-diff.ts（22-03 で新設した attachDecisionChanges のシグネチャ）
    - .planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md 243-329行 と 598-654行
  </read_first>
  <behavior>
    - loadWebSearchResults / loadReevalResults の per-file catch が失敗ファイルで console.warn を呼ぶ（Test: warn スパイ + 不正ファイル mock）
    - Test 39: main() が prev-portfolio-analysis.json を readFile で呼ぶが、既存の portfolio-research 隔離アサーション（readFile calls が portfolio-research を含まない）は依然 pass する
    - enrichedPortfolioAnalysis: portfolioAnalysis が null なら null、非 null なら holdings が attachDecisionChanges の結果に置換される
  </behavior>
  <action>
    実装:
    (1) 配線: Promise.all を10項目に拡張し末尾に `loadPrevPortfolioAnalysis()` を追加、分割代入に `prevPortfolioAnalysis` を追加。`loadPrevPortfolioAnalysis` を report-data-loaders.js の import に、`attachDecisionChanges` を `../portfolio/decision-diff.js` の import に追加。resolvePortfolioHoldingNews の後で `enrichedPortfolioAnalysis` を組み立てる: `portfolioAnalysis === null ? null : { ...portfolioAnalysis, holdings: attachDecisionChanges(portfolioAnalysis.holdings, prevPortfolioAnalysis?.holdings ?? null) }`。既存の generatePortfolioReportHtml 呼び出し（115行）に portfolioAnalysis の代わりに enrichedPortfolioAnalysis を渡す。
    (2) Pitfall 7 backfill: loadWebSearchResults（32-37行付近）と loadReevalResults（54-59行付近）の per-file inner catch を `catch (error) { console.warn(\`...load failed (${f}):\`, error instanceof Error ? error.message : error); return null; }` に変更（ファイル名/ラベルは各々に合わせる）。外側の readdir catch（return []）は無言のまま変更しない（D-15 は per-file inner catch のみ対象）。
    (3) テスト: generate-report.test.ts に (a) Pitfall 7 warn 検証テスト（websearch/reeval の不正ファイルで console.warn 呼び出し）、(b) Test 39 の拡張として readFile calls に prev-portfolio-analysis.json が含まれるアサーションを追加しつつ全 readFile calls が portfolio-research を含まないループアサーションを維持。fsMock は未マッチパスを ENOENT で reject するため loadPrevPortfolioAnalysis は null に解決され mockImplementation 変更は原則不要（22-PATTERNS.md 643-654行）。必要なら prev-portfolio-analysis.json 分岐を mock に追加。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "loadPrevPortfolioAnalysis" src/scripts/generate-report.ts` かつ `grep -q "attachDecisionChanges" src/scripts/generate-report.ts` が一致する。
    - Promise.all が10項目で、分割代入に prevPortfolioAnalysis が含まれる。
    - enrichedPortfolioAnalysis の組み立てが `portfolioAnalysis === null` 分岐を持ち、generatePortfolioReportHtml に enriched を渡す。
    - loadWebSearchResults / loadReevalResults の per-file catch が両方 console.warn を呼ぶ（`grep -c "console.warn" src/scripts/generate-report.ts` が backfill 2箇所分増える）。
    - `npx vitest run src/scripts/generate-report.test.ts` が green（Test 39 拡張 + warn テスト含む、portfolio-research 隔離アサーション維持）。
  </acceptance_criteria>
  <done>prev が並列ロードされ attachDecisionChanges で enriched な分析がレンダラーへ渡り、Pitfall 7 負債が回収され、テスト green。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 緊急バッジ + 判断変化バッジのレンダラー実装（D-16/D-17/D-18/UI-07）</name>
  <files>src/scripts/generate-portfolio-report.ts, src/scripts/generate-report.test.ts</files>
  <read_first>
    - src/scripts/generate-portfolio-report.ts（formatHoldingEvaluationsHtml 47-69行、社名一致バッジ 28-30行、decisionColor、escapeHtml import）
    - src/scripts/generate-report.test.ts（Portfolio Report describe 320行〜、validPortfolioAnalysis fixture 288-318行、toContain/not.toContain 様式）
    - .planning/phases/22-portfolio-analyst-re-evaluation/22-UI-SPEC.md 104-137行（バッジ確定実装 + 6視覚状態）
    - .planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md 332-392行 と 598-641行
  </read_first>
  <behavior>
    - Test 40: urgent:true の銘柄カードに `⚠ 緊急` と背景 `#ef4444` が現れる
    - Test 41: decisionChanged:true + previousDecision:"保持" の銘柄に `判断変更: 保持 →` が現れる
    - Test 42: decisionChanged 未設定（undefined）では `判断変更:` が現れない（false と同じく無、D-14）
    - Test 43: urgent+changed 両方でも border-left は decision 色（例 保持=`#10b981`）のまま（D-18、バッジで上書きされない）
    - 回帰: urgent も changed も無い既定カードは従来通り描画される
  </behavior>
  <action>
    実装: generate-portfolio-report.ts に2つの純関数ヘルパーを追加（社名一致ピル様式踏襲、22-UI-SPEC.md の確定実装を採用）:
    (1) `formatUrgentBadgeHtml(urgent: boolean): string` — urgent が false/未設定なら空文字。true なら背景 `#ef4444`・白文字 `#fff`・font-size 0.75rem・bold・padding 0.15rem 0.5rem・margin-left 0.5rem・border-radius 999px のピルで `⚠ 緊急`。
    (2) `formatDecisionChangedBadgeHtml(decisionChanged, previousDecision, decision): string` — 条件は必ず `decisionChanged !== true` で早期 return（undefined と false の両方で空文字、truthy チェック禁止、D-14）。true なら背景 `#f59e0b`・暗色文字 `#1a1a28`（白ではない）・同ピル様式で `判断変更: ${escapeHtml(previousDecision ?? "?")} → ${escapeHtml(decision)}`。
    formatHoldingEvaluationsHtml の `<h4>` 内、symbol/nameJa の後・既存 float:right decision span の前に、urgent-first 順で両ヘルパー呼び出しを挿入（`${formatUrgentBadgeHtml(h.urgent)}${formatDecisionChangedBadgeHtml(h.decisionChanged, h.previousDecision, h.decision)}`）。外側 div の `border-left-color:${color}` 行（decisionColor 由来）は絶対に変更しない（D-18）。urgent カードへの追加背景ティントは付けない（22-UI-SPEC.md 決定: single-signal-per-concern）。
    テスト: generate-report.test.ts の Portfolio Report describe に Test 40-43 を追加（22-PATTERNS.md 598-641行の確定形）。validPortfolioAnalysis fixture の各 holding に `urgent: false` を追加して 22-01 の必須 urgent 型と整合させる。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "⚠ 緊急" src/scripts/generate-portfolio-report.ts` と `grep -q "判断変更:" src/scripts/generate-portfolio-report.ts` が一致する。
    - formatDecisionChangedBadgeHtml が `decisionChanged !== true` の早期 return を持つ（truthy チェックでない、grep で `!== true` 確認）。
    - previousDecision と decision がバッジ内で escapeHtml を通っている。
    - Test 40（`⚠ 緊急` + `#ef4444`）、Test 41（`判断変更: 保持 →`）、Test 42（undefined でバッジ無し）、Test 43（border-left が decision 色維持）が green。
    - `border-left-color:${color}` の算出行が decisionColor(h.decision) のまま（urgent/decisionChanged で上書きされない）。
    - `npx vitest run src/scripts/generate-report.test.ts` 全 green（既存 Portfolio Report テスト回帰なし）。
  </acceptance_criteria>
  <done>緊急・判断変化の2バッジが描画され、border-left decision 色が維持され、undefined と false でバッジ非表示、テスト green。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/prev-portfolio-analysis.json → loadPrevPortfolioAnalysis → 型付きドメイン | ファイルベースの前日データがスキーマ境界を越える |
| LLM由来フィールド（previousDecision/decision/rationale/riskNote）→ HTML 文字列 | portfolio-analyst 由来テキストがレポート HTML へレンダリングされる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Tampering | prev-portfolio-analysis.json ハンドソフ整合性 | mitigate | loadPrevPortfolioAnalysis が portfolioAnalysisSchema.parse で検証し、失敗時 console.warn + null（fail-soft、D-15）。不正データはバッジ非表示（decisionChanged undefined）に収束 |
| T-22-09 | Tampering (XSS/HTML injection) | バッジ描画 | mitigate | previousDecision/decision はバッジ内で escapeHtml を通す。urgent は boolean（描画に文字列注入なし）。rationale/riskNote は既存どおり escapeHtml 済み |
| T-22-10 | Information Disclosure | Pitfall 7 無言 catch | mitigate | loadWebSearchResults/loadReevalResults の per-file catch に console.warn を追加し、schema drift/欠損を可視化（D-15、診断トレース確保） |
| T-22-11 | Tampering (report integrity) | decisionChanged===undefined を false 扱い | mitigate | レンダラーのバッジ条件を strict `=== true` にし、undefined（比較不能）と false（変化なし）を共に非表示（D-14、Test 42 で検証） |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージ導入なし（install タスクなし） |
</threat_model>

<verification>
- `npx vitest run src/scripts/report-data-loaders.test.ts src/scripts/generate-report.test.ts` green
- `npm test`（全スイート）が green（フルスイート・phase gate）
- loadPrevPortfolioAnalysis / attachDecisionChanges / 2バッジが grep で確認できる
- border-left decision 色維持（Test 43）
- フォールバックパス（portfolioAnalysis === null）はカード非描画のため影響なし（D-21、既存フォールバックテスト維持）
</verification>

<success_criteria>
- prev ローダーが fail-soft + console.warn で動作
- attachDecisionChanges が main() で配線され enriched 分析がレンダリングされる
- Pitfall 7 の per-file catch に console.warn が入る
- 緊急バッジ（赤）・判断変化バッジ（アンバー、前日→当日）が描画される
- border-left decision 色維持、undefined/false でバッジ非表示
- 全テストスイート green
</success_criteria>

<output>
Create `.planning/phases/22-portfolio-analyst-re-evaluation/22-04-SUMMARY.md` when done
</output>
