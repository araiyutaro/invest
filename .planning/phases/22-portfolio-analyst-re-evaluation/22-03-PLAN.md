---
phase: 22-portfolio-analyst-re-evaluation
plan: 03
type: tdd
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/portfolio/decision-diff.ts
  - src/portfolio/decision-diff.test.ts
autonomous: true
requirements: [PORT-05]
must_haves:
  truths:
    - "attachDecisionChanges が前日スナップショットと当日 holdings の decision enum 等値比較から decisionChanged を決定論的に計算する（LLM自己申告でない、D-11/D-13）"
    - "prevHoldings が null、または銘柄が前日リストに存在しない場合は decisionChanged = undefined（false と区別、D-14）"
    - "銘柄キー一致は normalizeHoldingSymbol で行い、表記揺れに耐える（D-13）"
    - "当日 holdings が常にループ駆動側で、すべての当日銘柄が出力に現れる（前日側でループしない）"
  artifacts:
    - path: "src/portfolio/decision-diff.ts"
      provides: "attachDecisionChanges 純関数"
      exports: ["attachDecisionChanges"]
      min_lines: 20
    - path: "src/portfolio/decision-diff.test.ts"
      provides: "差分ロジックの全ケーステスト"
      contains: "attachDecisionChanges"
  key_links:
    - from: "src/portfolio/decision-diff.ts"
      to: "src/portfolio/holding-news.ts"
      via: "normalizeHoldingSymbol を import（再実装しない）"
      pattern: "normalizeHoldingSymbol"
---

<objective>
前日判断との決定論的差分検出を担う純関数モジュール `src/portfolio/decision-diff.ts` を TDD で新設する。`attachDecisionChanges(holdings, prevHoldings)` は当日 holdings をループ駆動とし、`normalizeHoldingSymbol` でキー一致させ、decision enum の等値比較で `decisionChanged` を計算、`previousDecision` を付与する。prev が null または銘柄不一致の場合は `decisionChanged = undefined`（`false` と区別、D-14）。

Purpose: PORT-05「判断変化を LLM 自己申告ではなく TS 側で前日スナップショットとの差分から決定論的に検出」を、既存 `holding-news.ts` の `resolvePortfolioHoldingNews` と同型の純関数として提供する。22-04 の generate-report.ts 配線がこの関数を消費する。
Output: decision-diff.ts（純関数、I/O・throw なし）とユニットテスト。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md
@.planning/phases/22-portfolio-analyst-re-evaluation/22-01-SUMMARY.md

<interfaces>
依存する型（22-01 で拡張済み）:
  HoldingEvaluation（src/meeting/types.ts）: symbol / nameJa / decision / rationale / riskNote? / urgent /
    previousDecision?（TS付与）/ decisionChanged?（TS付与）

再利用ユーティリティ（既存、再実装禁止）:
  normalizeHoldingSymbol（src/portfolio/holding-news.ts 25-34行）: symbol.trim().toUpperCase()

構造前例（22-PATTERNS.md 25-69行）:
  resolvePortfolioHoldingNews（holding-news.ts 56-84行）— 当日=primary をループ駆動、
  secondary は Map で lookup-only、常に全 primary 要素を出力（fail-soft）。
  attachDecisionChanges は同じ shape に従う。前日側でループしないこと（当日銘柄の脱落防止）。

戻り値は ReadonlyArray<HoldingEvaluation>（22-01 で previousDecision?/decisionChanged? を
HoldingEvaluation に直接追加したため、別 export interface DecisionDiffResult は不要）。
</interfaces>
</context>

<feature>
  <name>attachDecisionChanges 決定論的差分検出</name>
  <files>src/portfolio/decision-diff.ts, src/portfolio/decision-diff.test.ts</files>
  <read_first>
    - src/portfolio/holding-news.ts（normalizeHoldingSymbol 25-34行、resolvePortfolioHoldingNews 56-84行の構造前例、import 様式 1-4行）
    - src/portfolio/holding-news.test.ts（TDD スタイル: describe/it、makeHolding 的ヘルパー、共有 fixture 最小化）
    - src/meeting/types.ts（22-01 拡張後の HoldingEvaluation）
    - .planning/phases/22-portfolio-analyst-re-evaluation/22-PATTERNS.md 25-97行（decision-diff.ts / .test.ts の確定パターンと必須ケース）
  </read_first>
  <behavior>
    Cases（RED で先に書く）:
    - prevHoldings === null → 全結果に previousDecision/decisionChanged プロパティが存在しない（`{...h}` のみ。undefined 値キーではなく「キーなし」。`not.toHaveProperty("decisionChanged")`）
    - 両方に存在・decision 同一 → decisionChanged === false、previousDecision === 前日 decision
    - 両方に存在・decision 相違（例 前日 "保持" → 当日 "一部売却"）→ decisionChanged === true、previousDecision === "保持"
    - 当日銘柄が prevHoldings に無い（保有リスト変更）→ previousDecision/decisionChanged プロパティなし（decisionChanged === undefined、D-14、false と明確に区別）
    - キー一致は normalizeHoldingSymbol 経由（前日 " mrna " / 当日 "MRNA" の空白・大小文字ズレでも一致し、decisionChanged が正しく計算される）
    - 当日 holdings に有り前日に無い銘柄があっても、その当日銘柄は必ず出力配列に含まれる（当日=ループ駆動）
  </behavior>
  <implementation>
    GREEN: src/portfolio/decision-diff.ts に `attachDecisionChanges(holdings: ReadonlyArray<HoldingEvaluation>, prevHoldings: ReadonlyArray<HoldingEvaluation> | null): ReadonlyArray<HoldingEvaluation>` を実装する。normalizeHoldingSymbol と HoldingEvaluation 型を import（再実装しない）。prevHoldings === null なら `holdings.map((h) => ({ ...h }))` を返す（新フィールド無し）。それ以外は prevHoldings から `normalizeHoldingSymbol(h.symbol)` をキーにした Map を1度だけ構築し、holdings.map で当日各銘柄をキー lookup。prev 不在なら `{ ...h }`（新フィールド無し = undefined）、prev 有りなら `{ ...h, previousDecision: prev.decision, decisionChanged: prev.decision !== h.decision }` を返す。throw なし・I/O なし・完全純関数（resolvePortfolioHoldingNews と同じ規律）。前日側ではループしない。
    REFACTOR: 必要なら Map 構築を helper に切り出す（純度・可読性維持）。
  </implementation>
  <verify>
    <automated>npx vitest run src/portfolio/decision-diff.test.ts</automated>
  </verify>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 検証済み in-memory 配列 → attachDecisionChanges | すでに schema 検証済みの HoldingEvaluation[] のみを受け取る純関数（新たな信頼境界を越えない） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Tampering (of report integrity) | decisionChanged の算出 | mitigate | decisionChanged は TS 側の decision enum 等値比較のみで決定（LLM 出力を参照しない）。PORT-05 の中核。相違/同一/undefined 各ケースをテストで検証 |
| T-22-07 | Repudiation | 比較不能を false と偽る | mitigate | prev null・銘柄不一致は decisionChanged=undefined とし false と区別（D-14）。not.toHaveProperty テストで検証 |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージ導入なし（install タスクなし） |
</threat_model>

<verification>
- `npx vitest run src/portfolio/decision-diff.test.ts` green（全ケース）
- decision-diff.ts が normalizeHoldingSymbol を import（`grep -q "normalizeHoldingSymbol" src/portfolio/decision-diff.ts`）
- 前日側でループしていない（当日=ループ駆動、目視 + 当日-only 銘柄が出力に含まれるテスト）
</verification>

<success_criteria>
- attachDecisionChanges が decision 等値比較で decisionChanged を決定論的に計算
- prev null / 銘柄不一致で undefined（false と区別）
- normalizeHoldingSymbol でキー一致
- 純関数（throw / I/O なし）、テスト green
</success_criteria>

<output>
Create `.planning/phases/22-portfolio-analyst-re-evaluation/22-03-SUMMARY.md` when done
</output>
