---
phase: 10-pipeline-timing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/collect-data.ts
  - src/scripts/collect-data.test.ts
  - .claude/commands/invest.md
autonomous: true
requirements:
  - METR-01
  - METR-02
must_haves:
  truths:
    - "collect-data.ts 実行後に tmp/pipeline-metrics.json が書き出され collectData.durationMs が 0 以上の整数である"
    - "/invest の最終出力に '═══ Pipeline Timing ═══' ブロックが表示される"
    - "Step 1〜4 の大カテゴリの下に各サブステップの実行時間がインデントされて表示される (D-02)"
    - "Total 行にパイプライン全体の実行時間が Xm YYs 形式で表示される"
    - "ステップがスキップされた場合に NaN ではなく 'スキップ' と表示される"
  artifacts:
    - path: "src/scripts/collect-data.ts"
      provides: "performance.now() 計測 + pipeline-metrics.json 書き出し"
      contains: "performance.now()"
    - path: "src/scripts/collect-data.test.ts"
      provides: "METR-01 テスト (pipeline-metrics.json 書き出し検証)"
      min_lines: 180
    - path: ".claude/commands/invest.md"
      provides: "ステップ境界タイムスタンプ記録 + Pipeline Timing 表示ブロック"
      contains: "Pipeline Timing"
  key_links:
    - from: "src/scripts/collect-data.ts"
      to: "tmp/pipeline-metrics.json"
      via: "writeFile(join(TMP_DIR, 'pipeline-metrics.json'), ...)"
      pattern: "pipeline-metrics\\.json"
    - from: ".claude/commands/invest.md"
      to: "tmp/pipeline-metrics.json"
      via: "node -e fs.writeFileSync/readFileSync"
      pattern: "pipeline-metrics\\.json"
---

<objective>
パイプライン全体とステップ別の実行時間を計測し、/invest の最終出力に D-03 フォーマットで表示する。

Purpose: パイプラインのどのステップにどれだけ時間がかかっているかを可視化し、ボトルネック特定と実行時間の把握を可能にする。
Output: collect-data.ts に performance.now() 計測を追加、invest.md に12ステップの境界タイムスタンプ記録と Pipeline Timing 表示ブロックを追加。
</objective>

<execution_context>
@/Users/arai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pipeline-timing/10-CONTEXT.md
@.planning/phases/10-pipeline-timing/10-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From src/scripts/collect-data.ts (current imports, line 1-2):
```typescript
import { writeFile, mkdir } from "node:fs/promises";
import { join } from "node:path";
```
Note: readFile は未 import。トップレベル import に追加が必要。

From src/scripts/collect-data.ts (TMP_DIR, line 12):
```typescript
const TMP_DIR = join(import.meta.dirname, "../../tmp");
```

From src/scripts/collect-data.ts (main function signature, line 14):
```typescript
export async function main() {
```

From src/scripts/collect-data.test.ts (fs mock, lines 4-7):
```typescript
vi.mock("node:fs/promises", () => ({
  mkdir: vi.fn().mockResolvedValue(undefined),
  writeFile: vi.fn().mockResolvedValue(undefined),
}));
```
Note: readFile が未モック。追加が必要。

From src/scripts/collect-data.test.ts (writeFileMock pattern, lines 79-83):
```typescript
const writeCalls = writeFileMock.mock.calls;
const marketJsonCall = writeCalls.find((call) =>
  String(call[0]).includes("market.json"),
);
expect(marketJsonCall).toBeDefined();
```

From .claude/commands/invest.md (existing node -e pattern, lines 32-42):
```bash
cd /Users/arai/invest && node -e "
const fs = require('fs');
const market = JSON.parse(fs.readFileSync('tmp/market.json', 'utf-8'));
...
"
```

From .claude/commands/invest.md (completion summary, lines 1335-1344):
```markdown
## パイプライン完了

「投資分析パイプライン完了」とユーザーに表示してください。

以下のサマリーをユーザーに表示してください:
- Step 1: データ収集 -- 完了
- Step 2: アナリストミーティング (3ラウンド) -- 完了
- Step 3: WebSearch + 再評価 + ポートフォリオ分析 + レポート生成 -- 完了
- Step 4: GitHub Pages デプロイ -- 完了
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: collect-data.ts に performance.now() 計測と pipeline-metrics.json 書き出しを追加 (METR-01)</name>
  <files>src/scripts/collect-data.ts, src/scripts/collect-data.test.ts</files>
  <read_first>
    - src/scripts/collect-data.ts (全体: imports, main() の構造, TMP_DIR 定数, 既存の writeFile パターン)
    - src/scripts/collect-data.test.ts (全体: vi.mock 定義, writeFileMock パターン, describe ブロック構造)
    - .planning/phases/10-pipeline-timing/10-PATTERNS.md (collect-data.ts セクション: 追加コードの全テンプレート)
  </read_first>
  <behavior>
    - Test A: main() 実行後に writeFile が "pipeline-metrics.json" パスで呼ばれ、書き込み内容の JSON に collectData.durationMs が存在し、typeof が "number" で、値が 0 以上である
    - Test B: fmt 関数が fmt(32000) === "0m 32s"、fmt(65000) === "1m 05s"、fmt(352000) === "5m 52s"、fmt(undefined) === "スキップ"、fmt(NaN) === "スキップ" を返す
  </behavior>
  <action>
    TDD で実装する。

    RED フェーズ:
    1. collect-data.test.ts の vi.mock("node:fs/promises") に readFile を追加する。readFile は vi.fn().mockRejectedValue(new Error("ENOENT")) でモックし、pipeline-metrics.json が未存在のケースをシミュレートする
    2. 既存の describe("collect-data script") ブロック末尾に Test A を追加する。writeFileMock.mock.calls から "pipeline-metrics.json" を含む呼び出しを検索し、JSON.parse した結果の collectData.durationMs を検証する
    3. 同ブロック末尾に Test B を追加する。fmt 関数はテストファイル内にインラインで定義する (invest.md の node -e スクリプトと同じロジック)。fmt の実装: ms が falsy または isNaN(ms) なら "スキップ" を返す。それ以外は Math.floor(ms/1000) で総秒数を求め、Math.floor(s/60) + "m " + String(s%60).padStart(2,"0") + "s" を返す
    4. npx vitest run で RED を確認する

    GREEN フェーズ:
    5. collect-data.ts の import 行に readFile を追加する: import { readFile, writeFile, mkdir } from "node:fs/promises"
    6. main() の先頭 (console.log("データ収集開始...") の直前) に const t0 = performance.now() を追加する
    7. main() の末尾 (console.log("データ収集完了") の直前) に以下のロジックを追加する:
       - const durationMs = Math.round(performance.now() - t0) で経過時間を計算
       - const metricsPath = join(TMP_DIR, "pipeline-metrics.json") でパスを構築
       - let metrics: Record<string, unknown> = {} で初期化
       - try { metrics = JSON.parse(await readFile(metricsPath, "utf-8")) } catch { /* ファイル未存在は正常 */ } で既存内容を読み込み
       - metrics.collectData = { durationMs } で計測値を設定
       - await writeFile(metricsPath, JSON.stringify(metrics, null, 2), "utf-8") で書き出し
    8. npx vitest run で GREEN を確認する

    per D-01: データ収集ステップの表示値は collectData.durationMs (performance.now() 計測値) を使用する。STATE.md の決定に従い Date.now() は使用しない。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/collect-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - collect-data.test.ts の vi.mock("node:fs/promises") に readFile: vi.fn() が含まれる
    - テスト "pipeline-metrics.json" を含む writeFile 呼び出しが存在し、parsed.collectData.durationMs >= 0 をアサートしている
    - テスト fmt(32000) === "0m 32s" と fmt(undefined) === "スキップ" をアサートしている
    - collect-data.ts の import 行に readFile が含まれる
    - collect-data.ts の main() 先頭に performance.now() 呼び出しがある
    - collect-data.ts の main() 末尾に pipeline-metrics.json への writeFile 呼び出しがある
    - npx vitest run src/scripts/collect-data.test.ts が全テストパスする
  </acceptance_criteria>
  <done>collect-data.ts が main() 実行時に performance.now() で計測した durationMs を tmp/pipeline-metrics.json に書き出し、既存テスト + 新規テスト 2 件がすべてパスする</done>
</task>

<task type="auto">
  <name>Task 2: invest.md にステップ境界タイムスタンプ記録と Pipeline Timing 表示ブロックを追加 (METR-01/METR-02)</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md (全体: Step 1〜4 の構造, 既存の node -e パターン, パイプライン完了セクション)
    - .planning/phases/10-pipeline-timing/10-PATTERNS.md (invest.md セクション: 挿入位置マップ, タイムスタンプ記録テンプレート, Pipeline Timing 表示ブロック)
    - .planning/phases/10-pipeline-timing/10-CONTEXT.md (D-01: ステップ粒度, D-02: 表示フォーマット, D-03: 表示例)
  </read_first>
  <action>
    invest.md に以下の3種類の変更を加える。

    A. pipelineStart 初期化 + pipeline-metrics.json クリア:
    「## Step 1: データ収集」セクション内、「市場データ収集を開始します...」表示指示の直前に、Bash コードブロックとして node -e スクリプトを追加する。このスクリプトは:
    - tmp/pipeline-metrics.json を { pipelineStart: Date.now() } で初期化する (既存ファイルがあれば上書き)
    - 「パイプラインタイミング計測を開始」とコンソールに表示する
    実行指示文は「まず、以下のBashコマンドでパイプラインタイミング計測を初期化してください:」

    B. 12ステップの境界タイムスタンプ記録:
    以下の各ステップの開始直前と完了直後に、node -e の Bash コードブロックを追加する。各スクリプトは pipeline-metrics.json を read-merge-write で更新し、{keyName}Start = Date.now() または {keyName}End = Date.now() を追記する。ファイル読み込み失敗時は m = {} にフォールバックする。

    挿入位置と key 名のマッピング (per D-01):
    1. round1Start/round1End: Step 2a (Round 1) の「以下5つの Agent ツールを同時に呼び出してください」の直前 / 「Round 1 完了: N/5 アナリスト成功」表示指示の直後
    2. tickerExtractStart/tickerExtractEnd: Step 2b (ティッカー抽出) の node -e bash コマンドの直前 / 「ティッカー抽出: N銘柄を特定」表示指示の直後
    3. round2Start/round2End: Step 2c (Round 2) の「以下5つの Agent ツールを同時に呼び出してください」の直前 / 「Round 2 完了: N/5 アナリスト成功」表示指示の直後
    4. moderatorIssuesStart/moderatorIssuesEnd: Step 2d (モデレーター論点整理) の「1つの Agent ツールを呼び出してください」の直前 / 「論点整理完了」表示指示の直後
    5. round3Start/round3End: Step 2e (Round 3) の「以下5つの Agent ツールを同時に呼び出してください」の直前 / 「Round 3 完了: N/5 アナリスト成功」表示指示の直後
    6. moderatorFinalStart/moderatorFinalEnd: Step 2f (モデレーター最終統合) の「1つの Agent ツールを呼び出してください」の直前 / 「ミーティング統合完了」表示指示の直後
    7. validationStart/validationEnd: Step 2g (バリデーション) の npx tsx validate-meeting.ts の直前 / 「Step 2 完了」表示指示の直後
    8. webSearchStart/webSearchEnd: Step 3a (WebSearch) の「WebSearchリサーチ: N銘柄を調査中...」表示指示の直前 / Step 3b (再評価) の「再評価完了: N/5 アナリスト成功」表示指示の直後
    9. portfolioStart/portfolioEnd: Step 3d (ポートフォリオ分析) の「ポートフォリオ分析を実行中...」表示指示の直前 / 「ポートフォリオ分析完了」表示指示の直後
    10. reportStart/reportEnd: Step 3c (HTMLレポート生成) の npx tsx generate-report.ts の直前 / 「レポート生成完了」表示指示の直後
    11. deployStart/deployEnd: Step 4 (自動デプロイ) の npx tsx update-index.ts の直前 / デプロイ結果表示指示の直後
    12. pipelineEnd: 「## パイプライン完了」セクション内、サマリー表示の直前

    各タイムスタンプ記録の node -e テンプレート (10-PATTERNS.md から):
    node -e "const fs=require('fs'); let m={}; try{m=JSON.parse(fs.readFileSync('/Users/arai/invest/tmp/pipeline-metrics.json','utf-8'))}catch(e){} m.{keyName}Start=Date.now(); fs.writeFileSync('/Users/arai/invest/tmp/pipeline-metrics.json',JSON.stringify(m,null,2));"

    各 node -e の前に「以下のBashコマンドで {ステップ名} の計測タイムスタンプを記録してください:」という指示文を付ける。

    C. Pipeline Timing 表示ブロック:
    「## パイプライン完了」セクション内、既存の完了サマリー（Step 1〜4 チェックリスト）の直後に、Pipeline Timing 表示用の node -e Bash コードブロックを追加する。表示指示文は「以下のBashコマンドで Pipeline Timing を表示してください:」。

    表示ブロックの node -e スクリプト仕様 (per D-02, D-03):
    - pipeline-metrics.json を readFileSync で読み込む。ファイル未存在時は「(タイミングデータなし)」を表示して process.exit(0)
    - fmt(ms) 関数: ms が falsy または isNaN(ms) なら "スキップ" を返す。正常値なら Math.floor(s/60)+"m "+String(s%60).padStart(2,"0")+"s" 形式
    - Step 1 の表示値は m.collectData.durationMs を使用する (performance.now() 計測値、per Pitfall 1 対策)
    - Step 2〜4 の各サブステップは m.{key}End - m.{key}Start の差分を fmt() に渡す
    - 各キーの欠損時に NaN にならないよう、fmt() 内の isNaN ガードで "スキップ" を返す (per Pitfall 3 対策)
    - Total は m.pipelineEnd - m.pipelineStart で計算し、未計測なら "(計測中)" を表示
    - 出力フォーマットは D-03 の表示例に完全一致させる

    注意事項:
    - 各 node -e は独立プロセスのため performance.now() は使用しない (Date.now() のみ)
    - 絶対パス /Users/arai/invest/tmp/pipeline-metrics.json を使用する (invest.md の既存 node -e パターンに合わせる)
    - 既存の invest.md の構造・指示文のスタイルを維持する
  </action>
  <verify>
    <automated>grep -c "pipeline-metrics.json" .claude/commands/invest.md</automated>
    <human-check>次回の /invest 実行時に Pipeline Timing ブロックが正しいフォーマットで表示されることを確認</human-check>
  </verify>
  <acceptance_criteria>
    - invest.md に "pipelineStart" を含む node -e コードブロックが Step 1 の直前に存在する
    - invest.md に round1Start, round1End, tickerExtractStart, tickerExtractEnd, round2Start, round2End, moderatorIssuesStart, moderatorIssuesEnd, round3Start, round3End, moderatorFinalStart, moderatorFinalEnd, validationStart, validationEnd, webSearchStart, webSearchEnd, portfolioStart, portfolioEnd, reportStart, reportEnd, deployStart, deployEnd, pipelineEnd の各キーを含む node -e コードブロックが存在する
    - invest.md の「パイプライン完了」セクション内に "═══ Pipeline Timing ═══" を含む node -e 表示ブロックが存在する
    - 表示ブロックの fmt 関数に isNaN(ms) ガードが含まれ、NaN 時に "スキップ" を返す
    - 表示ブロックに try/catch が含まれ、ファイル未存在時に "(タイミングデータなし)" を表示して process.exit(0) する
    - grep -c "pipeline-metrics.json" .claude/commands/invest.md の結果が 25 以上 (pipelineStart + 12 Start + 12 End + pipelineEnd + 表示ブロック)
  </acceptance_criteria>
  <done>invest.md の各ステップ境界に node -e タイムスタンプ記録が挿入され、パイプライン完了セクションに D-03 フォーマットの Pipeline Timing 表示ブロックが追加されている</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

このフェーズは内部計測データをローカルファイル (tmp/pipeline-metrics.json) に書き込むのみ。ユーザー入力なし、ネットワーク通信なし、機密データの処理なし。

| Boundary | Description |
|----------|-------------|
| なし | 信頼境界の交差なし — ローカルファイル I/O のみ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | tmp/pipeline-metrics.json | accept | ローカル tmp ファイル。計測データのみ、PII なし。改ざんの影響は表示値の不正確化のみ |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし。Node.js 組み込み API のみ使用 |
</threat_model>

<verification>
1. npx vitest run src/scripts/collect-data.test.ts が全テストパス (METR-01 テスト含む)
2. npx vitest run が既存テストを含め全パス (regression なし)
3. grep -c "pipeline-metrics.json" .claude/commands/invest.md の出力が 25 以上
4. grep -c "Pipeline Timing" .claude/commands/invest.md の出力が 1 以上
</verification>

<success_criteria>
- collect-data.ts が performance.now() でデータ収集時間を計測し、tmp/pipeline-metrics.json に collectData.durationMs として書き出す
- invest.md が各ステップ境界で Date.now() タイムスタンプを pipeline-metrics.json に記録する
- invest.md のパイプライン完了時に D-03 フォーマット (Step 階層付きリスト) で全ステップの実行時間を表示する
- 全自動テストがパスする
</success_criteria>

<output>
Create `.planning/phases/10-pipeline-timing/10-01-SUMMARY.md` when done
</output>
