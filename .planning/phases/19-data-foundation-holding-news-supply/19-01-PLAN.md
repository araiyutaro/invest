---
phase: 19-data-foundation-holding-news-supply
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/news/finnhub.ts
  - src/data/news/finnhub.test.ts
autonomous: true
requirements: [NEWS-04]
must_haves:
  truths:
    - "general/merger 記事の ticker は配列インデックス位置に関わらず常に undefined"
    - "company 記事の ticker は既存どおりティッカーシンボルを保持する"
    - "回帰テストが 3件以上の配列で全インデックスの ticker undefined を検証する"
  artifacts:
    - path: "src/data/news/finnhub.ts"
      provides: "index-as-ticker バグ修正済みの fetchNewsByCategory"
      contains: "toRawArticle(item)"
    - path: "src/data/news/finnhub.test.ts"
      provides: "NEWS-04 回帰テスト"
      contains: "toBeUndefined"
  key_links:
    - from: "src/data/news/finnhub.ts"
      to: "toRawArticle"
      via: "明示ラップ .map((item) => toRawArticle(item))"
      pattern: "map\\(\\(item\\) => toRawArticle\\(item\\)\\)"
---

<objective>
`src/data/news/finnhub.ts:43` の index-as-ticker 汚染バグを修正する。`.map(toRawArticle)` は `Array.prototype.map` が callback を `(item, index, array)` で呼ぶため、配列インデックス（数値）が `toRawArticle` の第2引数 `ticker?: string` に位置的に渡り込み、general/merger 記事全件に数値インデックスが `ticker` として混入している（NEWS-04）。

Purpose: 保有銘柄別ニュース抽出（Plan 02/03）が `article.ticker === holding.symbol` で照合するため、汚染のないティッカーデータを土台として確立する。PROJECT.md / PITFALLS.md Pitfall 3 が本修正を「データ土台」として先行実施すべきと明記。
Output: 修正済み finnhub.ts + 複数配列インデックス位置を検証する回帰テスト（Success Criterion 1）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-data-foundation-holding-news-supply/19-CONTEXT.md
@.planning/phases/19-data-foundation-holding-news-supply/19-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- 修正対象の現状シグネチャ。executor はコードベース探索不要。 -->

src/data/news/finnhub.ts:
- function toRawArticle(item: FinnhubNewsItem, ticker?: string): RawNewsArticle  （line 15）
- async function fetchNewsByCategory(apiKey, category): Promise<ReadonlyArray<RawNewsArticle>>  — バグ箇所 line 41-43
- async function fetchCompanyNews(apiKey, ticker): Promise<...>  — 正しい前例 line 69-71: `.map((item) => toRawArticle(item, ticker))`
- export async function fetchAllFinnhubNews(companyTickers): Promise<FinnhubNews>  — 返り値 { general, merger, company }

RawNewsArticle (src/data/news/types.ts): title, summary, source, url, publishedAt: Date, category, ticker?: string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEWS-04 回帰テストを先に追加（RED）してから finnhub.ts のバグを修正（GREEN）</name>
  <files>src/data/news/finnhub.test.ts, src/data/news/finnhub.ts</files>
  <read_first>
    - src/data/news/finnhub.ts （バグ箇所 line 41-43 と正しい前例 line 69-71 の両方を確認）
    - src/data/news/finnhub.test.ts （既存 describe ブロックの vi.spyOn(globalThis, "fetch") モック形と beforeEach/afterEach 規約を踏襲）
    - src/data/news/filter.ts calculatePriorityScore（line 235-257）— tickerBonus が `string[].includes(number)` で常に false だったため挙動不変であることをテストコメントに記す根拠
    - src/data/news/types.ts RawNewsArticle 型
  </read_first>
  <behavior>
    - RED: general カテゴリのモックレスポンスに 3件以上の配列（index 0,1,2）を返させ、`result.general` の全記事について `article.ticker` が `undefined` であることを検証するテストを追加する。修正前は index 1,2 が数値 1,2 を持ちテストが失敗する（index 0 は falsy な数値 0 でバグを隠すため、必ず 3件以上で複数インデックスを検証する）。
    - GREEN: fetchNewsByCategory の `.map(toRawArticle)` を `.map((item) => toRawArticle(item))` に修正するとテストが通る。
    - 不変条件: 既存の company 記事テスト（`ticker === "MRNA"` 等）は引き続き pass する（fetchCompanyNews は変更しない）。
  </behavior>
  <action>
    src/data/news/finnhub.test.ts に新しい `it` を追加する。テスト名は「general/merger記事は配列インデックス位置に関わらずtickerがundefinedになる（回帰テスト、NEWS-04）」とし、既存テストと同じ `vi.spyOn(globalThis, "fetch").mockImplementation` パターンで URL 部分文字列 `category=general` にマッチさせ、`[0,1,2].map(...)` で3件の general 記事モックを返す。`fetchAllFinnhubNews([])` を呼び、`result.general` が length 3 であること、および全 `article.ticker` が `toBeUndefined()` であることを検証する。テストの説明コメントに「本修正は calculatePriorityScore に対して挙動中立（tickerBonus は元々 string[].includes(number) で常に false）であり、型を number→undefined に正すもの」と明記（レビュアーが「修正が何もしない」と誤解しないため、PITFALLS.md Pitfall 3 準拠）。
    次に src/data/news/finnhub.ts の line 43 を `.map(toRawArticle)` から `.map((item) => toRawArticle(item))` に変更する（Claude's Discretion で推奨された明示ラップ形。fetchCompanyNews line 71 の既存正パターンと対称）。
    fetchCompanyNews / toRawArticle / schemas.ts のガードコードは変更しない（スコープ外・回帰リスクなし）。
  </action>
  <verify>
    <automated>npx vitest run src/data/news/finnhub.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/data/news/finnhub.ts が `.map((item) => toRawArticle(item))` を含み、`.map(toRawArticle)` を含まない
    - src/data/news/finnhub.test.ts が新規テスト内で `toBeUndefined` を含み、3件以上の配列モックを使う（`[0, 1, 2]` 等）
    - `npx vitest run src/data/news/finnhub.test.ts` が exit 0（全テスト green、既存 company テスト含む）
    - `npx vitest run` が exit 0（全 suite green、既存 filter.ts 等の下流消費者に回帰なし）
  </acceptance_criteria>
  <done>general/merger 記事の ticker が全配列インデックス位置で undefined になり、company 記事は既存どおりティッカーを保持する。回帰テストが複数インデックスを検証。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finnhub API → tmp/news.json | 外部APIレスポンス（未検証データ）が TS に流入し JSON ファイル化される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | fetchNewsByCategory の ticker フィールド | mitigate | index-as-ticker 汚染を明示ラップで除去し、回帰テストで全インデックス undefined を保証。下流の ticker 照合（Plan 02）が数値混入で誤動作するのを防ぐ |
| T-19-02 | Tampering | Finnhub レスポンスの数値 ticker が UI/照合に混入 | mitigate | 修正により general/merger の ticker は常に undefined。既存 schemas.ts の `typeof source.ticker === "string"` ガードは修正後 dead-but-harmless（本フェーズでは削除しない） |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | 本 Plan は新規パッケージ導入なし（既存 vitest のみ使用）。install タスクなしのため legitimacy gate 不要 |
</threat_model>

<verification>
- `npx vitest run` が全 suite green（既存 188件クラスの回帰なし）
- `grep -n "map(toRawArticle)" src/data/news/finnhub.ts` が 0件（バグ箇所が残っていない）
</verification>

<success_criteria>
データ収集パイプライン実行後、general/merger 記事の ticker が複数配列インデックス位置で検証しても常に undefined（Success Criterion 1）。company 記事の正当な ticker は不変。
</success_criteria>

<output>
Create `.planning/phases/19-data-foundation-holding-news-supply/19-01-SUMMARY.md` when done
</output>
