---
phase: 19-data-foundation-holding-news-supply
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - src/scripts/collect-data.ts
  - .claude/commands/invest.md
autonomous: true
requirements: [PORT-01]
must_haves:
  truths:
    - "collect-data 実行後、tmp/holding-news.json が12銘柄すべてのキーで生成される"
    - "ニュース取得失敗時も全12銘柄が空配列の holding-news.json が必ず書かれる"
    - "portfolio-analyst プロンプトに保有銘柄別関連ニュースの入力セクションが存在する"
    - "0件銘柄は明示的に『本日の関連ニュースなし』が列挙される"
    - "ニュースなし銘柄への言及禁止・創作禁止のガード指示が添付される"
  artifacts:
    - path: "src/scripts/collect-data.ts"
      provides: "holding-news.json 生成の統合（fail-soft付き）"
      contains: "buildHoldingNewsMap"
    - path: ".claude/commands/invest.md"
      provides: "portfolio-analyst の保有銘柄別ニュース入力セクション"
      contains: "保有銘柄別関連ニュース"
  key_links:
    - from: "src/scripts/collect-data.ts"
      to: "buildHoldingNewsMap"
      via: "news.json 書き出し直後に呼び出し（D-06）"
      pattern: "buildHoldingNewsMap\\(idArticles"
    - from: ".claude/commands/invest.md"
      to: "tmp/holding-news.json"
      via: "portfolio-analyst の読み込みファイルリスト + 解決埋め込み指示"
      pattern: "holding-news\\.json"
---

<objective>
Plan 02 の `buildHoldingNewsMap` を collect-data.ts パイプラインに統合し tmp/holding-news.json を生成（D-06/D-08）、さらに portfolio-analyst プロンプト（`.claude/commands/invest.md` Step 3d）に保有銘柄別関連ニュースの明示的入力セクションを追加する（D-07/D-11/D-12）。これで PORT-01「portfolio-analyst が保有銘柄別関連ニュースを入力として受け取る」を完結させる。

Purpose: 決定論的抽出（Plan 02）を実パイプラインに配線し、汚染のないティッカーデータ（Plan 01）に基づく保有銘柄別ニュースを portfolio-analyst の入力として供給する。
Output: 統合済み collect-data.ts + ニュース入力セクションを持つ invest.md。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-data-foundation-holding-news-supply/19-CONTEXT.md
@.planning/phases/19-data-foundation-holding-news-supply/19-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- 依存する Plan 02 の export と既存の統合ポイント。executor はコードベース探索不要。 -->

src/portfolio/holding-news.ts （Plan 02 で作成）:
- export function buildHoldingNewsMap(articles: ReadonlyArray<NewsArticleWithId>, holdings: ReadonlyArray<PortfolioHolding>): HoldingNewsFile
- HoldingNewsFile = Record<string, ReadonlyArray<{ id: string; matchType: "ticker"|"name"|"alias"; score: number }>>  （キーは holding.symbol）

src/scripts/collect-data.ts 現状の統合ポイント:
- line 59-64: `const idArticles = assignArticleIds(finalArticles);` の直後に news.json を書く（この try ブロック line 31-68 内）
- idArticles と PORTFOLIO_HOLDINGS は両方この時点でスコープ内
- news.json catch ブランチ（line 65-68）は失敗時 `writeFile(..."news.json", "[]")` を書く
- portfolio.json の fail-soft try/catch（line 70-82）が最も近い単一呼び出しテンプレート

.claude/commands/invest.md Step 3d（~line 1557-1633）:
- line 1557-1563: portfolio-analyst が Read するファイルリスト（tmp/portfolio.json, tmp/meeting-result.json, holdings.ts, tmp/news.json）
- line 1584-1585: 「## 保有銘柄一覧（全12銘柄、必ず全銘柄を評価すること）」の全12銘柄列挙規約
- news-curator セクション（line 1642 付近）: ID参照埋め込み指示の文言前例
- Step 3a prev-highlighted-stocks.json（line 165-172）: 条件付きファイル存在ゲートの文言前例
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: collect-data.ts に holding-news.json 生成を統合（D-06/D-08 fail-soft）</name>
  <files>src/scripts/collect-data.ts</files>
  <read_first>
    - src/scripts/collect-data.ts （news.json try/catch line 31-68 と portfolio.json fail-soft line 70-82、import 規約 line 1-11）
    - src/portfolio/holding-news.ts （Plan 02 の buildHoldingNewsMap シグネチャと HoldingNewsFile 形状）
    - src/portfolio/holdings.ts （PORTFOLIO_HOLDINGS）
    - .planning/phases/19-data-foundation-holding-news-supply/19-CONTEXT.md （D-06 統合点 / D-08 二重 fail-soft）
  </read_first>
  <action>
    import に `import { buildHoldingNewsMap } from "../portfolio/holding-news.js";` を追加（既存 import 規約 line 4-11 と同スタイル、.js 拡張子付き）。
    news.json 書き出し（line 59-64）の直後、同じ try ブロック内に holding-news 生成を追加する: `const holdingNews = buildHoldingNewsMap(idArticles, PORTFOLIO_HOLDINGS);` を呼び、`writeFile(join(TMP_DIR, "holding-news.json"), JSON.stringify(holdingNews, null, 2), "utf-8")` で書く（idArticles と PORTFOLIO_HOLDINGS は既にスコープ内）。抽出は idArticles への hard dependency があるため、news.json と同じ try ブロック内に置く。
    D-08 fail-soft: news.json の catch ブランチ（line 65-68）を拡張し、`writeFile("news.json", "[]")` に加えて全12銘柄が空配列の holding-news.json も必ず書く。フォールバックマップは `Object.fromEntries(PORTFOLIO_HOLDINGS.map((h) => [h.symbol, []]))` で構築し JSON 化する（`[]` ではなく全キー空配列オブジェクトである点が既存2前例と異なる。D-08 準拠）。日本語の失敗ログ「保有銘柄別ニュース抽出失敗（続行）」を含める。
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c collect-data.ts | grep -qx 0 && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/collect-data.ts が `import { buildHoldingNewsMap } from "../portfolio/holding-news.js"` を含む
    - `buildHoldingNewsMap(idArticles, PORTFOLIO_HOLDINGS)` の呼び出しが news.json 書き出しと同じ try ブロック内にある
    - catch ブランチが `Object.fromEntries(PORTFOLIO_HOLDINGS.map(` を含む全キー空配列フォールバックで holding-news.json を書く
    - `grep -c "holding-news.json" src/scripts/collect-data.ts` が 2 以上（成功パス + fail-soft パス）
    - `npx tsc --noEmit` が collect-data.ts に関する型エラーを出さない
  </acceptance_criteria>
  <done>collect-data 実行で tmp/holding-news.json が12銘柄キーで生成され、ニュース失敗時も全12銘柄空配列の holding-news.json が必ず書かれる。</done>
</task>

<task type="auto">
  <name>Task 2: portfolio-analyst プロンプトに保有銘柄別ニュース入力セクションを追加（D-07/D-11/D-12）</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md Step 3d（~line 1555-1640。読み込みファイルリスト line 1557-1563、全12銘柄列挙規約 line 1584-1585、JSON出力フォーマット、フィールド名ルール）
    - .claude/commands/invest.md Step 3a prev-highlighted-stocks.json 条件付きゲート（line 165-172）と news-curator ID参照埋め込み指示（~line 1642）
    - .planning/phases/19-data-foundation-holding-news-supply/19-CONTEXT.md （D-07 全文解決埋め込み/URL除外、D-11 全12銘柄列挙・0件明示、D-12 ガード指示）
    - .planning/phases/19-data-foundation-holding-news-supply/19-PATTERNS.md （invest.md セクションの3つの analog 文言前例）
  </read_first>
  <action>
    Step 3d の portfolio-analyst が Read するファイルリスト（line 1557-1563）に1行追加: `/Users/arai/invest/tmp/holding-news.json` — 全内容（保有銘柄別ニュースID参照。tmp/news.json と突き合わせて全文解決する）。tmp/news.json は既にリストにあるので追記のみ。
    portfolio-analyst プロンプト本体（「## 保有銘柄一覧（全12銘柄…）」セクション付近）に新セクション「## 保有銘柄別関連ニュース」を追加する。以下を満たすこと:
    - D-07 解決済み全文埋め込み: tmp/holding-news.json の各銘柄の記事ID を tmp/news.json と照合し、id + 見出し(title) + summary + ソース(source) + 公開日時(publishedAt) を解決して展開する。**URL は含めない**（news-curator と同方針、幻覚URL防止）。「URLやタイトルを直接創作してはならない、tmp/news.json から ID照合で解決すること」の news-curator line 1662 相当の **重要:** 太字命令文を添付。
    - D-11 全12銘柄必ず列挙: 「全12銘柄、必ず全銘柄を列挙」の既存 framing（line 1584-1585 相当）を再利用。0件銘柄には明示的に「本日の関連ニュースなし（ニュース不在は問題なしを意味しない）」を記載する。0件銘柄のセクション省略は禁止。
    - D-12 ガード指示: 「ニュースなしの銘柄は既存の材料（株価・ミーティング結果）のみで判断しニュースに言及しないこと。列挙されていないニュースを推測・創作しないこと」を **重要:** 太字命令として添付。
    - D-08 二重 fail-soft（条件付きゲート）: Step 3a の prev-highlighted-stocks.json（line 165-172）と同じ「（tmp/holding-news.json が存在する場合のみ以下を含めること）」開始ゲート /「（tmp/holding-news.json が存在しない場合はこのセクション全体を省略）」終了ゲートのペア文言でセクション全体を囲み、ファイル欠損時もニュースセクションなしで portfolio-analyst を続行できるようにする。
    JSON出力フォーマット・フィールド名ルール（overallComment/decision/rationale 等）は変更しない（本セクションは入力コンテキストの追加のみで出力スキーマは不変）。
  </action>
  <verify>
    <automated>grep -c "保有銘柄別関連ニュース" .claude/commands/invest.md | grep -qx 1 && grep -q "本日の関連ニュースなし" .claude/commands/invest.md && grep -q "推測・創作しない" .claude/commands/invest.md && echo PROMPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/invest.md が portfolio-analyst の Read リストに `tmp/holding-news.json` を含む
    - 「## 保有銘柄別関連ニュース」セクションが存在する
    - セクションが「本日の関連ニュースなし」の0件銘柄明示文言を含む（D-11）
    - セクションが「推測・創作しない」を含むガード指示を含む（D-12）
    - セクションが条件付き存在ゲート文言（「存在する場合のみ」「存在しない場合はこのセクション全体を省略」）で囲まれている（D-08）
    - URL を出力させない旨の **重要:** 命令が含まれる（D-07、`grep -q "URL" .claude/commands/invest.md` で該当セクションに URL 除外指示）
    - portfolio-analyst の JSON 出力フォーマット（overallComment/decision/rationale/riskNote フィールド名）が変更されていない
  </acceptance_criteria>
  <done>portfolio-analyst プロンプトに全12銘柄の保有銘柄別関連ニュースが明示的入力セクションとして含まれ、0件銘柄の明示・ガード指示・URL除外・条件付きゲートを備える。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/news.json（ニュースタイトル/summary） → portfolio-analyst プロンプト | 未検証の外部ニュース本文が LLM プロンプトに埋め込まれる（プロンプトインジェクション面） |
| tmp/holding-news.json（ID参照） → 解決埋め込み | LLM が記事本文を創作・URL幻覚するリスク |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering / Spoofing | ニュースタイトル/summary経由のプロンプトインジェクション | mitigate | ニュースは明示ラベル付き「入力データ」セクションとして埋め込み、D-12 ガード指示「列挙されていないニュースを推測・創作しないこと」で LLM の指示乗っ取り・幻覚を構造的に抑止。出力スキーマは固定（zod で下流検証）で、注入テキストがフィールド構造を変えられない |
| T-19-06 | Repudiation / Information Disclosure | LLM による URL/タイトルの幻覚出力 | mitigate | D-07: ID参照方式を徹底し URL をプロンプトに含めない。TS 側が tmp/news.json から ID照合で解決する既存 news-curator 方針を踏襲 |
| T-19-07 | Denial of Service | 0件・取得失敗時のパイプライン停止 | mitigate | D-08 二重 fail-soft: collect-data は全キー空配列 holding-news.json を必ず書き、invest.md はファイル欠損時もセクション省略で続行 |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージ導入なし（既存 buildHoldingNewsMap 配線のみ）。install タスクなしのため legitimacy gate 不要 |
</threat_model>

<verification>
- `npx tsc --noEmit` が全体で型エラーなし
- `grep -c "holding-news.json" src/scripts/collect-data.ts` が 2 以上
- invest.md の新セクションが D-07/D-11/D-12 と D-08 ゲートの全要素を含む（acceptance_criteria の grep 群）
</verification>

<success_criteria>
portfolio-analyst のプロンプトに、保有銘柄ごとの関連ニュースが明示的な入力セクションとして含まれている（Success Criterion 3）。collect-data 実行で tmp/holding-news.json が生成され、ニュース失敗時も fail-soft で全12銘柄空配列が書かれる。
</success_criteria>

<output>
Create `.planning/phases/19-data-foundation-holding-news-supply/19-03-SUMMARY.md` when done
</output>
