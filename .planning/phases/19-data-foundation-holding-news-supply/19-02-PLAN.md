---
phase: 19-data-foundation-holding-news-supply
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/portfolio/holdings.ts
  - src/portfolio/holding-news.ts
  - src/portfolio/holding-news.test.ts
autonomous: true
requirements: [PORT-01]
must_haves:
  truths:
    - "12銘柄すべてのキーを持つ holding-news マップが常に生成される（0件銘柄は空配列）"
    - "ticker一致が社名一致より優先され、上限超過時に ticker一致が残る"
    - "社名フォールバックは記事タイトルのみを照合し、summary言及ではマッチしない（D-03）"
    - "matchAliases（例: Joby, 名古屋銀）が照合に参加する（D-04）"
    - "1銘柄あたり最大5件に切り捨てられる（D-09）"
    - "社名フォールバックは日本株に限定されず全12銘柄に適用される（D-02）"
  artifacts:
    - path: "src/portfolio/holdings.ts"
      provides: "matchAliases フィールドを追加した PORTFOLIO_HOLDINGS"
      contains: "matchAliases"
    - path: "src/portfolio/holding-news.ts"
      provides: "決定論的な保有銘柄別ニュース抽出（純粋関数群）。出力はID参照+マッチメタ情報のみで記事本体を含まない（D-05）"
      exports: ["buildHoldingNewsMap", "matchesTicker", "matchesHoldingByName"]
      min_lines: 40
    - path: "src/portfolio/holding-news.test.ts"
      provides: "D-01〜D-10 をカバーするユニットテスト"
      contains: "buildHoldingNewsMap"
  key_links:
    - from: "src/portfolio/holding-news.ts"
      to: "calculatePriorityScore"
      via: "filter.js からのインポート（再実装しない）"
      pattern: "import.*calculatePriorityScore.*from.*filter"
    - from: "src/portfolio/holding-news.ts"
      to: "PortfolioHolding.matchAliases"
      via: "社名フォールバック照合"
      pattern: "matchAliases"
---

<objective>
決定論的な保有銘柄別ニュース抽出モジュール `src/portfolio/holding-news.ts` を TDD で新規作成し、`PORTFOLIO_HOLDINGS` に任意 `matchAliases` フィールドを追加する（PORT-01 のデータ抽出コア）。

Purpose: tmp/news.json の記事プールと12銘柄の保有リストから、ticker一致（主）+ 社名部分一致フォールバック（D-01/D-02）で、優先度スコア順・銘柄あたり上限5件（D-09/D-10）の holding-news マップを、副作用なく決定論的に生成する。Plan 03 がこれを collect-data.ts に統合し tmp/holding-news.json を書く。
Output: TDD 済みの純粋関数モジュール + matchAliases 付き holdings.ts。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-data-foundation-holding-news-supply/19-CONTEXT.md
@.planning/phases/19-data-foundation-holding-news-supply/19-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- 依存する既存の型・関数。executor はコードベース探索不要で直接使用する。 -->

src/portfolio/holdings.ts:
- interface PortfolioHolding { readonly symbol; readonly name; readonly nameJa; readonly sector; }
- const PORTFOLIO_HOLDINGS: ReadonlyArray<PortfolioHolding>  （12銘柄）

src/data/news/article-id.ts:
- interface NewsArticleWithId extends RawNewsArticle { readonly id: string; }   （id 例 "n01"）
- function assignArticleIds(articles): ReadonlyArray<NewsArticleWithId>

src/data/news/types.ts:
- interface RawNewsArticle { title; summary; source; url; publishedAt: Date; category; ticker?: string; }

src/data/news/filter.ts （直接インポートして再利用、再実装禁止）:
- export function calculatePriorityScore(article: RawNewsArticle, now: number, portfolioTickers: ReadonlyArray<string>): number

既存の純粋関数分割の前例（模倣対象）:
- src/data/news/article-id.ts assignArticleIds — ReadonlyArray へのイミュータブル transform、JSDoc で設計判断IDを引用
- src/data/news/filter.ts isDenylisted（line 201-213）— 単一目的の export 述語、タイトルのみ照合、JSDoc で Pitfall を引用
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PORTFOLIO_HOLDINGS に matchAliases フィールドを追加（D-04）</name>
  <files>src/portfolio/holdings.ts</files>
  <read_first>
    - src/portfolio/holdings.ts （全21行。既存の all-readonly 規約と12銘柄の並びを踏襲）
    - .planning/phases/19-data-foundation-holding-news-supply/19-CONTEXT.md （D-04 と specifics のエイリアス例・衝突回避原則）
  </read_first>
  <action>
    interface PortfolioHolding に `readonly matchAliases?: ReadonlyArray<string>;` を追加する（既存の全 readonly 規約に合わせる。任意フィールド）。
    PORTFOLIO_HOLDINGS の該当銘柄にのみエイリアスを付与する（D-04 のキュレーション原則: 一般語・人名と衝突する略称は登録しない）:
    - JOBY に `matchAliases: ["Joby"]`
    - 8522.T に `matchAliases: ["名古屋銀"]`
    - POWL には付与しない（"Powell" は FRB 議長と衝突するため正式名のみ）
    その他10銘柄は matchAliases を省略する。エイリアスは人間キュレーション前提で、自動短縮形生成はしない。
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c holdings.ts | grep -qx 0 && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - src/portfolio/holdings.ts の PortfolioHolding interface が `matchAliases?: ReadonlyArray<string>` を含む
    - JOBY エントリが `matchAliases: ["Joby"]` を含む
    - 8522.T エントリが `matchAliases: ["名古屋銀"]` を含む
    - POWL エントリに matchAliases が存在しない（`grep -A0 'symbol: "POWL"' src/portfolio/holdings.ts` の行に matchAliases なし）
    - `npx tsc --noEmit` が holdings.ts に関する型エラーを出さない
  </acceptance_criteria>
  <done>PortfolioHolding が任意 matchAliases を持ち、JOBY/8522.T のみキュレーション済みエイリアスを保有する。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: holding-news.ts 抽出モジュールを TDD で実装（RED→GREEN→REFACTOR）</name>
  <files>src/portfolio/holding-news.test.ts, src/portfolio/holding-news.ts</files>
  <read_first>
    - src/data/news/filter.ts （calculatePriorityScore line 235-257 の再利用対象と isDenylisted line 201-213 のタイトルのみ照合パターン）
    - src/data/news/filter.test.ts （makeArticle fixture-builder と describe-per-concern パターン）
    - src/data/news/article-id.ts / article-id.test.ts （イミュータブル transform とテストの immutability アサーション `expect(articles).toEqual(original)`）
    - src/portfolio/holdings.ts （Task 1 で matchAliases 追加後の PortfolioHolding 型と PORTFOLIO_HOLDINGS）
    - .planning/phases/19-data-foundation-holding-news-supply/19-CONTEXT.md （D-01〜D-10 の抽出仕様）
    - .planning/research/PITFALLS.md （Pitfall 4 JPカバレッジ / Pitfall 5 社名誤マッチ / Pitfall 10 上限）
  </read_first>
  <behavior>
    まず src/portfolio/holding-news.test.ts に以下を失敗テストとして書き（RED）、その後 holding-news.ts を実装して green にする。fixture-builder（makeArticleWithId, makeHolding）は filter.test.ts / article-id.test.ts の形を踏襲。
    - D-01/D-02: ticker一致（`article.ticker === holding.symbol`）が社名一致より優先。社名フォールバックは全12銘柄に均一適用（日本株限定にしない）
    - D-03: タイトルのみ照合。summary にのみ銘柄名を含みタイトルには含まない記事はマッチしない
    - D-04: matchAliases が照合に参加（title に "Joby" を含む記事が JOBY にマッチ。name は "Joby Aviation"）
    - D-08 fail-soft: articles が空でも12銘柄すべてのキーが存在し各値が空配列。関数は throw しない
    - D-09: 1銘柄あたり最大5件に切り捨て（6件以上マッチ時に5件）
    - D-10: 上限超過時は ticker一致を優先保持し、同一 matchType 内は calculatePriorityScore 降順で tie-break（filter.js の export 関数を直接インポートして使用、再実装しない）
    - immutability: buildHoldingNewsMap 呼び出し後、入力 articles / holdings 配列が不変（`toEqual(original)`）
  </behavior>
  <action>
    src/portfolio/holding-news.ts に純粋関数群を新規作成する（article-id.ts / filter.ts のイミュータブル・単一目的 export 分割を踏襲。入力は決して mutate せず spread で新オブジェクトを返す）。推奨分割（Claude's Discretion、命名は裁量）:
    - `matchesTicker(article, holding): boolean` — `article.ticker === holding.symbol` の厳密一致
    - `matchesHoldingByName(title: string, holding): boolean` — タイトルのみに対し holding.name / holding.nameJa / holding.matchAliases の部分一致（D-03、isDenylisted の JSDoc 規約に倣い設計判断IDをコメントで引用）
    - `matchArticlesForHolding(articles, holding): ReadonlyArray<{ article, matchType: "ticker" | "name" | "alias" }>` — 銘柄ごとの候補収集
    - `rankAndCapHoldingArticles(matches, now): ReadonlyArray<HoldingNewsEntry>` — D-10 切り捨て（ticker一致先行 → 同格は calculatePriorityScore 降順 → 上限5件）
    - `buildHoldingNewsMap(articles, holdings): HoldingNewsFile` — トップレベル orchestrator。D-08 fail-soft: 常に全 holding.symbol をキーに持ち、0件は空配列、throw しない
    holding-news.json の形状（D-05 = ID参照 + マッチメタ、記事フルコピー不採用）: `HoldingNewsEntry = { id: string; matchType: "ticker" | "name" | "alias"; score: number }`、`HoldingNewsFile = Record<string, ReadonlyArray<HoldingNewsEntry>>`（キーは holding.symbol）。id は NewsArticleWithId.id を参照し記事本体は保存しない。calculatePriorityScore は `import { calculatePriorityScore } from "../data/news/filter.js";` で再利用。portfolioTickers 引数には holdings の symbol 配列を渡す。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/holding-news.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/portfolio/holding-news.ts が `export function buildHoldingNewsMap` を含む
    - src/portfolio/holding-news.ts が `import { calculatePriorityScore }` を含み、時間減衰スコアを再実装していない（`grep -c "ageHours" src/portfolio/holding-news.ts` が 0）
    - holding-news.test.ts が D-03（title-only: summary言及はマッチしない）と D-08（空入力でも12キー）と D-09（cap 5）と D-10（ticker優先切り捨て）の各テストを含む
    - holding-news.test.ts が immutability アサーション（`toEqual` で入力配列不変）を含む
    - `npx vitest run src/portfolio/holding-news.test.ts` が exit 0
    - `npx vitest run` が exit 0（全 suite green）
  </acceptance_criteria>
  <done>buildHoldingNewsMap が12銘柄すべてのキーを持つ ID参照マップを決定論的に生成し、ticker優先・タイトルのみ社名フォールバック・上限5件・fail-soft をユニットテストで網羅する。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/news.json → holding-news 抽出 | 未検証の外部ニュース記事タイトルが社名フォールバック照合に流入する |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | matchesHoldingByName の社名部分一致 | mitigate | 照合はタイトルのみ（summary除外、D-03）+ matchAliases はキュレーション済みで一般語/人名衝突を排除（POWL 非登録）。誤マッチ拡大を構造的に抑制（PITFALLS Pitfall 5） |
| T-19-04 | Information Disclosure | holding-news.json の記事内容 | accept | D-05 により ID参照 + マッチメタのみ保存し記事フルコピーせず。個人ツールで PII なし。攻撃価値低 |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージ導入なし（既存 vitest / zod 不使用、filter.js 再利用のみ）。install タスクなしのため legitimacy gate 不要 |
</threat_model>

<verification>
- `npx vitest run` が全 suite green
- `grep -c "ageHours" src/portfolio/holding-news.ts` が 0（スコア再実装なし = filter.js 再利用）
- holding-news.test.ts が全12銘柄キー存在を検証するテストを含む
</verification>

<success_criteria>
tmp/news.json 相当の記事プールと12銘柄リストから、ticker一致による決定論的な保有銘柄別ニュースマッピング（優先度スコア順・銘柄あたり上限付き）が生成され、ユニットテストでカバーされている（Success Criterion 2）。
</success_criteria>

<output>
Create `.planning/phases/19-data-foundation-holding-news-supply/19-02-SUMMARY.md` when done
</output>
</output>
