---
phase: 17-pipeline-integration-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .claude/commands/invest.md
autonomous: false
requirements: [CURA-01, OPS-04]

must_haves:
  truths:
    - "Step 3d が portfolio-analyst と news-curator を1メッセージで2体並列起動する"
    - "news-curator の出力が tmp/news-curation.json に保存され、失敗時は1回リトライ後にファイル未作成で続行する"
    - "Step 3e が write-news-digest.ts を起動し、exit code に応じて [STEP:news-digest:OK] / [STEP:news-digest:FAIL:...] を出す"
    - "news-digest ステップの失敗は [PIPELINE:FAIL] を出さず Step 4 へ継続する（fail-soft）"
    - "パイプラインタイミング表に news-digest 行が追加される"
  artifacts:
    - path: ".claude/commands/invest.md"
      provides: "news-curator Agent（Step 3d）+ Step 3e ニュースダイジェスト生成ステップ + STEPマーカー + metrics"
      contains: "news-curator"
  key_links:
    - from: ".claude/commands/invest.md Step 3d"
      to: "tmp/news-curation.json"
      via: "news-curator エージェント出力の保存"
      pattern: "news-curation\\.json"
    - from: ".claude/commands/invest.md Step 3e"
      to: "src/scripts/write-news-digest.ts"
      via: "npx tsx による別プロセス起動"
      pattern: "write-news-digest\\.ts"
---

<objective>
`.claude/commands/invest.md`（日次パイプライン定義）に、Phase 17-01 で作った `write-news-digest.ts` を fail-soft で配線する。具体的には (1) Step 3d の portfolio-analyst 単体呼び出しを news-curator を含む2体並列呼び出しに拡張し（D-01/D-02）、D-03〜D-07 を満たすプロンプトと保存・リトライ処理を追加、(2) Step 3c の後・Step 4 の前に新しい `### Step 3e: ニュースダイジェスト生成` を挿入し、write-news-digest.ts を別プロセスで起動、exit code から `[STEP:news-digest:OK]`/`[STEP:news-digest:FAIL:...]` を出し分け、パイプラインは常に Step 4 へ継続（D-08/D-09/D-10）、(3) pipeline-metrics とタイミング表に news-digest 計測行を追加（D-11）。

**最重要（RESEARCH.md Pitfall 1）**: news-digest の失敗プロセスは既存 `report-generation`/`deploy` の「パイプラインを停止してください」文言を絶対に流用しない。`[PIPELINE:FAIL]` を出さず「Step 4 へ進んでください」と明示する。これが OPS-04 の回帰を防ぐ唯一の要。

Purpose: CURA-01（`/invest` 実行で 4紙目が自動生成）と OPS-04（fail-soft・専用STEPマーカーによる失敗可視化）を成立させる。
Output: `.claude/commands/invest.md`（Step 3d 拡張 + Step 3e 新設 + metrics/timing 追加）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pipeline-integration-orchestration/17-CONTEXT.md
@.planning/phases/17-pipeline-integration-orchestration/17-PATTERNS.md
@.planning/phases/17-pipeline-integration-orchestration/17-VALIDATION.md
@.planning/phases/17-pipeline-integration-orchestration/17-01-SUMMARY.md

<interfaces>
<!-- Plan 17-01 が作った CLI と、agent が出力すべき生JSON契約。 -->

呼び出す CLI（Plan 17-01 成果物）:
  src/scripts/write-news-digest.ts — `npx tsx src/scripts/write-news-digest.ts` で起動。
  exit 0 = キュレーション成功、exit 1 = 失敗（フォールバックHTMLは書き出し済み）。tmp/news-curation.json / tmp/news.json / tmp/meeting-result.json を読み docs/{date}/news-digest.html を書き出す。

news-curator が出力すべき生JSON契約（src/meeting/schemas.ts curatedArticleRawSchema, 201-217行）:
  { "leadIn": "リード文（2-3文）", "articles": [ { "id": "n07", "market": "us"|"japan"|"global", "importance": "high"|"medium"|"low", "commentary": "日本語解説（1-2文）", "tickers": ["NVDA"], "tickerNames": {"NVDA":"NVIDIA"} } ] }
  ※ title/url/source/publishedAt は出力しない（TS側が tmp/news.json から解決 = ID参照方式・URL幻覚防止）
  ※ market は小文字英語 enum、importance も小文字英語 enum（米国株/日本株/グローバル や rank 数値ではない）
  ※ commentary が空の記事は resolveNewsCuration に drop されるため必ず記入させる
  ※ articles は Agent 自身の重要度順で並べる（TS側は再ソートしない）

既存 in-file アンカー（invest.md）:
  - Step 3d: 1542行〜（portfolio-analyst 単体呼び出し 1564行、完了処理/リトライ 1631-1636、metrics start 1546-1553 / end 1642-1650）
  - Step 3c: 1654行〜（generate-report.ts 起動 1670-1674、hard-fail 1676-1680、[STEP:report-generation:OK] 1720）
  - Step 4: 1725行
  - タイミング表: 1878-1893行（ポートフォリオ分析 1889 / レポート生成 1890 / Step 4 デプロイ 1891）
  - 5体並列ヘッダ前例: 「以下5つの Agent ツールを同時に（1つのメッセージで並列）呼び出してください:」（148/519/899/1347行）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 3d に news-curator を2体目の並列Agentとして追加する（D-01〜D-07）</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md（1542-1650行: Step 3d 全体。portfolio-analyst のヘッダ・プロンプト・完了処理/リトライ・metrics ブロックの現状を必ず確認してから編集する）
    - .claude/commands/invest.md（148行: 「以下N つの Agent ツールを同時に（1つのメッセージで並列）呼び出してください:」の並列ヘッダ前例）
    - src/meeting/schemas.ts（201-217行: news-curator が出力すべき生JSON契約の正確なフィールド名と enum 値）
    - .planning/phases/17-pipeline-integration-orchestration/17-PATTERNS.md（222-281行: Step 3d 拡張の analog と file-handoff/retry テンプレ）
    - .planning/phases/17-pipeline-integration-orchestration/17-CONTEXT.md（D-01〜D-07）
  </read_first>
  <action>
    Step 3d（1542-1650行）を編集する。1564行の `**1つの Agent ツールを呼び出してください:**` を `**以下2つの Agent ツールを同時に（1つのメッセージで並列）呼び出してください:**` に変更し、既存 portfolio-analyst ブロック（name/model/prompt、1566-1629行）はそのまま保持する。その直後に2体目として news-curator ブロックを追加する: name=`news-curator`、model=`opus`（D-02）。

    news-curator の prompt は以下を満たす文面を新規に書く（Claude's Discretion だが D-03〜D-06 は必須）:
    - 役割: 個人投資家（保有ポートフォリオを持つ読者）のための編集者として、記事プールから重要記事10〜15件を厳選しダイジェストを編む
    - 入力プール: 事前に Read で読み込む `tmp/news.json` の各記事から **URL以外の6フィールドのみ**（id, title, summary, source, publishedAt, ticker）をプロンプトに埋め込む（D-03。url と category は含めない）
    - 選定・重要度: 市場全体へのインパクトを主軸に high/medium/low を判定。**ポートフォリオ保有銘柄・監視中銘柄に直接関係するニュースは優先度を上げる**（D-05）
    - 市場分類の例示（D-04）: Fed金融政策・米経済指標→us / 日銀・円相場→japan / 原油・地政学・世界経済→global / 個別企業は上場市場で判定
    - tickerNames の会社名は **英語正式名で統一**（例: NVDA→NVIDIA、カタカナ不可）（D-06）
    - 出力: JSONのみ出力の定型指示（portfolio-analyst の 1592-1595行の文言を流用）を付け、出力契約は `{ "leadIn": "...", "articles": [ { "id", "market"(us|japan|global), "importance"(high|medium|low), "commentary"(日本語1-2文), "tickers":[...], "tickerNames":{sym:英語社名} } ] }`。title/url/source/publishedAt は出力しないこと、market/importance は小文字英語 enum のみ、commentary は各記事必須（空だと落ちる）、articles は重要度順、leadIn はトップレベル必須（欠落すると空になる）を明記する。

    Step 3d 完了処理（1631-1636行）を拡張する: 保存先バレットに `news-curator` の出力 → `/Users/arai/invest/tmp/news-curation.json` を追加。portfolio-analyst のリトライ段落（1636行）と同一テンプレートで news-curator 用のリトライ段落を追加する（D-07: 「出力が有効な JSON でない場合は、エージェントを1回リトライしてください。2回目も失敗した場合は『警告: ニュースキュレーションの生成に失敗しました。フォールバック表示で続行します。』とユーザーに表示して続行してください（tmp/news-curation.json を作成しない）。」）。独自のリトライ機構を発明しない。

    metrics について: news-curator は portfolio-analyst と同じ並列窓で走る（D-01）ため、既存 `m.portfolioStart`/`m.portfolioEnd`（1551/1647行）が並列窓の実時間を既にカバーする。二重計上を避けるため、この並列窓に新しい serial 計測キーは追加しない（news-digest 固有の計測は Step 3e で行う）。
  </action>
  <verify>
    <automated>grep -q "news-curator" .claude/commands/invest.md && grep -q "以下2つの Agent ツールを同時に" .claude/commands/invest.md && grep -q "tmp/news-curation.json を作成しない" .claude/commands/invest.md && echo STEP3D_OK</automated>
  </verify>
  <acceptance_criteria>
    - invest.md が `news-curator` を含み、`model: ` の直近に `opus` を伴う
    - Step 3d のAgentヘッダがN=1からN=2の並列ヘッダに変わっている（`grep -q "以下2つの Agent ツールを同時に（1つのメッセージで並列）呼び出してください" .claude/commands/invest.md`）
    - news-curator prompt が市場enumの小文字英語（`us`,`japan`,`global`）と importance（`high`,`medium`,`low`）を指定している（`grep -q '"us".*"japan".*"global"\|us/japan/global\|us|japan|global' .claude/commands/invest.md` 相当で確認）
    - news-curator prompt が英語正式社名（例: NVIDIA）指定と、URL/titleを出力しないID参照方式を明記している
    - 完了処理に `tmp/news-curation.json` への保存バレットと、D-07 のリトライ→未作成続行テキスト「tmp/news-curation.json を作成しない」が存在する
    - portfolio-analyst の既存 prompt/保存/metrics は改変されていない（`grep -q "tmp/portfolio-analysis.json" .claude/commands/invest.md`）
  </acceptance_criteria>
  <done>Step 3d が portfolio-analyst と news-curator を2体並列で起動し、news-curator 出力が tmp/news-curation.json に保存・リトライ処理される</done>
</task>

<task type="auto">
  <name>Task 2: Step 3e を新設し write-news-digest.ts を fail-soft 起動・STEPマーカー・metrics/timing を配線する（D-08〜D-11）</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md（1654-1725行: Step 3c の generate-report.ts 起動・hard-fail・[STEP:report-generation:OK]・Step 4 見出しの位置を確認。Step 3e はこの間に挿入する）
    - .claude/commands/invest.md（1676-1680行: 流用してはいけない hard-fail「パイプラインを停止してください」+ [PIPELINE:FAIL] ブロック）
    - .claude/commands/invest.md（1878-1893行: タイミング表。news-digest 行を追加する箇所）
    - .claude/commands/invest.md（1546-1553行: node -e metrics タイムスタンプブロックの定型）
    - .planning/phases/17-pipeline-integration-orchestration/17-PATTERNS.md（285-343行: Step 3e の analog と「必須の逸脱」文言）
    - .planning/phases/17-pipeline-integration-orchestration/17-CONTEXT.md（D-08〜D-11）
  </read_first>
  <action>
    Step 3c の `[STEP:report-generation:OK]`（1720行）の後、`## Step 4`（1725行）の前に、新しい見出し `### Step 3e: ニュースダイジェスト生成` を挿入する。

    構成:
    1. `node -e` の metrics 定型ブロック（1546-1553行と同形）で `m.newsDigestStart = Date.now();` を記録。
    2. 「ニュースダイジェストを生成中...」とユーザー表示。
    3. Bashコマンド: `cd /Users/arai/invest && npx tsx src/scripts/write-news-digest.ts` を実行。
    4. **fail-soft 分岐（最重要）**: 「スクリプトの終了コードに関わらず、Step 4 へ進んでください（fail-soft, D-09）。終了コードが 0 の場合: `echo '[STEP:news-digest:OK]'`。終了コードが非0の場合: `echo '[STEP:news-digest:FAIL:キュレーション生成またはHTML書き出しに失敗（詳細はログのconsole.error出力を参照）]'`。**`[PIPELINE:FAIL]` は絶対に出力しないこと** — この失敗は既存3レポート・デプロイをブロックしない（OPS-04）。」と明記する。generate-report.ts の「パイプラインを停止してください」文言や `[PIPELINE:FAIL]` を絶対に流用・記述しない（Pitfall 1）。
    5. `node -e` の metrics 定型ブロックで `m.newsDigestEnd = Date.now();` を記録。

    タイミング表（1878-1893行）: レポート生成行（1890行 `m.reportEnd - m.reportStart`）の直後に、Step 3 グループ内（2スペースインデント）で `console.log('  ニュースダイジェスト      ' + fmt(m.newsDigestEnd - m.newsDigestStart));` を追加する（D-11。既存 fmt 呼び出し形と同一）。

    Step 3c の完了確認表示（HTMLレポート生成完了のユーザー向け一覧がある場合）に、4紙目 `news-digest.html` を加える（Claude's Discretion、該当表示が Step 3e 完了後に出るなら Step 3e 側で言及）。既存 `generate-report.ts` 起動・hard-fail・`[STEP:report-generation:*]` は一切改変しない。
  </action>
  <verify>
    <automated>grep -q "Step 3e: ニュースダイジェスト生成" .claude/commands/invest.md && grep -q "npx tsx src/scripts/write-news-digest.ts" .claude/commands/invest.md && grep -q "\[STEP:news-digest:OK\]" .claude/commands/invest.md && grep -q "\[STEP:news-digest:FAIL" .claude/commands/invest.md && grep -q "newsDigestStart" .claude/commands/invest.md && ! grep -q "news-digest.*PIPELINE:FAIL\|PIPELINE:FAIL.*news-digest" .claude/commands/invest.md && echo STEP3E_OK</automated>
  </verify>
  <acceptance_criteria>
    - invest.md が `### Step 3e: ニュースダイジェスト生成` 見出しを Step 3c の後・Step 4 の前に含む
    - `npx tsx src/scripts/write-news-digest.ts` の起動コマンドが存在する
    - `[STEP:news-digest:OK]` と `[STEP:news-digest:FAIL` の両マーカーが存在する
    - fail-soft 文言「Step 4 へ進んでください」相当が news-digest ブロックに存在し、news-digest 文脈に `[PIPELINE:FAIL]` が **存在しない**（`grep` で news-digest 近傍に PIPELINE:FAIL が出ない）
    - metrics キー `newsDigestStart` と `newsDigestEnd` が存在する
    - タイミング表に `ニュースダイジエスト` 行（`fmt(m.newsDigestEnd - m.newsDigestStart)`）が追加されている
    - 既存 `[STEP:report-generation:OK]` と generate-report.ts 起動行が無改変で残っている
  </acceptance_criteria>
  <done>Step 3e が write-news-digest.ts を fail-soft 起動し、exit code に応じた専用STEPマーカーを出し、metrics/timing に計測が加わる。パイプラインは失敗時も Step 4 へ継続する</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: /invest ライブ実行と fail-soft の人手検証（Success Criteria 3・4）</name>
  <what-built>
    Step 3d に news-curator（opus, 2体並列）を追加し、Step 3e で write-news-digest.ts を fail-soft 起動して news-digest.html を4紙目として生成、`[STEP:news-digest:OK]`/`[STEP:news-digest:FAIL:...]` マーカーを出すよう invest.md を配線した。プロセス境界分離により、キュレーション失敗は既存3レポート・デプロイをブロックしない。
  </what-built>
  <how-to-verify>
    自動確認できない実パイプライン挙動（Claude subagent 呼び出し・実ログへのマーカー到達）を検証します。以下を順に確認してください:

    1. **スクリプト単体 fail-soft スモーク（安全・git push なし）**: 既存の `tmp/meeting-result.json` と `tmp/news.json` がある状態で、`tmp/news-curation.json` を意図的に不正JSON（または削除）にして `cd /Users/arai/invest && npx tsx src/scripts/write-news-digest.ts; echo "exit=$?"` を実行。期待: `exit=1`、かつ `docs/<date>/news-digest.html` がフォールバックページとして存在する（`test -f`）。3レポート（daily-report/meeting-minutes/portfolio-report）の既存ファイルが変化していないこと。
    2. **正常系スモーク**: `tmp/news-curation.json` を有効な生JSON（id が tmp/news.json に実在、market/importance が小文字英語 enum、commentary 非空、leadIn 非空）にして同コマンドを実行。期待: `exit=0`、news-digest.html に記事カードが描画される。
    3. **ライブ `/invest`（任意・実デプロイを伴う）**: 実際に `/invest` を実行し、(a) `docs/YYYY-MM-DD/news-digest.html` が4紙目として生成され、(b) ログに `[STEP:news-digest:OK]`（または意図的失敗時 `[STEP:news-digest:FAIL:...]`）が記録され、(c) 失敗させても他3レポート生成とデプロイが完了し `[PIPELINE:OK]` になることを確認。
  </how-to-verify>
  <resume-signal>検証結果を報告してください（「approved」または不具合の内容）。ライブ実行を省略した場合はスクリプトスモーク（手順1・2）の結果のみで可。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| news-curator（LLM）→ tmp/news-curation.json | LLM生成JSONがパイプラインのファイル境界を越える |
| write-news-digest.ts プロセス → 既存3レポート/デプロイ | 別プロセスの失敗が他成果物に波及しうる境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Denial of Service | キュレーション失敗による他3レポート・デプロイ阻害 | mitigate | D-10 プロセス境界分離（別 `npx tsx` 起動、generate-report.ts の Promise.all に混ぜない）+ D-09 fail-soft 文言（`[PIPELINE:FAIL]` を出さず Step 4 継続）。既存 report-generation/deploy の hard-fail マーカーを流用しない |
| T-17-05 | Repudiation | 失敗の不可視化 | mitigate | 専用 `[STEP:news-digest:FAIL:理由]` マーカーをログに出す（OPS-04）。run.sh は EXIT_CODE ゲート方式のため FAIL マーカー出力は通知基盤に副作用なく安全 |
| T-17-01 | Tampering | news-curation.json の内容 | mitigate | 保存後の検証は write-news-digest.ts が呼ぶ `validateRawNewsCuration`（Plan 17-01）に委譲。invest.md 側は生出力を tmp/news-curation.json に保存するのみで独自解釈しない |
| T-17-SC | Tampering | npm 依存追加 | accept | 本フェーズは新規パッケージ0件。Package Legitimacy Gate 非該当 |
</threat_model>

<verification>
- `grep` 系アサーション（Task 1/2 の automated verify）が全て pass
- news-digest 文脈に `[PIPELINE:FAIL]` が存在しない（Pitfall 1 回帰なし）
- 既存 `[STEP:report-generation:*]` / generate-report.ts 起動が無改変
- 人手検証（Task 3）でスクリプト fail-soft スモークが exit=1 + フォールバック生成 + 3レポート無影響を示す
</verification>

<success_criteria>
- `/invest` 実行で docs/YYYY-MM-DD/news-digest.html が4紙目として生成される（CURA-01）
- news-curator が tmp/news.json を読み tmp/news-curation.json を ID参照方式で書き出す
- キュレーション失敗時も他3レポート生成・デプロイが完了する（OPS-04）
- 成功時 `[STEP:news-digest:OK]` / 失敗時 `[STEP:news-digest:FAIL:...]` がログに記録される
</success_criteria>

<output>
Create `.planning/phases/17-pipeline-integration-orchestration/17-02-SUMMARY.md` when done
</output>
