---
phase: 17-pipeline-integration-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/write-news-digest.ts
  - src/scripts/write-news-digest.test.ts
autonomous: true
requirements: [CURA-01, OPS-04]

must_haves:
  truths:
    - "正常な tmp/news-curation.json + tmp/news.json + tmp/meeting-result.json で docs/{date}/news-digest.html が書き出される"
    - "tmp/news-curation.json が欠損（ENOENT）でもフォールバックHTMLが書き出され、プロセスは exit code 1 で終了する"
    - "tmp/news-curation.json が不正（構造/enum違反）でもフォールバックHTMLが書き出され、プロセスは exit code 1 で終了する"
    - "スクリプトは検証・ID解決・HTML描画を Phase 15/16 の既存関数に委譲し、独自ロジックを持たない"
  artifacts:
    - path: "src/scripts/write-news-digest.ts"
      provides: "news-digest.html を書き出す fail-soft CLI オーケストレーター（main() を export）"
      min_lines: 40
      contains: "generateNewsDigestHtml"
    - path: "src/scripts/write-news-digest.test.ts"
      provides: "正常/欠損/不正の3シナリオを mocked fs で検証する vitest テスト"
      contains: "news-digest.html"
  key_links:
    - from: "src/scripts/write-news-digest.ts"
      to: "src/meeting/schemas.ts"
      via: "validateRawNewsCuration + resolveNewsCuration の呼び出し"
      pattern: "validateRawNewsCuration|resolveNewsCuration"
    - from: "src/scripts/write-news-digest.ts"
      to: "src/scripts/generate-news-digest.ts"
      via: "generateNewsDigestHtml の呼び出し"
      pattern: "generateNewsDigestHtml"
---

<objective>
`docs/{date}/news-digest.html` を書き出す専用CLIオーケストレーター `src/scripts/write-news-digest.ts` を新規実装する。このスクリプトは Phase 15 の検証・ID解決関数（`validateRawNewsCuration` / `resolveNewsCuration`）と Phase 16 の描画関数（`generateNewsDigestHtml`）を薄く連結するだけの配線コードであり、ビジネスロジックは一切持たない（RESEARCH.md「Don't Hand-Roll」）。

D-08（失敗時もフォールバックページを常に書き出す）と D-10（プロセス境界による fail-soft 分離・exit code を OK/FAIL シグナルにする）を、`generate-report.ts` とは独立した別プロセスとして実現する。既存 `generate-report.ts` は無改修（回帰リスクゼロ）。

Purpose: CURA-01（4紙目の自動生成）の生成本体と、OPS-04（fail-soft）のプロセス境界分離を成立させる。
Output: `src/scripts/write-news-digest.ts`（新規本体）+ `src/scripts/write-news-digest.test.ts`（新規テスト）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pipeline-integration-orchestration/17-CONTEXT.md
@.planning/phases/17-pipeline-integration-orchestration/17-PATTERNS.md
@.planning/phases/17-pipeline-integration-orchestration/17-VALIDATION.md

<interfaces>
<!-- Phase 15/16 の既存契約。executor はこれらを直接呼び出す（再実装禁止）。 -->

From src/meeting/schemas.ts (既存・無改修):
  export function validateRawNewsCuration(data: unknown): RawNewsCuration; // 構造/enum違反で throw する
  export interface NewsArticlePoolEntry { readonly id: string; readonly title: string; readonly url: string; readonly source: string; readonly publishedAt: string; readonly ticker?: string; }
  export function resolveNewsCuration(raw: RawNewsCuration, pool: ReadonlyArray<NewsArticlePoolEntry>, date: string, generatedAt: string): NewsCuration; // throw しない。不明/重複ID・空commentary を drop、15件にソフトクランプ

From src/scripts/generate-news-digest.ts (既存・無改修):
  export function generateNewsDigestHtml(curation: NewsCuration | null, date: string): string; // null で「生成できませんでした」フォールバックページを返す（Phase 16 D-12）

Raw agent 出力契約（src/meeting/schemas.ts curatedArticleRawSchema / rawNewsCurationSchema, 201-217行）:
  { leadIn: string, articles: [ { id: string, market: "us"|"japan"|"global", importance: "high"|"medium"|"low", commentary: string, tickers: string[], tickerNames: Record<string,string> } ] }
  ※ title/url/source/publishedAt は agent 出力に含まれず、resolveNewsCuration が pool から解決する
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: write-news-digest.test.ts に3シナリオの失敗するテストを書く（RED）</name>
  <files>src/scripts/write-news-digest.test.ts</files>
  <read_first>
    - src/scripts/generate-report.test.ts（analog: `vi.mock("node:fs/promises")` セットアップと `describe("3-report output")` ブロックの `main()` 呼び出し + writeFile 呼び出しアサーションの手本、382-509行）
    - src/scripts/generate-news-digest.test.ts（既存の NewsCuration フィクスチャ形状を再利用するため）
    - src/meeting/schemas.ts（201-291行: raw agent 出力契約と resolveNewsCuration の drop 挙動）
    - .planning/phases/17-pipeline-integration-orchestration/17-PATTERNS.md（168-218行: テストファイルの mock 設定・per-test conditional readFile・process.exit spy パターン）
  </read_first>
  <action>
    `src/scripts/write-news-digest.test.ts` を新規作成する。`generate-report.test.ts` と同じ様式で `vi.mock("node:fs/promises", ...)` により `readFile`/`writeFile`/`mkdir` をモックし（`readdir` は不要なので含めない）、`vi.spyOn(process, "exit").mockImplementation((() => {}) as never)` で exit をスパイする。`beforeEach` で writeFile/mkdir/readFile を mockClear、`afterEach` で `vi.restoreAllMocks()`。各テストは `const { main } = await import("./write-news-digest.js")` で対象を読み込み `await main()` を実行する。conditional readFile mock は path 部分文字列（"meeting-result.json" / "news-curation.json" / "news.json"）で分岐させる。

    3つのテストを書く:
    - Test 1「正常系」: meeting-result.json→`{date:"2026-06-24"}`、news-curation.json→有効な raw JSON文字列（leadIn 非空、articles に id="n01"・market="us"・importance="high"・commentary 非空・tickers・tickerNames を1件以上）、news.json→id="n01" を含む pool 配列。アサート: writeFile の呼び出しのうち path が "news-digest.html" と "2026-06-24" を含むものが存在する。process.exit が引数 1 で呼ばれていない（成功パス）。
    - Test 2「curation欠損（ENOENT）」: meeting-result.json は正常、news-curation.json は `mockRejectedValue(new Error("ENOENT"))`。アサート: writeFile が "news-digest.html" を含む path で呼ばれる（D-08: フォールバックを常に書き出す）。process.exit が `1` で呼ばれる。
    - Test 3「curation不正（enum違反）」: meeting-result.json は正常、news-curation.json→`{"leadIn":"x","articles":[{"id":"n01","market":"USA","importance":"high","commentary":"y"}]}`（market が不正enumで validateRawNewsCuration が throw する）、news.json→pool。アサート: writeFile が "news-digest.html" を含む path で呼ばれる。process.exit が `1` で呼ばれる。

    この時点では `write-news-digest.ts` が未実装なので import 失敗によりテストは RED になる。テストファイル内でフィクスチャの date は "2026-06-24"（`generate-report.test.ts` の既存 validMeetingResult と同じ）を使う。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/write-news-digest.test.ts 2>&1 | grep -qE "Cannot find module|failed|FAIL" && echo RED_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/write-news-digest.test.ts が存在し、`vi.mock("node:fs/promises"` と `vi.spyOn(process, "exit"` を含む
    - テストが3つ定義されている: 正常系・curation欠損・curation不正（`grep -c "it(" src/scripts/write-news-digest.test.ts` が 3 以上）
    - `npx vitest run src/scripts/write-news-digest.test.ts` が RED（write-news-digest.ts 未実装のため import 解決失敗で失敗）
    - 各テストが `writeFile` 呼び出しの path 部分文字列 "news-digest.html" をアサートしている
    - 失敗系2テストが `process.exit` の引数 `1` をアサートしている
  </acceptance_criteria>
  <done>3シナリオの失敗テストが存在し、実装未着手で RED になる</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write-news-digest.ts を実装してテストを通す（GREEN）</name>
  <files>src/scripts/write-news-digest.ts</files>
  <read_first>
    - src/scripts/write-news-digest.test.ts（Task 1 で作成したテスト。これを満たす実装を書く）
    - src/scripts/generate-report.ts（analog: import 順・`TMP_DIR`/`DOCS_DIR` 定数・`main()` + `fileURLToPath` エントリポイントガード、1-12行と89-129行）
    - src/scripts/validate-meeting.ts（analog: 最小 exit-code CLI 規約）
    - src/scripts/report-data-loaders.ts（analog: `loadPortfolioAnalysis()` の catch→console.error→null グレースフルパターン、74-82行）
    - src/meeting/schemas.ts（243-291行: resolveNewsCuration は throw しないこと・validateRawNewsCuration は throw することの確認）
    - .planning/phases/17-pipeline-integration-orchestration/17-PATTERNS.md（133-164行: 推奨フルシェイプ）
  </read_first>
  <behavior>
    - 正常系: meeting-result.json から date を読む → news-curation.json を読み validateRawNewsCuration → news.json を pool として読む → resolveNewsCuration → generateNewsDigestHtml(curation, date) → docs/{date}/news-digest.html を書き出す → 正常終了（exit 0）
    - curation欠損/不正: 内側 try の readFile ENOENT または validateRawNewsCuration の throw を catch → generateNewsDigestHtml(null, date) でフォールバックHTMLを書き出す（D-08） → console.error でログ → process.exit(1)（D-10: exit code が OK/FAIL シグナル）
    - 「書き出し成功」と「curation成功」を混同しない（Pitfall 2）: exit code は curation 検証の成否を表し、writeFile の成否ではない
  </behavior>
  <action>
    `src/scripts/write-news-digest.ts` を新規作成する。import は `node:fs/promises` の `readFile`/`writeFile`/`mkdir`、`node:path` の `join`、`node:url` の `fileURLToPath`、`../meeting/schemas.js` の `validateRawNewsCuration`/`resolveNewsCuration` と型 `NewsArticlePoolEntry`、`./generate-news-digest.js` の `generateNewsDigestHtml`。`generate-report.ts` と同じく `const TMP_DIR = join(import.meta.dirname, "../../tmp")` と `const DOCS_DIR = join(import.meta.dirname, "../../docs")` を定義する。

    `export async function main(): Promise<void>` を実装する。まず meeting-result.json を読み `date` を取り出す（date は上流で `validateMeetingResult` 済みの信頼できる値。news-curation.json 由来の値は絶対に date に使わない — 脅威 T-17-03）。`join(DOCS_DIR, date)` を `mkdir(..., { recursive: true })`。内側 `try` で: news-curation.json を readFile（欠損時 ENOENT を throw）→ `validateRawNewsCuration(JSON.parse(...))`（構造/enum違反で throw）→ news.json を readFile して pool として JSON.parse → `resolveNewsCuration(raw, pool, date, new Date().toISOString())` → `generateNewsDigestHtml(curation, date)` → `writeFile(join(dateDir, "news-digest.html"), html, "utf-8")` → console.log で件数を出力。内側 `catch(error)` で: `generateNewsDigestHtml(null, date)` のフォールバックHTMLを `writeFile` で **必ず** 書き出し → `console.error("news-digest fallback:", ...)` → `process.exit(1)`。

    ファイル末尾に `if (process.argv[1] === fileURLToPath(import.meta.url)) { main().catch((error) => { console.error("Fatal error:", error); process.exit(1); }); }` のエントリポイントガードを置く（`main()` を test が import できるよう export を維持）。HTML生成・検証・ID解決は必ず既存関数へ委譲し、独自のHTML文字列組み立てや独自スキーマ・独自 `.find()` ルックアップを書かない。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/write-news-digest.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/write-news-digest.test.ts` が GREEN（3テスト全通過）
    - `npx tsc --noEmit` が型エラー0で終了
    - write-news-digest.ts が `export async function main` を含む
    - write-news-digest.ts が `validateRawNewsCuration`・`resolveNewsCuration`・`generateNewsDigestHtml` を呼び出す（`grep -c "validateRawNewsCuration\|resolveNewsCuration\|generateNewsDigestHtml" src/scripts/write-news-digest.ts` が 3 以上）
    - write-news-digest.ts が catch ブロック内に `generateNewsDigestHtml(null` と `process.exit(1)` の両方を含む（D-08 + D-10）
    - write-news-digest.ts が `fileURLToPath(import.meta.url)` エントリポイントガードを含む
    - `npx vitest run`（全スイート）が回帰なし（180件以上 green）
  </acceptance_criteria>
  <done>3テストが通り、型検査も通る。スクリプトは既存関数への薄いオーケストレーターである</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM出力（news-curation.json）→ TSスクリプト | 信頼できないLLM生成JSONがスクリプトに入る境界 |
| meeting-result.json → docs/ パスセグメント | date 文字列が `join(DOCS_DIR, date)` のパス要素になる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | news-curation.json の構造/enum | mitigate | 検証は必ず `validateRawNewsCuration`（Phase 15 zod）に委譲。独自の緩い検証パスを作らない。不正時は throw → catch → フォールバック |
| T-17-02 | Tampering | 描画するHTML本文 | mitigate | HTMLは必ず `generateNewsDigestHtml`（Phase 16 escapeHtml/safeHref）経由。curation フィールドをファイルパスや文字列にそのまま連結しない |
| T-17-03 | Tampering | `join(DOCS_DIR, date)` のパストラバーサル | mitigate | date は上流で `^\d{4}-\d{2}-\d{2}$` 相当の検証を経た meeting-result.json からのみ取得。LLM生成の news-curation.json から date を導出しない |
| T-17-SC | Tampering | npm 依存追加 | accept | 本フェーズは新規パッケージ0件（RESEARCH.md 確認済み）。Package Legitimacy Gate は非該当 |
</threat_model>

<verification>
- `npx vitest run` 全スイートが green（既存179 + 新規3、回帰なし）
- `npx tsc --noEmit` が型エラー0
- write-news-digest.ts が Phase 15/16 の関数を再実装せず呼び出しのみで構成されている（schemas.ts / generate-news-digest.ts は無改修）
</verification>

<success_criteria>
- `src/scripts/write-news-digest.ts` が存在し、正常系で news-digest.html を書き出し、欠損/不正時にフォールバックHTMLを書き出して exit 1 する
- 3シナリオのユニットテストが green
- 既存テストスイートに回帰がない
</success_criteria>

<output>
Create `.planning/phases/17-pipeline-integration-orchestration/17-01-SUMMARY.md` when done
</output>
