---
phase: 14-report-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/report-charts.ts
  - src/scripts/report-charts.test.ts
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "A sector performance horizontal bar chart renders as an inline SVG string, sorted descending (D-05)"
    - "A VIX line chart renders as an inline SVG string with 20/30 threshold reference lines (D-06)"
    - "Both charts render a fixed data-error copy block when their input array is empty (D-04)"
    - "Both charts scale responsively via viewBox + width:100% with no fixed pixel width on the svg root (D-04)"
  artifacts:
    - path: "src/scripts/report-charts.ts"
      provides: "renderSectorBarChart + renderVixLineChart pure SVG functions + SectorDatum/VixDatum types"
      contains: "export function renderSectorBarChart"
    - path: "src/scripts/report-charts.test.ts"
      provides: "Unit coverage for sort order, colors, empty state, viewBox"
      contains: "renderVixLineChart"
  key_links:
    - from: "src/scripts/report-charts.ts"
      to: "src/scripts/report-utils.ts::escapeHtml"
      via: "import for interpolated text escaping"
      pattern: "import.*escapeHtml.*report-utils"
---

<objective>
Build the inline-SVG chart module (D-04/D-05/D-06) as pure `(data) => string` functions matching
the codebase's existing `formatXxxHtml` convention. No external chart library, offline-viewable
(UI-02, UI-SPEC `Tool: none`).

Purpose: These render functions are the reusable primitives Plan 03 splices into the Daily Report.
Building/testing them in isolation keeps SVG-math correctness verifiable without the report pipeline.
Output: `src/scripts/report-charts.ts` with `renderSectorBarChart` + `renderVixLineChart` (+ their
`SectorDatum` / `VixDatum` interfaces) and `src/scripts/report-charts.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-report-ui/14-RESEARCH.md
@.planning/phases/14-report-ui/14-PATTERNS.md
@.planning/phases/14-report-ui/14-UI-SPEC.md

<interfaces>
<!-- The exact SVG implementations for BOTH functions are written verbatim in 14-RESEARCH.md -->
<!-- "Architecture Patterns → Pattern 1" (sector bar chart AND VIX line chart). Reuse them as-is. -->
Reuse (src/scripts/report-utils.ts): `export function escapeHtml(text: string): string` — the single
sanctioned HTML escaper; import as `import { escapeHtml } from "./report-utils.js";`.

Convention analog (src/scripts/generate-daily-report.ts): every `formatXxxHtml(result): string` is a
pure function, guards empty input with `if (x.length === 0) return ...` first, picks colors via a
ternary, and escapes every interpolated string.

UI-SPEC contract values (14-UI-SPEC.md → Chart Contracts):
- Sector: sort DESC by changePercent; positive `#10b981`, negative `#ef4444`; bar height 24px, gap 8px;
  labels 13px/400 `#e0e0e0`; container full-width (bars scale to width, no fixed px).
- VIX: line stroke `#3b82f6` width 2px; dots r=3px same color; threshold lines dashed `#6b7280` at
  y=20 and y=30 with 13px/400 gray labels "20"/"30"; axis labels 13px/400 `#888`; `viewBox`-based
  (e.g. `viewBox="0 0 600 200"`), `width:100%`, NO fixed pixel width/height on the `<svg>` element.
- Empty state copy: heading "データ取得エラー"; body "{VIX推移 / セクターパフォーマンス}データを取得できませんでした。次回のレポート生成をお待ちください。"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: renderSectorBarChart (horizontal bar chart, D-05)</name>
  <files>src/scripts/report-charts.ts, src/scripts/report-charts.test.ts</files>
  <read_first>
    - .planning/phases/14-report-ui/14-RESEARCH.md (Pattern 1 → renderSectorBarChart full implementation — reuse verbatim)
    - src/scripts/report-utils.ts (escapeHtml signature)
    - src/scripts/generate-daily-report.ts (formatMarketOverviewHtml / formatSectorRecommendationsHtml empty-guard + ternary-color convention)
    - .planning/phases/14-report-ui/14-UI-SPEC.md (Sector Performance chart contract + empty-state copy)
    - .planning/phases/14-report-ui/14-PATTERNS.md (report-charts.ts pattern assignment)
  </read_first>
  <behavior>
    - Given sectors [{sector:"Tech", changePercent:2.1}, {sector:"Energy", changePercent:-1.4}, {sector:"Health", changePercent:0.5}], output orders Tech before Health before Energy (descending), Tech/Health bars use fill "#10b981", Energy bar uses fill "#ef4444".
    - Given sectors [], output contains "データ取得エラー" and "セクターパフォーマンスデータを取得できませんでした" and NO <svg>.
    - Output <svg> root contains `viewBox="0 0` and `width="100%"` and does NOT set a fixed pixel `width="400"` (or any px) attribute on the svg root.
    - A sector name containing "<" is escaped (no raw "<script"-style substring survives).
  </behavior>
  <action>
    Create src/scripts/report-charts.ts. Import `escapeHtml` from "./report-utils.js". Export
    `interface SectorDatum { readonly sector: string; readonly changePercent: number }` and
    `export function renderSectorBarChart(sectors: ReadonlyArray<SectorDatum>): string`. Implement exactly per
    14-RESEARCH.md Pattern 1 sector example: empty guard returns a `<div class="chart-empty">` with heading
    "データ取得エラー" and the sector empty-state body copy; otherwise sort a copy descending by changePercent,
    compute maxAbs, barHeight 24, gap 8, per-row `<rect>` colored `#10b981` (changePercent >= 0) or `#ef4444`,
    a `<text>` label (escapeHtml on sector name) and a signed value label using `.toFixed(2)`, and a root
    `<svg viewBox="0 0 <chartWidth> <svgHeight>" width="100%" role="img" aria-label="セクターパフォーマンス">`.
    Do NOT put a fixed pixel `width`/`height` on the svg root. Create src/scripts/report-charts.test.ts with a
    `describe`/`it` block tagged "sector" asserting: descending order via `indexOf` comparisons on rendered
    sector names, presence of `#10b981` and `#ef4444`, empty array → contains "データ取得エラー", root contains
    `viewBox="0 0` and `width="100%"`, and an escaped "<" input does not appear raw.
  </action>
  <verify>
    <automated>npx vitest run src/scripts/report-charts.test.ts -t "sector"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function renderSectorBarChart" src/scripts/report-charts.ts` succeeds.
    - `grep -q 'from "./report-utils.js"' src/scripts/report-charts.ts` succeeds (escapeHtml imported, not re-implemented).
    - Test asserts indexOf(Tech) < indexOf(Energy) in output (descending order behavior assertion).
    - Test asserts empty-array output contains "データ取得エラー" and no "<svg".
    - Test asserts output contains `viewBox="0 0` and `width="100%"`.
    - `npx vitest run src/scripts/report-charts.test.ts -t "sector"` exits 0.
  </acceptance_criteria>
  <done>renderSectorBarChart + SectorDatum exported; sector tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: renderVixLineChart (line chart with 20/30 thresholds, D-06)</name>
  <files>src/scripts/report-charts.ts, src/scripts/report-charts.test.ts</files>
  <read_first>
    - .planning/phases/14-report-ui/14-RESEARCH.md (Pattern 1 → renderVixLineChart full implementation — reuse verbatim)
    - src/scripts/report-charts.ts (SectorDatum module from Task 1, escapeHtml import already present)
    - .planning/phases/14-report-ui/14-UI-SPEC.md (VIX History chart contract + empty-state copy)
  </read_first>
  <behavior>
    - Given history [{date:"2026-06-01",close:18},{date:"2026-06-02",close:22}], output contains a <polyline> with stroke "#3b82f6" width 2, at least two <circle r="3" fill="#3b82f6">, and two dashed threshold lines stroke "#6b7280" with labels "20" and "30".
    - Given history [], output contains "データ取得エラー" and "VIX推移データを取得できませんでした" and NO <polyline>.
    - Output <svg> root uses viewBox + width="100%" with no fixed pixel width/height on the svg root.
    - First/last date axis labels are escaped via escapeHtml.
  </behavior>
  <action>
    In src/scripts/report-charts.ts add `interface VixDatum { readonly date: string; readonly close: number }` (export it)
    and `export function renderVixLineChart(history: ReadonlyArray<VixDatum>): string`. Implement exactly per
    14-RESEARCH.md Pattern 1 VIX example: empty guard returns `<div class="chart-empty">` with heading "データ取得エラー"
    and the VIX empty-state body copy; otherwise use width 600 / height 200 viewBox, `minV = Math.min(...values, 15)`,
    `maxV = Math.max(...values, 35)` so the 20/30 lines stay visible, a `<polyline>` stroke `#3b82f6` width 2, `<circle>`
    dots r=3 fill `#3b82f6`, a threshold-line helper drawing dashed (`stroke-dasharray="4,4"`) `#6b7280` lines at level 20
    and 30 with gray numeric labels, first/last date axis labels 13px fill `#888` wrapped in escapeHtml, and a root
    `<svg viewBox="0 0 600 200" width="100%" role="img" aria-label="VIX推移">` with no fixed pixel width/height on the svg root.
    Extend src/scripts/report-charts.test.ts with a block tagged "vix" asserting stroke color, both threshold lines +
    "20"/"30" labels, empty array → "VIX推移データを取得できませんでした", and viewBox/width="100%" presence.
  </action>
  <verify>
    <automated>npx vitest run src/scripts/report-charts.test.ts -t "vix"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function renderVixLineChart" src/scripts/report-charts.ts` succeeds.
    - Test asserts output contains `stroke="#3b82f6"` and two occurrences of `stroke="#6b7280"` (threshold lines).
    - Test asserts output contains threshold labels "20" and "30".
    - Test asserts empty-array output contains "VIX推移データを取得できませんでした" and no "<polyline".
    - Test asserts output contains `viewBox="0 0 600 200"` and `width="100%"`.
    - `npx vitest run src/scripts/report-charts.test.ts` (full file) exits 0.
  </acceptance_criteria>
  <done>renderVixLineChart + VixDatum exported; all report-charts tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market data (API-derived) → SVG string | Sector names / dates interpolated into inline SVG markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | HTML/SVG injection via unescaped sector name or date | mitigate | Route every interpolated string through `escapeHtml` (V5, RESEARCH Security Domain); test asserts a "<" input is escaped |
| T-14-04 | Denial of Service | empty/partial data producing NaN SVG coordinates | mitigate | length===0 guard renders the fixed data-error copy block instead of an SVG; polyline math never runs on empty input |
</threat_model>

<verification>
- `npx vitest run src/scripts/report-charts.test.ts` green (sector + vix + empty-state + viewBox cases).
- Both render functions are pure (no fs, no network, no class) — same shape as existing formatXxxHtml functions.
</verification>

<success_criteria>
Two reusable inline-SVG chart renderers exist, match the UI-SPEC contract exactly, degrade to the
fixed "データ取得エラー" copy on empty input, and scale responsively (viewBox + width:100%) for 375px.
</success_criteria>

<output>
Create `.planning/phases/14-report-ui/14-02-SUMMARY.md` when done
</output>
