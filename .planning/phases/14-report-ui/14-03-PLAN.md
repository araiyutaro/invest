---
phase: 14-report-ui
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - src/scripts/generate-daily-report.ts
  - src/scripts/generate-report.ts
  - src/scripts/generate-report.test.ts
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Generated Daily Report HTML contains an inline sector bar chart SVG and a VIX line chart SVG"
    - "When market data is missing, the Daily Report shows the chart data-error copy instead of crashing"
    - "market.json is loaded and threaded from generate-report.ts into generateDailyReportHtml"
  artifacts:
    - path: "src/scripts/generate-daily-report.ts"
      provides: "generateDailyReportHtml with optional marketData param + chart sections"
      contains: "renderSectorBarChart"
    - path: "src/scripts/generate-report.ts"
      provides: "loadMarketData() + market.json threaded to daily report"
      contains: "loadMarketData"
  key_links:
    - from: "src/scripts/generate-report.ts::main"
      to: "src/scripts/generate-daily-report.ts::generateDailyReportHtml"
      via: "4th marketData argument"
      pattern: "generateDailyReportHtml\\("
    - from: "src/scripts/generate-daily-report.ts"
      to: "src/scripts/report-charts.ts"
      via: "renderSectorBarChart / renderVixLineChart import"
      pattern: "report-charts"
---

<objective>
Wire the Plan 02 chart renderers into the Daily Report and close the data-plumbing gap so the
Plan 01 `vixHistory` + existing `sectors` from `tmp/market.json` actually reach the HTML (UI-02).

Purpose: RESEARCH.md finding 3 — `market.json`'s `sectors`/`vixHistory` never reach
`generateDailyReportHtml` today (`MeetingResult` carries no sector/VIX data). This plan loads
`market.json` in `generate-report.ts` and threads it through as a 4th argument.
Output: extended `generateDailyReportHtml` (optional `marketData` param + two chart sections),
`loadMarketData()` in `generate-report.ts`, and new chart test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-report-ui/14-RESEARCH.md
@.planning/phases/14-report-ui/14-PATTERNS.md
@.planning/phases/14-01-SUMMARY.md
@.planning/phases/14-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (src/scripts/report-charts.ts) -->
- `export function renderSectorBarChart(sectors: ReadonlyArray<SectorDatum>): string`
- `export function renderVixLineChart(history: ReadonlyArray<VixDatum>): string`
- `export interface SectorDatum { readonly sector: string; readonly changePercent: number }`
- `export interface VixDatum { readonly date: string; readonly close: number }`

<!-- From src/scripts/generate-daily-report.ts (current) -->
- `export function generateDailyReportHtml(result: MeetingResult, webSearchResults: ReadonlyArray<WebSearchResult>, reevalResults: ReadonlyArray<ReevaluationOutput>): string`
- Composes sections via `const marketSection = formatMarketOverviewHtml(result); ...` then splices `${marketSection}` etc. into a template literal (lines ~208-253). Existing section headers use the `<hr><h2>...</h2>` convention.

<!-- From src/scripts/generate-report.ts (current) -->
- `export function generateHtml(result, webSearchResults, reevalResults)` (line 57) → calls `generateDailyReportHtml(result, webSearchResults, reevalResults)` (line 62)  [KEEP 3-arg — see note below]
- `loadWebSearchResults()` / `loadReevalResults()` (lines 13-55): try/readFile/JSON.parse → catch → return `[]`
- `main()` (line 65): `Promise.all([...loaders])` fan-out (line 71); `const dailyHtml = generateDailyReportHtml(meetingResult, webSearchResults, reevalResults)` (line 83)
- `TMP_DIR` constant is already in scope for the loaders.

<!-- BACKWARD-COMPAT NOTE (verified): generateHtml is called 3-arg in ~8 existing generate-report.test.ts cases. -->
<!-- Therefore generateDailyReportHtml's new marketData param MUST be OPTIONAL with a default -->
<!-- `= { sectors: [], vixHistory: [] }` so generateHtml and existing tests keep compiling/passing unchanged. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional marketData param + chart sections to generateDailyReportHtml</name>
  <files>src/scripts/generate-daily-report.ts, src/scripts/generate-report.test.ts</files>
  <read_first>
    - src/scripts/generate-daily-report.ts (generateDailyReportHtml section-composition + splice pattern, lines ~208-253)
    - src/scripts/report-charts.ts (renderSectorBarChart / renderVixLineChart + SectorDatum/VixDatum from Plan 02)
    - src/scripts/generate-report.test.ts ("Daily Report" describe block, lines ~142-204 — mirror its call style)
    - .planning/phases/14-report-ui/14-PATTERNS.md (generate-daily-report.ts extension guidance)
    - .planning/phases/14-report-ui/14-UI-SPEC.md (chart section titles "セクターパフォーマンス"/"VIX推移")
  </read_first>
  <behavior>
    - generateDailyReportHtml(result, [], [], { sectors:[{sector:"Tech",changePercent:2}], vixHistory:[{date:"2026-06-01",close:18}] }) output contains a "セクターパフォーマンス" section with an <svg> and a "VIX推移" section with an <svg>.
    - generateDailyReportHtml(result, [], [], { sectors:[], vixHistory:[] }) output contains "データ取得エラー" twice (both empty charts) and still returns valid HTML (no throw).
    - generateDailyReportHtml(result, [], []) (3-arg, default) still returns HTML and does not throw (backward compat).
  </behavior>
  <action>
    In src/scripts/generate-daily-report.ts import `renderSectorBarChart`, `renderVixLineChart`, and types `SectorDatum`,
    `VixDatum` from "./report-charts.js". Add a 4th parameter to generateDailyReportHtml:
    `marketData: { sectors: ReadonlyArray<SectorDatum>; vixHistory: ReadonlyArray<VixDatum> } = { sectors: [], vixHistory: [] }`
    (optional with default so existing 3-arg callers in generate-report.ts::generateHtml and existing tests keep working).
    Build `const sectorChartSection` = `<hr><h2>セクターパフォーマンス</h2>` followed by `renderSectorBarChart(marketData.sectors)`
    wrapped in a container `<div class="chart-container">`, and `const vixChartSection` = `<hr><h2>VIX推移</h2>` followed by
    `renderVixLineChart(marketData.vixHistory)` in a container div. Splice both into the returned template literal directly
    after the existing market-overview section, using the same `${...}` splice style as `marketSection`/`sectorSection`.
    In src/scripts/generate-report.test.ts, inside the "Daily Report" describe block, add cases tagged "chart": one calling
    generateDailyReportHtml with a 4th marketData arg containing non-empty sectors+vixHistory (assert both section titles +
    `<svg` present), and one with empty arrays (assert two "データ取得エラー" occurrences).
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts -t "chart"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "renderSectorBarChart" src/scripts/generate-daily-report.ts` and `grep -q "renderVixLineChart" src/scripts/generate-daily-report.ts` succeed.
    - generateDailyReportHtml's marketData param has a default value (3-arg calls still compile).
    - Test asserts non-empty market data output contains "セクターパフォーマンス", "VIX推移", and "<svg".
    - Test asserts empty market data output contains "データ取得エラー" (both charts).
    - `npx vitest run src/scripts/generate-report.test.ts -t "chart"` exits 0.
  </acceptance_criteria>
  <done>generateDailyReportHtml renders both chart sections; chart test cases green; existing 3-arg tests unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Load market.json in generate-report.ts and thread it into main()</name>
  <files>src/scripts/generate-report.ts</files>
  <read_first>
    - src/scripts/generate-report.ts (loadWebSearchResults/loadReevalResults loader pattern lines 13-55; main() Promise.all fan-out line 71; generateDailyReportHtml call line 83)
    - src/data/market.ts (VixHistoryPoint / SectorPerformance shapes written to market.json)
    - .planning/phases/14-report-ui/14-RESEARCH.md (Code Examples → "Loading market.json in generate-report.ts")
    - .planning/phases/14-report-ui/14-PATTERNS.md (generate-report.ts loadMarketData analog)
  </read_first>
  <action>
    In src/scripts/generate-report.ts add an interface `MarketData { readonly sectors: ReadonlyArray<{ sector: string; symbol: string; changePercent: number }>; readonly vixHistory: ReadonlyArray<{ date: string; close: number }> }`
    and an async `loadMarketData(): Promise<MarketData>` following the loadReevalResults try/catch-empty-fallback style: readFile
    `join(TMP_DIR, "market.json")`, JSON.parse, guard `Array.isArray(parsed.sectors) ? ... : []` and same for `vixHistory`, and on any
    error return `{ sectors: [], vixHistory: [] }`. Add `loadMarketData()` to the `Promise.all` fan-out in main() (line ~71) and pass the
    resulting `marketData` as the 4th argument to `generateDailyReportHtml(...)` at line ~83. Do NOT change generateHtml's signature (it
    stays 3-arg and relies on generateDailyReportHtml's default marketData).
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async function loadMarketData" src/scripts/generate-report.ts` succeeds.
    - `grep -q "market.json" src/scripts/generate-report.ts` succeeds.
    - main()'s generateDailyReportHtml call passes a 4th argument (the loaded marketData).
    - generateHtml signature unchanged (still 3 params) — `grep -q "export function generateHtml" src/scripts/generate-report.ts` still matches the 3-param form.
    - `npx vitest run src/scripts/generate-report.test.ts` (full file) exits 0.
  </acceptance_criteria>
  <done>loadMarketData reads market.json with empty-fallback and main() threads it into the Daily Report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/market.json (file) → generate-report.ts | JSON file (possibly missing/partial) parsed and rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Denial of Service | missing/corrupt market.json crashing report generation | mitigate | loadMarketData try/catch returns `{ sectors: [], vixHistory: [] }`; chart renderers show empty-state copy (Plan 02) |
| T-14-06 | Tampering | non-array sectors/vixHistory in market.json | mitigate | `Array.isArray(...)` guards coerce non-array values to `[]` before rendering |
</threat_model>

<verification>
- `npx vitest run src/scripts/generate-report.test.ts` green (chart-present + chart-empty + all existing Daily Report cases).
- Both generateDailyReportHtml call sites in generate-report.ts remain consistent (generateHtml 3-arg via default, main() 4-arg).
</verification>

<success_criteria>
The Daily Report HTML embeds the sector bar chart and VIX line chart from real `market.json` data,
and degrades to the "データ取得エラー" copy when that data is absent (UI-02, success criterion 2).
</success_criteria>

<output>
Create `.planning/phases/14-report-ui/14-03-SUMMARY.md` when done
</output>
