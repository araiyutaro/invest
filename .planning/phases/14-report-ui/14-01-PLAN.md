---
phase: 14-report-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/market.ts
  - src/data/market.test.ts
  - src/scripts/collect-data.test.ts
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "tmp/market.json contains a vixHistory array of {date, close} objects after data collection"
    - "VIX history fetch failure degrades to an empty array without crashing the pipeline"
    - "Existing collect-data test mock stays type-consistent with the new fetchAllMarketData return shape"
  artifacts:
    - path: "src/data/market.ts"
      provides: "fetchVixHistory() + VixHistoryPoint + vixHistory in fetchAllMarketData"
      contains: "export async function fetchVixHistory"
    - path: "src/data/market.test.ts"
      provides: "Unit coverage for VIX mapping/error tolerance"
      contains: "fetchVixHistory"
  key_links:
    - from: "src/data/market.ts::fetchAllMarketData"
      to: "src/data/market.ts::fetchVixHistory"
      via: "Promise.all third element"
      pattern: "fetchVixHistory\\("
---

<objective>
Add 30-day VIX history collection to the market-data layer so the daily pipeline writes a
`vixHistory` field into `tmp/market.json`, satisfying decisions D-06/D-07/D-08 (UI-02).

Purpose: The VIX line chart (Plan 02/03) needs a `{date, close}[]` time series that does not exist
today — `MeetingResult.marketOverview` has no VIX history and `market.json` never carried one.
Output: `fetchVixHistory()` + `VixHistoryPoint` exports in `src/data/market.ts`, `vixHistory`
threaded through `fetchAllMarketData()`, a new `src/data/market.test.ts`, and an updated
`collect-data.test.ts` mock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-report-ui/14-CONTEXT.md
@.planning/phases/14-report-ui/14-RESEARCH.md
@.planning/phases/14-report-ui/14-PATTERNS.md

<interfaces>
<!-- Extracted from src/data/market.ts — executor uses these directly, no exploration needed. -->
Existing exports (src/data/market.ts):
- `export interface SectorPerformance { readonly sector: string; readonly symbol: string; readonly changePercent: number }`
- `export async function fetchMarketIndices(): Promise<ReadonlyArray<MarketIndex>>`
- `export async function fetchSectorPerformance(): Promise<ReadonlyArray<SectorPerformance>>`
- `export async function fetchAllMarketData()` — currently `return { indices, sectors } as const`
- Client init (line 1-3): `import YahooFinance from "yahoo-finance2"; const yahooFinance = new YahooFinance({ suppressNotices: ["yahooSurvey"] });`
- Error-tolerant helper pattern (`fetchQuoteSafe`, lines 55-65): try / catch → `console.error(...)` → return fallback (never throws)

yahoo-finance2 v3.13.2 `chart()` contract (from node_modules types, per RESEARCH.md):
- `chart(symbol, { period1: Date | string | number })` → resolves `{ quotes: Array<{ date: Date; close: number | null }> }`
- `period1: "30d ago"` is INVALID (Pitfall 1) — use an explicit Date
- `quotes[].date` is a `Date` object; `JSON.stringify` would emit a full ISO datetime, not `YYYY-MM-DD` (Pitfall 2)

collect-data.test.ts mockMarketData (lines 10-15) currently: `{ indices: [...], sectors: [] }` — no `vixHistory` key (Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fetchVixHistory + thread vixHistory through fetchAllMarketData</name>
  <files>src/data/market.ts, src/data/market.test.ts</files>
  <read_first>
    - src/data/market.ts (current fetchQuoteSafe / fetchSectorPerformance / fetchAllMarketData implementation and error-tolerance style)
    - src/data/news/finnhub.test.ts (external-API mocking discipline: vi.mock / beforeEach / afterEach reset)
    - src/scripts/collect-data.test.ts (existing mockMarketData shape it must stay consistent with)
    - .planning/phases/14-report-ui/14-RESEARCH.md (Code Examples → "Extending market.ts with VIX history"; Pitfalls 1 and 2)
  </read_first>
  <behavior>
    - Given chart() resolves quotes [{date: Date(2026-06-01T13:30Z), close: 18.2}, {date: Date, close: null}], fetchVixHistory returns [{date: "2026-06-01", close: 18.2}] — the null-close entry is filtered out.
    - Given chart() rejects/throws, fetchVixHistory resolves to [] (does not throw) and logs an error.
    - Each returned date is exactly YYYY-MM-DD (10 chars, no "T", no time component).
    - fetchAllMarketData() resolves an object whose own keys include indices, sectors, AND vixHistory.
  </behavior>
  <action>
    In src/data/market.ts add `export interface VixHistoryPoint { readonly date: string; readonly close: number }`.
    Add `export async function fetchVixHistory(): Promise<ReadonlyArray<VixHistoryPoint>>` that computes
    `const period1 = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000)`, calls
    `yahooFinance.chart("^VIX", { period1 })`, then returns `result.quotes.filter(q => q.close !== null).map(q => ({ date: q.date.toISOString().slice(0, 10), close: q.close as number }))`.
    Wrap in try/catch matching fetchQuoteSafe: on error `console.error("Failed to fetch VIX history:", error)` and `return []`.
    Extend `fetchAllMarketData()` so its `Promise.all` destructures `[indices, sectors, vixHistory]` from
    `[fetchMarketIndices(), fetchSectorPerformance(), fetchVixHistory()]` and returns `{ indices, sectors, vixHistory } as const`.
    Create src/data/market.test.ts mocking the yahoo-finance2 default export as a class whose `chart` method is a `vi.fn()`
    (mock the constructor: `vi.mock("yahoo-finance2", () => ({ default: vi.fn().mockImplementation(() => ({ chart: chartMock, quote: quoteMock })) }))`),
    following the beforeEach/afterEach vi.restoreAllMocks discipline from finnhub.test.ts. Assert: mapping+null-filter, YYYY-MM-DD date shape (no "T"), and chart() rejection → [].
  </action>
  <verify>
    <automated>npx vitest run src/data/market.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function fetchVixHistory" src/data/market.ts` succeeds.
    - `grep -q "vixHistory" src/data/market.ts` succeeds and `fetchAllMarketData` return object literal contains the `vixHistory` key.
    - market.test.ts asserts a chart() rejection resolves to `[]` (behavior assertion present).
    - market.test.ts asserts a returned date string has length 10 and does not contain "T".
    - `npx vitest run src/data/market.test.ts` exits 0.
  </acceptance_criteria>
  <done>fetchVixHistory + VixHistoryPoint exported, fetchAllMarketData returns vixHistory, market.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: Keep collect-data.test.ts mock consistent with new return shape</name>
  <files>src/scripts/collect-data.test.ts</files>
  <read_first>
    - src/scripts/collect-data.test.ts (mockMarketData object, lines ~10-15)
    - src/data/market.ts (updated fetchAllMarketData return shape from Task 1)
    - .planning/phases/14-report-ui/14-RESEARCH.md (Pitfall 6)
  </read_first>
  <action>
    In src/scripts/collect-data.test.ts add `vixHistory: []` as a key to the `mockMarketData` object literal (currently
    holding `indices` and `sectors`) so the mock's inferred shape matches `fetchAllMarketData()`'s updated return type.
    Do not add new assertions; this is a type/shape-consistency edit only.
  </action>
  <verify>
    <automated>npx vitest run src/scripts/collect-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "vixHistory" src/scripts/collect-data.test.ts` succeeds inside the mockMarketData object.
    - `npx vitest run src/scripts/collect-data.test.ts` exits 0.
  </acceptance_criteria>
  <done>mockMarketData includes vixHistory: []; collect-data.test.ts passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance API → market.ts | External API response (VIX quotes) enters the pipeline; may be partial/null |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Denial of Service | fetchVixHistory (chart() failure crashing collect-data) | mitigate | try/catch returns `[]` on any error, matching fetchQuoteSafe; pipeline degrades, never crashes |
| T-14-02 | Tampering | null/NaN closes flowing into downstream chart math | mitigate | filter `q.close !== null` before mapping; only finite numbers stored in vixHistory |
</threat_model>

<verification>
- `npx vitest run src/data/market.test.ts src/scripts/collect-data.test.ts` green.
- `fetchAllMarketData()` return object literal contains `vixHistory`.
</verification>

<success_criteria>
tmp/market.json will carry a `vixHistory: [{date, close}]` array (D-08) once the pipeline runs, and
partial/failed VIX fetches degrade gracefully to an empty array.
</success_criteria>

<output>
Create `.planning/phases/14-report-ui/14-01-SUMMARY.md` when done
</output>
