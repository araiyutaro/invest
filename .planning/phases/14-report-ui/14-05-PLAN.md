---
phase: 14-report-ui
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/report-utils.ts
  - src/scripts/report-utils.test.ts
  - docs/portfolio.html
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "The shared report stylesheet emits a mobile media query so all 3 generated reports reflow at 375px"
    - "Tables scroll horizontally (or stack) at <=768px with no viewport overflow"
    - "Interactive elements meet a 44px tap-target minimum at <=768px"
    - "portfolio.html is mobile-responsive while keeping its green accent theme"
  artifacts:
    - path: "src/scripts/report-utils.ts"
      provides: "generateBaseStyles responsive @media block"
      contains: "max-width: 768px"
    - path: "src/scripts/report-utils.test.ts"
      provides: "Unit coverage that the media query is emitted"
      contains: "generateBaseStyles"
    - path: "docs/portfolio.html"
      provides: "Static responsive media query in portfolio.html"
      contains: "max-width: 768px"
  key_links:
    - from: "src/scripts/report-utils.ts::generateBaseStyles"
      to: "generate-daily-report.ts / generate-meeting-minutes.ts / generate-portfolio-report.ts"
      via: "shared style source consumed by all 3 report generators"
      pattern: "generateBaseStyles"
---

<objective>
Make the three generated report surfaces and the static `portfolio.html` mobile-responsive
(D-09/D-10/D-11): add an `@media (max-width: 768px)` block to the shared `generateBaseStyles()` (which
covers daily-report / meeting-minutes / portfolio-report in one edit) and to `docs/portfolio.html`'s
own inline `<style>`, preserving the existing dark theme (UI-01).

Purpose: `report-utils.ts` is the single shared style source for all 3 report generators, so one
media-query edit there covers 3 of the 5 HTML surfaces; `docs/portfolio.html` has its own static
`<style>` (does not call `generateBaseStyles`) and needs the equivalent block added directly.
Output: responsive `generateBaseStyles`, new `src/scripts/report-utils.test.ts`, responsive
`docs/portfolio.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-report-ui/14-RESEARCH.md
@.planning/phases/14-report-ui/14-PATTERNS.md
@.planning/phases/14-report-ui/14-UI-SPEC.md

<interfaces>
<!-- Responsive CSS snippet is written verbatim in 14-RESEARCH.md → Code Examples → -->
<!-- "Responsive CSS addition to generateBaseStyles (D-09/D-10/D-11)". Reuse it. -->
src/scripts/report-utils.ts:
- `export function generateBaseStyles(accentColor: string): string` (lines 70-168) returns a `<style>...</style>`
  template literal; append the media query just before the closing `</style>`.
- Consumed by generate-daily-report.ts (`#3b82f6`), generate-meeting-minutes.ts (`#f59e0b`), generate-portfolio-report.ts (`#10b981`).

docs/portfolio.html (verified): own inline `<style>` block lines 7-91 (bg `#0f0f1a`, accent `#10b981`,
`.container{max-width:720px}`, `.report-item`/`.report-links` rules); does NOT call generateBaseStyles.

UI-SPEC Mobile Responsive Contract: `@media (max-width: 768px)` → body padding 2rem→1rem, `.container`
max-width 100%; tables at 375px use `overflow-x: auto` OR stacked `display: block` (implementer's
discretion); all interactive tap targets >=44px height; viewport meta already present everywhere.

NOTE (planner decision): the responsive test lives in a NEW file src/scripts/report-utils.test.ts
(NOT extending generate-report.test.ts) to avoid a same-wave file-write collision with Plan 03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add responsive media query to generateBaseStyles + cover it with a test</name>
  <files>src/scripts/report-utils.ts, src/scripts/report-utils.test.ts</files>
  <read_first>
    - src/scripts/report-utils.ts (generateBaseStyles return template, end of the style block lines ~165-167)
    - .planning/phases/14-report-ui/14-RESEARCH.md (Code Examples → responsive CSS snippet)
    - .planning/phases/14-report-ui/14-UI-SPEC.md (Mobile Responsive Contract)
    - .planning/phases/14-report-ui/14-PATTERNS.md (report-utils.ts extension point)
  </read_first>
  <behavior>
    - generateBaseStyles("#3b82f6") output contains "@media (max-width: 768px)".
    - That output contains a table rule with "overflow-x: auto".
    - That output contains "min-height: 44px" for interactive tap targets.
    - The existing dark-theme rules (e.g. "#0f0f1a") are still present (no regression).
  </behavior>
  <action>
    In src/scripts/report-utils.ts, inside generateBaseStyles, append an `@media (max-width: 768px)` block immediately before the
    closing `</style>` of the returned template literal, per the RESEARCH responsive snippet: `body { padding: 1rem; }`,
    `.container { max-width: 100%; }`, `table { display: block; overflow-x: auto; white-space: nowrap; }`, `h1 { font-size: 1.4rem; }`,
    and `.report-links a, summary { min-height: 44px; display: inline-flex; align-items: center; }`. Do not modify any existing
    non-media rules (D-03 preserve). Create src/scripts/report-utils.test.ts importing generateBaseStyles, calling it with "#3b82f6",
    and asserting the output contains "@media (max-width: 768px)", "overflow-x: auto", "min-height: 44px", and still contains "#0f0f1a".
  </action>
  <verify>
    <automated>npx vitest run src/scripts/report-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "max-width: 768px" src/scripts/report-utils.ts` succeeds inside generateBaseStyles.
    - Test asserts output contains "@media (max-width: 768px)", "overflow-x: auto", and "min-height: 44px".
    - Test asserts output still contains "#0f0f1a" (theme not regressed).
    - `npx vitest run src/scripts/report-utils.test.ts` exits 0.
  </acceptance_criteria>
  <done>generateBaseStyles emits the responsive media query; report-utils.test.ts green; 3 report surfaces covered.</done>
</task>

<task type="auto">
  <name>Task 2: Add responsive media query to docs/portfolio.html</name>
  <files>docs/portfolio.html</files>
  <read_first>
    - docs/portfolio.html (inline style block lines 7-91, accent #10b981, REPORT_ENTRIES marker at line 115)
    - .planning/phases/14-report-ui/14-UI-SPEC.md (Mobile Responsive Contract)
    - .planning/phases/14-report-ui/14-PATTERNS.md (portfolio.html shares index.html style vocabulary; OPS-02 commit constraint)
  </read_first>
  <action>
    Add an `@media (max-width: 768px)` block to docs/portfolio.html's inline style block: body padding 1rem, `.container` max-width 100%,
    `table { display: block; overflow-x: auto; white-space: nowrap; }`, and `.report-links a, summary { min-height: 44px; display: inline-flex; align-items: center; }`.
    Preserve the existing `#0f0f1a` background and `#10b981` accent per D-03 (do NOT apply hero/accordion — portfolio.html is out of
    scope for D-01/D-02 per RESEARCH A3). Commit the change (OPS-02 checksum silently reverts uncommitted edits to this file).
  </action>
  <verify>
    <automated>bash -c 'grep -q "max-width: 768px" docs/portfolio.html && grep -q "overflow-x: auto" docs/portfolio.html && grep -q "10b981" docs/portfolio.html'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/portfolio.html` style block contains `@media (max-width: 768px)` and a table `overflow-x: auto` rule.
    - `#10b981` accent and `#0f0f1a` background are still present (D-03 preserved).
    - No hero/accordion markup added to portfolio.html (scope guard).
    - The change is committed (not left uncommitted) so OPS-02 checksum does not revert it.
  </acceptance_criteria>
  <done>docs/portfolio.html is responsive at <=768px while keeping its green-accent dark theme; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/run.sh OPS-02 checksum → docs/portfolio.html | Uncommitted edits to this file are reverted by the next pipeline run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Repudiation | uncommitted manual edit to docs/portfolio.html silently reverted by OPS-02 | accept | Task 2 requires committing the change; documented in action — OPS-02 invariant preserved |
| T-14-11 | Tampering | responsive edit accidentally regressing existing theme rules | mitigate | test asserts "#0f0f1a" still present; media-query is additive, existing rules untouched |
</threat_model>

<verification>
- `npx vitest run src/scripts/report-utils.test.ts` green.
- docs/portfolio.html contains the responsive media query; green accent + dark bg preserved.
- Manual (UI-SPEC): open a generated report and portfolio.html at 375px — no horizontal overflow, tables scroll, links tappable.
</verification>

<success_criteria>
All 5 HTML surfaces reflow cleanly at 375px (3 via generateBaseStyles, index.html via Plan 04,
portfolio.html here), with 44px tap targets and the preserved dark theme (UI-01, D-09/D-10/D-11).
</success_criteria>

<output>
Create `.planning/phases/14-report-ui/14-05-SUMMARY.md` when done
</output>
