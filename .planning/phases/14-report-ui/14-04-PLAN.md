---
phase: 14-report-ui
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/update-index.ts
  - src/scripts/update-index.test.ts
  - docs/index.html
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "index.html renders the newest report as a hero section and older reports grouped into month accordions"
    - "The newest month accordion is open by default; earlier months are collapsed"
    - "Each existing entry keeps its actual link set (2-link older entries are not given fabricated links)"
    - "index.html regeneration is deterministic and preserves the static head/style scaffold"
  artifacts:
    - path: "src/scripts/update-index.ts"
      provides: "Rewritten updateIndexHtml: parse + month-group + hero + accordion render"
      contains: "month-group"
    - path: "src/scripts/update-index.test.ts"
      provides: "Unit coverage for grouping / open attribute / link preservation"
      contains: "details"
    - path: "docs/index.html"
      provides: "Static hero/accordion CSS + responsive media query + region markers"
      contains: ".month-group"
  key_links:
    - from: "src/scripts/update-index.ts::updateIndexHtml"
      to: "docs/index.html region markers"
      via: "regenerate content strictly between REPORT_ENTRIES markers"
      pattern: "REPORT_ENTRIES"
---

<objective>
Redesign `docs/index.html` (D-01/D-02/D-03): newest report as a hero section, older reports grouped
into month-based `<details>` accordions (latest month `open`), pure CSS/HTML (no JS), preserving the
Bloomberg dark theme. This requires rewriting `updateIndexHtml()` from an append-only marker insert
into a parse-regroup-regenerate algorithm (RESEARCH.md finding 1; no in-repo analog — Pattern 2 is
the reference implementation).

Purpose: The current flat, incrementally-appended `<li>` list (74 mixed-format entries) cannot be
month-grouped by CSS alone. Output: rewritten `src/scripts/update-index.ts::updateIndexHtml`, new
`src/scripts/update-index.test.ts`, and static hero/accordion/responsive CSS + region markers in
`docs/index.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-report-ui/14-CONTEXT.md
@.planning/phases/14-report-ui/14-RESEARCH.md
@.planning/phases/14-report-ui/14-PATTERNS.md
@.planning/phases/14-report-ui/14-UI-SPEC.md

<interfaces>
<!-- Reference implementation (parseExistingEntries / groupByMonth / renderAccordion) is written -->
<!-- verbatim in 14-RESEARCH.md → "Pattern 2: Month-grouped accordion regeneration". Reuse it. -->
Current src/scripts/update-index.ts:
- `const MARKER = "<!-- REPORT_ENTRIES -->";`
- `function buildIndexEntry(date): string` (lines 15-24) emits `<li class="report-item">` with `.report-date` + `.report-links` (KEEP these class names).
- `async function updateIndexHtml(date)` (lines 39-52): readFile, dup-date early-return, `content.replace(MARKER, MARKER + entry)`, writeFile. THIS GETS REWRITTEN.
- `async function updatePortfolioHtml(date)` (lines 54-77): KEEP UNCHANGED (portfolio.html is out of scope for hero/accordion per RESEARCH A3; its responsive CSS is Plan 05).
- `main()` (line 79): `Promise.all([updateIndexHtml(date), updatePortfolioHtml(date)])`.

Current docs/index.html (verified): `<style>` block lines 7-58 (bg `#0f0f1a`, accent `#3b82f6`,
`.container{max-width:720px}`, `.report-item`/`.report-date`/`.report-links` rules); single
`<!-- REPORT_ENTRIES -->` marker at line 64 followed by ~74 flat `<li class="report-item">` entries;
NO closing marker exists yet.

UI-SPEC contract: accordion summary label `{YYYY}年{M}月 ({N}件)` e.g. "2026年7月 (3件)"; hero headline
Display 28px/700; `<summary>` Heading 20px/700.

DECISION D-04-A (planner): source entries by RE-PARSING the existing docs/index.html `<li>` blocks
(RESEARCH recommendation (a) full-regen), NOT readdir(docs/). Rationale: self-contained,
unit-testable via a readFile mock, idempotent. Resolves RESEARCH Open Question #1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite updateIndexHtml into parse + month-group + hero + accordion</name>
  <files>src/scripts/update-index.ts, src/scripts/update-index.test.ts</files>
  <read_first>
    - src/scripts/update-index.ts (current updateIndexHtml + buildIndexEntry + updatePortfolioHtml)
    - .planning/phases/14-report-ui/14-RESEARCH.md (Pattern 2 reference impl: parseExistingEntries/groupByMonth/renderAccordion; Anti-Patterns: do not assume 3 links; Pitfalls 3 and 5)
    - docs/index.html (current marker + flat entry structure)
    - src/scripts/generate-report.test.ts ("3-report output" describe, node:fs/promises mock pattern)
    - .planning/phases/14-report-ui/14-UI-SPEC.md (accordion summary copy format, hero typography)
  </read_first>
  <behavior>
    - Given index.html with entries across 2 months (2026-07-01 with 3 links, 2026-06-10 with only 2 links) plus a new date 2026-07-02, output contains exactly 2 details blocks with class month-group; the 2026-07 block has " open", the 2026-06 block does NOT.
    - The 2026-06-10 (2-link) entry retains exactly its 2 original anchor hrefs — no fabricated 3rd link.
    - The newest entry (2026-07-02) also appears in a hero block (class "hero") containing that date.
    - Running updateIndexHtml twice on the same input yields byte-identical output (deterministic ordering).
    - Content outside the REPORT_ENTRIES region (the head/style scaffold) is unchanged.
  </behavior>
  <action>
    Rewrite `updateIndexHtml(date)` in src/scripts/update-index.ts to: (1) readFile docs/index.html; (2) parse all existing
    `<li class="report-item">` blocks via regex into objects `{ date, links: {href,label}[] }`, preserving each entry's ACTUAL link
    set (do not assume 3 links — RESEARCH Anti-Pattern); (3) merge the new `date` (build its entry with the standard 3 report links)
    and de-duplicate by date (new date wins if already present); (4) group by `date.slice(0,7)`, sort months descending, and within
    each month sort entries by date descending; (5) render the single newest entry as a hero block using a `hero` CSS class (Display
    headline showing the latest date + its report links), and render one details block per month using
    `class="month-group"` with ` open` only when it is index 0 after the descending sort, and a summary text of the form
    `{y}年{Number(mo)}月 ({items.length}件)`; (6) escapeHtml all interpolated dates/labels; (7) replace ONLY the content strictly
    between `<!-- REPORT_ENTRIES -->` and `<!-- /REPORT_ENTRIES -->` with hero + accordions, preserving all markup outside the markers,
    then a single writeFile (Pitfall 3 — no partial/streaming writes). Keep updatePortfolioHtml unchanged. Add helper functions
    parseExistingEntries, groupByMonth, renderAccordion, renderHero. Create src/scripts/update-index.test.ts mocking node:fs/promises:
    readFile resolves a fixture index.html containing both markers and entries across 2+ months including one 2-link entry; assert the
    behaviors above via the writeFile mock's captured argument.
  </action>
  <verify>
    <automated>npx vitest run src/scripts/update-index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "month-group" src/scripts/update-index.ts` and `grep -q "hero" src/scripts/update-index.ts` succeed.
    - Test asserts exactly one details block with " open" for the newest month and none for older months.
    - Test asserts the 2-link fixture entry's two hrefs are preserved and no 3rd (portfolio) href was fabricated for it.
    - Test asserts hero block contains the latest date.
    - Test asserts two invocations on identical input produce identical writeFile output (determinism).
    - `npx vitest run src/scripts/update-index.test.ts` exits 0.
  </acceptance_criteria>
  <done>updateIndexHtml regenerates hero + month accordions deterministically between region markers; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add hero/accordion + responsive CSS and region markers to docs/index.html</name>
  <files>docs/index.html</files>
  <read_first>
    - docs/index.html (existing style block lines 7-58 and REPORT_ENTRIES marker at line 64)
    - .planning/phases/14-report-ui/14-UI-SPEC.md (Typography/Color/Spacing/Mobile Responsive contract)
    - .planning/phases/14-report-ui/14-PATTERNS.md (existing index.html style vocabulary; OPS-02 commit constraint)
  </read_first>
  <action>
    Edit docs/index.html static markup: (1) add a closing `<!-- /REPORT_ENTRIES -->` marker immediately after the last existing
    `<li class="report-item">` entry so Task 1's algorithm has a bounded region to rewrite; (2) in the inline style block add `.hero`
    rules (Display headline 28px/700, accent `#3b82f6` bottom border, md/lg spacing per UI-SPEC 8-pt scale), `.month-group` and
    `.month-group > summary` rules (summary Heading 20px/700, panel background `#1e1e2e`, summary min-height 44px, cursor pointer), and
    an `@media (max-width: 768px)` block (body padding 1rem, `.container` max-width 100%, Display font-size 22px, Heading font-size 18px,
    `.report-links a` and `summary` min-height 44px with inline-flex align-items center). Preserve existing `#0f0f1a` background,
    `#3b82f6` accent, `.container` max-width 720px, and the existing `.report-item`/`.report-date`/`.report-links` rules per D-03.
    Commit the change normally (OPS-02 checksum in scripts/run.sh silently reverts any uncommitted edit to this file).
  </action>
  <verify>
    <automated>bash -c 'grep -q "/REPORT_ENTRIES" docs/index.html && grep -q "max-width: 768px" docs/index.html && grep -q "month-group" docs/index.html && grep -q "hero" docs/index.html && grep -q "0f0f1a" docs/index.html && grep -q "3b82f6" docs/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/index.html` contains a `<!-- /REPORT_ENTRIES -->` closing marker.
    - `docs/index.html` style block contains `.hero`, `.month-group`, and `@media (max-width: 768px)`.
    - Existing theme tokens `#0f0f1a` and `#3b82f6` are still present (D-03 preserved).
    - The change is committed (not left uncommitted) so OPS-02 checksum does not revert it.
  </acceptance_criteria>
  <done>docs/index.html has bounded region markers and hero/accordion/responsive CSS; theme preserved; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/index.html (prior output) → update-index.ts | The script re-parses its own previously generated HTML back into structured data |
| scripts/run.sh OPS-02 checksum → docs/index.html | Uncommitted edits to this file are reverted by the next pipeline run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | HTML injection via unescaped date/label during regeneration | mitigate | escapeHtml on every interpolated date/label in hero + accordion render |
| T-14-08 | Tampering | non-deterministic Map/iteration order committing broken/churning HTML (Pitfall 3) | mitigate | explicit descending `.sort()` on months and within-month dates; single writeFile; determinism test |
| T-14-09 | Repudiation | uncommitted manual edit to docs/index.html silently reverted by OPS-02 | accept | documented in action; Task 2 requires committing the change — no bypass of the OPS-02 invariant |
</threat_model>

<verification>
- `npx vitest run src/scripts/update-index.test.ts` green.
- docs/index.html region markers + hero/accordion/responsive CSS present; dark theme tokens preserved.
- Manual (UI-SPEC): open a regenerated index.html at 375px — no horizontal overflow, accordions tap-friendly.
</verification>

<success_criteria>
index.html shows the latest report as a hero and older reports in month accordions (latest open),
built with pure CSS/HTML, on the preserved-but-modernized Bloomberg dark theme (UI-01, D-01/D-02/D-03).
</success_criteria>

<output>
Create `.planning/phases/14-report-ui/14-04-SUMMARY.md` when done
</output>
