---
phase: 08-news-filter-module
plan: 02
type: tdd
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/data/news/filter.ts
  - src/data/news/filter.test.ts
  - src/data/news/rss-sources.ts
autonomous: true
requirements:
  - DEDUP-03
  - FILT-01
  - FILT-02
must_haves:
  truths:
    - "スポーツ選手が優勝 はdenylistで除外される"
    - "スポーツ用品株が高騰 はdenylistで除外されない (投資キーワード例外)"
    - "25時間前のpublishedAtを持つ記事が除外される"
    - "23時間前のpublishedAtを持つ記事が残る"
    - "rss-sources.tsに title.slice(0, 50) が存在しない"
  artifacts:
    - path: "src/data/news/filter.ts"
      provides: "filterByRelevance, filterByTime, isDenylisted, filterNewsArticles完全版"
      contains: "DENYLIST_PATTERNS"
    - path: "src/data/news/filter.test.ts"
      provides: "FILT-01, FILT-02, DEDUP-03 全テストケース追加"
      min_lines: 150
    - path: "src/data/news/rss-sources.ts"
      provides: "50文字プレフィックスdedup削除済み"
  key_links:
    - from: "src/data/news/filter.ts"
      to: "DENYLIST_PATTERNS + FINANCIAL_EXCEPTION_KEYWORDS"
      via: "isDenylisted 関数内で両方参照"
      pattern: "DENYLIST_PATTERNS.*some|FINANCIAL_EXCEPTION_KEYWORDS.*some"
    - from: "src/data/news/rss-sources.ts"
      to: "src/data/news/filter.ts"
      via: "dedup責務の移譲 (rss-sources.ts内dedupが削除されfilter.tsに一元化)"
      pattern: "title\\.slice.*0.*50"
---

<objective>
関連性denylistフィルタ・24時間時間フィルタをTDDで実装し、rss-sources.tsの50文字プレフィックスdedupを完全削除する。

Purpose: 非投資記事の除外と古い記事の排除により、アナリストに供給するニュースの品質を向上させる。rss-sources.tsの旧式dedupをfilter.tsに一元化する (D-04)。
Output: filter.ts にフィルタ関数群追加、filter.test.ts にFILT テスト追加、rss-sources.ts からdedup削除。
</objective>

<execution_context>
@/Users/arai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-news-filter-module/08-CONTEXT.md
@.planning/phases/08-news-filter-module/08-RESEARCH.md
@.planning/phases/08-news-filter-module/08-PATTERNS.md
@.planning/phases/08-news-filter-module/08-01-SUMMARY.md

<interfaces>
<!-- Plan 08-01 で作成された型とエクスポート。Executor は 08-01-SUMMARY.md と実ファイルを確認すること。 -->

From src/data/news/types.ts (08-01 で追加):
```typescript
export interface NewsFilterStats {
  readonly raw: number;
  readonly afterUrlDedup: number;
  readonly afterTitleDedup: number;
  readonly afterRelevance: number;
  readonly final: number;
}

export interface NewsFilterResult {
  readonly articles: ReadonlyArray<RawNewsArticle>;
  readonly stats: NewsFilterStats;
}
```

From src/data/news/filter.ts (08-01 で作成):
```typescript
export function filterNewsArticles(
  articles: ReadonlyArray<RawNewsArticle>
): NewsFilterResult;
```

From src/data/news/rss-sources.ts (削除対象 lines 155-161):
```typescript
const seen = new Set<string>();
const deduplicated = allArticles.filter((article) => {
  const key = article.title.slice(0, 50);
  if (seen.has(key)) return false;
  seen.add(key);
  return true;
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN -- FILT-01/02 失敗テスト追加と実装</name>
  <files>src/data/news/filter.test.ts, src/data/news/filter.ts</files>
  <read_first>
    - src/data/news/filter.test.ts (Plan 08-01 で作成済みの全テスト内容)
    - src/data/news/filter.ts (Plan 08-01 で実装済みのdedup関数群)
    - src/data/news/types.ts (NewsFilterStats, NewsFilterResult 型)
    - .planning/phases/08-news-filter-module/08-RESEARCH.md (Pattern 3: denylist + 例外ルール、24hフィルタ実装例)
    - .planning/phases/08-news-filter-module/08-CONTEXT.md (D-05: 除外+例外ルール、D-06: 3ジャンル、D-07: denylistのみ、D-08: 全ソース24h)
  </read_first>
  <behavior>
    - FILT-01: 「プロ野球選手が引退を発表」-> denylist除外される (スポーツカテゴリ)
    - FILT-01: 「人気俳優が結婚を発表」-> denylist除外される (芸能カテゴリ)
    - FILT-01: 「台風15号が関東に接近」-> denylist除外される (天気カテゴリ)
    - FILT-01: 「スポーツ用品株が高騰」-> denylist除外されない (投資キーワード「株」が例外ルールで保護, D-05)
    - FILT-01: 「オリンピック関連銘柄の決算が好調」-> denylist除外されない (「決算」が例外ルールで保護, D-05)
    - FILT-01: 「日銀が金利政策を発表」-> denylist対象外 (政治・社会は除外しない, D-06)
    - FILT-01: 「テスラが新型EVを発表」-> denylist対象外 (テクノロジー記事は除外対象外)
    - FILT-02: publishedAt が25時間前 -> 除外される
    - FILT-02: publishedAt が23時間前 -> 残る
    - FILT-02: publishedAt が new Date(0) (エポック) -> 除外される (フォールバック日時は最古扱い)
    - filterNewsArticles統合: 全4Pass (URL dedup -> Title dedup -> Relevance -> Time) 経由で stats の全フィールドが正しいカウントを返す
  </behavior>
  <action>
    filter.test.ts に describe ブロックを追加する:
    4. "Relevance filter (FILT-01)" -- denylistによる除外テスト6件以上。makeArticle の title を娯楽・スポーツ・天気にし、filterNewsArticles 呼び出し後に result.articles に含まれないことを assert。投資キーワード例外テスト (「スポーツ用品株が高騰」が残る) を含む。
    5. "Time filter (FILT-02)" -- 25h前記事除外、23h前記事残留、エポック日付記事除外の3テスト。publishedAt を new Date(Date.now() - N * 60 * 60 * 1000) で生成。
    6. "filterNewsArticles integration" -- 全4Passを通るテスト。重複URL記事・類似タイトル記事・denylist該当記事・古い記事を混合した入力で、stats の raw, afterUrlDedup, afterTitleDedup, afterRelevance, final が正しいことを assert。

    テストを実行し FILT テストが FAIL であることを確認する (RED)。

    次に filter.ts を修正する:

    DENYLIST_PATTERNS 定数を定義: ReadonlyArray[RegExp] で娯楽・スポーツ・天気の3カテゴリ (D-06)。
    - スポーツ: /スポーツ|野球|サッカー|テニス|競馬|ゴルフ|バスケ|オリンピック/
    - 芸能: /芸能|タレント|俳優|歌手|アイドル|ドラマ|映画|コンサート/
    - 天気: /天気|台風|気象|豪雨|大雪/

    FINANCIAL_EXCEPTION_KEYWORDS 定数を定義: ReadonlyArray[RegExp] で投資関連キーワード (D-05)。
    - /株|株価|上場|決算|業績|利益|売上/
    - /金利|為替|円安|円高|インフレ/
    - /投資|ファンド|ETF|債券|IPO|M&A|買収/

    isDenylisted 関数: タイトルのみを照合対象とする (summaryは対象外 -- RESEARCH.md Pitfall 5)。DENYLIST_PATTERNS.some() でマッチしたら FINANCIAL_EXCEPTION_KEYWORDS.some() で例外チェック。denylist一致 かつ 例外なし -> true (除外)。

    filterByRelevance 関数: articles.filter(a => !isDenylisted(a.title))。

    filterByTime 関数: const oneDayAgo = Date.now() - 24 * 60 * 60 * 1000; articles.filter(a => a.publishedAt.getTime() > oneDayAgo)。finnhub.ts 39-42行目のパターンを踏襲。

    filterNewsArticles 関数を更新: Pass1(URL dedup) -> Pass2(Title dedup) -> Pass3(filterByRelevance) -> Pass4(filterByTime) の全4Passパイプライン。stats に afterRelevance と final を正しいカウントで記録。

    テストを実行して全テスト (DEDUP + FILT) が GREEN であることを確認してコミットする。
  </action>
  <verify>
    <automated>cd /Users/arai/invest && npx vitest run src/data/news/filter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/data/news/filter.test.ts が全テスト PASS
    - filter.ts に DENYLIST_PATTERNS が ReadonlyArray[RegExp] として定義されている
    - filter.ts に FINANCIAL_EXCEPTION_KEYWORDS が ReadonlyArray[RegExp] として定義されている
    - filter.ts に isDenylisted 関数が存在し、title のみを照合対象にしている
    - filter.ts の filterByTime が Date.now() - 24 * 60 * 60 * 1000 を使用している
    - filter.ts の filterNewsArticles が Pass1->Pass2->Pass3->Pass4 の順で実行している
    - filter.test.ts に "FILT-01" を含む it ブロックが4件以上、"FILT-02" が3件以上存在する
    - コミットメッセージが "feat(08-02): implement relevance and time filters" 形式
  </acceptance_criteria>
  <done>全FILT テストがグリーン。denylist+例外ルールフィルタと24h時間フィルタがテストで保証済み。filterNewsArticles が全4Passパイプラインを実行する</done>
</task>

<task type="auto">
  <name>Task 2: rss-sources.ts の50文字プレフィックスdedup削除 (DEDUP-03, D-04)</name>
  <files>src/data/news/rss-sources.ts</files>
  <read_first>
    - src/data/news/rss-sources.ts (現在の fetchAllRssNews 関数全体、特に 150-166行目)
    - .planning/phases/08-news-filter-module/08-PATTERNS.md (削除対象ブロック lines 155-161、削除後のコード)
  </read_first>
  <action>
    rss-sources.ts の fetchAllRssNews 関数内から50文字プレフィックスdedup ブロック (155-161行目付近) を完全に削除する (per D-04)。具体的には:

    1. 以下の7行を削除する:
       - const seen = new Set[string]();
       - const deduplicated = allArticles.filter((article) => {
       -   const key = article.title.slice(0, 50);
       -   if (seen.has(key)) return false;
       -   seen.add(key);
       -   return true;
       - });

    2. return 文を修正する:
       - 変更前: return deduplicated.sort(...)
       - 変更後: return allArticles.sort((a, b) => b.publishedAt.getTime() - a.publishedAt.getTime())
       allArticles を直接ソートして返す。

    削除後、grep で "title.slice(0, 50)" と "seen.has" が rss-sources.ts に存在しないことを確認する。npm test で全テストスイートがパスすることを確認してコミットする。
  </action>
  <verify>
    <automated>cd /Users/arai/invest && grep -c "title.slice(0, 50)" src/data/news/rss-sources.ts && echo "FAIL: dedup still exists" || echo "OK: dedup removed" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "title.slice(0, 50)" src/data/news/rss-sources.ts が 0 を返す
    - grep -c "seen.has" src/data/news/rss-sources.ts が 0 を返す
    - rss-sources.ts の fetchAllRssNews が allArticles を直接 sort して return している
    - npm test が全体で PASS する
    - コミットメッセージが "refactor(08-02): remove 50-char prefix dedup from rss-sources" 形式
  </acceptance_criteria>
  <done>rss-sources.ts の50文字プレフィックスdedupが完全削除され、dedup責務が filter.ts に一元化されている。全テストがグリーン</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSS/API -> filter.ts | filter.ts は内部信頼データ (RawNewsArticle[]) のみ受け取る。外部入力の直接処理なし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | DENYLIST_PATTERNS regex | accept | 全パターンは単純なキーワード OR 一致。バックトラッキングなし。入力は最大160タイトル。ReDoS リスクなし |
| T-08-05 | Denial of Service | filterByTime Date.now() | accept | Date.now() は1回呼び出しのみ。NTPジャンプによる影響は単一パイプライン実行では無視できる (performance.now() はPhase 10で計測用に使用) |
| T-08-06 | Repudiation | rss-sources.ts dedup削除 | accept | 削除はgitコミットで追跡可能。既存テストスイートが回帰を検出 |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | このプランでは新規パッケージのインストールなし |
</threat_model>

<verification>
npx vitest run src/data/news/filter.test.ts -- 全テストPASS (DEDUP + FILT)
npm test -- 全体テストスイートPASS
grep -c "title.slice(0, 50)" src/data/news/rss-sources.ts -- 0 (削除確認)
grep -c "seen.has" src/data/news/rss-sources.ts -- 0 (削除確認)
grep -c "DENYLIST_PATTERNS" src/data/news/filter.ts -- 1以上
grep -c "filterByTime" src/data/news/filter.ts -- 1以上
</verification>

<success_criteria>
1. filter.test.ts に FILT-01/02 の全テストケースが存在し、全てグリーン
2. filter.ts が denylist+例外ルールフィルタと24h時間フィルタを実装している
3. filterNewsArticles が 4-Pass パイプライン (URL dedup -> Title dedup -> Relevance -> Time) を実行する
4. rss-sources.ts の50文字プレフィックスdedupが完全削除されている
5. npm test が全体でPASS
</success_criteria>

<output>
Create `.planning/phases/08-news-filter-module/08-02-SUMMARY.md` when done
</output>
