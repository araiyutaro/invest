---
phase: 29-daily-tracking-data-supply
plan: 03
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - .claude/commands/invest.md
autonomous: false
requirements: [OPS-06, TRAC-01, TRAC-02, TRAC-03]
tags: [watchlist, pipeline, invest-md, step-wiring, fail-soft]

must_haves:
  truths:
    - "invest.md に Step 2i（追跡データ供給）が Step 2h（ウォッチリスト更新）直後・Step 3（レポート系）より前に配線されている（D-01）"
    - "Step 2i は終了コードに関わらず次ステップへ進む fail-soft 配線で、スクリプト自身の [STEP:watchlist-data:*] を尊重し追加 echo しない（D-03, Step 2h 踏襲）"
    - "Step 2i の失敗が既存4レポートの生成・デプロイをブロックしない（OPS-06）"
    - "Step 3.0 / Step 3-P の rm -rf クリーンアップ対象に tmp/watchlist-*.json を追加しない（Pitfall 5: ルート直下 tmp/*.json は上書き運用）"
  artifacts:
    - ".claude/commands/invest.md — Step 2i セクション（collect-watchlist-data.ts 実行の fail-soft 配線）"
  key_links:
    - "Step 2i の入力（data/watchlist.json = Step 2h 更新済み / tmp/news.json = Step 1 生成 / tmp/technicals.json = Step 2b 生成）が全て Step 2i より前に存在する順序が構造的に保証される"
    - "Step 2i の出力（tmp/watchlist-technicals.json / tmp/watchlist-news.json）が Phase 30 の判定ステップの入力になる下流契約"
---

<objective>
Phase 29 の CLI（29-02 成果物 `src/scripts/collect-watchlist-data.ts`）を日次パイプライン `.claude/commands/invest.md` に新 Step 2i として配線する。Step 2h（ウォッチリスト更新）直後・Step 3（WebSearch リサーチ & レポート生成）より前に配置し（D-01）、Phase 30 の判定ステップが常に当日確定済みのウォッチリストに対する追跡データを参照できる順序を構造的に保証する。

Step 2h（write-watchlist.ts の配線）と同一の fail-soft 様式を踏襲する:
- 終了コードに関わらず次ステップへ進む（D-03）
- スクリプト自身が stderr へ出す `[STEP:watchlist-data:OK]` / `[STEP:watchlist-data:FAIL:<reason>]` をそのまま尊重し、invest.md 側で追加 echo しない
- `[PIPELINE:FAIL]` は絶対に出力しない（OPS-06）
- このステップの失敗は既存4レポートの生成・デプロイを一切ブロックしない

Purpose: OPS-06 のパイプライン継続契約を配線レベルで満たし、TRAC-01/02/03 のデータ供給を日次自動実行に組み込む。

Output: `.claude/commands/invest.md` に Step 2i セクションを挿入。
</objective>

<artifacts_this_phase_produces>
**このプラン（29-03）で新規作成:**
- invest.md 内の新セクション「### Step 2i: 追跡データ供給（ウォッチリスト銘柄）」
- 実行コマンド `cd /Users/arai/invest && npx tsx src/scripts/collect-watchlist-data.ts`

**依存する既存/前プラン成果物（無改変）:** Step 2h セクション（配線様式の手本）、Step 2b（tmp/technicals.json 生成源）、Step 1（tmp/news.json 生成源）、collect-watchlist-data.ts（29-02 成果物）。
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-daily-tracking-data-supply/29-CONTEXT.md
@.planning/phases/29-daily-tracking-data-supply/29-RESEARCH.md
@.claude/commands/invest.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: invest.md に Step 2i（追跡データ供給）を fail-soft で配線する</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md（Step 2h セクション 1251-1276行 = 配線様式の手本。Step 2b 419-446行 = collect-technicals 呼び出し様式・tmp/technicals.json 生成箇所。Step 3.0 1283-1301行 = rm -rf クリーンアップ scope の確認。Step 3 見出し 1279行 = Step 2i の挿入上限）
    - src/scripts/collect-watchlist-data.ts（29-02 成果物。実行コマンド・入出力ファイル・STEP マーカー様式の確認）
    - .planning/phases/29-daily-tracking-data-supply/29-CONTEXT.md（D-01 実行位置、D-03 STEP マーカー、Integration Points）
    - .planning/phases/29-daily-tracking-data-supply/29-RESEARCH.md（Pitfall 5: tmp クリーンアップ scope はディレクトリのみ、ルート直下 tmp/*.json は上書き運用で明示 rm 不要）
  </read_first>
  <action>
`.claude/commands/invest.md` の Step 2h セクション（`### Step 2h: ウォッチリスト更新` 〜 Step 2h 末尾の fail-soft 説明ブロック、1251-1276行）の直後、かつ `## Step 3: WebSearch リサーチ & レポート生成`（1279行）より前に、新セクション `### Step 2i: 追跡データ供給（ウォッチリスト銘柄）` を Edit で挿入する（既存の Step 2h / Step 3 セクションは変更しない、追記のみ）。

新セクションの内容（Step 2h の構造を踏襲、日本語）:
- ユーザー向け表示文言（例:「ウォッチリスト銘柄の追跡データ（株価・テクニカル・ニュース）を収集中...」）
- 実行 Bash コマンド: `cd /Users/arai/invest && npx tsx src/scripts/collect-watchlist-data.ts`
- fail-soft 配線説明: 終了コードに関わらず次ステップ（Step 3）へ進む（D-03/OPS-06）。collect-watchlist-data.ts は `data/watchlist.json`（Step 2h で当日更新済みのアクティブ銘柄）+ `tmp/technicals.json`（Step 2b 生成の同日キャッシュ）+ `tmp/news.json`（Step 1 生成の記事プール）を入力に、`tmp/watchlist-technicals.json` と `tmp/watchlist-news.json` の2ファイルを出力する。**必ず write-watchlist.ts（Step 2h）より後に実行すること**（D-01、当日確定済みウォッチリストを入力とする前提）。
- STEP マーカー説明: スクリプト自身が stderr に `[STEP:watchlist-data:OK]` / `[STEP:watchlist-data:FAIL:<reason>]` を出力する設計（Phase 29 成果物）。stderr の `[STEP:watchlist-data:*]` 行をそのまま尊重し、invest.md 側で追加 echo しない（Step 2h の write-watchlist.ts と同じ扱い）。
- fail-soft 保証文: このステップの失敗は既存4レポートの生成・デプロイを一切ブロックしない（OPS-06）。`[PIPELINE:FAIL]` は絶対に出力しないこと — 追跡データ供給の失敗は、両出力ファイルを有効な空JSONで縮退させ次のステップへ進む。

**Step 3.0（1291-1295行）および Step 3-P（1323-1327行）の `rm -rf` クリーンアップコマンドは変更しない** — tmp/watchlist-technicals.json / tmp/watchlist-news.json をこれらの rm 対象に**追加しない**（Pitfall 5: 両ファイルはルート直下の tmp/*.json で collect-watchlist-data.ts が毎回 writeFile 上書きするため明示 rm 不要。既存の tmp/technicals.json / tmp/holding-news.json / tmp/portfolio.json と同じ上書き運用）。

Edit は scoped replacement で行う（invest.md 全体の Write は禁止）。挿入は Step 2h 末尾と Step 3 見出しの間に限定する。
  </action>
  <verify>
    <automated>grep -c "### Step 2i: 追跡データ供給" .claude/commands/invest.md</automated>
    <automated>grep -c "collect-watchlist-data.ts" .claude/commands/invest.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Step 2i" .claude/commands/invest.md` が 1（新セクション見出しが1つ存在）
    - `grep -c "collect-watchlist-data.ts" .claude/commands/invest.md` が 1 以上（実行コマンドが存在）
    - source assertion: Step 2i が Step 2h より後・Step 3 より前 — `awk '/### Step 2h:/{h=NR} /### Step 2i:/{i=NR} /## Step 3:/{s=NR} END{print (h<i && i<s) ? "OK" : "BAD"}' .claude/commands/invest.md` が `OK`
    - source assertion: STEP マーカー説明が含まれる — `grep -c "\[STEP:watchlist-data:" .claude/commands/invest.md` が 1 以上
    - source assertion: [PIPELINE:FAIL] を出さない旨の記載 — `grep -c "PIPELINE:FAIL" .claude/commands/invest.md` の該当箇所が Step 2i でも「出力しない」文脈（既存 Step 2h と同一様式）
    - source assertion: Step 3.0/3-P の rm -rf に watchlist-technicals/watchlist-news を追加していない — `grep -c "rm -rf.*watchlist-technicals\|rm -rf.*watchlist-news" .claude/commands/invest.md` が 0（Pitfall 5）
    - source assertion: 既存 Step 2h セクションが無傷 — `grep -c "### Step 2h: ウォッチリスト更新" .claude/commands/invest.md` が 1、`grep -c "write-watchlist.ts" .claude/commands/invest.md` が 1 以上
  </acceptance_criteria>
  <done>invest.md に Step 2i が Step 2h 直後・Step 3 より前に fail-soft 配線され、collect-watchlist-data.ts の実行・入出力・STEP マーカー尊重・パイプライン継続保証が記載されている。Step 3.0/3-P の rm -rf は変更されていない。既存 Step 2h は無傷。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Step 2i 実機実行と既存4レポート継続の human-verify</name>
  <action>実機（launchd 毎朝8時実行 or ローカル手動実行）で Step 2i の動作を確認するヒューマンチェックポイント。Claude 側の自動作業は完了済み（Step 2i 配線 = Task 1）。以下の <how-to-verify> の手順で人間が実機の実行ログと出力ファイルを検証し、既存4レポートの生成・デプロイが継続することを確認する。実機検証が翌朝の launchd 実行待ちになる場合は HUMAN-UAT として追跡する。</action>
  <what-built>invest.md に Step 2i（追跡データ供給）が配線され、collect-watchlist-data.ts が日次パイプラインに組み込まれた。launchd（毎朝8時）またはローカル手動実行で、Step 2h（ウォッチリスト更新）の直後に Step 2i が実行され、tmp/watchlist-technicals.json と tmp/watchlist-news.json が生成され、失敗しても既存4レポートの生成・デプロイが継続することを実機で確認する必要がある（静的解析では確認不能）。</what-built>
  <how-to-verify>
1. ローカルでパイプラインを手動実行するか、次回 launchd 実行（翌朝8時）のログを確認する。
2. launchd ログまたは実行ログで、Step 2h（`[STEP:watchlist:OK]` or `:FAIL:`）の直後に Step 2i が実行され、stderr に `[STEP:watchlist-data:OK]`（または `[STEP:watchlist-data:FAIL:<reason>]`）が出力されていることを確認する。
3. 実行後、`tmp/watchlist-technicals.json` が `{generatedAt, snapshots: [...]}` 形状の有効JSONであること、`tmp/watchlist-news.json` がアクティブ銘柄をキーに持つ有効JSON（HoldingNewsFile）であることを確認する（アクティブ0件の日は両方とも空スナップショット/空マップの有効JSONで正常）。
4. Step 2i のログに `[PIPELINE:FAIL]` が**出ていない**こと、および既存4レポート（daily / portfolio / meeting-minutes / news-digest）が通常どおり docs/{date}/ に生成・デプロイされていることを確認する。
5. （任意）ウォッチリストに複数銘柄がある場合、`⚠ 取得失敗: <tickers>` ログの有無とステップ実行時間を launchd ログで確認し、レート制限の警戒サイン（Pitfall 3）を観測できる形式になっていることを確認する。
  </how-to-verify>
  <resume-signal>「approved」と入力するか、観測された問題（STEP マーカー未出力・出力ファイル不正・レポート生成阻害等）を報告してください。実機検証が翌朝の launchd 実行待ちになる場合は、その旨（HUMAN-UAT として追跡）を報告してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invest.md（オーケストレーション）→ collect-watchlist-data.ts（CLI） | パイプラインステップの起動境界。CLI の終了コードは invest.md 側で無視され、STEP マーカー文字列が状態伝達の正準手段（Phase 28 Step 2h 踏襲） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-07 | Denial of Service | Step 2i の失敗が [PIPELINE:FAIL] に波及し既存4レポート生成・デプロイを阻害 | high | mitigate | Step 2h と同一の fail-soft 配線（終了コード無視・追加 echo なし・[PIPELINE:FAIL] 禁止）。human-verify チェックポイントで既存4レポート継続を実機確認（OPS-06） |
| T-29-08 | Tampering | Step 3.0/3-P の rm -rf に出力ファイルを誤追加し Phase 30 が読む前に削除 | medium | mitigate | Pitfall 5 の明示回避 — rm -rf 対象はディレクトリのみ、ルート直下 tmp/*.json は上書き運用。acceptance_criteria の grep で rm -rf への watchlist-* 追加ゼロを構造的に検証 |
| T-29-09 | Elevation of Privilege | 順序ミス（Step 2h より前に Step 2i を配置）で当日未確定のウォッチリストを参照 | medium | mitigate | D-01 の順序保証。acceptance_criteria の awk 判定（h<i && i<s）で Step 2h < Step 2i < Step 3 を構造的に検証 |
| T-29-SC | Tampering | npm/pip/cargo installs | high | accept | 新規パッケージのインストールなし（invest.md 配線のみ、コード変更なし。Package Legitimacy Gate 非該当） |
</threat_model>

<verification>
- `grep -c "### Step 2i" .claude/commands/invest.md` が 1
- awk 順序判定で Step 2h < Step 2i < Step 3 が `OK`
- rm -rf への watchlist-technicals/watchlist-news 追加ゼロ（Pitfall 5）
- 既存 Step 2h / Step 3 セクション無傷（write-watchlist.ts 記載残存）
- human-verify チェックポイント承認（実機で Step 2i 実行・出力ファイル生成・既存4レポート継続を確認、または HUMAN-UAT 追跡）
</verification>

<success_criteria>
- OPS-06: Step 2i が fail-soft 配線され、失敗しても既存4レポートの生成・デプロイが継続する
- D-01: Step 2i が Step 2h 直後・Step 3 より前に配置される順序が構造的に保証される
- TRAC-01/02/03: 追跡データ供給が日次自動実行パイプラインに組み込まれる
- Pitfall 5 回避: 出力2ファイルが rm -rf 対象に追加されない
- 実機検証（human-verify）で Step 2i の実行と既存4レポート継続が確認される（または HUMAN-UAT として追跡）
</success_criteria>

<output>
Create `.planning/phases/29-daily-tracking-data-supply/29-03-SUMMARY.md` when done
</output>
