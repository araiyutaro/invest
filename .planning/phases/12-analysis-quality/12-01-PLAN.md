---
phase: 12-analysis-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/invest.md
autonomous: true
requirements:
  - ANLQ-01
must_haves:
  truths:
    - "Step 2.0 実行後、tmp/prev-highlighted-stocks.json が前日データありの場合は作成される"
    - "前日データなしの場合、コンソールに「前日データなし」と表示され通常の Round 1 が実行される"
    - "Round 1 の各アナリストプロンプト（全5エージェント）に「## 前日の推奨銘柄」セクションが存在する"
    - "各銘柄の注入フォーマットが ticker, averageScore, verdict, agentScores を含む"
    - "各アナリストへの指示に「前日推奨銘柄への見解変化を明示的に述べること」が含まれる"
    - "前日データなしの場合、Round 1 プロンプトから前日セクションが省略される旨が明記されている"
  artifacts:
    - path: ".claude/commands/invest.md"
      provides: "投資分析パイプライン制御（前日データ注入対応版）"
      contains: "prev-highlighted-stocks.json"
  key_links:
    - from: "invest.md Step 2.0"
      to: "tmp/prev-highlighted-stocks.json"
      via: "Bash node -e でファイル書き出し"
      pattern: "prev-highlighted-stocks"
    - from: "invest.md Step 2a 各エージェントプロンプト"
      to: "tmp/prev-highlighted-stocks.json"
      via: "## 前日の推奨銘柄セクション"
      pattern: "前日の推奨銘柄"
---

<objective>
invest.md の Round 1 パイプラインに前日推奨銘柄データを注入する。

Purpose: アナリストが前日の推奨銘柄を認識した上で本日の分析を行い、見解変化を明示的に発言に含めることで、継続的な議論の流れを構築する（ANLQ-01）。

Output: Step 2.0 に前日データ読み込みロジックが追加され、Step 2a 全5エージェントのプロンプトに「## 前日の推奨銘柄」セクションが含まれた invest.md。
</objective>

<execution_context>
@/Users/arai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/arai/invest/.planning/PROJECT.md
@/Users/arai/invest/.planning/ROADMAP.md
@/Users/arai/invest/.planning/STATE.md
@/Users/arai/invest/.planning/phases/12-analysis-quality/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 2.0 に前日データ読み込みロジックを追加</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    .claude/commands/invest.md の Step 2.0 セクション（「### Step 2.0: 準備」から「### Step 2a:」の直前まで）を完全に読む。特に以下の2箇所の正確な文字列を確認する:
    1. 「mkdir -p /Users/arai/invest/tmp/round-1 /Users/arai/invest/tmp/round-2 /Users/arai/invest/tmp/round-3」を含む Bash コマンドブロック（closing ``` まで）
    2. 「次に、以下のファイルを Read ツールで読み込んでください（後のステップで Agent prompt に埋め込むために必要です）:」という指示文
  </read_first>
  <action>
    Step 2.0 の mkdir コマンドブロック（closing ``` の行）の直後、「次に、以下のファイルを Read ツールで読み込んでください」の指示文の直前に、以下の2要素を挿入する:

    要素 A — 指示文: 「次に、以下のBashコマンドで前日の推奨銘柄データを確認してください:」

    要素 B — Bash コマンドブロック（node -e を使用）の内容:
    - const fs = require('fs')
    - try ブロック: fs.readFileSync('/Users/arai/invest/tmp/meeting-result.json', 'utf-8') を JSON.parse する
    - JSON パース成功かつ highlightedStocks が配列で length > 0 の場合:
      - fs.writeFileSync('/Users/arai/invest/tmp/prev-highlighted-stocks.json', JSON.stringify(prev.highlightedStocks, null, 2)) で前日データを保存する
      - console.log で '[前日データ] ' + prev.highlightedStocks.length + '銘柄: ' + 各銘柄を '{ticker}(スコア:{averageScore}/10, {verdict})' 形式に join してカンマ区切りで表示
    - catch ブロック: console.log('前日データなし')

    要素 C — 条件分岐説明文（Bash ブロックの直後）:
    「`tmp/prev-highlighted-stocks.json` が作成された場合（前日データあり）は Round 1 の各アナリストプロンプトに後述の「## 前日の推奨銘柄」セクションを含めてください。作成されなかった場合（前日データなし）は通常通り Round 1 を実行し、前日セクション全体を省略してください。」

    IMPORTANT（D-03 per CONTEXT.md）: 前日データ不在時はサイレントスキップ。tmp/meeting-result.json が存在しない初回実行を含む全ての欠損ケースで catch に落として「前日データなし」ログのみ表示し処理続行する。エラー終了してはならない。

    挿入後の順序（Step 2.0 内）:
    1. mkdir bash ブロック
    2. [NEW] 前日データ確認 bash ブロック + 条件説明
    3. 「次に、以下のファイルを Read ツールで読み込んでください」（既存、移動しない）
  </action>
  <verify>
    <automated>grep -c "prev-highlighted-stocks" /Users/arai/invest/.claude/commands/invest.md</automated>
  </verify>
  <done>
    - grep コマンドの出力が 2 以上（Bash writeFileSync 内と条件説明文内の最低2箇所）
    - grep "前日データなし" .claude/commands/invest.md に1行ヒット
    - grep "\[前日データ\]" .claude/commands/invest.md に1行ヒット
  </done>
</task>

<task type="auto">
  <name>Task 2: Step 2a 全5エージェントプロンプトに前日データセクションを追加</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    .claude/commands/invest.md の「### Step 2a: Round 1 — 分析プレゼンテーション」セクション全体を読む。5エージェント（fundamentals-r1, tenbagger-r1, macro-r1, technical-r1, risk-manager-r1）の各プロンプトテンプレートのコードブロック内の以下パターンを確認する:
    - 各ブロックに `[tmp/news.json の全内容]` という行が存在する
    - 直後に `以下のJSONフォーマットのみを出力してください。他のテキストは一切出力しないでください。` がある
    - 5エージェントのコードブロックは "agentId": "fundamentals"/"tenbagger"/"macro"/"technical"/"risk-manager" で識別できる
  </read_first>
  <action>
    Step 2a の5つのエージェントブロックそれぞれについて、分析指示コードブロック内（バッククォート3つで囲まれた部分）の `[tmp/news.json の全内容]` の行の直後、かつ `以下のJSONフォーマットのみを出力してください。他のテキストは一切出力しないでください。` の行の直前に、以下の同一テキストを挿入する（5エージェント全てに同じ内容を追加）:

    ---挿入テキスト BEGIN（コードブロック内に挿入）---

    （tmp/prev-highlighted-stocks.json が存在する場合のみ以下を含めること）
    ## 前日の推奨銘柄
    前日のミーティングでチームが注目した銘柄と評価スコアです。本日の市場データ・ニュースを踏まえ、これらの銘柄への見解が変化したかどうかを明示的に述べてください。

    [tmp/prev-highlighted-stocks.json の各銘柄の ticker, averageScore, verdict, agentScores フィールドを全て展開してください。表示例: {ticker}: 前日スコア {averageScore}/10 / 判定: {verdict} / 各エージェントスコア要約: {agentScores の agentRole+score を列挙}]

    前日推奨銘柄について、今日の市場データ・ニュースを踏まえて見解が変化したか明示すること。
    （tmp/prev-highlighted-stocks.json が存在しない場合はこのセクション全体を省略）

    ---挿入テキスト END---

    5エージェントの識別と挿入順序:
    1. fundamentals-r1 ブロック: "agentId": "fundamentals" を含むコードブロック内の当該位置
    2. tenbagger-r1 ブロック: "agentId": "tenbagger" を含むコードブロック内の当該位置
    3. macro-r1 ブロック: "agentId": "macro" を含むコードブロック内の当該位置（マクロは「マクロ経済の視点から」で始まる指示）
    4. technical-r1 ブロック: "agentId": "technical" を含むコードブロック内の当該位置
    5. risk-manager-r1 ブロック: "agentId": "risk-manager" を含むコードブロック内の当該位置

    D-01 per CONTEXT.md: 注入するのは prev-highlighted-stocks.json（= highlightedStocks フィールド）の ticker, averageScore, verdict, agentScores のみ。marketOverview や sectorRecommendations は含めない。
    D-02 per CONTEXT.md: 各アナリストに「前日推奨銘柄への見解変化を明示的に述べること」の指示を追加すること。
  </action>
  <verify>
    <automated>grep -c "前日の推奨銘柄" /Users/arai/invest/.claude/commands/invest.md</automated>
  </verify>
  <done>
    - grep コマンドの出力が 6 以上（Task 1 の条件説明で1箇所 + 5エージェント）
    - grep -c "前日推奨銘柄について" .claude/commands/invest.md の出力が 5
    - grep -c "prev-highlighted-stocks.json が存在する場合のみ" .claude/commands/invest.md の出力が 5
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → tmp/meeting-result.json | 前日実行のパイプライン出力ファイル。自システム生成のため信頼できるが、ファイル破損・不在を想定する |
| tmp/prev-highlighted-stocks.json → Round 1 agents | 抽出・保存した前日データを LLM プロンプトに注入する境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Denial of Service | Step 2.0 Bash での meeting-result.json 読み込み | mitigate | try/catch でファイル不在・JSON 破損を捕捉し「前日データなし」でグレースフルスキップ（D-03） |
| T-12-02 | Tampering | prev-highlighted-stocks.json の内容 | accept | 自システム生成ファイル。外部入力でないため低リスク |
| T-12-03 | Information Disclosure | LLM プロンプトへの前日スコア注入 | accept | 内部投資データ。LLM への送信は既存パイプラインと同一リスクレベル |
</threat_model>

<verification>
フェーズ全体の確認:

```bash
# Task 1 の確認
grep -c "prev-highlighted-stocks" /Users/arai/invest/.claude/commands/invest.md
# 期待値: 2 以上

grep "前日データなし" /Users/arai/invest/.claude/commands/invest.md
# 期待値: 1行ヒット

# Task 2 の確認
grep -c "前日の推奨銘柄" /Users/arai/invest/.claude/commands/invest.md
# 期待値: 5

grep -c "見解が変化したか明示" /Users/arai/invest/.claude/commands/invest.md
# 期待値: 5
```
</verification>

<success_criteria>
- Step 2.0 に tmp/meeting-result.json 読み込みと prev-highlighted-stocks.json 書き出しの Bash ロジックが追加されている
- 前日データなし時は「前日データなし」ログのみ出力し、パイプラインは通常続行する（D-03）
- Round 1 の全5エージェントプロンプトに「## 前日の推奨銘柄」セクションが存在し、ticker/averageScore/verdict/agentScores を含む指示がある（D-01, D-02）
- 前日データ不在時にセクション省略の条件分岐が明示されている（D-03）
</success_criteria>

<output>
完了時: `.planning/phases/12-analysis-quality/12-01-SUMMARY.md` を作成する
</output>
