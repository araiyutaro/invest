---
phase: 20-holding-card-news-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/portfolio/holding-news.ts
  - src/portfolio/holding-news.test.ts
  - src/scripts/report-utils.ts
  - src/scripts/generate-news-digest.ts
  - src/scripts/report-data-loaders.ts
  - src/scripts/report-data-loaders.test.ts
autonomous: true
requirements: [UI-05, UI-06]

must_haves:
  truths:
    - "resolvePortfolioHoldingNews が正常IDを見出し/ソース/URL/JST日時/matchTypeへ解決する"
    - "news.json に存在しないIDはそのエントリのみ drop され console.warn が出る（幻覚URL防止の最終ガード）"
    - "銘柄間のID混入が起きない（holdingNews のキーを起点にループし、プールを独立再フィルタしない）"
    - "loadHoldingNews / loadNewsPool は欠損・パース失敗でも throw せず {} / [] を返す（fail-soft）"
    - "リゾルバーの結果キーは normalizeHoldingSymbol で正規化され、参照側が同一関数を使うことで銘柄シンボルの表記揺れ（例 '8522' vs '8522.T'、前後空白、大小文字）によるキー不一致が構造的に発生しない"
    - "閲覧者影響: 後続 Wave 2 が消費できる契約（ResolvedHoldingNewsItem 型・resolvePortfolioHoldingNews/normalizeHoldingSymbol の関数シグネチャ）が確定し、news.json プールに実在するIDのみ（幻覚URLを構造的に排除したデータ）が下流のレポート表示へ渡る"
  artifacts:
    - path: "src/portfolio/holding-news.ts"
      provides: "resolvePortfolioHoldingNews() / normalizeHoldingSymbol() と ResolvedHoldingNewsItem 型"
      contains: "resolvePortfolioHoldingNews"
    - path: "src/scripts/report-data-loaders.ts"
      provides: "loadNewsPool() / loadHoldingNews() fail-soft ローダー"
      exports: ["loadNewsPool", "loadHoldingNews"]
    - path: "src/scripts/report-utils.ts"
      provides: "汎化された safeHref() / formatPublishedAtJst()"
      exports: ["safeHref", "formatPublishedAtJst"]
  key_links:
    - from: "src/scripts/generate-news-digest.ts"
      to: "src/scripts/report-utils.ts"
      via: "import { safeHref, formatPublishedAtJst }"
      pattern: "import.*report-utils"
    - from: "src/portfolio/holding-news.ts"
      to: "src/meeting/schemas.ts"
      via: "import type NewsArticlePoolEntry"
      pattern: "NewsArticlePoolEntry"
---

<objective>
Phase 20 の決定論的データ層を構築する。LLM/エージェント出力のURLを一切信用せず、`tmp/holding-news.json`（Phase 19 が出力した銘柄別ID参照）を `tmp/news.json`（記事プール）と照合解決する純粋関数 `resolvePortfolioHoldingNews()` を追加し、両ファイルを fail-soft で読む `loadNewsPool()` / `loadHoldingNews()` を追加する。あわせて銘柄シンボル正規化の単一情報源 `normalizeHoldingSymbol()` を追加し（リゾルバーのキー生成と Wave 2 の参照側が同一関数を共有して表記揺れによるキー不一致を構造的に防ぐ）、news-digest 内に埋もれている `safeHref` / `formatPublishedAtJst` を `report-utils.ts` へ汎化し、Wave 2 のレンダリング層が共通利用できるようにする。

Purpose: 幻覚URLを構造的に防止する信頼できるデータ供給を先に完結させ、Wave 2 のレンダリング/配線層が消費できる契約（型・関数）を確定する（インターフェースファースト）。
Output: `resolvePortfolioHoldingNews()` + `ResolvedHoldingNewsItem` 型 + `normalizeHoldingSymbol()`、`loadNewsPool()`/`loadHoldingNews()`、汎化済み `safeHref`/`formatPublishedAtJst`、および各ユニットテスト。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-holding-card-news-display/20-CONTEXT.md
@.planning/phases/20-holding-card-news-display/20-RESEARCH.md
@.planning/phases/20-holding-card-news-display/20-PATTERNS.md

<interfaces>
<!-- Wave 2 が消費する契約。executor はこれらを実装対象とする。コードベース探索不要。 -->

既存の再利用型（src/portfolio/holding-news.ts に定義済み・変更不可）:
- type HoldingNewsMatchType = "ticker" | "name" | "alias"
- interface HoldingNewsEntry { readonly id: string; readonly matchType: HoldingNewsMatchType; readonly score: number }
- type HoldingNewsFile = Record<string, ReadonlyArray<HoldingNewsEntry>>

既存の記事プール型（src/meeting/schemas.ts:227 に定義済み・変更不可・本フェーズでのプール読込に使う正しい型）:
- interface NewsArticlePoolEntry { readonly id: string; readonly title: string; readonly url: string; readonly source: string; readonly publishedAt: string; readonly ticker?: string }
  ※ publishedAt は JSON往復後は必ず string。NewsArticleWithId（publishedAt: Date）は使わない（Pitfall 3）。

本プランが新規に作る契約:
- interface ResolvedHoldingNewsItem { readonly id: string; readonly title: string; readonly source: string; readonly url: string; readonly publishedAt: string; readonly matchType: HoldingNewsMatchType }
  ※ score は含めない（D-06: 優先度スコアは内部値のため非表示。型に含めないことで構造的に防止）
- function normalizeHoldingSymbol(symbol: string): string  // 銘柄シンボル正規化の単一情報源。trim + toUpperCase（例 " 8522.t " → "8522.T"）。リゾルバーのキー生成と Wave 2 の参照側が同一関数を共有し、表記揺れによるキー不一致を構造的に排除する（Open Questions Q2 の決定）
- function resolvePortfolioHoldingNews(holdingNews: HoldingNewsFile, pool: ReadonlyArray<NewsArticlePoolEntry>): Record<string, ReadonlyArray<ResolvedHoldingNewsItem>>  // 結果のキーは normalizeHoldingSymbol 済み
- async function loadNewsPool(): Promise<ReadonlyArray<NewsArticlePoolEntry>>  // 失敗時 []
- async function loadHoldingNews(): Promise<HoldingNewsFile>  // 失敗時 {}
- function safeHref(url: string): string | null  // http(s) のみ
- function formatPublishedAtJst(publishedAtIso: string): string  // ja-JP / Asia/Tokyo / 例 "7/3 09:15"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: normalizeHoldingSymbol() + resolvePortfolioHoldingNews() と ResolvedHoldingNewsItem 型を追加</name>
  <files>src/portfolio/holding-news.ts, src/portfolio/holding-news.test.ts</files>
  <read_first>
    - src/portfolio/holding-news.ts（既存の型 HoldingNewsFile/HoldingNewsEntry/HoldingNewsMatchType と JSDoc の D-XX 注記スタイルを確認）
    - src/portfolio/holding-news.test.ts（既存 describe ブロック・ファクトリ・ALL_12_HOLDINGS フィクスチャの流儀を確認）
    - src/meeting/schemas.ts の resolveNewsCuration（poolById = new Map(pool.map(...)) による O(1) 照合 + 未知ID console.warn+drop の稼働中前例）と NewsArticlePoolEntry 型定義（227行目付近）
    - src/meeting/schemas.ts の rawHoldingSchema（150-160行）と holdingEvaluationSchema.transform（162-172行）: symbol は `z.string()` のみで transform も `symbol: raw.symbol` で無正規化（表記揺れリスクの実コード根拠。Open Questions Q2 の RESOLVED 決定はこの事実に基づく — 参照側 h.symbol を信用せず normalizeHoldingSymbol でキーを構造的に一致させる）
    - .planning/phases/20-holding-card-news-display/20-RESEARCH.md の Pitfall 3（publishedAt 型）と Pitfall 5（銘柄間ID混入）と Open Questions Q2（RESOLVED）
  </read_first>
  <behavior>
    - 正規化キー生成: 結果マップのキーは normalizeHoldingSymbol(symbol)（trim + toUpperCase）で正規化される。holdingNews のキーが " 8522.t " のように前後空白/小文字を含んでも結果キーは "8522.T" になる
    - normalizeHoldingSymbol 単体: 前後空白除去 + 大文字化のみ。内部文字は変えない（"8522.T" → "8522.T", "mrna" → "MRNA", " hii " → "HII"）
    - 正常解決: pool に存在するIDは {id, title, source, url, publishedAt, matchType} に解決される。matchType は holdingNews エントリ側の値を引き継ぐ。score は結果に含めない
    - 未知IDのdrop: pool に存在しないIDのエントリは結果から除外され、console.warn が呼ばれる。同一銘柄内の他の解決可能エントリは影響を受けず解決される
    - 銘柄間ID混入防止（Pitfall 5 / D-10 必須）: pool に記事Xが存在し holdingNews では HII 配下にのみ X が列挙されている場合、MRNA の解決結果に X は含まれない（X.ticker === "MRNA" でも含まれない）。実装は Object.entries(holdingNews) を起点にループし、プールを ticker 等で独立再フィルタしないことを証明するテスト
    - 供給順の維持: 各銘柄の解決結果は holdingNews の供給順のまま（並べ替えなし。D-04/D-05）
    - throw しない: 空 holdingNews / 空 pool / 全ID未検出のいずれでも throw せず、キーごとに配列を返す
  </behavior>
  <action>
    まず `normalizeHoldingSymbol(symbol: string): string` を holding-news.ts に追加・export する。実装は `return symbol.trim().toUpperCase();`（銘柄シンボル正規化の単一情報源。Open Questions Q2 の RESOLVED 決定 — 米国ティッカーは大文字、日本株は数値+".T" のため trim+toUpperCase で安全に正準化でき、内部文字は変わらない）。JSDoc に「Wave 2 の参照側（generate-portfolio-report.ts）も同じ関数でキーを生成することで、LLM(portfolio-analyst)出力の HoldingEvaluation.symbol 表記揺れによるキー不一致を構造的に防ぐ（schemas.ts の symbol は z.string() のみで無正規化）」と明記する。
    次に `resolvePortfolioHoldingNews(holdingNews: HoldingNewsFile, pool: ReadonlyArray<NewsArticlePoolEntry>): Record<string, ReadonlyArray<ResolvedHoldingNewsItem>>` を holding-news.ts に追加する（schemas.ts ではなく holding-news.ts に配置 — 型的凝集性を優先、RESEARCH.md Assumption A1 / Open Questions Q1 RESOLVED）。実装は `const poolById = new Map(pool.map((a) => [a.id, a]))` を作り、`Object.entries(holdingNews)` の各 [symbol, entries] をループし、各 entry.id を poolById.get で照合、未検出時は `console.warn` で `[holding-news] 不明な記事IDをdrop: <id> (symbol=<symbol>)` を出力して continue（D-10）、検出時は id/title/source/url/publishedAt/matchType を持つ ResolvedHoldingNewsItem を push する。結果マップへの代入キーは必ず `result[normalizeHoldingSymbol(symbol)] = resolved` とし、生の symbol を直接キーにしない。score は破棄（D-06）。プール側からの独立再フィルタ（pool.filter(a => a.ticker === symbol) 等）は禁止（Pitfall 5）。型 `ResolvedHoldingNewsItem`（readonly id/title/source/url/publishedAt: string/matchType: HoldingNewsMatchType、score を含めない）を export する。pool の型注釈は必ず `NewsArticlePoolEntry`（`import type { NewsArticlePoolEntry } from "../meeting/schemas.js"`）を使い、NewsArticleWithId は使わない（Pitfall 3）。JSDoc は既存ファイルの D-XX 注記スタイルに合わせ D-06/D-10 を参照。テストは holding-news.test.ts に `describe("normalizeHoldingSymbol", ...)`（trim/大文字化/内部文字不変のケース）と `describe("resolvePortfolioHoldingNews", ...)`（正規化キー生成 = " 8522.t " のキーが結果では "8522.T" になるケースを含む上記 behavior 全ケース）を追加し、NewsArticlePoolEntry ファクトリ（id/title/url/source/publishedAt のデフォルト付き）でカバーする。console.warn は `vi.spyOn(console, "warn").mockImplementation(() => {})` でスパイする。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/holding-news.test.ts -t "resolvePortfolioHoldingNews"; npx vitest run src/portfolio/holding-news.test.ts -t "normalizeHoldingSymbol"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/portfolio/holding-news.test.ts` が green
    - holding-news.ts が `export function resolvePortfolioHoldingNews`、`export function normalizeHoldingSymbol`、`export interface ResolvedHoldingNewsItem` を含む（各 `grep -c` で 1 以上）
    - resolver の結果キー代入が normalizeHoldingSymbol を経由している（`grep -n "normalizeHoldingSymbol(symbol)" src/portfolio/holding-news.ts` が resolver 関数内で 1 件以上）
    - 正規化キー生成テストが存在し green（" 8522.t " → "8522.T" のケース、テスト名に「正規化」を含む）
    - ResolvedHoldingNewsItem に `score` フィールドが存在しない（`grep -n "score" src/portfolio/holding-news.ts` の結果に ResolvedHoldingNewsItem 定義内の score が無い）
    - 銘柄間ID混入テストが存在し green（テスト名に「銘柄間」を含む）
    - 未知IDテストで console.warn 呼び出しがアサートされ green
    - resolver 実装が `pool.filter` によるプール再フィルタを含まない（`grep -n "pool.filter" src/portfolio/holding-news.ts` が resolver 関数内で 0 件）
  </acceptance_criteria>
  <done>normalizeHoldingSymbol による正準キー生成・正常解決・未知IDドロップ+warn・銘柄間ID混入防止・供給順維持・no-throw の全テストが green、score 非含有が構造的に保証される。</done>
</task>

<task type="auto">
  <name>Task 2: safeHref / formatPublishedAtJst を report-utils.ts へ汎化（挙動不変リファクタ）</name>
  <files>src/scripts/report-utils.ts, src/scripts/generate-news-digest.ts</files>
  <read_first>
    - src/scripts/generate-news-digest.ts の safeHref（60-63行付近: http/https スキームのみ許可）と formatPublishedAtJst（31-42行付近: ja-JP / Asia/Tokyo / month:numeric,day:numeric,hour:2-digit,minute:2-digit,hour12:false）の現行実装、および両関数の呼び出し箇所（formatArticleCardHtml 内）
    - src/scripts/report-utils.ts の escapeHtml の export スタイル（8-15行付近）
  </read_first>
  <action>
    現在 generate-news-digest.ts でモジュールプライベートな `safeHref` と `formatPublishedAtJst` を report-utils.ts へ移設し、両方に `export` を付ける。ロジックは一字一句変えない（safeHref: `url.startsWith("http://") || url.startsWith("https://") ? url : null`、コメント T-16-02-03 を維持。formatPublishedAtJst: `toLocaleString("ja-JP", { timeZone: "Asia/Tokyo", month: "numeric", day: "numeric", hour: "2-digit", minute: "2-digit", hour12: false })`、コメント D-02 を維持）。generate-news-digest.ts からはローカル定義を削除し、`import { escapeHtml, generateBaseStyles, safeHref, formatPublishedAtJst } from "./report-utils.js"` に統合する（既存の escapeHtml/generateBaseStyles import 行を拡張）。これは純粋リファクタであり、既存の news-digest テストは無変更で全て green のままでなければならない。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts -t "News Digest"; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - report-utils.ts が `export function safeHref` と `export function formatPublishedAtJst` を含む（各 `grep -c` で 1）
    - generate-news-digest.ts にローカルな `function safeHref` / `function formatPublishedAtJst` 定義が残っていない（`grep -c "function safeHref" src/scripts/generate-news-digest.ts` が 0）
    - generate-news-digest.ts が report-utils.js から safeHref/formatPublishedAtJst を import している（`grep -n "safeHref" src/scripts/generate-news-digest.ts` が import 行にヒット）
    - `npx tsc --noEmit` がエラーなし
    - 既存 news-digest 関連テストが無変更で green（`npm test` の該当 describe が pass）
  </acceptance_criteria>
  <done>両ヘルパーが report-utils.ts から export され、news-digest はそれを import、挙動不変で既存テスト全 green、tsc パス。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: loadNewsPool() / loadHoldingNews() fail-soft ローダーと新規テスト</name>
  <files>src/scripts/report-data-loaders.ts, src/scripts/report-data-loaders.test.ts</files>
  <read_first>
    - src/scripts/report-data-loaders.ts の loadPortfolioAnalysis（74-82行: try/catch/console.error/フォールバック の shape。ただし zod.parse は本タスクでは使わない）と TMP_DIR 定義（`join(import.meta.dirname, "../../tmp")`）
    - src/scripts/write-news-digest.ts の news.json 読込（23-24行: `JSON.parse(raw) as ReadonlyArray<NewsArticlePoolEntry>` の no-zod 型アサーション前例）
    - src/scripts/generate-report.test.ts の先頭（1-9行: `vi.mock("node:fs/promises", ...)` の fs モック規約）
    - src/portfolio/holding-news.ts の HoldingNewsFile 型、src/meeting/schemas.ts の NewsArticlePoolEntry 型
  </read_first>
  <behavior>
    - loadNewsPool 正常系: news.json を読めたら JSON.parse 結果を NewsArticlePoolEntry[] として返す
    - loadNewsPool 失敗系: readFile が ENOENT で reject、または JSON.parse が失敗した場合、throw せず [] を返し console.error が呼ばれる
    - loadHoldingNews 正常系: holding-news.json を読めたら JSON.parse 結果を HoldingNewsFile として返す
    - loadHoldingNews 失敗系: 欠損/パース失敗時、throw せず {} を返し console.error が呼ばれる（D-09: 欠損は全銘柄0件と同一扱い）
  </behavior>
  <action>
    report-data-loaders.ts に `loadNewsPool(): Promise<ReadonlyArray<NewsArticlePoolEntry>>` と `loadHoldingNews(): Promise<HoldingNewsFile>` を追加する。実装は loadPortfolioAnalysis の try/catch/console.error/フォールバック shape を踏襲するが、zod.parse は使わず write-news-digest.ts と同じ型アサーション（`JSON.parse(raw) as ReadonlyArray<NewsArticlePoolEntry>` / `as HoldingNewsFile`）を使う（自社TS生成物のため zod 不要、RESEARCH.md Pattern 2）。catch では console.error に `"News pool load failed:"` / `"Holding news load failed:"` と `error instanceof Error ? error.message : error` を出力し、loadNewsPool は `[]`、loadHoldingNews は `{}` を返す（D-09、決して throw しない）。読み込むファイルは `join(TMP_DIR, "news.json")` / `join(TMP_DIR, "holding-news.json")`。import は `import type { NewsArticlePoolEntry } from "../meeting/schemas.js"` と `import type { HoldingNewsFile } from "../portfolio/holding-news.js"` を追加。新規テストファイル report-data-loaders.test.ts を作成し、generate-report.test.ts の fs モック規約（`vi.mock("node:fs/promises", ...)`）を使い、`vi.mocked(readFile).mockResolvedValueOnce(...)` で正常系、`mockRejectedValueOnce(new Error("ENOENT"))` で欠損系、不正JSON文字列で parse 失敗系を検証する。console.error は `vi.spyOn(console, "error")` でスパイし、失敗系で呼ばれること・戻り値が [] / {} であること・throw しないことをアサートする。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/report-data-loaders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - report-data-loaders.test.ts が新規に存在し `npx vitest run src/scripts/report-data-loaders.test.ts` が green
    - report-data-loaders.ts が `export async function loadNewsPool` と `export async function loadHoldingNews` を含む
    - 両ローダーの実装に zod.parse が含まれない（`grep -n "Schema.parse\|zod" src/scripts/report-data-loaders.ts` が新規2関数内で 0 件）
    - 欠損/パース失敗テストで戻り値が `[]` / `{}` かつ throw しないことがアサートされ green
    - 失敗系で console.error が呼ばれることがアサートされ green
  </acceptance_criteria>
  <done>両ローダーが fail-soft（[] / {}、no-throw、console.error）で動作し、新規テストファイルで正常系・欠損系・パース失敗系が全 green。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/*.json → resolver/loader | ビルド時にディスクから読む自社生成JSON。実行時の形状不正（Pitfall 3 の publishedAt 型等）や、news.json の url フィールド（元は Finnhub 外部API由来）が信頼境界を越える |
| resolver 出力 → （Wave 2）HTML | 本プランは HTML を出力しないが、resolver が確定した url/title/source が下流のHTML属性・テキストに埋め込まれる。resolver 側の未知IDドロップが下流の幻覚URL防止の根幹 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering/Spoofing | resolvePortfolioHoldingNews | mitigate | ID参照方式: news.json プールに存在するIDのみ解決し、未知IDは console.warn+drop（D-10）。LLM/エージェント出力のURLは一切参照しない |
| T-20-05 | Tampering | resolvePortfolioHoldingNews（銘柄間ID混入） | mitigate | Object.entries(holdingNews) を起点にループし、プールを独立再フィルタしない（Pitfall 5）。cross-holding leakage テストで構造的に保証 |
| T-20-07 | Denial of Service (silent) | normalizeHoldingSymbol / キー不一致 | mitigate | リゾルバーのキー生成と Wave 2 参照側が同一の normalizeHoldingSymbol を共有し、LLM出力 symbol の表記揺れ（schemas.ts で無正規化）によるサイレント0件表示を構造的に防ぐ（Open Questions Q2 RESOLVED / Pitfall 2） |
| T-20-06 | Denial of Service | loadNewsPool / loadHoldingNews | mitigate | fail-soft: 欠損/パース失敗で throw せず [] / {} を返す（D-09）。1ファイルの破損がレポート生成全体を止めない |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | 本フェーズは新規パッケージインストールなし（RESEARCH.md: 既存 zod/vitest のみ、Package Legitimacy Gate 対象外）。install タスクが存在しないため slopcheck 不要 |
</threat_model>

<verification>
- `npx vitest run src/portfolio/holding-news.test.ts src/scripts/report-data-loaders.test.ts` が全 green
- `npx tsc --noEmit` がエラーなし
- `npm test`（全206+新規テスト）が green（Wave マージ時）
</verification>

<success_criteria>
- normalizeHoldingSymbol が銘柄シンボルの単一正規化情報源として機能し、resolver 結果キーが正準化される
- resolvePortfolioHoldingNews が正常解決・未知IDドロップ+warn・銘柄間ID混入防止・供給順維持・no-throw を満たす
- ResolvedHoldingNewsItem に score が含まれない（D-06 の構造的保証）
- loadNewsPool/loadHoldingNews が fail-soft（[] / {}、no-throw、console.error）
- safeHref/formatPublishedAtJst が report-utils.ts から export され、news-digest 挙動不変
</success_criteria>

<output>
Create `.planning/phases/20-holding-card-news-display/20-01-SUMMARY.md` when done
</output>
</content>
</invoke>
