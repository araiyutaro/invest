---
phase: 14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step
verified: 2026-07-01T22:40:00Z
status: human_needed
score: 4/4 must-haves verified (code-level)
overrides_applied: 0
human_verification:
  - test: "次回のlaunchd自動実行（翌朝07:00, D-11）でlogs/invest-*.logに実際に[STEP:*]マーカーが到達しているか、および正常終了時に「パイプライン正常完了」通知が出るかを確認する"
    expected: "生成されたJSONLログ内にinvest.mdのecho '[STEP:...]'呼び出しに対応する文字列が実際に出現する"
    why_human: "claude CLIの--output-format stream-jsonが実運用でBashツールのstdoutをどう出力するかは、実際のCLI実行（外部サービス呼び出し）でしか確認できない。既存ログ（invest-2026-07-01_070005.log, 07:00生成）は本フェーズの修正コミット（22:18-22:19）より前のものであり、修正後の実行はまだ1件も存在しない"
  - test: "claudeが意図的または実際に非0で終了した場合、EXIT_CODEが正しく伝播し、terminal-notifierの異常終了通知（Basso音）に失敗ステップ名が含まれるかを実機で確認する"
    expected: "「パイプライン異常終了 ({step}で失敗, exit: N)」形式の通知がmacOS通知センターに表示される"
    why_human: "GUI通知の実際の表示・音・文言は静的解析では確認不能。D-11でHuman-UAT項目として明示的に事後確認予定と記録されている（マージ前必須ゲートではない, D-10）"
---

# Phase 14.1: Close gap OPS-01/OPS-03 Verification Report

**Phase Goal:** 自動実行パイプライン（launchd経由）が実際に失敗ステップ名をlogs/invest-*.logに記録し、EXIT_CODEが実際のclaude CLI終了コードを反映し、失敗時のmacOS通知に失敗ステップ名が含まれる。合わせてdeployステップのシェルインジェクション脆弱性（WR-04）とrun.sh内の軽微な技術的負債（WR-01/WR-03）、update-index.tsのFAILハンドリング欠如（D-08）を解消する
**Verified:** 2026-07-01T22:40:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `scripts/run.sh`の`claude`呼び出しが`--output-format stream-json --verbose`を使用し、`[STEP:*]`マーカーがlogs/invest-*.logに実際に到達する | ✓ VERIFIED (code) / pending production confirmation | `scripts/run.sh:40` contains `--output-format stream-json --verbose`. Structurally correct per Claude CLI's documented stream-json+verbose behavior (tool call stdout surfaces in JSONL). Actual reachability in a real log has not occurred since the fix (see Human Verification) |
| 2 | `EXIT_CODE`が`\|\| true`によるマスキングなしで`claude`呼び出しの実際の終了コードを保持し、失敗時にterminal-notifierの異常終了通知（失敗ステップ名付き）が発火する | ✓ VERIFIED (code) / pending production confirmation | `scripts/run.sh:35,41`: `EXIT_CODE=0` init + `\|\| EXIT_CODE=$?` capture, no `PIPESTATUS`/`\|\| true` masking remains on the claude invocation. Isolated bash reproduction: `EXIT_CODE=0; ( exit 5 ) >> /dev/null 2>&1 \|\| EXIT_CODE=$?` → `EXIT_CODE=5`. Failure branch (`run.sh:70-75`) extracts `FAILED_STEP` via `grep -o '\[STEP:[^:]*:FAIL' \| tail -1 \| sed -E 's/\[STEP:([^:]*):FAIL/\1/'` — isolated test against a fake JSONL log line returns `deploy` correctly. Real GUI notification firing not yet observed (see Human Verification) |
| 3 | deployステップの`git commit`/`git push`がLLM生成の`date`値を正規表現検証してから`spawnSync`の引数配列形式で実行し、シェルインジェクションが成立しない | ✓ VERIFIED | `.claude/commands/invest.md:1767-1770`: `if (!/^\d{4}-\d{2}-\d{2}$/.test(date)) { ...; process.exit(1); }` runs before any git command. `invest.md:1790,1795`: `spawnSync('git', ['commit', '-m', commitMsg], {...})` and `spawnSync('git', ['push', 'origin', 'master'], {...})` — argument-array form, no `shell: true` option, so shell interpretation of `date` is structurally impossible regardless of validation. `node --check` on the extracted script body passes (verified). Old `execSync('git commit -m ...' + date + ...)` string-concatenation call fully removed (`grep -c "execSync('git commit -m"` returns 0) |
| 4 | `update-index.ts`の実行失敗時に`[STEP:deploy:FAIL:...]`マーカーが出力されてからパイプラインが停止する | ✓ VERIFIED | `.claude/commands/invest.md:1749-1753`: FAIL-branch instruction block inserted immediately after the `update-index.ts` invocation (line 1746) and before the "デプロイを開始します" prose (line 1755), matching the exact established pattern used for `generate-report.ts` (lines 1676-1680). Ordering confirmed: invocation (1746) < FAIL marker block (1749-1753) < deploy-start prose (1755). `grep -c "STEP:deploy:FAIL"` returns 2 (pre-existing `git push に失敗` + new `update-index.tsが失敗`) |

**Score:** 4/4 truths verified at code level. Truths 1 and 2 additionally require one real production execution to confirm actual runtime behavior (D-10/D-11 — explicitly scoped by the plan as a post-merge Human-UAT item, not a merge gate).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/run.sh` | stream-json output format, EXIT_CODE capture fix, failed-step notification, PROTECT_FILES array, grep -F | ✓ VERIFIED | All acceptance criteria from 14.1-01-PLAN.md Tasks 1-3 confirmed present (see consolidated check below). `bash -n` passes. Anti-pattern scan clean (no TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) |
| `.claude/commands/invest.md` | update-index.ts FAIL handling, deploy date regex + spawnSync | ✓ VERIFIED | All acceptance criteria from 14.1-02-PLAN.md Tasks 1-2 confirmed present. `node --check` on extracted deploy script passes. Anti-pattern scan clean |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `scripts/run.sh` claude invocation | `logs/invest-*.log` | `>> "$LOG_FILE" 2>&1` redirect with `--output-format stream-json --verbose` | WIRED (structural) | Redirect target/structure unchanged per D-03; only output-format flag added. Actual JSONL content reaching the log with STEP markers not yet observed in a real run since the fix (committed 22:18-22:19, next scheduled run tomorrow 07:00 per `com.arai.invest-agent.plist` `StartCalendarInterval`) |
| `scripts/run.sh` `EXIT_CODE` variable | terminal-notifier failure branch | `\|\| EXIT_CODE=$?` capture + `grep -o '\[STEP:[^:]*:FAIL'` extraction | WIRED | Verified via isolated bash reproduction (idiom) and isolated extraction test (fake JSONL → "deploy") |
| `invest.md` `meeting-result.json`の`date`値 | `spawnSync('git', ['commit', '-m', commitMsg], ...)` | 正規表現バリデーションゲート `/^\d{4}-\d{2}-\d{2}$/` | WIRED | Validation guard precedes both `git add` and the commit/push spawnSync calls; `spawnSync` array form used with no shell option |
| `invest.md` update-index.ts呼び出し失敗 | `[STEP:deploy:FAIL:...]` / `[PIPELINE:FAIL]` | 既存STEPマーカー規約に沿ったFAILハンドリング追加 | WIRED (instructional pattern) | Matches exact established prose+echo pattern used for report-generation/data-collection FAIL branches (Phase 13 convention). Note: like all other STEP-marker FAIL branches in this file, enforcement relies on the agent (Claude) following the natural-language instruction during `/invest` execution — this is a pre-existing architectural characteristic of `invest.md`, not a new weakness introduced by this phase |
| launchd (`com.arai.invest-agent.plist`) | `scripts/run.sh` | `ProgramArguments: [/bin/bash, /Users/arai/invest/scripts/run.sh]` | WIRED | Confirmed via plist inspection; job is loaded (`launchctl list` shows `com.arai.invest-agent`) |

### Data-Flow Trace (Level 4)

Not applicable in the traditional sense (no UI component rendering fetched data). The relevant "data flow" is: `claude` CLI stdout → JSONL log file → grep-based failure-step extraction → notification message. This flow's logic was traced and unit-verified in isolation (see Key Link table above); end-to-end production confirmation is the one open item, tracked as Human Verification.

### Consolidated Verification Commands (re-run by this verifier, using real system grep — see note below)

```
Plan 01: bash -n scripts/run.sh && grep -qc -- '--output-format stream-json --verbose' scripts/run.sh \
  && grep -q '>> "$LOG_FILE" 2>&1 || EXIT_CODE=$?' scripts/run.sh && ! grep -q 'PIPESTATUS' scripts/run.sh \
  && grep -q "grep -o" scripts/run.sh && grep -q 'PROTECT_FILES=("docs/index.html" "docs/portfolio.html")' scripts/run.sh \
  && grep -q 'grep -F "$PROJECT_DIR/$f"' scripts/run.sh
→ ALL CHECKS PASSED

Plan 02: grep -q "STEP:deploy:FAIL:update-index.tsが失敗" .claude/commands/invest.md \
  && [ "$(grep -c 'STEP:deploy:FAIL' .claude/commands/invest.md)" -eq 2 ] \
  && grep -q "const { execSync, spawnSync } = require" .claude/commands/invest.md \
  && [ "$(grep -c 'spawnSync' .claude/commands/invest.md)" -ge 2 ] \
  && ! grep -q "execSync('git commit -m" .claude/commands/invest.md \
  && grep -q "不正なdate形式" .claude/commands/invest.md \
  && [ "$(grep -c '\.status !== 0' .claude/commands/invest.md)" -ge 2 ]
→ ALL CHECKS PASSED
```

**Note on tooling:** this interactive shell's `grep` is shimmed to `ugrep -G` by Claude Code tooling and mis-handles `$` mid-pattern (false negative on literal `EXIT_CODE=$?` and `grep -F "$PROJECT_DIR/$f"` patterns). This is an artifact of the verifier's shell environment, not a defect in `scripts/run.sh` — confirmed via `/usr/bin/grep` (system grep, same binary that runs under `#!/bin/bash` launchd execution) and via `od -c` byte-level inspection of the exact lines.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| OPS-01 | 14.1-01-PLAN.md, 14.1-02-PLAN.md | 自動実行（launchd）でパイプラインが途中失敗した場合、失敗ステップを特定するエラーログが出力される | ✓ SATISFIED (code) / production confirmation pending | Truths 1 and 4 above |
| OPS-03 | 14.1-01-PLAN.md, 14.1-02-PLAN.md | 自動実行の開始/完了/失敗がmacOS通知で報告される | ✓ SATISFIED (code) / production confirmation pending | Truths 2 above |

No orphaned requirements — both IDs declared in ROADMAP.md's phase 14.1 entry and in both plans' frontmatter, consistent with each other.

**Note (info, non-blocking):** `.planning/REQUIREMENTS.md` traceability table (lines 60,62) still shows `OPS-01 | Phase 13 | Complete` and `OPS-03 | Phase 13 | Complete`, unchanged since the v2.3 audit reopened them as unsatisfied and this phase (14.1) closed the gap. Recommend updating the traceability table to reflect the Phase 13→14.1 handoff for future audit clarity. This is a documentation-freshness issue, not a code gap.

### Anti-Patterns Found

None. Scanned `scripts/run.sh` (full file) and the diffs of all 5 commits (`afcdad1`, `4c871e9`, `4093a4f`, `b45bc12`, `69e8411`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` and empty-implementation patterns — all clean.

### Scope-decision cross-check (D-09/D-10/D-11)

- D-09 (WR-02 Step 3c/3d ordering, out of scope): confirmed absent from both plans' action text — correctly excluded, no unintended scope creep.
- D-10 (real dry-run not required pre-merge): explicitly noted in 14.1-01-PLAN.md `<verification>` section — consistent with this verifier's treatment of truths 1/2 as code-verified-but-production-pending rather than blocking gaps.
- D-11 (next launchd run as Human-UAT): reflected in both SUMMARY.md "Next Phase Readiness" sections. Confirmed via `logs/` inspection that no post-fix run has occurred yet (latest log `invest-2026-07-01_070005.log` predates the 22:18-22:19 fix commits; `com.arai.invest-agent.plist` next scheduled run is 07:00 the following day) — this is a genuine, not-yet-observable production behavior, correctly routed to Human Verification rather than being treated as a code gap.

### Human Verification Required

#### 1. STEP marker log reachability (next launchd run)

**Test:** After the next scheduled launchd execution (07:00), inspect `logs/invest-*.log` for the presence of `[STEP:...]` marker substrings within the JSONL content.
**Expected:** At minimum `[STEP:data-collection:START]` and subsequent step markers appear as literal substrings somewhere in the JSONL stream (e.g., inside a `tool_result` or `tool_use` block's text/input field).
**Why human:** Requires an actual `claude` CLI invocation (external service call, ~40 min pipeline run) which cannot be triggered or observed synchronously during verification; no post-fix log exists yet to inspect.

#### 2. Failure notification content and firing (real or simulated failure)

**Test:** Trigger or wait for a pipeline failure and confirm the macOS notification via `terminal-notifier` displays "パイプライン異常終了 ({失敗ステップ名}で失敗, exit: N)" with Basso sound, or the exit-code-only fallback when no STEP:FAIL marker exists.
**Expected:** Notification Center shows the message with the correct extracted step name matching the log's last `[STEP:*:FAIL]` marker.
**Why human:** GUI notification display, sound, and real-world failure triggering cannot be verified via static analysis; this was the exact gap the v2.3 audit found in Phase 13's original (GUI-only-scoped) Human-UAT check, so this phase's Human-UAT must explicitly cover both the exit-code logic and the notification content together.

### Gaps Summary

No code-level gaps found. All 4 roadmap success criteria are backed by correct, verifiable code changes:
- `scripts/run.sh` root-cause fix (stream-json format + EXIT_CODE capture) matches the plan's exact specified idiom, confirmed via isolated bash reproduction.
- Failed-step-name notification extraction logic confirmed correct via isolated test.
- `invest.md` deploy step shell-injection vulnerability (WR-04/WR-05, D-06) is fully closed — `spawnSync` argument-array form structurally prevents shell interpretation regardless of the regex guard, and the regex guard itself is present and correctly placed before any git command.
- `invest.md` update-index.ts FAIL handling (D-08) added, matching the established pattern exactly, in the correct order.
- WR-01 (grep -F) and WR-03 (PROTECT_FILES array) tech debt resolved.
- All 5 task commits exist in git history and match their claimed diffs.

The only open item is production confirmation of runtime behavior (whether the stream-json output actually causes STEP markers to land in the log, and whether the notification actually fires with correct content) — this was a deliberate, documented scope decision by the phase itself (D-10: not a merge gate; D-11: explicit post-merge Human-UAT item for the next scheduled run). Per verification protocol, the presence of a legitimate human-verification item forces `status: human_needed` even though all code-level truths are verified, rather than `passed`. This is not a fabricated caution — the `logs/` directory confirms no execution has occurred since the fix landed.

---

_Verified: 2026-07-01T22:40:00Z_
_Verifier: Claude (gsd-verifier)_
