# Phase 14.1: Close gap: OPS-01/OPS-03 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-01
**Phase:** 14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step
**Areas discussed:** STEPマーカー到達方式, 修正スコープ, 検証の厳格さ

---

## STEPマーカー到達方式

### Q1: STEPマーカーをログファイルに届ける方式は？

| Option | Description | Selected |
|--------|-------------|----------|
| 直接ファイル書き込み（推奨） | invest.md内のechoを`>> "$LOG_FILE"`に変更。CLI出力フォーマットに非依存だが約18箇所の修正が必要 | |
| 出力フォーマット変更 | `--output-format stream-json --verbose`でツール出力をJSONストリーム化。invest.mdの修正不要だがログがJSON化 | ✓ |

**User's choice:** 出力フォーマット変更
**Notes:** run.shの1行変更で済み、invest.mdの18箇所を触らずに済む点を優先。

### Q2: JSONL形式のログの可読性をどうするか？

| Option | Description | Selected |
|--------|-------------|----------|
| 生JSONLのまま保存（推奨） | run.shは現状通りリダイレクトするだけ。調査は`grep -o`で対応 | ✓ |
| 抽出済みクリーンログも併記 | STEP/PIPELINE行だけを抽出した別ファイルも生成 | |

**User's choice:** 生JSONLのまま保存
**Notes:** 実装を最小限にする方針。

### Q3: 失敗時のmacOS通知の内容を強化するか？

| Option | Description | Selected |
|--------|-------------|----------|
| 失敗ステップ名も含める（推奨） | ログから最後の`[STEP:*:FAIL:...]`を抽出し通知に含める | ✓ |
| 現状維持 | exit codeのみ | |

**User's choice:** 失敗ステップ名も含める
**Notes:** —

---

## 修正スコープ

### Q1: WR-04/WR-05相当のシェルインジェクション修正を含めるか？

| Option | Description | Selected |
|--------|-------------|----------|
| 含める（推奨） | 監査が明示的に推奨。deployステップのdate値exec連結を修正 | ✓ |
| 含めない | 別フェーズ/tech debtとして後回し | |

**User's choice:** 含める
**Notes:** —

### Q2: run.shの軽微な技術的負債（WR-01, WR-03）も同時に修正するか？

| Option | Description | Selected |
|--------|-------------|----------|
| 含める（推奨） | どちらもrun.sh内の1行程度の修正 | ✓ |
| 含めない | OPS-01/OPS-03コア修正のみに限定 | |

**User's choice:** 含める
**Notes:** —

### Q3: update-index.ts呼び出しにFAILハンドリングを追加するか？（WARNING2）

| Option | Description | Selected |
|--------|-------------|----------|
| 追加する（推奨） | OPS-01の目的（全ステップの障害可観測性）に直接合致 | ✓ |
| 追加しない | 別途後回し | |

**User's choice:** 追加する
**Notes:** —

### Q4: WR-02（Step 3c/3dのドキュメント順序不一致）も修正するか？

| Option | Description | Selected |
|--------|-------------|----------|
| 含めない（推奨） | バグではなくドキュメント可読性の問題。スコープ外とする | ✓ |
| 含める | どうせinvest.mdを触るので一緒に直す | |

**User's choice:** 含めない
**Notes:** Deferred Ideasへ記録。

---

## 検証の厳格さ

### Q1: 実機ドライラン（意図的にclaude呼び出しを失敗させてEXIT_CODE/ログ到達を確認）を必須にするか？

| Option | Description | Selected |
|--------|-------------|----------|
| 実機ドライラン必須（推奨） | Phase 13の「静的チェックのみ」がこのバグを見逃した反省を踏まえ、実際に失敗させて確認 | |
| コードレビューで十分 | ロジックを目視で追えば十分 | ✓ |

**User's choice:** コードレビューで十分
**Notes:** 監査の指摘（静的チェックのみが根本原因の一つ）を認識した上での判断。

### Q2: 次回のlaunchd自動実行で事後確認する予定はあるか？

| Option | Description | Selected |
|--------|-------------|----------|
| する（推奨） | 翌朝以降の実ログを確認し、STEPマーカー/EXIT_CODEが機能しているかHuman-UAT項目として残す | ✓ |
| 不要 | デプロイ後の確認は特に予定しない | |

**User's choice:** する
**Notes:** プリマージのゲートにはしないが、実運用での事後確認として残す。

---

## Claude's Discretion

- `set +e`/`if`分岐等、EXIT_CODE捕捉の具体的なbash実装方法
- terminal-notifierメッセージの具体的な文言・フォーマット
- date値バリデーションの正規表現・エラー処理の詳細（execSync引数配列化 vs spawnSync等）
- 失敗ステップ抽出ロジックの実装場所（run.sh内かnode -eスクリプト内か）

## Deferred Ideas

- WR-02（invest.md内 Step 3c/3d のドキュメント順序不一致）— バグではなく可読性の問題。別途tech debtとして後回し
