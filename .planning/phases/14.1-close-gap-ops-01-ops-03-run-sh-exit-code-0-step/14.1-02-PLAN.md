---
phase: 14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.claude/commands/invest.md]
autonomous: true
requirements: [OPS-01, OPS-03]
must_haves:
  truths:
    - "update-index.tsが失敗した場合、[STEP:deploy:FAIL:update-index.tsが失敗]マーカーとPIPELINE:FAILマーカーが出力されてからパイプラインが停止する"
    - "deployステップのgit commit/pushが不正なdate値をシェルに解釈させず、正規表現バリデーションで弾く"
    - "deployステップでgit commit/pushが失敗した場合、[STEP:deploy:FAIL]マーカーが出力されパイプラインが停止する（spawnSync移行後もexecSync時と同じ検知が保たれる）"
  artifacts:
    - path: ".claude/commands/invest.md"
      provides: "update-index.ts FAILハンドリング追加・deployのspawnSync化+date正規表現検証"
      contains: "spawnSync"
  key_links:
    - from: "invest.md meeting-result.jsonのdate値"
      to: "spawnSync('git', ['commit', '-m', commitMsg], ...)"
      via: "正規表現バリデーションゲート /^\\d{4}-\\d{2}-\\d{2}$/ を通過した場合のみ実行"
      pattern: "spawnSync\\('git'"
    - from: "update-index.ts呼び出し失敗"
      to: "[STEP:deploy:FAIL:...] / [PIPELINE:FAIL]"
      via: "既存STEPマーカー規約（report-generation/data-collectionと同形）に沿ったFAILハンドリング追加"
      pattern: "STEP:deploy:FAIL:update-index"
---

<objective>
`.claude/commands/invest.md`のdeployステップ（Step 4）にある2つの実運用ギャップを修正する: (1) LLM（moderator-final）が生成した`meeting-result.json`の`date`値が未検証のまま`execSync`のシェル文字列に連結されているシェルインジェクション脆弱性（WR-04/D-06）、(2) `update-index.ts`呼び出しに他ステップと同様の明示的FAILハンドリングが存在しないため、想定外の例外がgit pushまでサイレントに通過しうる観測性ギャップ（D-08、OPS-01の一部）。

Purpose: v2.3マイルストーン監査が同一修正フェーズでのバンドルを推奨した2件のギャップを閉じる。WR-04はセキュリティルール「ALWAYS validate user input」に対する明確な違反であり、D-08はOPS-01（失敗ステップの可観測性）の意図がupdate-index.tsだけ抜け落ちている状態を解消する。
Output: `.claude/commands/invest.md`（修正済み） — update-index.ts FAILハンドリングブロック追加、deployのdate検証+spawnSync化
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-CONTEXT.md
@.planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-PATTERNS.md
@.planning/v2.3-MILESTONE-AUDIT.md
@.planning/phases/12-analysis-quality/12-REVIEW.md
@src/meeting/schemas.ts
</context>

<interfaces>
Existing STEP-marker and node -e try/catch conventions this plan must replicate exactly.

From `.claude/commands/invest.md` (report-generation FAIL pattern, lines 1676-1680 — analog to copy for Task 1):
- Prose: "`generate-report.ts` がエラーで終了した場合は、以下を実行してからパイプラインを停止してください:"
- Bash block containing: `echo '[STEP:report-generation:FAIL:レポート生成スクリプトがエラーで終了]'` then `echo '[PIPELINE:FAIL] ステップ: report-generation, エラー: レポート生成スクリプトがエラーで終了'`

From `.claude/commands/invest.md` (deploy step's existing STEP markers, for reference — do not duplicate step ids):
- `echo '[STEP:deploy:START]'` at line 1728
- `echo '[STEP:deploy:OK]'` at lines 1789 and 1793
- `echo '[STEP:deploy:FAIL:git push に失敗]'` at line 1797

From `src/meeting/schemas.ts:42` (established date-format convention, reuse this exact regex source): `date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit FAIL handling for update-index.ts invocation (D-08)</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    .claude/commands/invest.md (lines ~1670-1750 — read this range: covers the report-generation FAIL-branch analog and the update-index.ts block that currently has no FAIL branch)
    .planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-CONTEXT.md (D-08)
    .planning/v2.3-MILESTONE-AUDIT.md (integration gap description: update-index.ts に明示的なFAIL分岐がない点)
  </read_first>
  <action>
    Immediately after the existing update-index.ts bash block (`cd /Users/arai/invest && npx tsx src/scripts/update-index.ts`, currently at lines 1745-1747) and before the "デプロイを開始します..." prose line (currently line 1749), insert a new prose+bash block matching the exact shape used for the generate-report.ts FAIL branch (lines 1676-1680, see `<interfaces>` above): a Japanese instruction sentence "`update-index.ts` がエラーで終了した場合は、以下を実行してからパイプラインを停止してください:" followed by a bash block containing exactly two echo statements: `echo '[STEP:deploy:FAIL:update-index.tsが失敗]'` and `echo '[PIPELINE:FAIL] ステップ: deploy, エラー: update-index.tsが失敗'`.

    Use step id `deploy` (not a new step id) since `update-index.ts` runs as a sub-action within the existing Step 4/deploy step — `[STEP:deploy:START]` is already emitted at line 1728, `[STEP:deploy:OK]` at lines 1789/1793, and `[STEP:deploy:FAIL:git push に失敗]` at line 1797. Do not introduce a separate step name (e.g. do NOT use `update-index` as the step id) — this would break the existing single-`deploy`-step observability contract.
  </action>
  <acceptance_criteria>
    - `grep -c "STEP:deploy:FAIL:update-index.tsが失敗" .claude/commands/invest.md` returns 1
    - `grep -c "PIPELINE:FAIL\] ステップ: deploy, エラー: update-index.tsが失敗" .claude/commands/invest.md` returns 1
    - Ordering check: the line number from `grep -n "npx tsx src/scripts/update-index.ts" .claude/commands/invest.md` is less than the line number from `grep -n "update-index.tsが失敗" .claude/commands/invest.md`, which is in turn less than the line number from `grep -n "デプロイを開始します" .claude/commands/invest.md`
    - `grep -c "STEP:deploy:FAIL" .claude/commands/invest.md` returns 2 (the pre-existing `git push に失敗` variant plus the new `update-index.tsが失敗` variant)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "STEP:deploy:FAIL:update-index.tsが失敗" .claude/commands/invest.md && [ "$(grep -c 'STEP:deploy:FAIL' .claude/commands/invest.md)" -eq 2 ]</automated>
  </verify>
  <done>A FAIL-handling instruction block (matching the existing report-generation/data-collection pattern) exists immediately after the update-index.ts invocation and before the deploy-start prose, emitting `[STEP:deploy:FAIL:update-index.tsが失敗]` and `[PIPELINE:FAIL]` markers, closing the silent-failure gap identified in the v2.3 audit.</done>
</task>

<task type="auto">
  <name>Task 2: Fix deploy shell-injection vulnerability with correct spawnSync failure-detection semantics (D-06, WR-04)</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    .claude/commands/invest.md (lines ~1751-1799 — the full deploy node -e script including the try/catch and the STEP:deploy:OK/FAIL branching instructions that follow it)
    .planning/phases/12-analysis-quality/12-REVIEW.md (WR-04, lines 138-163 — vulnerability description and suggested fix direction)
    src/meeting/schemas.ts (line 42 — established date regex convention, reuse exactly)
  </read_first>
  <action>
    In the deploy `node -e` script (currently lines 1752-1783), make these concrete changes:

    (1) Change `const { execSync } = require('child_process');` to `const { execSync, spawnSync } = require('child_process');` — `execSync` remains needed for the untouched `git add docs/` call and the `git diff --staged --quiet` no-op check, neither of which interpolates the `date` value.

    (2) Immediately after `const date = result.date;` (currently line 1758) and before the `git add docs/` execSync call (currently line 1761), insert a validation guard using the exact regex `/^\d{4}-\d{2}-\d{2}$/` (same convention as `src/meeting/schemas.ts:42`): if `date` fails this test, call `console.error('不正なdate形式: ' + date)` then `process.exit(1)` immediately, before any git command runs.

    (3) Inside the existing try block that currently calls `execSync('git commit -m \"report: ' + date + ' daily update\"', { stdio: 'inherit' });` and `execSync('git push origin master', { stdio: 'inherit' });` (currently lines 1776-1778), replace both calls with `spawnSync`, argument-array form: build `const commitMsg = 'report: ' + date + ' daily update';`, then call `spawnSync('git', ['commit', '-m', commitMsg], { stdio: 'inherit' })` storing the return value (e.g. `const commitResult = ...`), and `spawnSync('git', ['push', 'origin', 'master'], { stdio: 'inherit' })` storing the return value (e.g. `const pushResult = ...`).

    (4) CRITICAL semantic correction (not present in the WR-04 review's suggested snippet, and required to avoid reintroducing the same class of bug fixed in Plan 01/Task 1): `spawnSync` does NOT throw on a nonzero child exit code — unlike `execSync`, which throws and would have triggered the surrounding `catch (commitErr)` block. Therefore, after each `spawnSync` call, explicitly check the returned object's `.status` property: if `commitResult.status !== 0` or `pushResult.status !== 0`, manually invoke the same failure path as the catch block (`console.error('デプロイエラー: ...')` then `process.exit(1)`), so a failed `git commit` or `git push` still causes this node script to exit non-zero. This preserves the existing `[STEP:deploy:FAIL:git push に失敗]` detection contract (lines 1795-1799) which depends entirely on this script's exit code. Note that `spawnSync` does not throw for a missing `git` binary either — in that case it returns `.status === null` and populates `.error`, which the `.status !== 0` check above already catches (since `null !== 0`); no separate ENOENT handling is needed. Keep the outer `try { ... } catch (commitErr) { ... }` wrapper only as a safety net for unexpected exceptions elsewhere in this script block (e.g. the JSON-parsing/file-read logic that runs before this point), not as the mechanism that detects git command failures.

    Leave the `git add docs/` execSync call, the `git diff --staged --quiet` no-op check (including its `process.exit(0)` early-exit for the no-changes case), and the surrounding JSON-read logic completely unchanged — only the two date-interpolating commit/push calls plus the new date-validation guard are in scope.
  </action>
  <acceptance_criteria>
    - `grep -c "spawnSync" .claude/commands/invest.md` returns 2 or more (both git commit and git push calls converted)
    - `grep -c "不正なdate形式" .claude/commands/invest.md` returns 1
    - `grep -c "\\\\d{4}-\\\\d{2}-\\\\d{2}" .claude/commands/invest.md` (literal match for the regex source, same convention as schemas.ts:42) returns 1 or more within the deploy section
    - `grep -c "execSync('git commit -m" .claude/commands/invest.md` returns 0 (old vulnerable string-concatenation call fully removed)
    - `grep -c "\\.status !== 0" .claude/commands/invest.md` returns 2 or more (explicit exit-status checks present for both commit and push, compensating for spawnSync's non-throwing behavior)
    - `grep -c "const { execSync, spawnSync } = require" .claude/commands/invest.md` returns 1
    - Syntax check: extract the deploy script body (from the unique post-fix line `const { execSync, spawnSync } = require('child_process');` through to the line that is exactly a closing double-quote for the `node -e` string) and confirm it parses as valid JavaScript via `node --check`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "const { execSync, spawnSync } = require" .claude/commands/invest.md && [ "$(grep -c 'spawnSync' .claude/commands/invest.md)" -ge 2 ] && ! grep -q "execSync('git commit -m" .claude/commands/invest.md && awk '/const \{ execSync, spawnSync \} = require\(.child_process.\);/,/^"$/' .claude/commands/invest.md | sed '$d' | node --check /dev/stdin</automated>
  </verify>
  <done>The deploy node -e script validates `date` against `/^\d{4}-\d{2}-\d{2}$/` before any git command runs and exits 1 with a Japanese error message on invalid format; `git commit` and `git push` are invoked via `spawnSync` with argument arrays (no shell string interpolation of `date`); both spawnSync results are checked for non-zero `.status` and treated as failure (matching the pre-existing STEP:deploy:FAIL detection contract); `git add`/`git diff --staged --quiet` logic is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `moderator-final` LLM agent output -> deploy shell command | `tmp/meeting-result.json` is AI-generated content (not a trusted system file); its `date` field is read and used to construct a git commit message |
| `/invest` command execution -> git commit/push | The deploy step runs `git commit`/`git push` against the user's real repository with the user's credentials, unattended (no human review of the LLM-generated content before execution) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-05 | Tampering (Injection) | invest.md deploy `node -e` script — `date` field interpolated into git commit message | mitigate | Validate `date` against `/^\d{4}-\d{2}-\d{2}$/` before any git command runs (fail fast with `process.exit(1)` on mismatch); replace `execSync` string-concatenation with `spawnSync('git', [...])` argument-array form, which bypasses shell interpretation of the `date` value entirely (D-06/WR-04, Task 2) |
| T-14.1-06 | Repudiation | invest.md deploy `node -e` script — spawnSync exit-status handling | mitigate | Explicitly check `.status !== 0` on both the commit and push `spawnSync` calls and call `process.exit(1)` on failure, since `spawnSync` (unlike `execSync`) does not throw on a nonzero child exit code — without this check, a failed `git commit`/`git push` would silently report success, defeating the existing `[STEP:deploy:FAIL]` detection contract (Task 2) |
| T-14.1-07 | Repudiation | invest.md `update-index.ts` invocation (Step 4) | mitigate | Add an explicit FAIL branch (`[STEP:deploy:FAIL:update-index.tsが失敗]` + `[PIPELINE:FAIL]`) matching the existing pattern used for data-collection/report-generation steps, closing the silent-failure gap flagged in the v2.3 audit (D-08, Task 1) |

</threat_model>

<verification>
Run the following consolidated check after both tasks are complete:

```bash
grep -q "STEP:deploy:FAIL:update-index.tsが失敗" .claude/commands/invest.md \
  && [ "$(grep -c 'STEP:deploy:FAIL' .claude/commands/invest.md)" -eq 2 ] \
  && grep -q "const { execSync, spawnSync } = require" .claude/commands/invest.md \
  && [ "$(grep -c 'spawnSync' .claude/commands/invest.md)" -ge 2 ] \
  && ! grep -q "execSync('git commit -m" .claude/commands/invest.md \
  && grep -q "不正なdate形式" .claude/commands/invest.md \
  && [ "$(grep -c '\.status !== 0' .claude/commands/invest.md)" -ge 2 ] \
  && echo "ALL CHECKS PASSED"
```
</verification>

<success_criteria>
- update-index.ts invocation is followed by an explicit `[STEP:deploy:FAIL:update-index.tsが失敗]` / `[PIPELINE:FAIL]` branch
- Deploy `node -e` script validates `date` against `/^\d{4}-\d{2}-\d{2}$/` before running any git command
- `git commit`/`git push` use `spawnSync` with argument arrays, not `execSync` string concatenation
- Both `spawnSync` results are checked for non-zero `.status` and treated as failure
- Extracted deploy script body passes `node --check`
</success_criteria>

<output>
Create `.planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-02-SUMMARY.md` when done
</output>
