---
phase: 14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/run.sh]
autonomous: true
requirements: [OPS-01, OPS-03]
must_haves:
  truths:
    - "logs/invest-*.log に [STEP: マーカー文字列が実際に出現する（claude呼び出しが --output-format stream-json でツール呼び出し出力を記録するため、D-01/D-02/D-03）"
    - "claude CLIが非0で終了した場合、EXIT_CODEがその実際の終了コードを保持し、run.shが非0でexitする（|| true によるマスキングが解消されている、D-04）"
    - "失敗時のterminal-notifier通知メッセージに失敗したステップ名（例: deploy）が含まれる（D-05）"
    - "HTMLチェックサム比較（grep）・PROTECT_FILESイテレーションがパス内特殊文字やword-splitting/glob展開による誤動作を起こさない（D-07: WR-01/WR-03）"
    - "スコープ外・検証方針の決定が本プランに反映されている: WR-02のStep 3c/3d順序不一致は対象外（D-09）、実機ドライランは merge 前必須としない（D-10）、次回launchd自動実行時のログ確認をHuman-UAT項目として記録（D-11）"
  artifacts:
    - path: "scripts/run.sh"
      provides: "stream-json出力フォーマット追加・EXIT_CODE捕捉修正・失敗ステップ名付き通知・grep -F固定文字列マッチ・PROTECT_FILES配列化"
      contains: "--output-format stream-json --verbose"
  key_links:
    - from: "scripts/run.sh claude呼び出し"
      to: "logs/invest-*.log"
      via: ">> \"$LOG_FILE\" 2>&1 リダイレクト（構造は不変、出力フォーマットのみ変更）"
      pattern: "output-format stream-json"
    - from: "scripts/run.sh EXIT_CODE変数"
      to: "terminal-notifier 失敗分岐（失敗ステップ名抽出込み）"
      via: "|| EXIT_CODE=$? による捕捉 + grep -o '\\[STEP:[^:]*:FAIL' による抽出"
      pattern: "EXIT_CODE=\\$\\?"
---

<objective>
v2.3マイルストーン監査（`.planning/v2.3-MILESTONE-AUDIT.md`）で発見された、`scripts/run.sh`の単一箇所（39-41行目）に起因するOPS-01（STEPマーカー未到達）とOPS-03（EXIT_CODE常時0）の2つの実運用バグを修正する。合わせて、13-REVIEW.mdで指摘済みの軽微な技術的負債（WR-01: grepの正規表現誤マッチ、WR-03: PROTECT_FILESの未クォート展開リスク）を解消する。

Purpose: Phase 13で実装されたはずの「障害の可観測性」（失敗ステップのログ記録・macOS通知）が、根本原因（`claude -p`の既定output-format text + `|| true`によるPIPESTATUSマスキング）により実運用では機能していなかった。この1ファイルの修正でOPS-01/OPS-03を実質的に満たす。
Output: `scripts/run.sh`（修正済み） — stream-json出力フォーマット、正しいEXIT_CODE捕捉、失敗ステップ名付き通知、grep -F、PROTECT_FILES配列
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-CONTEXT.md
@.planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-PATTERNS.md
@.planning/v2.3-MILESTONE-AUDIT.md
@.planning/phases/13-operational-stability/13-REVIEW.md
@scripts/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix claude invocation output format and EXIT_CODE capture (OPS-01 + OPS-03 root-cause fix)</name>
  <files>scripts/run.sh</files>
  <read_first>
    scripts/run.sh (current lines 35-41 — the claude invocation and the broken EXIT_CODE line)
    .planning/phases/13-operational-stability/13-REVIEW.md (CR-01, lines 32-59 — exact root cause and fix idiom for the EXIT_CODE bug)
    .planning/v2.3-MILESTONE-AUDIT.md (lines 75-79 — root cause description for both OPS-01 and OPS-03, confirmed via real log inspection and bash reproduction)
    .planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-CONTEXT.md (D-01 through D-04)
  </read_first>
  <action>
    In scripts/run.sh, locate the block that currently reads: `claude --dangerously-skip-permissions \ -p "/invest" \ --model claude-sonnet-4-6 \ --max-turns 200 \ >> "$LOG_FILE" 2>&1 || true` (lines 35-39), immediately followed by the standalone line `EXIT_CODE=${PIPESTATUS[0]:-$?}` (line 41).

    Replace this entire sequence with the following changes: (1) add a new line `EXIT_CODE=0` immediately before the `claude --dangerously-skip-permissions` line, initializing the variable for the success case (per D-04, CR-01 fix idiom). (2) Add the flags `--output-format stream-json --verbose` to the `claude` invocation's argument list, placed after `--max-turns 200` and before the `>> "$LOG_FILE" 2>&1` redirect (per D-01) — this causes Bash-tool stdout (including invest.md's ~18 existing `echo '[STEP:...]'` calls, which need no changes themselves) to be emitted as part of the JSONL stream instead of being discarded by the default `text` output format that only surfaces the final assistant message. (3) Change the trailing `|| true` to `|| EXIT_CODE=$?` so a nonzero `claude` exit status is captured into `EXIT_CODE` rather than masked (per D-04, CR-01) — this idiom is exempt from `set -e`/`errexit` (right-hand side of `||`), so no `set +e` bracketing is needed anywhere in the script. (4) Delete the now-redundant standalone `EXIT_CODE=${PIPESTATUS[0]:-$?}` line entirely — `EXIT_CODE` is now set exclusively by the step-1 initialization (success path) or the `|| EXIT_CODE=$?` idiom (failure path).

    Do not touch the existing `>> "$LOG_FILE" 2>&1` redirect target/structure (per D-03 — log stays raw JSONL, no reformatting or additional summary file). Do not touch any other `|| true` usage in the file (the log-rotation `find ... -delete 2>/dev/null || true` line near the end of the file is unrelated and must remain as-is).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/run.sh` exits 0 (syntax valid)
    - `grep -c -- '--output-format stream-json --verbose' scripts/run.sh` returns 1
    - `grep -n 'EXIT_CODE=0' scripts/run.sh` shows a line number strictly less than the line number reported by `grep -n 'claude --dangerously-skip-permissions' scripts/run.sh` (initialization precedes the invocation)
    - `grep -c '>> "\$LOG_FILE" 2>&1 || EXIT_CODE=\$?' scripts/run.sh` returns 1 (exact fixed line present)
    - `grep -c '>> "\$LOG_FILE" 2>&1 || true' scripts/run.sh` returns 0 (old broken pattern for the claude invocation is gone)
    - `grep -c 'PIPESTATUS' scripts/run.sh` returns 0 (old masking line fully removed)
    - `grep -c '2>/dev/null || true' scripts/run.sh` returns 1 (confirms the unrelated log-rotation `|| true` was left untouched)
    - Isolated idiom check (proves the exact pattern propagates a nonzero exit code correctly under `set -e` without early script termination): `bash -c 'set -euo pipefail; EXIT_CODE=0; ( exit 5 ) >> /dev/null 2>&1 || EXIT_CODE=$?; echo "EXIT_CODE=$EXIT_CODE"'` outputs `EXIT_CODE=5`
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/run.sh && grep -qc -- '--output-format stream-json --verbose' scripts/run.sh && ! grep -q 'PIPESTATUS' scripts/run.sh && ! grep -q '2>&1 || true' scripts/run.sh</automated>
  </verify>
  <done>scripts/run.sh's claude invocation includes `--output-format stream-json --verbose`; EXIT_CODE is initialized to 0 before the call and captured via `|| EXIT_CODE=$?` on failure; the standalone PIPESTATUS line is removed; bash -n passes.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance failure notification with extracted failed-step name (OPS-03 enhancement)</name>
  <files>scripts/run.sh</files>
  <read_first>
    scripts/run.sh (current lines 67-71 — the terminal-notifier if/else branch; must be read AFTER Task 1's edits are applied since Task 1 shifts nothing before this block but changes EXIT_CODE semantics this task relies on)
    .planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-CONTEXT.md (D-05 and the `<specifics>` section — example message "パイプライン異常終了 (deployで失敗, exit: 1)" and the `grep -o '\[STEP:[^]]*\]'` extraction primitive)
  </read_first>
  <action>
    In the `if [ "$EXIT_CODE" -eq 0 ]; then ... else ... fi` block that calls `terminal-notifier` (currently lines 67-71), keep the success branch (`-message "パイプライン正常完了" -sound Glass`) unchanged.

    In the failure (`else`) branch, before the `terminal-notifier` call, add logic to extract the last STEP failure marker from `$LOG_FILE`: use `grep -o` with the pattern `\[STEP:[^:]*:FAIL` against `"$LOG_FILE"`, pipe to `tail -1` to get the most recent match, then strip the `[STEP:` prefix and `:FAIL` suffix with `sed -E 's/\[STEP:([^:]*):FAIL/\1/'`, storing the final step-name string in a variable named `FAILED_STEP`. This must scan the raw JSONL content of `$LOG_FILE` as a literal substring search — do not assume plain-text lines (consistent with Task 1's stream-json/D-02 raw-log decision); JSON string escaping does not affect `[`, `]`, or `:` characters so the marker substring survives intact inside the JSONL.

    If `FAILED_STEP` is non-empty, call `terminal-notifier` with `-message "パイプライン異常終了 (${FAILED_STEP}で失敗, exit: $EXIT_CODE)"`. If `FAILED_STEP` is empty (no marker found — e.g. the CLI crashed before any STEP echo ran), fall back to the original message format `"パイプライン異常終了 (exit: $EXIT_CODE)"`. Preserve `-title "Investment Agent"` and `-sound Basso` unchanged in both branches.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/run.sh` exits 0
    - `grep -c "grep -o" scripts/run.sh` returns 1
    - `grep -c 'FAILED_STEP' scripts/run.sh` returns 3 or more (assignment + at least one conditional check + interpolation in the message string)
    - `grep -c 'で失敗' scripts/run.sh` returns 1 (new message format present)
    - `grep -c 'パイプライン異常終了 (exit: \$EXIT_CODE)' scripts/run.sh` returns 1 (fallback message preserved for the no-marker-found case)
    - Isolated extraction check (proves the exact idiom works against JSON-embedded marker text): running `printf '%s\n' '{"type":"tool_result","text":"[STEP:deploy:FAIL:git push に失敗]"}' > /tmp/faketest.log && grep -o '\[STEP:[^:]*:FAIL' /tmp/faketest.log | tail -1 | sed -E 's/\[STEP:([^:]*):FAIL/\1/'` outputs `deploy`
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/run.sh && grep -q "grep -o" scripts/run.sh && grep -q "FAILED_STEP" scripts/run.sh</automated>
  </verify>
  <done>Failure-branch terminal-notifier message includes the extracted failed step name when a `[STEP:*:FAIL]` marker exists in the log (e.g. "deployで失敗"), falls back to the original exit-code-only message when no marker is found; success branch is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Fix WR-01 (grep regex fragility) and WR-03 (PROTECT_FILES unquoted expansion) tech debt</name>
  <files>scripts/run.sh</files>
  <read_first>
    scripts/run.sh (current lines 27, 29, 46, 48 — PROTECT_FILES declaration, both for-loops, and the checksum grep)
    .planning/phases/13-operational-stability/13-REVIEW.md (WR-01 lines 65-75, WR-03 lines 89-102 — exact fixes already fully specified)
  </read_first>
  <action>
    Convert the `PROTECT_FILES="docs/index.html docs/portfolio.html"` string declaration (currently line 27) into a bash array literal: `PROTECT_FILES=("docs/index.html" "docs/portfolio.html")`. Update both `for f in $PROTECT_FILES; do` loop headers (currently at lines 29 and 46 — the checksum-record loop and the checksum-verify loop) to `for f in "${PROTECT_FILES[@]}"; do` (quoted array expansion, per WR-03).

    Separately, change the checksum-lookup line `EXPECTED=$(grep "$PROJECT_DIR/$f" "$CHECKSUM_FILE" | awk '{print $1}')` (currently line 48) to use fixed-string matching: `EXPECTED=$(grep -F "$PROJECT_DIR/$f" "$CHECKSUM_FILE" | awk '{print $1}')` (per WR-01).

    These two fixes are independent of Task 1/2's line ranges (35-41 and 67-71) — no overlap.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/run.sh` exits 0
    - `grep -c 'PROTECT_FILES=("docs/index.html" "docs/portfolio.html")' scripts/run.sh` returns 1
    - `grep -c 'for f in "\${PROTECT_FILES\[@\]}"; do' scripts/run.sh` returns 2 (both loops updated)
    - `grep -c 'for f in \$PROTECT_FILES; do' scripts/run.sh` returns 0 (old unquoted-string loop form fully removed)
    - `grep -c 'grep -F "\$PROJECT_DIR/\$f"' scripts/run.sh` returns 1
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/run.sh && grep -q 'PROTECT_FILES=("docs/index.html" "docs/portfolio.html")' scripts/run.sh && grep -q 'grep -F "\$PROJECT_DIR/\$f"' scripts/run.sh</automated>
  </verify>
  <done>PROTECT_FILES is a bash array iterated with quoted `${PROTECT_FILES[@]}` expansion in both loops; the checksum lookup uses `grep -F` for literal string matching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| launchd (unattended scheduled execution) -> scripts/run.sh | System-triggered execution reading local files (checksum paths, log paths) and invoking external binaries (`claude`, `terminal-notifier`, `git`, `shasum`) with no interactive human oversight |
| scripts/run.sh log content -> notification message | `$LOG_FILE` content (now raw JSONL from `claude --output-format stream-json`) is parsed via `grep`/`sed` to build a user-facing notification string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-01 | Tampering | scripts/run.sh:48 checksum lookup `grep` | mitigate | Use `grep -F` for fixed-string path matching so `.` and other regex metacharacters in `$PROJECT_DIR/$f` cannot cause a wrong checksum line to match (WR-01, Task 3) |
| T-14.1-02 | Tampering | scripts/run.sh:27,29,46 `PROTECT_FILES` loop | mitigate | Convert to a bash array iterated with quoted `"${PROTECT_FILES[@]}"`, eliminating word-splitting/glob-expansion risk on the protected-file list (WR-03, Task 3) |
| T-14.1-03 | Repudiation | scripts/run.sh:35-41 claude invocation exit-code capture | mitigate | Replace `|| true` masking with `EXIT_CODE=0` + `|| EXIT_CODE=$?` idiom so a failed pipeline run can never be silently reported/logged as successful (CR-01/D-04, Task 1) |
| T-14.1-04 | Information Disclosure | scripts/run.sh:28 `CHECKSUM_FILE` written to world-readable `/tmp` | accept | Out of scope for this phase's locked decision D-07 (only WR-01/WR-03 addressed); low risk on a single-user local macOS machine; tracked as tech debt IN-02 in 13-REVIEW.md |

</threat_model>

<verification>
Run the following consolidated check after all three tasks are complete:

```bash
bash -n scripts/run.sh \
  && grep -qc -- '--output-format stream-json --verbose' scripts/run.sh \
  && grep -q '>> "$LOG_FILE" 2>&1 || EXIT_CODE=$?' scripts/run.sh \
  && ! grep -q 'PIPESTATUS' scripts/run.sh \
  && grep -q "grep -o" scripts/run.sh \
  && grep -q 'PROTECT_FILES=("docs/index.html" "docs/portfolio.html")' scripts/run.sh \
  && grep -q 'grep -F "$PROJECT_DIR/$f"' scripts/run.sh \
  && echo "ALL CHECKS PASSED"
```

Note (D-11, not a merge gate): a real launchd dry-run confirming actual log reachability and notification content in production remains a post-merge Human-UAT follow-up item, tracked separately — not required before this plan is considered complete.
</verification>

<success_criteria>
- `scripts/run.sh` claude invocation includes `--output-format stream-json --verbose`
- `EXIT_CODE` is captured via `EXIT_CODE=0` + `|| EXIT_CODE=$?` with no `PIPESTATUS`/`|| true` masking remaining on the claude invocation
- Failure notification message includes the extracted failed step name when available
- `PROTECT_FILES` is a bash array; checksum lookup uses `grep -F`
- `bash -n scripts/run.sh` exits 0
</success_criteria>

<output>
Create `.planning/phases/14.1-close-gap-ops-01-ops-03-run-sh-exit-code-0-step/14.1-01-SUMMARY.md` when done
</output>
