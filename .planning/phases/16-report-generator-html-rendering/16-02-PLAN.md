---
phase: 16-report-generator-html-rendering
plan: 02
type: tdd
wave: 2
depends_on: [16-01]
files_modified:
  - src/scripts/generate-news-digest.ts
  - src/scripts/generate-news-digest.test.ts
autonomous: true
requirements: [CURA-03, CURA-04, CURA-06, CURA-07, CURA-08, CURA-09, UI-03]
must_haves:
  truths:
    - "各記事に見出し（元記事への escapeHtml 済み href リンク, D-01）・ソース名・JST絶対時刻（D-02）が3行カード構成（D-03）で表示される"
    - "各記事に日本語の「なぜ重要か」解説コメントが表示される"
    - "記事が市場別（米国株→日本株→グローバル固定順, D-05）グループ内で重要度順（high→medium→low, D-07）に配列される"
    - "各記事に High/Medium/Low バッジが importance と同一ソースから導出されて表示される（D-08 配色）"
    - "各記事に会社名併記（D-04）のティッカーピル（D-09 リンクなし）が表示され、社名欠落時はシンボルのみにフォールバックする"
    - "ページ冒頭に「今日の市場を動かすもの」リード文が表示される"
    - "curation が null の場合（D-12）と articles が空配列の場合（D-06）で、区別された文言のグレースフルなフルHTMLページが返る（0件市場グループも見出し常時表示, D-06）"
    - "既存3レポートと同じダークテーマCSS・モバイル対応が適用され、ページ内ナビは追加しない（D-11）。タイトルは英語+日本語副題（D-13）"
  artifacts:
    - path: "src/scripts/generate-news-digest.ts"
      provides: "generateNewsDigestHtml(curation: NewsCuration | null, date: string): string ピュア関数レンダラー"
      exports: ["generateNewsDigestHtml"]
      min_lines: 90
    - path: "src/scripts/generate-news-digest.test.ts"
      provides: "CURA-03/04/06/07/08/09 + UI-03 のユニットテスト（正常系・null・空配列・0件グループ・社名欠落の5ケース）"
  key_links:
    - from: "src/scripts/generate-news-digest.ts"
      to: "src/scripts/report-utils.ts"
      via: "escapeHtml と generateBaseStyles を import"
      pattern: "from \"\\./report-utils\\.js\""
    - from: "src/scripts/generate-news-digest.ts"
      to: "src/meeting/types.ts"
      via: "NewsCuration / CuratedArticle 型を import"
      pattern: "from \"\\.\\./meeting/types\\.js\""
    - from: "見出しリンク href"
      to: "escapeHtml + rel=noopener"
      via: "全補間箇所（href含む）を escapeHtml し target=_blank に rel=noopener noreferrer を付与"
      pattern: "rel=\"noopener noreferrer\""
---

<objective>
Phase 15 の確定済み契約 `NewsCuration | null` を入力に、news-digest.html の本文HTML文字列を返すピュア関数レンダラー `src/scripts/generate-news-digest.ts` を TDD で実装する。`generate-portfolio-report.ts`（132行）の構造をミラーしつつ、2値ではなく3値フォールバック分岐（null / 空配列 / 通常）に拡張し、市場別グルーピング・重要度安定ソート・重要度バッジ・会社名併記ティッカーピル・リード文を、既存3レポートと同一の Bloomberg 風ダークテーマ（パープルアクセント）で描画する。

Purpose: これが本フェーズの中核。CURA-03/04/06/07/08/09 と UI-03 を満たす。書き出し・パイプライン統合は Phase 17 スコープであり、本プランは I/O を持たない純関数のみを実装する。

Output: `generateNewsDigestHtml(curation, date)` をエクスポートする新規レンダラーと、5ケースを網羅するテストファイル。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-report-generator-html-rendering/16-CONTEXT.md
@.planning/phases/16-report-generator-html-rendering/16-RESEARCH.md
@.planning/phases/16-report-generator-html-rendering/16-PATTERNS.md
@.planning/phases/16-report-generator-html-rendering/16-01-SUMMARY.md

<interfaces>
<!-- Plan 01 で確定済みの入力契約（executor はこれを直接使う） -->

src/meeting/types.ts:
```typescript
export interface CuratedArticle {
  readonly id: string;
  readonly title: string;
  readonly url: string;
  readonly source: string;
  readonly publishedAt: string; // ISO 8601 文字列（Date型ではない）
  readonly market: "us" | "japan" | "global";
  readonly importance: "high" | "medium" | "low";
  readonly commentary: string;
  readonly tickers: ReadonlyArray<string>;
  readonly tickerNames?: Readonly<Record<string, string>>; // symbol -> 会社名（Plan 01 追加, D-04）
}
export interface NewsCuration {
  readonly date: string;
  readonly generatedAt: string;
  readonly leadIn: string; // CURA-09 リード文
  readonly articles: ReadonlyArray<CuratedArticle>;
}
```

src/scripts/report-utils.ts（再利用する既存エクスポート）:
```typescript
export function escapeHtml(text: string): string; // & < > " ' をエスケープ
export function generateBaseStyles(accentColor: string): string; // ダークテーマ+レスポンシブCSS。"#8b5cf6" は Plan 01 で ACCENT_VARIANTS 登録済み
```

手本（generate-portfolio-report.ts）の構造:
- 先頭: import escapeHtml/generateBaseStyles + type
- color-mapping ヘルパ（decisionColor 相当）
- 小さな format*Html 純関数群
- トップレベル export: styles = generateBaseStyles(accent) → timestamp → null分岐（フルHTMLシェル）→ 通常分岐（同シェルに fragment 補間）
</interfaces>
</context>

<feature>
  <name>generateNewsDigestHtml — news-digest.html 本文レンダラー</name>
  <files>src/scripts/generate-news-digest.ts, src/scripts/generate-news-digest.test.ts</files>
  <read_first>
    - src/scripts/generate-portfolio-report.ts（構造の直接の手本 — import・color helper・format*Html群・null分岐フルシェル・通常分岐の全パターン）
    - src/scripts/report-utils.ts（escapeHtml と generateBaseStyles のシグネチャ、.agent-card スタイル）
    - src/meeting/types.ts（Plan 01 で tickerNames 追加後の CuratedArticle / NewsCuration）
    - src/scripts/generate-report.test.ts の describe("Portfolio Report") ブロック（await import + toContain + null分岐テストの慣例）
    - .planning/phases/16-report-generator-html-rendering/16-RESEARCH.md（Pattern 1〜4 のコード例、Pitfall 2〜4）
  </read_first>
  <behavior>
    RED フェーズで generate-news-digest.test.ts に以下を記述する（各 it は `await import("./generate-news-digest.js")` + `toContain` 慣例、fixture はモジュール top-level const）。
    fixture: (1) validCuration — 3+記事、us/japan/global と high/medium/low を跨ぐ、tickerNames 付き記事を含む / (2) 社名欠落記事（tickerNames 無し）を含むケース / (3) 空配列 curation / (4) null。

    - CURA-03 見出し: 出力に各記事の `<a href="..." target="_blank" rel="noopener noreferrer">` があり、href が escapeHtml 済み（`&` を含むURLは `&amp;` に変換）、ソース名と JST時刻文字列を含む
    - CURA-04 解説コメント: 各記事の commentary テキストが出力に含まれる
    - CURA-06 重要度順: 同一市場グループ内で high 記事が medium/low より前に出現する（出力文字列上のインデックス比較）
    - CURA-07 バッジ: 出力に "High"/"Medium"/"Low" ラベルと importance 由来の色 #ef4444 / #f59e0b / #6b7280 が含まれる
    - CURA-08 ティッカー: tickerNames 付き記事は "NVDA エヌビディア" 形式のピルを含み、tickerNames 欠落記事はシンボルのみ（例 "AAPL"）のピルを含む
    - CURA-09 リード文: leadIn テキストが出力の市場セクションより前（ページ冒頭付近）に出現する
    - UI-03 テーマ: 出力に "#8b5cf6"（generateBaseStyles 経由）と "<!DOCTYPE html>" と viewport meta を含む
    - フォールバック（null）: `generateNewsDigestHtml(null, "2026-07-02")` が "<!DOCTYPE html>" と "生成できませんでした" を含み、記事カードを含まない
    - フォールバック（空配列）: articles=[] の curation が "厳選記事なし" を含み、"生成できませんでした" は含まない（Pitfall 2 — 2つの文言を混同しない）
    - 0件市場グループ: 一部市場のみ記事がある curation で、記事0件の市場も見出しラベル（例 "グローバル"）と "本日の該当記事なし" を出力する（D-06、3セクション常時表示）
    - 市場順: 出力上で "米国株" が "日本株" より前、"日本株" が "グローバル" より前（D-05 固定順）
  </behavior>
  <implementation>
    GREEN フェーズで generate-news-digest.ts を実装する。シグネチャは `export function generateNewsDigestHtml(curation: NewsCuration | null, date: string): string`（RESEARCH.md Open Question 1 の推奨に確定 — null 時にタイトル日付が必要なため date を第2引数で受ける。Phase 17 呼び出し側は meetingResult.date を渡す）。

    構造（generate-portfolio-report.ts をミラー）:
    - import: `escapeHtml`, `generateBaseStyles` を "./report-utils.js" から / `NewsCuration`, `CuratedArticle` を "../meeting/types.js" から
    - `MARKET_ORDER`: `[{value:"us",label:"米国株"},{value:"japan",label:"日本株"},{value:"global",label:"グローバル"}]`（D-05 固定順）
    - `IMPORTANCE_ORDER`: `{high:0, medium:1, low:2}`（ソートキーとバッジ導出の単一ソース, CURA-06/07）
    - `importanceColor`: high=#ef4444 / medium=#f59e0b / low=#6b7280（D-08）
    - `importanceBadgeHtml`: High/Medium/Low ラベルを色付き span で（バッジは importance から導出, CURA-07）
    - `formatPublishedAtJst(iso: string)`: `new Date(iso).toLocaleString("ja-JP", {timeZone:"Asia/Tokyo", month:"numeric", day:"numeric", hour:"2-digit", minute:"2-digit", hour12:false})`。`Date.now()` は絶対に使わない（D-02, アーカイブ整合性）
    - `sortByImportance(articles)`: `[...articles].sort((a,b)=>IMPORTANCE_ORDER[a.importance]-IMPORTANCE_ORDER[b.importance])`（ネイティブ安定ソート, イミュータブル, CURA-06）
    - `formatTickerPillsHtml(a)`: a.tickers を map、各 symbol につき `a.tickerNames?.[symbol]` があれば `${symbol} ${name}` なければ symbol のみ（D-04 フォールバック）、`escapeHtml` した上でピル span（リンクなし, D-09）
    - `safeHref(url)`: `url.startsWith("http://")||url.startsWith("https://")` を検証し、非 http(s) はリンクを張らずプレーンテキスト見出しにフォールバック（javascript:/data: スキーム防御, RESEARCH.md Security Domain 最終防衛線）
    - `formatArticleCardHtml(a)`: 3行構成（D-03）— 1行目 バッジ+見出しリンク（`<a href="${escapeHtml(safeHref)}" target="_blank" rel="noopener noreferrer">${escapeHtml(a.title)}</a>`, D-01, Pitfall 3/4）/ 2行目 ソース・JST時刻・ティッカーピルのメタ行 / 3行目 `escapeHtml(a.commentary)`。.agent-card ベースを再利用
    - `formatMarketGroupsHtml(articles)`: MARKET_ORDER を map、各市場の記事を filter + sortByImportance、0件なら見出し+"本日の該当記事なし"、1件以上ならカード群。見出しラベルは escapeHtml（D-06）
    - `formatLeadInHtml(leadIn)`: "今日の市場を動かすもの" 見出し + escapeHtml(leadIn) をページ冒頭ブロックに（CURA-09）
    - `renderShell(styles, timestamp, date, bodyHtml)`: 共通の `<!DOCTYPE html>...` シェル（タイトル "News Digest - ${escapeHtml(date)}", 日本語副題 "AI厳選ニュースダイジェスト" D-13, generateBaseStyles("#8b5cf6")）。3分岐でシェル重複を避けるため抽出
    - トップレベル `generateNewsDigestHtml(curation, date)`: styles=generateBaseStyles("#8b5cf6"), timestamp=`new Date().toLocaleString("ja-JP",{timeZone:"Asia/Tokyo"})`。分岐1 curation===null → renderShell + "本日のニュースキュレーションは生成できませんでした"（D-12）。分岐2 curation.articles.length===0 → renderShell + "本日は厳選記事なし"（D-06後半）。分岐3 → renderShell(formatLeadInHtml + formatMarketGroupsHtml)。
    - try/catch は入れない（ピュア関数、I/O・エラー境界は Phase 17 の generate-report.ts 側）
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| キュレーションデータ → HTML出力 | commentary/leadIn/tickerNames は LLM由来、title/url/source は Phase 15 でプール解決済み（LLM非エコー）。全てが最終的にGitHub Pagesの静的HTMLへ描画される |
| news-digest.html → 外部リンク先 | 本コードベース初の `<a href target="_blank">` 描画（reverse tabnabbing の新規面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering | formatArticleCardHtml の href/title/commentary/tickerNames 補間 | mitigate | 全補間箇所（href属性含む）を `escapeHtml()` に通す。既存 report-utils.escapeHtml を再利用（Pitfall 3, ASVS V5）。テストで `&`→`&amp;` の href エスケープを検証 |
| T-16-02-02 | Tampering / Spoofing | 見出しリンク target="_blank" | mitigate | `rel="noopener noreferrer"` を target="_blank" と常にセットで付与（reverse tabnabbing, Pitfall 4） |
| T-16-02-03 | Tampering | href の URL スキーム | mitigate | safeHref で `http(s)://` 以外のスキーム（javascript:/data:）はリンク化せずプレーンテキスト見出しにフォールバック（RESEARCH.md Security Domain 最終防衛線） |
| T-16-02-04 | Information Disclosure | JST時刻レンダリング | accept | publishedAt 文字列からのみ導出、Date.now() 不使用でアーカイブ整合性を保つ（D-02）。機微情報なし |
| T-16-02-SC | Tampering | npm installs | mitigate | 新規パッケージインストールなし（RESEARCH.md Package Legitimacy Audit: 対象パッケージなし）。既存 vitest/typescript のみ |
</threat_model>

<verification>
- `npx vitest run src/scripts/generate-news-digest.test.ts` が全パス（5ケース + CURA-03/04/06/07/08/09/UI-03 全アサーション）
- `npm test`（全体スイート）が全パス、Phase 15/既存レポートテストに回帰なし
- `npx tsc --noEmit` が型エラー0
- `grep -c "rel=\"noopener noreferrer\"" src/scripts/generate-news-digest.ts` が 1 以上
- `grep -c "Date.now()" src/scripts/generate-news-digest.ts` が 0（D-02 相対時刻禁止）
</verification>

<success_criteria>
- generateNewsDigestHtml(curation, date) が null/空配列/通常の3値分岐で区別された正しいHTMLを返す
- 市場別グルーピング（固定順・0件グループも見出し表示）+ グループ内重要度安定ソートが機能する
- 見出しリンクが escapeHtml 済み href + target=_blank + rel=noopener noreferrer + スキーム検証で安全に描画される
- 会社名併記ティッカーピル・重要度バッジ・リード文・JST絶対時刻が表示される
- 既存3レポートと同じダークテーマ（#8b5cf6 アクセント）・モバイル対応が適用される
- 全テストグリーン、既存スイートに回帰なし
</success_criteria>

<output>
Create `.planning/phases/16-report-generator-html-rendering/16-02-SUMMARY.md` when done
</output>
