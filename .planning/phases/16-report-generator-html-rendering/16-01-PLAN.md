---
phase: 16-report-generator-html-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/meeting/types.ts
  - src/meeting/schemas.ts
  - src/meeting/schemas.test.ts
  - src/scripts/report-utils.ts
  - src/scripts/report-utils.test.ts
autonomous: true
requirements: [CURA-08, UI-03]
must_haves:
  truths:
    - "CuratedArticle は tickerNames?（symbol→会社名の任意マップ）を持てる（D-04）"
    - "tickerNames を含むキュレーションJSONがバリデーション・解決を通過し、既存のPhase 15テストが1件も壊れない（D-04 加法的変更）"
    - "generateBaseStyles(\"#8b5cf6\") がパープルの light/lighter グラデーション（#a78bfa / #c4b5fd）を出力する（D-10）"
  artifacts:
    - path: "src/meeting/types.ts"
      provides: "CuratedArticle.tickerNames? オプショナルフィールド"
      contains: "tickerNames"
    - path: "src/meeting/schemas.ts"
      provides: "curatedArticleRawSchema の tickerNames フィールド + resolveNewsCuration での透過"
      contains: "tickerNames"
    - path: "src/scripts/report-utils.ts"
      provides: "ACCENT_VARIANTS の #8b5cf6 エントリ"
      contains: "#8b5cf6"
  key_links:
    - from: "src/meeting/schemas.ts resolveNewsCuration"
      to: "CuratedArticle.tickerNames"
      via: "resolved.push に tickerNames: item.tickerNames を追加"
      pattern: "tickerNames:\\s*item\\.tickerNames"
---

<objective>
Phase 16 レンダラー（Plan 02）が消費する2つの加法的な契約変更を先行して確定する。(1) `CuratedArticle` に会社名併記（D-04）を可能にする `tickerNames?` フィールドを非破壊で追加し、zodスキーマと `resolveNewsCuration` に通す。(2) news-digest 用アクセントカラー（パープル #8b5cf6, D-10）を `report-utils.ts` の `ACCENT_VARIANTS` に追加する。

Purpose: Plan 02 のレンダラーは `a.tickerNames?.[symbol]` を参照し `generateBaseStyles("#8b5cf6")` を呼ぶため、その型・スキーマ・スタイル基盤を先に用意する。すべて既存ファイルへの加法的変更であり、Phase 15 の既存テストを壊してはならない（RESEARCH.md Pitfall 1）。

Output: 更新された types.ts / schemas.ts / report-utils.ts と、加法変更を検証する新規テストアサーション。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-report-generator-html-rendering/16-CONTEXT.md
@.planning/phases/16-report-generator-html-rendering/16-RESEARCH.md
@.planning/phases/16-report-generator-html-rendering/16-PATTERNS.md

<interfaces>
<!-- 変更対象の現在の契約（executor はこれらを加法的に拡張するのみ） -->

src/meeting/types.ts (CuratedArticle, 現状 — tickers は ReadonlyArray<string> のまま変えない):
```typescript
export interface CuratedArticle {
  readonly id: string;
  readonly title: string;
  readonly url: string;
  readonly source: string;
  readonly publishedAt: string; // ISO 8601
  readonly market: "us" | "japan" | "global";
  readonly importance: "high" | "medium" | "low";
  readonly commentary: string;
  readonly tickers: ReadonlyArray<string>;
}
```

加法対象の前例（同ファイル HoldingEvaluation.riskNote? — 末尾オプショナル readonly の慣例）:
```typescript
  readonly riskNote?: string;
```

src/meeting/schemas.ts (curatedArticleRawSchema, 現状):
```typescript
const curatedArticleRawSchema = z
  .object({
    id: z.string().min(1),
    market: z.enum(["us", "japan", "global"]),
    importance: z.enum(["high", "medium", "low"]),
    commentary: z.string().optional().default(""),
    tickers: z.array(z.string()).optional().default([]),
  })
  .passthrough();
```

src/meeting/schemas.ts (resolveNewsCuration の resolved.push サイト — tickers は pool.ticker と Set マージ、tickerNames は pool に情報がないため純粋透過):
```typescript
    resolved.push({
      id: item.id,
      title: source.title,
      ...
      tickers: source.ticker ? [...new Set([source.ticker, ...item.tickers])] : item.tickers,
    });
```

src/scripts/report-utils.ts (ACCENT_VARIANTS, 現状 — 4色目を追加):
```typescript
const ACCENT_VARIANTS: Record<string, { light: string; lighter: string }> = {
  "#3b82f6": { light: "#60a5fa", lighter: "#93c5fd" },
  "#f59e0b": { light: "#fbbf24", lighter: "#fcd34d" },
  "#10b981": { light: "#34d399", lighter: "#6ee7b7" },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CuratedArticle.tickerNames を契約に加法追加（type + schema + resolver）</name>
  <files>src/meeting/types.ts, src/meeting/schemas.ts, src/meeting/schemas.test.ts</files>
  <read_first>
    - src/meeting/types.ts（CuratedArticle と HoldingEvaluation.riskNote? の加法慣例を確認）
    - src/meeting/schemas.ts（curatedArticleRawSchema と resolveNewsCuration の resolved.push サイト）
    - src/meeting/schemas.test.ts（makeRawArticle / makePoolEntry ファクトリと既存の tickers: ["AAPL"] / toEqual(["AAPL","MSFT"]) アサーション — これらは絶対に変更しない）
  </read_first>
  <behavior>
    - Test: tickerNames を含む raw JSON を validateRawNewsCuration → resolveNewsCuration に通すと、解決後の CuratedArticle.tickerNames にそのマップ（例 { NVDA: "エヌビディア" }）が透過される
    - Test: tickerNames を省略した raw JSON では解決後の tickerNames がデフォルトの空オブジェクト {} になる
    - Test（回帰）: 既存の tickers マージテスト（source.ticker + item.tickers の Set 結合、toEqual(["AAPL","MSFT"]) 等）が一切変更なしでパスし続ける
  </behavior>
  <action>
    types.ts: `CuratedArticle` インターフェース末尾に `readonly tickerNames?: Readonly<Record<string, string>>;`（symbol→会社名の任意マップ, D-04）を追加する。`HoldingEvaluation.riskNote?` と同じ末尾オプショナル readonly の書式に合わせる。既存の `tickers: ReadonlyArray<string>` の型は変更しない（RESEARCH.md Pitfall 1 の load-bearing 制約 — `{symbol,name?}[]` 化は禁止、既存 schemas.test.ts のアサーションを壊す）。

    schemas.ts: `curatedArticleRawSchema` の `.object({...})` に `tickerNames: z.record(z.string(), z.string()).optional().default({}),` を追加する（zod v4 は `z.record` の2引数形式が必須）。`resolveNewsCuration` の `resolved.push({...})` に `tickerNames: item.tickerNames,` を1行追加する。pool 側に会社名情報はないためマージロジックは書かず純粋透過（tickers の Set マージロジックは変更しない）。drop/warn の既存パスも変更しない。

    schemas.test.ts: 既存の `makeRawArticle` / `makePoolEntry` ファクトリ（overrides 方式）を使い、tickerNames の透過を検証する新規 it を追加する。(1) `makeRawArticle({ tickerNames: { NVDA: "エヌビディア" } })` を解決 → 結果の該当記事 `.tickerNames` が `{ NVDA: "エヌビディア" }` に等しい、(2) tickerNames を渡さない既定ケースで `.tickerNames` が `{}` になる、の2アサーションを追加する。既存の fixture・アサーションは1文字も変更しない。
  </action>
  <verify>
    <automated>npx vitest run src/meeting/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tickerNames" src/meeting/types.ts` が CuratedArticle 内のオプショナルフィールド定義にヒットする
    - `grep -n "tickerNames" src/meeting/schemas.ts` が curatedArticleRawSchema の定義行と resolveNewsCuration の resolved.push 行の両方にヒットする
    - `src/meeting/types.ts` の `tickers` フィールドが `ReadonlyArray<string>` のまま（`{symbol` を含まない）: `grep -c "tickers: ReadonlyArray<string>" src/meeting/types.ts` が 1 を返す
    - `npx vitest run src/meeting/schemas.test.ts` が全パス（既存テスト含め0失敗）し、新規の tickerNames 透過テスト2件を含む
    - `npx tsc --noEmit` が型エラー0
  </acceptance_criteria>
  <done>tickerNames が type/schema/resolver を非破壊で通過し、既存Phase 15テストが全パス、新規透過テストがグリーン</done>
</task>

<task type="auto">
  <name>Task 2: ACCENT_VARIANTS にパープル #8b5cf6 を追加（D-10）</name>
  <files>src/scripts/report-utils.ts, src/scripts/report-utils.test.ts</files>
  <read_first>
    - src/scripts/report-utils.ts（ACCENT_VARIANTS マップ 1-5行目、generateBaseStyles のフォールバック挙動 80行目付近）
    - src/scripts/report-utils.test.ts（既存の generateBaseStyles("#3b82f6") + toContain 構造アサーションの書式）
  </read_first>
  <action>
    report-utils.ts: `ACCENT_VARIANTS` マップに4色目 `"#8b5cf6": { light: "#a78bfa", lighter: "#c4b5fd" },`（Tailwind violet-500/400/300）を追加する（D-10）。既存3エントリ（#3b82f6 / #f59e0b / #10b981）は変更しない。`generateBaseStyles` 関数本体は変更不要（既存の `ACCENT_VARIANTS[accentColor] ?? {...}` フォールバックがそのまま動く）。

    report-utils.test.ts: 既存の toContain 書式に合わせ、`generateBaseStyles("#8b5cf6")` の出力が `#8b5cf6`（accent）・`#a78bfa`（light）・`#c4b5fd`（lighter）を含むことを検証する新規 it を追加する。既存4テストは変更しない。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/report-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "#8b5cf6" src/scripts/report-utils.ts` が ACCENT_VARIANTS 内のエントリにヒットする
    - `grep -c "#a78bfa" src/scripts/report-utils.ts` が 1 以上を返す
    - `npx vitest run src/scripts/report-utils.test.ts` が全パスし、パープルアクセントの新規アサーションを含む
    - 既存3エントリ（#3b82f6 / #f59e0b / #10b981）が残存: `grep -c "#3b82f6" src/scripts/report-utils.ts` が 1 以上
  </acceptance_criteria>
  <done>generateBaseStyles("#8b5cf6") がパープルのグラデーション variant を出力し、既存アクセントテストに回帰がない</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/Agent 出力 → tickerNames 契約 | キュレーションAgentが将来出力する会社名マップ（Phase 17）が zod 契約を越えて型に入る |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | curatedArticleRawSchema.tickerNames | mitigate | `z.record(z.string(), z.string())` で構造検証、非文字列値は zod が拒否。値のHTMLエスケープはレンダリング側（Plan 02）の責務 |
| T-16-01-02 | Denial of Service | resolveNewsCuration | accept | tickerNames は `.optional().default({})` で欠落・不正時も throw せずグレースフル（既存 console.warn+continue 方針を踏襲、新規エラーパスなし） |
| T-16-01-SC | Tampering | npm installs | mitigate | 本プランは新規パッケージインストールなし（zod/vitest/typescript の既存再利用のみ、RESEARCH.md Package Legitimacy Audit 参照）。対象パッケージなし |
</threat_model>

<verification>
- `npx vitest run src/meeting/schemas.test.ts src/scripts/report-utils.test.ts` が全パス
- `npx tsc --noEmit` が型エラー0
- 既存 Phase 15 テストに回帰なし
</verification>

<success_criteria>
- CuratedArticle が tickerNames? を持ち、schema/resolver を非破壊で透過する
- ACCENT_VARIANTS に #8b5cf6 が追加され generateBaseStyles がパープルグラデーションを出力する
- 既存テスト全パス + 新規加法テストがグリーン
</success_criteria>

<output>
Create `.planning/phases/16-report-generator-html-rendering/16-01-SUMMARY.md` when done
</output>
