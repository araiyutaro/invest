---
phase: 16-report-generator-html-rendering
plan: 03
type: tdd
wave: 1
depends_on: [16-02]
gap_closure: true
files_modified:
  - src/scripts/generate-news-digest.test.ts
  - src/scripts/report-utils.ts
  - src/scripts/generate-news-digest.ts
autonomous: true
requirements: [CURA-08]
must_haves:
  truths:
    - "各記事の関連ティッカーが、背景・パディング・角丸・ピル間の余白を持つ視覚的な「ピル」として描画される（.ticker-pill CSS がダークテーマで定義される）"
    - "1記事に複数ティッカーがある場合（例 NVDA + MSFT）、各ピルが区切りをもって描画され、社名と次シンボルが連結して判読不能になることがない"
    - ".news-meta メタ行（ソース・時刻・ティッカー）にダークテーマの CSS スタイルが適用される"
    - "複数ティッカーを持つ記事のユニットテストが存在し、CSS 未定義・区切りなしの欠陥を検出できる"
  artifacts:
    - path: "src/scripts/report-utils.ts"
      provides: "generateBaseStyles 内の .ticker-pill / .news-meta CSS ルール（ダークテーマ・レスポンシブ）"
      contains: ".ticker-pill"
    - path: "src/scripts/generate-news-digest.ts"
      provides: "formatTickerPillsHtml の区切りありピル結合（複数ティッカー可読性）"
      contains: "ticker-pill"
    - path: "src/scripts/generate-news-digest.test.ts"
      provides: "複数ティッカー記事のテストケース + .ticker-pill/.news-meta CSS 定義存在アサーション"
  key_links:
    - from: "src/scripts/generate-news-digest.ts formatTickerPillsHtml"
      to: "src/scripts/report-utils.ts generateBaseStyles"
      via: "<span class=\"ticker-pill\"> が参照するクラスを generateBaseStyles が定義する"
      pattern: "\\.ticker-pill"
    - from: "generate-news-digest.ts formatArticleCardHtml"
      to: "report-utils.ts generateBaseStyles"
      via: "<p class=\"news-meta\"> が参照するクラスを generateBaseStyles が定義する"
      pattern: "\\.news-meta"
---

<objective>
Phase 16 の検証（16-VERIFICATION.md）で唯一ブロッキングとなった Truth #5 / CURA-08 の欠陥を閉じる。`formatTickerPillsHtml` が生成する `<span class="ticker-pill">` と `formatArticleCardHtml` が生成する `<p class="news-meta">` に対応する CSS が `generateBaseStyles`（report-utils.ts）に一切定義されておらず、ティッカーが「ピル」という UI 要件（D-09）を視覚的に満たしていない。加えて `formatTickerPillsHtml` が複数ピルを `.join("")` で区切りなしに連結しているため、1記事に複数ティッカーがある場合（NVDA + MSFT 等）に社名と次シンボルが連結して判読不能になる。

Purpose: CURA-08（関連ティッカータグ表示）のUI要件を成立させる。既存12テストは全記事が単一ティッカーのフィクスチャで欠陥をすり抜けていたため、複数ティッカーのテストを RED として先に追加する（TDD）。

Output: 複数ティッカーのテストケースを含む更新テスト、`.ticker-pill`/`.news-meta` の CSS 定義、区切りありのピル結合。既存プラン 16-01/16-02 は変更しない。スコープは CSS + 区切り + テストの追補のみ。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-report-generator-html-rendering/16-VERIFICATION.md
@.planning/phases/16-report-generator-html-rendering/16-02-PLAN.md

<interfaces>
<!-- 既存の該当箇所（executor はこれを直接使う） -->

src/scripts/generate-news-digest.ts formatTickerPillsHtml（L49-58, 現状 — 区切りなし join）:
- a.tickers を map し、各 symbol につき a.tickerNames?.[symbol] があれば `${symbol} ${name}` なければ symbol、escapeHtml した上で `<span class="ticker-pill">…</span>`
- 現状 `.join("")` — ここが区切りなし連結の欠陥

src/scripts/generate-news-digest.ts formatArticleCardHtml（L77-81）:
- メタ行を `<p class="news-meta">${source} ・ ${timeJst}${metaTail}</p>` で出力

src/scripts/report-utils.ts generateBaseStyles（L80-185）:
- accentColor を受け ACCENT_VARIANTS から light/lighter を導出
- `<style>…</style>` を返す。既存クラス: .agent-card（background #1e1e2e, border-radius 8px, padding 1.2rem, border-left 4px）, .discussion-card, .timestamp（color #888, font-size 0.85rem）
- @media (max-width: 768px) レスポンシブブロックあり
- 追加する .ticker-pill / .news-meta はこの `<style>` 内、@media ブロックの前に挿入する
</interfaces>
</context>

<feature>
  <name>ティッカーピル CSS 定義 + 複数ティッカー区切り修正</name>
  <files>src/scripts/generate-news-digest.test.ts, src/scripts/report-utils.ts, src/scripts/generate-news-digest.ts</files>

  <task type="auto" tdd="true">
  <name>Task 1 (RED): 複数ティッカー記事テスト + CSS 定義存在アサーションを追加</name>
  <files>src/scripts/generate-news-digest.test.ts</files>
  <read_first>
    - src/scripts/generate-news-digest.test.ts（既存12テストの慣例 — top-level const fixture、`await import("./generate-news-digest.js")`、`toContain` アサーション、`describe("generateNewsDigestHtml")` ブロック）
    - src/scripts/generate-news-digest.ts（formatTickerPillsHtml の現状 join、formatArticleCardHtml の news-meta 出力、CuratedArticle の tickers/tickerNames の使い方）
    - .planning/phases/16-report-generator-html-rendering/16-VERIFICATION.md（gaps: の missing 3項目 — CSS定義追加・区切り追加・複数ティッカーテスト追加）
  </read_first>
  <behavior>
    RED フェーズ。既存の `describe("generateNewsDigestHtml")` ブロック内に新規 it を追加する。新規 top-level fixture `articleUsMultiTicker`（market "us"、importance "high"、tickers ["NVDA","MSFT"]、tickerNames {NVDA:"エヌビディア", MSFT:"マイクロソフト"}）と、それを含む `multiTickerCuration`（leadIn 非空、articles にこの1記事を含む）を追加する。fixture の url/source/publishedAt/commentary は既存 articleUsHigh に倣う（url は https スキーム）。
    - CURA-08 複数ティッカー: 出力に "NVDA エヌビディア" と "MSFT マイクロソフト" の両方を含む
    - CURA-08 区切り: 出力に隣接ピルが区切りなしで連結された文字列（`</span><span class="ticker-pill">`）を **含まない**（`expect(html).not.toContain('</span><span class="ticker-pill">')`）。現状 `.join("")` はこれを直接生成するため RED になる
    - CSS 定義（ピル）: 出力（generateBaseStyles 経由の style を含む完全HTML）に CSS セレクタ文字列 ".ticker-pill" を含む。現状 report-utils.ts 未定義のため RED
    - CSS 定義（メタ行）: 出力に CSS セレクタ文字列 ".news-meta" を含む。現状未定義のため RED
    既存12テストは削除・改変しない（追加のみ）。
  </behavior>
  <action>
    src/scripts/generate-news-digest.test.ts に、既存 fixture 群（articleUsHigh 等の直後）へ `articleUsMultiTicker` と `multiTickerCuration` を追加する。articleUsMultiTicker は id "us-multi"、tickers ["NVDA","MSFT"]、tickerNames {NVDA:"エヌビディア", MSFT:"マイクロソフト"}、market "us"、importance "high"、url は "https://example.com/news/multi"（http(s) スキーム）、source/publishedAt/commentary/title は任意の妥当値。`describe("generateNewsDigestHtml")` 末尾に it を1件追加し、`generateNewsDigestHtml(multiTickerCuration, baseDate)` の出力に対し4アサーションを行う: (1) toContain "NVDA エヌビディア"、(2) toContain "MSFT マイクロソフト"、(3) not.toContain 文字列 `</span><span class="ticker-pill">`（区切りなし連結の禁止）、(4) toContain ".ticker-pill"（CSS 定義存在）と toContain ".news-meta"（CSS 定義存在）。この時点で新規 it は RED（3/4 アサーションが現状コードで失敗）であることを確認する。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-news-digest.test.ts 2>&1 | grep -q "ticker-pill\|failed\|FAIL" && echo "RED confirmed (new test fails on current code)"</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/generate-news-digest.test.ts に `articleUsMultiTicker` および `multiTickerCuration` の const が存在する（`grep -c "articleUsMultiTicker" src/scripts/generate-news-digest.test.ts` >= 2）
    - 新規 it が `not.toContain('</span><span class="ticker-pill">')` と `toContain(".ticker-pill")` と `toContain(".news-meta")` を含む
    - `npx vitest run src/scripts/generate-news-digest.test.ts` を実行すると新規テストが FAIL する（RED — 現状コードに CSS 定義と区切りがないため）
    - 既存12件の it は改変されておらず、追加のみである（`grep -c "  it(" src/scripts/generate-news-digest.test.ts` が 13 以上）
  </acceptance_criteria>
  <done>複数ティッカー fixture と RED テストが追加され、現状コードで新規テストが失敗する</done>
  </task>

  <task type="auto" tdd="true">
  <name>Task 2 (GREEN): .ticker-pill/.news-meta CSS 追加 + 区切りありピル結合</name>
  <files>src/scripts/report-utils.ts, src/scripts/generate-news-digest.ts</files>
  <read_first>
    - src/scripts/report-utils.ts（generateBaseStyles L80-185 の `<style>` 本文、.agent-card L160-166・.discussion-card L168-175・.timestamp L155-159 のダークテーマ配色パターン、@media L176-182 の位置）
    - src/scripts/generate-news-digest.ts（formatTickerPillsHtml L49-58 の join、escapeHtml 適用箇所）
    - src/scripts/generate-news-digest.test.ts（Task 1 で追加した RED テストのアサーション）
  </read_first>
  <action>
    (1) src/scripts/report-utils.ts の generateBaseStyles が返すテンプレートリテラル内、@media (max-width: 768px) ブロックの直前に `.ticker-pill` と `.news-meta` の CSS ルールを追加する。`.ticker-pill` はダークテーマのピルとして: display inline-block、background #2a2a3e、color #c4b5fd、font-size 0.8rem、padding 0.15rem 0.5rem、margin-right 0.4rem、margin-bottom 0.3rem、border-radius 999px、border 1px solid #3f3f5a。`.news-meta` はメタ行として: color #888、font-size 0.85rem、margin-bottom 0.5rem（既存 .timestamp の配色に整合）。既存クラス（.agent-card 等）は変更しない。
    (2) src/scripts/generate-news-digest.ts の formatTickerPillsHtml で `.join("")` を `.join(" ")`（半角スペース区切り）に変更し、隣接ピルが区切りなしで連結しないようにする。escapeHtml(label) の適用は現状のまま維持する（削除・変更しない）。
    GREEN 後、Task 1 の新規テストを含む全テストが通ることを確認する。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-news-digest.test.ts src/scripts/report-utils.test.ts && npx tsc --noEmit 2>&1 | grep -v "finnhub.ts\|collect-data.test.ts" | grep -c "error TS" | grep -q "^0$" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.ticker-pill" src/scripts/report-utils.ts` >= 1（CSS 定義が存在）
    - `grep -c "\.news-meta" src/scripts/report-utils.ts` >= 1（CSS 定義が存在）
    - `grep -c 'join("")' src/scripts/generate-news-digest.ts` が 0（区切りなし join が formatTickerPillsHtml から除去された）
    - `grep -c "escapeHtml(label)" src/scripts/generate-news-digest.ts` >= 1（ティッカー/社名の HTML エスケープが維持されている — T-16-03-01）
    - `npx vitest run src/scripts/generate-news-digest.test.ts` が全パス（Task 1 の新規テスト含む 13 件以上、GREEN）
    - `npx vitest run`（全体スイート）が全パス、既存3レポート・Phase 15 に回帰なし
    - Phase 16 対象ファイルの `npx tsc --noEmit` 型エラー 0（finnhub.ts / collect-data.test.ts の既存 pre-existing エラーは対象外）
  </acceptance_criteria>
  <done>.ticker-pill/.news-meta CSS が generateBaseStyles に定義され、複数ピルが区切りをもって描画され、全テスト（新規含む）がグリーン</done>
  </task>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| キュレーションデータ（ticker/社名）→ HTML出力 | tickerNames は LLM由来、tickers は Phase 15 プール解決値。CSS ピルとして静的HTMLに描画される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering | formatTickerPillsHtml の label（symbol + tickerNames 社名）補間 | mitigate | 既存の `escapeHtml(label)` 適用を維持する（削除・改変しない）。区切り変更（join(" ")）は補間値をエスケープする箇所を通さないため XSS 面を増やさない。acceptance_criteria で `escapeHtml(label)` の残存を grep 検証 |
| T-16-03-02 | Tampering | 追加 CSS（.ticker-pill/.news-meta）は静的リテラル | accept | CSS ルールは動的補間を含まない固定文字列。ユーザー入力が CSS に流れ込む経路なし |
| T-16-03-SC | Tampering | npm installs | mitigate | 新規パッケージインストールなし。既存 vitest/typescript のみ使用 |
</threat_model>

<verification>
- `npx vitest run src/scripts/generate-news-digest.test.ts` が全パス（複数ティッカー新規テスト含む 13 件以上）
- `npx vitest run` 全体スイートが全パス、既存3レポート・Phase 15 に回帰なし
- `grep -c "\.ticker-pill" src/scripts/report-utils.ts` >= 1 かつ `grep -c "\.news-meta" src/scripts/report-utils.ts` >= 1
- `grep -c 'join("")' src/scripts/generate-news-digest.ts` == 0
- `grep -c "escapeHtml(label)" src/scripts/generate-news-digest.ts` >= 1（エスケープ維持）
- `npx tsc --noEmit`（Phase 16 対象ファイル）型エラー 0
</verification>

<success_criteria>
- 検証ギャップ Truth #5 / CURA-08 が解消: ティッカーが背景・角丸・余白を持つ視覚的ピルとして描画される
- 複数ティッカー記事が区切りをもって可読に描画され、社名と次シンボルの連結が発生しない
- .news-meta メタ行にダークテーマスタイルが適用される
- 複数ティッカーのテストが欠陥を検出可能な形で存在し、全テストグリーン
- 既存プラン 16-01/16-02 は未変更、既存スイートに回帰なし
- escapeHtml による HTML エスケープが維持されている（T-16-03-01）
</success_criteria>

<output>
Create `.planning/phases/16-report-generator-html-rendering/16-03-SUMMARY.md` when done
</output>
</content>
</invoke>
