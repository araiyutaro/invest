---
phase: 24-digest-meeting-cross-reference
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/scripts/generate-news-digest.ts
  - src/scripts/generate-news-digest.test.ts
  - src/scripts/report-utils.ts
autonomous: true
requirements: [XREP-01]
must_haves:
  truths:
    - "An annotated article renders a <p class=\"digest-crossref-row\"> chip row BELOW .news-meta and ABOVE the commentary paragraph"
    - "An article with no annotation (crossRefMap undefined or missing key) renders byte-identical HTML to the pre-Phase-24 output"
    - "Every chip string (prefix, symbol, verdict, keyword) passes through escapeHtml"
    - "Ticker-match chip verdict substring is wrapped in <strong style=\"color:...\"> using verdictColor()"
    - "Theme-match chip shows the fixed prefix '🗣 関連テーマ:' + keyword; ticker-match chip shows '🗣 ミーティング言及:' + symbol (+ verdict when present)"
  artifacts:
    - path: "src/scripts/generate-news-digest.ts"
      provides: "Renderer extended with optional crossRefMap param + chip formatter"
      exports: ["generateNewsDigestHtml"]
    - path: "src/scripts/report-utils.ts"
      provides: ".digest-crossref-chip / .digest-crossref-row CSS in generateBaseStyles"
      contains: "digest-crossref-chip"
    - path: "src/scripts/generate-news-digest.test.ts"
      provides: "Tests for annotated render, escaping, and byte-identical empty case"
  key_links:
    - from: "src/scripts/generate-news-digest.ts"
      to: "src/meeting/digest-crossref.ts"
      via: "import type { DigestCrossRef }"
      pattern: "DigestCrossRef"
    - from: "src/scripts/generate-news-digest.ts"
      to: "src/scripts/report-utils.ts"
      via: "import { verdictColor, escapeHtml }"
      pattern: "verdictColor"
    - from: "generateNewsDigestHtml"
      to: "formatArticleCardHtml"
      via: "threads crossRefMap?.[a.id] through formatMarketGroupsHtml"
      pattern: "crossRefMap"
---

<objective>
Extend the existing static-HTML renderer `src/scripts/generate-news-digest.ts` with an ADDITIVE optional `crossRefMap` parameter so cross-reference chips render on annotated articles, while 0-annotation output stays byte-identical to today. Add the `.digest-crossref-chip` / `.digest-crossref-row` CSS to `report-utils.ts`. Implements XREP-01 rendering per D-07, D-08, D-09, D-12 and the UI-SPEC Component Contract.

Purpose: Present the deterministic annotations from Plan 01 as passive chips reusing 100% existing design tokens (no new hex colors), with the empty case guaranteed byte-identical (a testable contract).
Output: extended `generate-news-digest.ts`, new CSS in `report-utils.ts`, extended `generate-news-digest.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-digest-meeting-cross-reference/24-UI-SPEC.md
@.planning/phases/24-digest-meeting-cross-reference/24-PATTERNS.md
@.planning/phases/24-digest-meeting-cross-reference/24-RESEARCH.md

<interfaces>
<!-- Contracts from Plan 01 (src/meeting/digest-crossref.ts) — import, do not redefine. -->
```typescript
export interface DigestTickerMatch { readonly symbol: string; readonly verdict?: "強気" | "中立" | "弱気"; }
export interface DigestThemeMatch { readonly keyword: string; }
export interface DigestCrossRef { readonly tickerMatches: ReadonlyArray<DigestTickerMatch>; readonly themeMatches: ReadonlyArray<DigestThemeMatch>; }
export type DigestCrossRefMap = Record<string, DigestCrossRef>;
```

Existing renderer anchors (src/scripts/generate-news-digest.ts):
- line 1: `import { escapeHtml, generateBaseStyles, safeHref, formatPublishedAtJst } from "./report-utils.js";`
- line 47: `function formatArticleCardHtml(a: CuratedArticle): string`
- line 66: `function formatMarketGroupsHtml(articles: ReadonlyArray<CuratedArticle>): string` (line 71 maps `formatArticleCardHtml`)
- line 104: `export function generateNewsDigestHtml(curation: NewsCuration | null, date: string): string`

Reused (src/scripts/report-utils.ts, already exported — import, do not reimplement):
```typescript
export function escapeHtml(text: string): string;        // line 26
export function verdictColor(verdict: string): string;   // line 90 — 強気:#10b981 弱気:#ef4444 default:#f59e0b
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend renderer tests — annotated render, escaping, byte-identical empty case (RED)</name>
  <read_first>
    - src/scripts/generate-news-digest.test.ts (full — existing fixture-per-scenario convention; existing cases MUST keep passing unmodified)
    - src/scripts/generate-news-digest.ts (lines 47-74, 104-130 — current card + group + top-level render)
    - .planning/phases/24-digest-meeting-cross-reference/24-UI-SPEC.md (Component Contract: exact chip markup, placement, empty-state byte-identical rule)
    - .planning/phases/24-digest-meeting-cross-reference/24-RESEARCH.md (Validation Architecture → renderer test rows)
  </read_first>
  <behavior>
    - Byte-identical omitted-arg: calling `generateNewsDigestHtml(curation, date)` (no 3rd arg) on an existing fixture produces output identical to the pre-change baseline — assert by reusing an existing fixture and confirming NO `digest-crossref-row` substring appears.
    - Byte-identical empty-map key: an article whose id maps to `{ tickerMatches: [], themeMatches: [] }` renders NO `digest-crossref-row` element (defensive empty-arrays case).
    - Ticker chip with verdict: crossRefMap { "n01": { tickerMatches:[{symbol:"NVDA",verdict:"強気"}], themeMatches:[] } } → output contains `class="digest-crossref-row"`, the text `🗣 ミーティング言及:`, `NVDA`, and a `<strong style="color:#10b981">` wrapping `強気`.
    - Ticker chip without verdict: {symbol:"FXY"} → contains `🗣 ミーティング言及:` and `FXY`, and NO `<strong` for that chip.
    - Theme chip: { themeMatches:[{keyword:"Healthcare"}] } → contains `🗣 関連テーマ:` and `Healthcare`.
    - Escaping: a crafted keyword like `<img src=x>` renders escaped (`&lt;img`) — no raw `<img` in output.
    - Placement: the `digest-crossref-row` <p> appears AFTER the `news-meta` <p> and BEFORE the commentary <p> in the card string.
  </behavior>
  <action>
    Extend `src/scripts/generate-news-digest.test.ts` (do NOT modify existing cases). Add a `describe("crossref annotations (XREP-01, D-08/D-09/D-12)")` block. Add fixtures/crossRefMaps for each behavior above. For the byte-identical case, reuse an EXISTING fixture unchanged and call `generateNewsDigestHtml(curation, date)` with no 3rd argument, asserting the result does NOT contain `digest-crossref-row`. Import the `DigestCrossRefMap` type from `../meeting/digest-crossref.js`. Run `npx vitest run src/scripts/generate-news-digest.test.ts` and confirm new cases FAIL while pre-existing cases still PASS (RED for the new cases only).
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-news-digest.test.ts 2>&1 | grep -q "crossref" && npx vitest run src/scripts/generate-news-digest.test.ts 2>&1 | grep -q "FAIL\|failed\|✗\|×" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "digest-crossref-row" src/scripts/generate-news-digest.test.ts` >= 1.
    - `grep -c "ミーティング言及\|関連テーマ" src/scripts/generate-news-digest.test.ts` >= 2.
    - Test file has a case invoking `generateNewsDigestHtml` with exactly 2 args asserting absence of `digest-crossref-row`.
    - `npx vitest run src/scripts/generate-news-digest.test.ts` shows the NEW crossref cases failing (RED) and existing cases still passing.
  </acceptance_criteria>
  <done>New crossref render/escape/placement/byte-identical test cases exist and fail; existing suite unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chip renderer + CSS to pass (GREEN)</name>
  <read_first>
    - src/scripts/generate-news-digest.ts (lines 36-45 formatTickerPillsHtml join pattern; 47-64 formatArticleCardHtml; 66-74 formatMarketGroupsHtml; 104-130 generateNewsDigestHtml)
    - src/scripts/report-utils.ts (lines 26-33 escapeHtml; 90-96 verdictColor; ~194-209 .ticker-pill / .news-meta CSS inside generateBaseStyles)
    - src/scripts/generate-news-digest.test.ts (the failing spec from Task 1)
    - .planning/phases/24-digest-meeting-cross-reference/24-UI-SPEC.md (New CSS block lines 163-178; Chip Markup exact strings)
    - .planning/phases/24-digest-meeting-cross-reference/24-PATTERNS.md (formatDigestCrossRefChipsHtml translation)
  </read_first>
  <action>
    In `src/scripts/generate-news-digest.ts`: add `import type { DigestCrossRef } from "../meeting/digest-crossref.js"` and add `verdictColor` to the existing report-utils import on line 1. Add a new private function `formatDigestCrossRefChipsHtml(annotation: DigestCrossRef | undefined): string` that returns `""` when annotation is undefined OR when both tickerMatches and themeMatches are empty (byte-identical empty case — return empty string, NOT an empty tag, NOT whitespace). Otherwise build chip spans: for each ticker match, `<span class="digest-crossref-chip">🗣 ミーティング言及: {escapeHtml(symbol)}{verdictPart}</span>` where verdictPart is `` (empty) when verdict is undefined else ` <strong style="color:${verdictColor(verdict)}">${escapeHtml(verdict)}</strong>`; for the theme match, `<span class="digest-crossref-chip">🗣 関連テーマ: {escapeHtml(keyword)}</span>`. escapeHtml the fixed prefixes too is not required (they are static literals with no unsafe chars) BUT every DYNAMIC value (symbol, verdict, keyword) MUST pass through escapeHtml — no exceptions. Join chips with a single space and wrap in `\n      <p class="digest-crossref-row">...chips...</p>`. Order: tickerMatches first, then themeMatches (Plan 01 already caps 2+1).
    Extend `formatArticleCardHtml(a: CuratedArticle, annotation?: DigestCrossRef): string` (append optional 2nd param). Insert the result of `formatDigestCrossRefChipsHtml(annotation)` BETWEEN the `.news-meta` <p> and the commentary <p> (D-08). Extend `formatMarketGroupsHtml(articles, crossRefMap?: Readonly<Record<string, DigestCrossRef>>)` to thread `crossRefMap?.[a.id]` — change every `formatArticleCardHtml` call (line 71 and any sibling ternary branch) to `(a) => formatArticleCardHtml(a, crossRefMap?.[a.id])`. Extend `generateNewsDigestHtml(curation, date, crossRefMap?: Readonly<Record<string, DigestCrossRef>>)` (append optional 3rd param) and pass `crossRefMap` into `formatMarketGroupsHtml` at line 128.
    In `src/scripts/report-utils.ts`: add the `.digest-crossref-chip` and `.digest-crossref-row` CSS rules inside `generateBaseStyles`, adjacent to `.ticker-pill`, using EXACTLY the values from UI-SPEC New CSS: chip background `#2a2a3e`, color `#c4b5fd`, font-size `0.8rem`, padding `0.15rem 0.5rem`, margin-right `0.4rem`, margin-bottom `0.3rem`, border-radius `999px`, border `1px solid #a78bfa` (distinct meeting accent per D-09); row margin-bottom `0.5rem`. Invent NO new hex values.
    Run `npx vitest run src/scripts/generate-news-digest.test.ts` and iterate until GREEN. Then run `npm test` full suite.
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-news-digest.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/generate-news-digest.test.ts` exits 0 (all cases including byte-identical empty pass).
    - `npm test` full suite exits 0.
    - `grep -c "digest-crossref-chip\|digest-crossref-row" src/scripts/report-utils.ts` >= 2 (both CSS rules present).
    - `grep -c "border: 1px solid #a78bfa" src/scripts/report-utils.ts` === 1 (distinct accent border).
    - `src/scripts/generate-news-digest.ts` imports `DigestCrossRef` from `../meeting/digest-crossref.js` and `verdictColor` from `./report-utils.js`.
    - `grep -c "escapeHtml" src/scripts/generate-news-digest.ts` increased (dynamic chip values escaped).
    - No new hex color introduced: `grep -v '^#' src/scripts/report-utils.ts | grep -oE '#[0-9a-fA-F]{6}' | sort -u` contains only pre-existing palette values (#8b5cf6/#a78bfa/#c4b5fd/#2a2a3e/#3f3f5a/#10b981/#ef4444/#f59e0b and existing chrome).
  </acceptance_criteria>
  <done>Chips render below .news-meta with escaped text and verdict color; empty case byte-identical; CSS reuses existing tokens; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DigestCrossRef strings (LLM/meeting-derived) → HTML output | symbol/verdict/keyword originate from zod z.string() (not enums) — treated as untrusted for rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering / Information Disclosure (XSS via injected chip text) | formatDigestCrossRefChipsHtml | mitigate | 100% of dynamic interpolated values (symbol, verdict, keyword) pass through `escapeHtml` before insertion — identical control to every other field in formatArticleCardHtml. Tested via the `<img src=x>` keyword escaping case. |
| T-24-01-B | Tampering (layout break / regression on unmatched articles) | formatArticleCardHtml empty case | mitigate | Empty annotation returns literal `""` (not empty tag/whitespace); guaranteed byte-identical output enforced by the omitted-arg regression test. |
| V5 Input Validation | — | renderer | accept | Inputs pre-validated upstream; renderer re-escapes rather than re-validates. |
| V2/V3/V4/V6 | — | — | N/A | Static HTML string generation — no auth/session/access-control/crypto surface. |

No package installs in this plan (T-24-SC not applicable).
</threat_model>

<verification>
- `npx vitest run src/scripts/generate-news-digest.test.ts` exits 0.
- `npm test` full suite exits 0.
- Manual (deferred to live pipeline, per VALIDATION.md): chip visually appears below .news-meta on matched articles in the next launchd-generated news-digest.html.
</verification>

<success_criteria>
- Renderer additively extended; 0-annotation output byte-identical (regression-tested).
- Chips render with correct prefixes, escaping, verdict color, and placement.
- CSS reuses existing purple palette with the distinct #a78bfa accent border; zero new hex.
</success_criteria>

<output>
Create `.planning/phases/24-digest-meeting-cross-reference/24-02-SUMMARY.md` when done.
</output>
