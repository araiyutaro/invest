---
phase: 24-digest-meeting-cross-reference
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/meeting/digest-crossref.ts
  - src/meeting/digest-crossref.test.ts
autonomous: true
requirements: [XREP-01]
must_haves:
  truths:
    - "Article ticker present in highlightedStocks produces a ticker annotation with symbol + verdict"
    - "Article ticker present only in scoredTickers produces a ticker annotation with symbol and NO verdict"
    - "Theme fallback matches a real 'Name (TICKER)' sector string against the title after stripping the trailing parenthetical"
    - "A ticker match on an article suppresses any theme evaluation for that article (early-continue)"
    - "Per article, ticker matches capped at 2 and theme matches capped at 1, in stable original order"
    - "buildDigestCrossRefMap never throws on empty/missing arrays and returns an empty map for zero overlap"
    - "Articles with zero matches are absent as keys from the returned map"
  artifacts:
    - path: "src/meeting/digest-crossref.ts"
      provides: "Pure deterministic digest-meeting matcher + DigestCrossRef contract"
      exports: ["buildDigestCrossRefMap", "DigestCrossRef", "DigestCrossRefMap", "DigestTickerMatch", "DigestThemeMatch"]
    - path: "src/meeting/digest-crossref.test.ts"
      provides: "Unit tests for the matcher (ticker/theme/priority/cap/fail-soft)"
      contains: "buildDigestCrossRefMap"
  key_links:
    - from: "src/meeting/digest-crossref.ts"
      to: "src/portfolio/holding-news.ts"
      via: "import { normalizeHoldingSymbol }"
      pattern: "normalizeHoldingSymbol"
    - from: "src/meeting/digest-crossref.ts"
      to: "src/meeting/types.ts"
      via: "import type { CuratedArticle, NewsCuration, MeetingResult }"
      pattern: "from \"./types.js\""
---

<objective>
Create the new pure-function module `src/meeting/digest-crossref.ts` that maps each `CuratedArticle` in a `NewsCuration` to a deterministic cross-reference annotation describing how the article relates to the day's meeting (`MeetingResult`). This is the foundation contract that Plan 02 (renderer) and Plan 03 (pipeline) consume. Implements XREP-01 matching per locked decisions D-01..D-06, D-10, D-11, D-15.

Purpose: Deterministic ticker-priority + theme-keyword matching (no LLM, no hallucination), mirroring the tested design of `src/portfolio/holding-news.ts` applied to a new join key (article ID instead of holding symbol).
Output: `src/meeting/digest-crossref.ts` (pure, no I/O, no throw) + `src/meeting/digest-crossref.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-digest-meeting-cross-reference/24-CONTEXT.md
@.planning/phases/24-digest-meeting-cross-reference/24-RESEARCH.md
@.planning/phases/24-digest-meeting-cross-reference/24-PATTERNS.md
@.planning/phases/24-digest-meeting-cross-reference/24-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

Input types (from src/meeting/types.ts — read-only, DO NOT modify):
```typescript
export interface CuratedArticle {
  readonly id: string;              // join key (n01..n80)
  readonly title: string;           // theme matching source (title ONLY, D-03)
  readonly tickers: ReadonlyArray<string>;  // ticker matching source (D-01)
  readonly market: "us" | "japan" | "global";
  readonly importance: "high" | "medium" | "low";
  // ...url/source/publishedAt(ISO string)/commentary/tickerNames omitted, not used by matcher
}
export interface NewsCuration {
  readonly articles: ReadonlyArray<CuratedArticle>;
  // ...date/generatedAt/leadIn omitted
}
export interface MeetingResult {
  readonly sectorRecommendations: ReadonlyArray<{ readonly sector: string; /* e.g. "Healthcare (XLV)" */ }>;
  readonly highlightedStocks: ReadonlyArray<{ readonly ticker: string; readonly verdict: "強気" | "中立" | "弱気"; /* ... */ }>;
  readonly roundSummary: { readonly scoredTickers: ReadonlyArray<string>; /* ... */ };
  // ...many other fields omitted, not used by matcher
}
```

Reused function (from src/portfolio/holding-news.ts:32-34, already exported — import, DO NOT reimplement, D-15):
```typescript
export function normalizeHoldingSymbol(symbol: string): string; // trim + toUpperCase only
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define DigestCrossRef contract and write failing matcher tests (RED)</name>
  <read_first>
    - src/portfolio/holding-news.test.ts (full — mirror its factory-helper + describe-per-decision test structure)
    - src/portfolio/holding-news.ts (full — the matcher design template being ported)
    - src/meeting/types.ts (CuratedArticle / NewsCuration / MeetingResult exact shapes)
    - .planning/phases/24-digest-meeting-cross-reference/24-RESEARCH.md (Pitfall 1 = sector "(TICKER)" strip; Pitfall 5 = JP ticker "6326.T" fixture format; Validation Architecture → Test Map)
    - .planning/phases/24-digest-meeting-cross-reference/24-PATTERNS.md (digest-crossref.ts + digest-crossref.test.ts sections)
  </read_first>
  <behavior>
    - Ticker match (D-02/D-05): article with tickers ["XLV"] where highlightedStocks has {ticker:"XLV", verdict:"強気"} → map["<id>"].tickerMatches === [{ symbol:"XLV", verdict:"強気" }], themeMatches === [].
    - scoredTickers-only (D-05): article tickers ["FXY"] where "FXY" is in roundSummary.scoredTickers but NOT in highlightedStocks → tickerMatches === [{ symbol:"FXY" }] (verdict undefined).
    - Theme match with realistic fixture (D-03/D-06, Pitfall 1): sectorRecommendations [{sector:"Healthcare (XLV)"}], article title "New Healthcare policy shakes markets" with NO matching tickers → themeMatches === [{ keyword:"Healthcare" }], tickerMatches === [].
    - Case-insensitive theme (D-03): title "...healthcare..." lowercase still matches "Healthcare (XLV)".
    - Empty stripped keyword guard: sector "(XLV)" (strips to "") must NEVER match any title (no annotation produced).
    - Priority/early-continue (D-04): article matching both a ticker AND a sector keyword → only tickerMatches populated, themeMatches === [].
    - Cap (D-10): article with 3 ticker matches + 2 theme candidates → max 2 tickerMatches (first two in article.tickers order), max 1 themeMatch, stable original order (no re-sort).
    - JP ticker (Pitfall 5): fixture uses "6326.T" style (not bare "7203"); normalizeHoldingSymbol makes " 6326.t " match "6326.T".
    - Fail-soft (D-11): buildDigestCrossRefMap with empty highlightedStocks/sectorRecommendations/scoredTickers arrays does not throw and returns {}.
    - Zero-overlap: unmatched article id is absent as a key (Object.keys does not include it) — enables the renderer's byte-identical empty case.
  </behavior>
  <action>
    First define the contract in `src/meeting/digest-crossref.ts` (skeleton only): export interface `DigestTickerMatch` { readonly symbol: string; readonly verdict?: "強気" | "中立" | "弱気" }, export interface `DigestThemeMatch` { readonly keyword: string }, export interface `DigestCrossRef` { readonly tickerMatches: ReadonlyArray<DigestTickerMatch>; readonly themeMatches: ReadonlyArray<DigestThemeMatch> }, export type `DigestCrossRefMap` = Record<string, DigestCrossRef>, and export function signature `buildDigestCrossRefMap(curation: NewsCuration, meetingResult: MeetingResult): DigestCrossRefMap` returning `{}` as a temporary stub so the test file compiles and fails on assertions (RED, not a compile error).
    Then create `src/meeting/digest-crossref.test.ts` mirroring `holding-news.test.ts`: add two factory helpers `makeCuratedArticle(overrides: Partial<CuratedArticle> & { id: string }): CuratedArticle` (defaults: title "デフォルトタイトル", url "https://example.com/a", source "TestSource", publishedAt a fixed ISO STRING not a Date, market "us", importance "medium", commentary "", tickers []) and `makeMeetingResult(overrides: Partial<MeetingResult>): MeetingResult` (minimal valid defaults with empty sectorRecommendations / highlightedStocks arrays and roundSummary.scoredTickers []). Organize `describe` blocks named per decision: `describe("digest-crossref ticker match (D-01,D-02,D-05)")`, `describe("digest-crossref theme match (D-03,D-06)")`, `describe("digest-crossref priority (D-04)")`, `describe("digest-crossref cap (D-10)")`, `describe("digest-crossref fail-soft (D-11)")`. Write every behavior above as an `it(...)` assertion. MUST include at least one `sector: "Healthcare (XLV)"` fixture and at least one `"6326.T"` ticker fixture. Run `npx vitest run src/meeting/digest-crossref.test.ts` and confirm the suite executes and FAILS on assertions (RED).
  </action>
  <verify>
    <automated>npx vitest run src/meeting/digest-crossref.test.ts 2>&1 | grep -q "FAIL\|failed\|✗\|×" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `src/meeting/digest-crossref.ts` exists and contains `export function buildDigestCrossRefMap` and `export interface DigestCrossRef`.
    - `src/meeting/digest-crossref.test.ts` exists; `grep -c "Healthcare (XLV)" src/meeting/digest-crossref.test.ts` >= 1; `grep -c "6326.T" src/meeting/digest-crossref.test.ts` >= 1.
    - `npx vitest run src/meeting/digest-crossref.test.ts` executes (no TS compile error) and reports failing assertions (RED).
    - Test file imports types from `./types.js` and `buildDigestCrossRefMap` from `./digest-crossref.js`.
  </acceptance_criteria>
  <done>Contract types exported, test file present with realistic fixtures, suite runs and fails on assertions (not on compile).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement buildDigestCrossRefMap matcher to pass (GREEN)</name>
  <read_first>
    - src/meeting/digest-crossref.ts (the skeleton + contract from Task 1)
    - src/meeting/digest-crossref.test.ts (the failing spec from Task 1)
    - src/portfolio/holding-news.ts (lines 32-34 normalizeHoldingSymbol; 105-111 titleIncludesAny; 149-165 matchArticlesForHolding early-continue; 172-192 rank-and-cap — port these primitives)
    - .planning/phases/24-digest-meeting-cross-reference/24-PATTERNS.md (digest-crossref.ts section — exact translation guidance)
  </read_first>
  <action>
    Implement `src/meeting/digest-crossref.ts` as a pure module mirroring `holding-news.ts` discipline (every function pure, zero I/O, never throws). Import `normalizeHoldingSymbol` from `../portfolio/holding-news.js` (D-15, reuse — do NOT reimplement trim/toUpperCase) and `import type { CuratedArticle, NewsCuration, MeetingResult } from "./types.js"`. Do NOT import `calculatePriorityScore` or anything from `src/data/news/filter.js` (Pitfall 4 anti-pattern — no recency scoring).
    Build a `verdictByTicker` Map<string,"強気"|"中立"|"弱気"> from `meetingResult.highlightedStocks` keyed by `normalizeHoldingSymbol(s.ticker)`, and a `scoredTickerSet` Set<string> from `meetingResult.roundSummary.scoredTickers.map(normalizeHoldingSymbol)`.
    Build a `sectorKeywords: string[]` by mapping each `sectorRecommendations[].sector` through the strip transform `sector.replace(/\s*\([^)]*\)\s*$/, "").trim()` (LOCKED per Assumption A3 — strips trailing " (TICKER)"), and FILTER OUT any resulting empty string (so a bare "(XLV)" cannot match every title).
    Add a private helper `titleIncludesAny(title, candidates)` ported verbatim from holding-news.ts:105-111 (lowercase title once, `.some(c => lowerTitle.includes(c.toLowerCase()))`).
    Iterate `curation.articles` in original order. For each article: collect ticker matches by iterating `article.tickers` in order, for each `norm = normalizeHoldingSymbol(t)`: if `verdictByTicker.has(norm)` push `{ symbol: norm, verdict: verdictByTicker.get(norm) }`, else if `scoredTickerSet.has(norm)` push `{ symbol: norm }` (no verdict). If ticker matches length > 0: set `map[article.id] = { tickerMatches: tickerMatches.slice(0,2), themeMatches: [] }` and `continue` (D-04 early-continue — never evaluate theme). Otherwise resolve theme: find the FIRST `sectorKeywords` entry (original order) where `titleIncludesAny(article.title, [keyword])` is true; if found set `map[article.id] = { tickerMatches: [], themeMatches: [{ keyword }] }` (cap 1). Articles with no match get NO key. Return the map. Handle `curation` with empty articles and a `meetingResult` with all-empty arrays without throwing (return {}).
    Run `npx vitest run src/meeting/digest-crossref.test.ts` and iterate until GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/meeting/digest-crossref.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/meeting/digest-crossref.test.ts` exits 0 (all assertions pass, GREEN).
    - `src/meeting/digest-crossref.ts` imports `normalizeHoldingSymbol` from `../portfolio/holding-news.js`.
    - `grep -c "calculatePriorityScore\|data/news/filter" src/meeting/digest-crossref.ts` === 0 (Pitfall 4 anti-pattern absent).
    - `grep -c 'replace(/\\\\s\*\\\\([^)]\*\\\\)\\\\s\*\$/' src/meeting/digest-crossref.ts` >= 1 OR the strip regex `\([^)]*\)` is present (parenthetical strip implemented).
    - Module performs zero file/network I/O (no `fs`, no `readFile`, no `fetch` imports).
    - `npm test` full suite still exits 0 (no regression in sibling suites).
  </acceptance_criteria>
  <done>Matcher passes all unit tests; ticker-priority, scoredTickers-only, theme-strip, cap, and fail-soft behaviors verified; no forbidden imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meeting-result.json / news-curation.json → matcher | LLM-generated JSON (schema-validated upstream only as z.string(), not enums) crosses into string-matching logic. No user/network input in this module. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-A | Information Disclosure / Tampering (XSS) | Strings produced by buildDigestCrossRefMap (symbol, verdict, keyword) | transfer | Matcher produces RAW strings and does NOT render HTML; escaping is the renderer's responsibility (Plan 02 must pass 100% of these strings through escapeHtml). Documented here so the consumer never treats matcher output as pre-sanitized. |
| T-24-01-D | Denial of Service | buildDigestCrossRefMap on malformed-but-typed data (empty arrays, missing fields) | mitigate | Pure no-throw discipline (D-11): functions tolerate empty/absent arrays via safe defaults, never throw. Verified by the fail-soft test cases. |
| V5 Input Validation | — | matcher | accept (N/A re-validation) | Inputs are already zod-validated upstream (schemas.ts); matcher is a pure consumer and must NOT re-validate or throw. |
| V2/V3/V4/V6 | — | — | N/A | No auth, sessions, access control, or cryptography surface in a pure string matcher. |

No package installs in this plan (T-24-SC not applicable — zero new dependencies).
</threat_model>

<verification>
- `npx vitest run src/meeting/digest-crossref.test.ts` exits 0.
- `npm test` (full suite) exits 0 — no regression.
- Matcher output for a zero-overlap article omits the article ID key entirely (enables Plan 02 byte-identical empty rendering).
</verification>

<success_criteria>
- Deterministic ticker-priority + theme-keyword matcher implemented and unit-tested (XREP-01 backend).
- DigestCrossRef contract exported for downstream Plan 02/03 consumption.
- Realistic "Name (TICKER)" sector and "6326.T" JP ticker fixtures present and green.
- Zero forbidden imports (no calculatePriorityScore, no I/O).
</success_criteria>

<output>
Create `.planning/phases/24-digest-meeting-cross-reference/24-01-SUMMARY.md` when done.
</output>
