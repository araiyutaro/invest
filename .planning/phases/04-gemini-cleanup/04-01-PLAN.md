---
phase: 04-gemini-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gemini.ts
  - src/data/charts.ts
  - src/data/research.ts
  - src/meeting/runner.ts
  - src/report/generator.ts
  - src/data/news/analyzer.ts
  - src/index.ts
  - src/portfolio/runner.ts
  - src/report/portfolio-generator.ts
  - scripts/run.sh
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03

must_haves:
  truths:
    - "@google/generative-ai と @google/genai が package.json の dependencies に存在しない"
    - "GEMINI_API_KEY 環境変数への参照がコードベース全体から存在しない"
    - "npx tsc --noEmit がエラーなしで完了する"
    - "npm test で全テストが PASS する"
  artifacts:
    - path: "package.json"
      provides: "@google/* パッケージが除去された依存関係"
      contains: "yahoo-finance2"
    - path: "src/scripts/collect-data.ts"
      provides: "Gemini 非依存のデータ収集スクリプト"
  key_links:
    - from: "src/scripts/collect-data.ts"
      to: "src/portfolio/data.ts"
      via: "import"
      pattern: "portfolio/data"
    - from: "src/scripts/collect-data.ts"
      to: "src/portfolio/holdings.ts"
      via: "import"
      pattern: "portfolio/holdings"
---

<objective>
Gemini API に関連するファイル・パッケージ・環境変数参照をコードベースから完全除去する。

Purpose: v2.0 では /invest スキルコマンドが全パイプラインを担うため、v1.0 Gemini 依存ファイルは不要。
Output: Gemini 依存なし、tsc コンパイル成功、全テスト PASS の状態。
</objective>

<execution_context>
@/Users/arai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/arai/invest/.planning/PROJECT.md
@/Users/arai/invest/.planning/ROADMAP.md
@/Users/arai/invest/.planning/STATE.md
@/Users/arai/invest/.planning/phases/04-gemini-cleanup/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gemini依存ファイル削除</name>
  <files>
    src/gemini.ts,
    src/data/charts.ts,
    src/data/research.ts,
    src/meeting/runner.ts,
    src/report/generator.ts,
    src/data/news/analyzer.ts,
    src/index.ts,
    src/portfolio/runner.ts,
    src/report/portfolio-generator.ts
  </files>
  <action>
    以下の9ファイルを削除する（per D-01）。これらはすべて v1.0 専用ファイルで v2.0 パイプラインからは参照されていない。

    削除対象:
    - src/gemini.ts — Gemini API クライアントラッパー（GEMINI_API_KEY を参照）
    - src/data/charts.ts — NanoBanana チャート画像生成（@google/genai 依存、GEMINI_API_KEY 参照）
    - src/data/research.ts — Google Search Grounding リサーチ（@google/generative-ai 依存、GEMINI_API_KEY 参照）
    - src/meeting/runner.ts — v1.0 ミーティングランナー（gemini.ts 依存）
    - src/report/generator.ts — v1.0 レポートジェネレータ（v2.0 の generate-report.ts で置換済み）
    - src/data/news/analyzer.ts — ニュース分析（gemini.ts 依存）
    - src/index.ts — v1.0 エントリポイント（runner.ts, generator.ts を import）
    - src/portfolio/runner.ts — Portfolio ミーティングランナー（gemini.ts 依存）
    - src/report/portfolio-generator.ts — Portfolio レポートジェネレータ（gemini.ts 依存）

    削除コマンド:
    rm /Users/arai/invest/src/gemini.ts
    rm /Users/arai/invest/src/data/charts.ts
    rm /Users/arai/invest/src/data/research.ts
    rm /Users/arai/invest/src/meeting/runner.ts
    rm /Users/arai/invest/src/report/generator.ts
    rm /Users/arai/invest/src/data/news/analyzer.ts
    rm /Users/arai/invest/src/index.ts
    rm /Users/arai/invest/src/portfolio/runner.ts
    rm /Users/arai/invest/src/report/portfolio-generator.ts

    注意: src/portfolio/data.ts と src/portfolio/holdings.ts は残す（collect-data.ts が import している）。
    scripts/run.sh は GEMINI_API_KEY 参照なしを確認済み（ただし src/index.ts を呼び出しているため更新対象 — Task 2 で対応）。
  </action>
  <verify>
    <automated>ls /Users/arai/invest/src/gemini.ts 2>/dev/null && echo "FAIL: still exists" || echo "PASS: deleted"</automated>
    <automated>grep -r "GEMINI_API_KEY" /Users/arai/invest/src --include="*.ts" 2>/dev/null | wc -l | xargs -I{} sh -c 'if [ {} -eq 0 ]; then echo "PASS: no GEMINI_API_KEY refs"; else echo "FAIL: {} refs remain"; fi'</automated>
  </verify>
  <done>9ファイルが削除され、src/ 配下に GEMINI_API_KEY への参照が存在しない</done>
</task>

<task type="auto">
  <name>Task 2: npm パッケージ削除 + scripts/run.sh 更新</name>
  <files>package.json, package-lock.json, scripts/run.sh</files>
  <action>
    1. npm パッケージ削除（per D-03）:
       cd /Users/arai/invest && npm uninstall @google/generative-ai @google/genai

       これにより package.json の dependencies から両パッケージが除去され、node_modules からもアンインストールされる。

    2. scripts/run.sh 更新（D-06 対応）:
       現在の run.sh は `npx tsx src/index.ts` を呼び出しているが、src/index.ts は Task 1 で削除済み。
       v2.0 では /invest スキルコマンドがエントリポイントのため、run.sh の自動実行ロジックは v2.0 対応前にスクリプトから呼び出し行を削除して無害化する。

       具体的には run.sh 内の以下の行を削除またはコメントアウト:
       - `npx tsx src/index.ts >> "$PROJECT_DIR/logs/invest.log" 2>&1`
       - その後の `docs/` への git add/commit/push ブロック（v2.0 では reports/ に変更済みのため）

       run.sh に GEMINI_API_KEY への参照がないことは確認済みのため、環境変数の修正は不要。
  </action>
  <verify>
    <automated>cd /Users/arai/invest && node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); const d = {...(p.dependencies||{}), ...(p.devDependencies||{})}; const found = ['@google/generative-ai','@google/genai'].filter(k => k in d); found.length === 0 ? console.log('PASS: packages removed') : console.log('FAIL:', found)"</automated>
  </verify>
  <done>@google/generative-ai と @google/genai が package.json から除去され、node_modules からアンインストールされている。scripts/run.sh が削除済み src/index.ts を参照していない</done>
</task>

<task type="auto">
  <name>Task 3: TypeScript コンパイル + テスト検証</name>
  <files></files>
  <action>
    以下の順序で検証を実施する（per D-07, D-08）:

    1. TypeScript コンパイル確認:
       cd /Users/arai/invest && npx tsc --noEmit
       削除ファイルを参照している残存コードがある場合はコンパイルエラーになる。エラーが発生した場合は該当ファイルの import を修正する。

    2. 残存 Gemini 参照の最終確認:
       grep -r "GEMINI_API_KEY\|@google/generative-ai\|@google/genai\|from.*gemini" /Users/arai/invest/src --include="*.ts"
       ヒットがあれば除去する。

    3. テスト実行:
       cd /Users/arai/invest && npm test
       既存テスト (collect-data, validate-meeting, generate-report) が全て PASS することを確認する。
       テストが FAIL した場合は、削除ファイルへの依存がテストコードに残っていないか確認し修正する。
  </action>
  <verify>
    <automated>cd /Users/arai/invest && npx tsc --noEmit && echo "PASS: tsc clean"</automated>
    <automated>cd /Users/arai/invest && npm test</automated>
    <automated>grep -r "GEMINI_API_KEY\|@google/generative-ai\|@google/genai" /Users/arai/invest/src --include="*.ts" 2>/dev/null | grep -v "^Binary" | wc -l | xargs -I{} sh -c 'if [ {} -eq 0 ]; then echo "PASS: no Gemini refs"; else echo "FAIL: {} Gemini refs remain"; fi'</automated>
  </verify>
  <done>npx tsc --noEmit がエラーなし、npm test が全テスト PASS、ソースコードに Gemini 参照が存在しない</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry | パッケージ削除操作（既存パッケージの除去のみ、新規インストールなし） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | package.json | accept | npm uninstall は公式 npm CLI を使用、依存ツリーへの影響は tsc --noEmit + npm test で検証 |
| T-04-02 | Information Disclosure | src/gemini.ts | mitigate | GEMINI_API_KEY 参照コードを削除。.env 自体はリポジトリ外のためユーザーが手動で対応 |
</threat_model>

<verification>
Phase 4 完了条件:
1. `grep -r "@google/generative-ai\|@google/genai" package.json` → ヒットなし
2. `grep -r "GEMINI_API_KEY" src/` → ヒットなし
3. `npx tsc --noEmit` → エラーなし
4. `npm test` → 全テスト PASS
</verification>

<success_criteria>
- @google/generative-ai と @google/genai が package.json の dependencies に存在しない
- src/ 配下のすべての .ts ファイルに GEMINI_API_KEY, @google/generative-ai, @google/genai への参照が存在しない
- npx tsc --noEmit がエラーなしで完了する
- npm test で collect-data, validate-meeting, generate-report の全テストが PASS する
</success_criteria>

<output>
完了後に `.planning/phases/04-gemini-cleanup/04-01-SUMMARY.md` を作成する
</output>
