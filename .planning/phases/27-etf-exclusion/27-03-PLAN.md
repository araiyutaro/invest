---
phase: 27-etf-exclusion
plan: 03
type: execute
wave: 2
depends_on: [27-02]
files_modified:
  - .claude/commands/invest.md
autonomous: true
requirements: [ETF-01, ETF-02]
must_haves:
  truths:
    - "全5アナリスト（Round 1, Step 2a）のプロンプトに ETF・投資信託・インデックスファンドを picks に含めない旨の指示が含まれる（D-10, ETF-01, Success Criterion 1）"
    - "Step 2f モデレーター『重要な注意事項』に同趣旨の highlightedStocks 除外指示が含まれる（D-11）"
    - "Step 2g で validate-meeting.ts の直前に filter-etf-stocks.ts が実行されるよう wiring される（D-07, ETF-02）"
    - "filter-etf-stocks.ts の失敗は fail-soft で扱われ [STEP:etf-exclusion:OK/FAIL] を出し [PIPELINE:FAIL] は出さない（D-08, D-02）"
  artifacts:
    - ".claude/commands/invest.md（Step 2a 5ブロック + Step 2f + Step 2g wiring を編集）"
  key_links:
    - "Step 2g の filter-etf-stocks.ts 呼び出しは Plan 02 が作る src/scripts/filter-etf-stocks.ts を指す — 実行順は Step 2f（モデレーター出力保存）→ filter-etf-stocks.ts → validate-meeting.ts"
    - "書き戻された ETF-free な highlightedStocks が validate-meeting.ts・サマリー表示・下流レポート生成・翌日注入すべてに自然波及する"
---

<objective>
ETF除外の第1層（確率的プロンプト指示）と第2層のパイプライン統合を `.claude/commands/invest.md` に配線する。(1) Round 1 の全5アナリストブロック（Step 2a）と Step 2f モデレーター統合プロンプトに、ETF・投資信託・インデックスファンドを推奨銘柄から除外する明示指示を追記（D-10/D-11、ETF-01）。(2) Step 2g のバリデーション内、`validate-meeting.ts` 実行の直前に `filter-etf-stocks.ts`（Plan 02）の呼び出しと fail-soft STEP マーカーロジックを挿入（D-07/D-08、ETF-02 のパイプライン統合部）。

Purpose: 二層防御を実パイプラインに接続する最終配線。Layer 1（プロンプト）は best-effort、Layer 2（TS決定論フィルタ）が構造的保証を担う（Pitfall 1）。フィルタ済みデータが下流の全レポート・翌日注入・Phase 28 ウォッチリスト入場に自然波及する。
Output: 編集済み invest.md（5アナリストブロック + モデレーターブロック + Step 2g wiring）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-etf-exclusion/27-CONTEXT.md
@.planning/phases/27-etf-exclusion/27-RESEARCH.md
@.planning/phases/27-etf-exclusion/27-PATTERNS.md

# 依存: Plan 02 が作る CLI（Step 2g wiring が指す実スクリプト）
@.planning/phases/27-etf-exclusion/27-02-SUMMARY.md
# 編集対象
@.claude/commands/invest.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5アナリストブロック + モデレーターブロックに ETF除外プロンプト指示を追記（Layer 1, D-10/D-11）</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md（Step 2a の5アナリストブロック: 「注意: picksのtickerは必ず英数字ティッカー形式（例: AAPL, 7203.T）で記入してください。」が5回同一出現する行の直後に追記。Step 2f モデレーターの「## 重要な注意事項」箇条書きリストの末尾に追記）
    - .planning/phases/27-etf-exclusion/27-PATTERNS.md（Analog A / Analog B: 追記する正確な日本語文言と挿入位置。5ブロック同一文言・モデレーターは箇条書き1項目追加）
    - .planning/phases/27-etf-exclusion/27-RESEARCH.md（Pattern 3: 挿入点の検証済み正確テキスト。ETF-01 の全アナリスト適用要件）
  </read_first>
  <action>
    invest.md の Step 2a にある5つのアナリストプロンプトブロックそれぞれで、「注意: picksのtickerは必ず英数字ティッカー形式（例: AAPL, 7203.T）で記入してください。」の行の直後（同ブロックの「ニュース・市況から新規の注目銘柄を…」行の前）に、既存行のインデントに合わせて新しい1行を追記する。追記文言は各ブロック同一で: 「ETF・投資信託・インデックスファンド（例: SPY, QQQ, 1306.T等）は picks に含めないこと。個別企業株のみを推奨してください。」。5ブロックすべてに適用すること（D-10、grep で5回出現を後で検証）。

    次に Step 2f モデレーター統合プロンプトの「## 重要な注意事項」箇条書きリスト末尾（「- レポート内容は日本語で記述」行の直後）に、既存の箇条書きインデント（`    - ` プレフィックス）に合わせて新しい箇条書き1項目を追記する。追記文言: 「- ETF・投資信託・インデックスファンドは highlightedStocks に含めないこと（個別企業株のみ）。TS側でも quoteType による除外チェックを行うため、疑わしい銘柄は含めないこと」（D-11）。

    既存の picks 出力契約・注意事項の他の行は一切変更しないこと。src/agents/*.ts の systemPrompt は触らない（D-12: 指示の正準位置は invest.md のみ、二重管理を避ける）。挿入は既存ブロックへの追記のみで、新しいプロンプトセクションは作らない。
  </action>
  <verify>
    <automated>test $(grep -c "ETF・投資信託・インデックスファンド（例: SPY, QQQ, 1306.T等）は picks に含めないこと" .claude/commands/invest.md) -eq 5 && grep -q "ETF・投資信託・インデックスファンドは highlightedStocks に含めないこと" .claude/commands/invest.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 5アナリストブロックの picks 除外指示がちょうど5回出現する（`grep -c "ETF・投資信託・インデックスファンド（例: SPY, QQQ, 1306.T等）は picks に含めないこと" .claude/commands/invest.md` == 5）
    - モデレーター（Step 2f）の highlightedStocks 除外指示が1回出現する（`grep -c "ETF・投資信託・インデックスファンドは highlightedStocks に含めないこと" .claude/commands/invest.md` == 1）
    - 既存の「注意: picksのtickerは必ず英数字ティッカー形式」行が依然として5回存在する（追記であって置換でないことの証明）
    - src/agents/ 配下のファイルが変更されていない（このタスクの files_modified に含まれない）
  </acceptance_criteria>
  <done>全5アナリストプロンプト（Step 2a）と Step 2f モデレーターに ETF除外指示が追記され、既存の picks 出力契約は保持されている（ETF-01, Success Criterion 1 のプロンプト層）。</done>
</task>

<task type="auto">
  <name>Task 2: Step 2g に filter-etf-stocks.ts 実行と fail-soft STEP マーカーを wiring（Layer 2統合, D-07/D-08）</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md（Step 2g: 「## Step 2g: バリデーション」直下、pipeline-metrics タイムスタンプ記録の bash ブロックの後、`cd /Users/arai/invest && npx tsx src/scripts/validate-meeting.ts` の bash ブロックの直前が挿入点。D-07: filter は validate-meeting.ts より前に実行する）
    - .planning/phases/27-etf-exclusion/27-PATTERNS.md（Analog C: Step 3f の fail-soft STEP マーカーブロック構造を厳密に mirror。intro→bash→「終了コードに関わらず次へ」→条件付き echo [STEP:name:OK/FAIL]→「[PIPELINE:FAIL] は絶対に出力しないこと」警告）
    - .planning/phases/27-etf-exclusion/27-RESEARCH.md（Pattern 4: Step 2g 挿入の確定日本語プロンプト文言。fail-soft 継続・validate-meeting.ts より前・[PIPELINE:FAIL] 禁止）
    - .planning/phases/27-etf-exclusion/27-02-SUMMARY.md（Plan 02 が作った filter-etf-stocks.ts の実 CLI パス・出力する [filter-etf-stocks] FAIL: 行の形式を確認し STEP マーカー要約と整合させる）
  </read_first>
  <action>
    invest.md の Step 2g 内、pipeline-metrics タイムスタンプ記録の bash ブロック（`m.validationStart = Date.now();` を含むブロック）の直後、かつ既存の `cd /Users/arai/invest && npx tsx src/scripts/validate-meeting.ts` bash ブロックの直前に、Step 3f の fail-soft STEP マーカーブロックと同一構造の新しいプロンプト段落を挿入する。

    挿入内容は次の要素を Step 3f 様式で構成する: (1) 「以下のBashコマンドを実行してください:」の導入、(2) `cd /Users/arai/invest && npx tsx src/scripts/filter-etf-stocks.ts` の bash ブロック、(3) 「終了コードに関わらず、次のバリデーションステップへ進んでください（fail-soft, D-02）。filter-etf-stocks.ts は tmp/meeting-result.json の highlightedStocks から yahoo-finance2 の quoteType 照合によりETFを決定論的に除外し、同ファイルを書き戻す。必ず validate-meeting.ts より前に実行すること」という説明（D-07: 除外後データを以降のバリデーション・サマリー・レポート生成が参照する理由を明記）、(4) 終了コード0の場合に `echo '[STEP:etf-exclusion:OK]'` を実行、(5) 終了コード非0の場合に標準エラー出力の `[filter-etf-stocks] FAIL:` 行に続く理由を短く要約して `echo '[STEP:etf-exclusion:FAIL:<短い理由>]'` を実行、(6) 「[PIPELINE:FAIL] は絶対に出力しないこと（D-02）— このステップの失敗は既存4レポートの生成・デプロイを一切ブロックしない。フィルタが失敗した場合、元の tmp/meeting-result.json（未フィルタ）のまま次のステップへ進む」という警告。

    STEP マーカー名は `etf-exclusion`（D-08）。挿入後、既存の validate-meeting.ts bash ブロックはそのまま直後に続く（変更なし）。pipeline-metrics ブロック・validate-meeting.ts ブロック・サマリー表示は一切変更しないこと。マーカー文字列は Step 3f と同じ echo 形式で記述する。
  </action>
  <verify>
    <automated>grep -q "npx tsx src/scripts/filter-etf-stocks.ts" .claude/commands/invest.md && grep -q "STEP:etf-exclusion:OK" .claude/commands/invest.md && grep -q "STEP:etf-exclusion:FAIL" .claude/commands/invest.md && awk '/npx tsx src\/scripts\/filter-etf-stocks.ts/{f=NR} /npx tsx src\/scripts\/validate-meeting.ts/{v=NR} END{exit !(f>0 && v>0 && f<v)}' .claude/commands/invest.md && echo "OK: filter runs before validate"</automated>
  </verify>
  <acceptance_criteria>
    - filter-etf-stocks.ts 実行の bash ブロックが存在する（`grep -c "npx tsx src/scripts/filter-etf-stocks.ts" .claude/commands/invest.md` >= 1）
    - `[STEP:etf-exclusion:OK]` と `[STEP:etf-exclusion:FAIL` の echo が両方存在する
    - filter-etf-stocks.ts の実行行が validate-meeting.ts の実行行より前に位置する（awk による行番号順序チェックが成功）
    - `[PIPELINE:FAIL]` を出力しない旨の警告文が Step 2g の新ブロックに含まれる（挿入テキストに "PIPELINE:FAIL" と "絶対に出力しない" 相当が存在）
    - 既存の validate-meeting.ts bash ブロックが依然として存在し変更されていない（`grep -c "npx tsx src/scripts/validate-meeting.ts" .claude/commands/invest.md` >= 1）
    - fail-soft の説明文に「validate-meeting.ts より前に実行」の趣旨が含まれる（D-07）
  </acceptance_criteria>
  <done>Step 2g で filter-etf-stocks.ts が validate-meeting.ts の直前に実行されるよう wiring され、fail-soft STEP マーカー（[STEP:etf-exclusion:OK/FAIL]）と [PIPELINE:FAIL] 禁止が Step 3f 様式で記述されている（ETF-02 パイプライン統合, D-07/D-08/D-02）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| パイプライン定義(invest.md) → CLIスクリプト | invest.md は filter-etf-stocks.ts の実行タイミング（validate-meeting.ts との相対順序）を決定する。スクリプト自身ではなくオーケストレーション層が順序を保証 |
| LLMプロンプト指示(Layer 1) → LLM出力 | プロンプト追記は確率的ガイダンス。構造的保証は Layer 2（TS決定論フィルタ）が担う |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-06 | Denial of Service | Step 2g の filter-etf-stocks.ts 実行 | high | mitigate | D-02/D-08 fail-soft wiring: 終了コードに関わらず次ステップへ進み、[STEP:etf-exclusion:FAIL] を出しても [PIPELINE:FAIL] は絶対に出さない。フィルタ障害が既存4レポートの生成・デプロイをブロックしない構造を invest.md 側で保証 |
| T-27-07 | Tampering | プロンプト追記による既存契約の破損 | medium | mitigate | 追記は既存ブロックへの1行/1項目追加のみ（置換なし）。acceptance_criteria の grep で既存「注意: picks…」行の5回残存と validate-meeting.ts ブロック不変を検証し、意図しない削除・改変を構造的に検出 |
| T-27-08 | Repudiation | ETF除外の可視性 | low | accept | 除外の監査ログ（D-13）は Plan 02 の CLI が stdout に出力し launchd ログに残る。invest.md 側は [STEP:etf-exclusion:OK/FAIL] マーカーで実行有無を記録。永続監査ファイルは Phase 28 の管轄（D-14）で本フェーズはログで十分 |
</threat_model>

<verification>
- Task 1: `grep -c "…picks に含めないこと" .claude/commands/invest.md` == 5、モデレーター指示 == 1
- Task 2: filter-etf-stocks.ts 実行が validate-meeting.ts より前（awk 順序チェック）、STEP マーカー OK/FAIL 両存在
- 手動（ライブ, 27-VALIDATION.md Manual-Only）: 翌営業日 launchd 実行後、tmp/round-1/*.json の picks と tmp/meeting-result.json の highlightedStocks に ETF が含まれないこと、ログに `[STEP:etf-exclusion:OK]` を確認
</verification>

<success_criteria>
- 全5アナリストプロンプトに ETF を picks から除外する明示指示が含まれる（Success Criterion 1, ETF-01）
- Step 2f モデレーターに highlightedStocks 除外指示が含まれる（D-11）
- Step 2g で filter-etf-stocks.ts が validate-meeting.ts の直前に実行される（D-07, ETF-02 統合）
- フィルタ失敗は fail-soft で扱われ [PIPELINE:FAIL] を出さない（D-02, D-08）
- 既存の picks 出力契約・バリデーション・サマリー表示は保持される
</success_criteria>

<artifacts>
## Artifacts this phase produces（Plan 03）

**編集ファイル:**
- `.claude/commands/invest.md`

**追加されるプロンプト指示行（Layer 1）:**
- Step 2a 5ブロック共通（×5）: 「ETF・投資信託・インデックスファンド（例: SPY, QQQ, 1306.T等）は picks に含めないこと。個別企業株のみを推奨してください。」
- Step 2f モデレーター箇条書き（×1）: 「- ETF・投資信託・インデックスファンドは highlightedStocks に含めないこと（個別企業株のみ）。TS側でも quoteType による除外チェックを行うため、疑わしい銘柄は含めないこと」

**追加されるパイプライン wiring（Layer 2統合, Step 2g）:**
- bash 実行行: `cd /Users/arai/invest && npx tsx src/scripts/filter-etf-stocks.ts`（validate-meeting.ts の直前）
- STEP マーカー echo: `[STEP:etf-exclusion:OK]` / `[STEP:etf-exclusion:FAIL:<短い理由>]`
- fail-soft 説明段落 + `[PIPELINE:FAIL]` 禁止警告
</artifacts>

<output>
Create `.planning/phases/27-etf-exclusion/27-03-SUMMARY.md` when done
</output>
