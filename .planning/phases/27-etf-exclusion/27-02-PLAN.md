---
phase: 27-etf-exclusion
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/scripts/filter-etf-stocks.ts
  - src/scripts/filter-etf-stocks.test.ts
autonomous: true
requirements: [ETF-02]
must_haves:
  truths:
    - "filter-etf-stocks.ts が tmp/meeting-result.json を読み、highlightedStocks の ticker を yahoo-finance2 の単一 batch quote([...tickers]) 1回で照合する（D-05）"
    - "個別銘柄の quoteType が取得できない場合でも throw せず、その銘柄を fail-closed で除外して処理継続する（D-01, Success Criterion 3）"
    - "batch quote() 呼び出しや読み込み自体が失敗した場合は fail-soft: 元の tmp/meeting-result.json を一切書き換えず終了する（D-02）"
    - "除外が発生した銘柄は ETF判定 / lookup失敗 を区別して stdout に記録される（D-13）"
    - "meeting-result.json は highlightedStocks の要素除去のみで書き戻され、スキーマにフィールドは追加されない（D-09）"
  artifacts:
    - "src/scripts/filter-etf-stocks.ts（fail-soft CLI ラッパー: network + file I/O、Plan 01 の filterEtfStocks を import）"
    - "src/scripts/filter-etf-stocks.test.ts（fail-soft/fail-closed 分離・partial失敗継続・書き戻し検証。yahoo-finance2 + fs/promises モック）"
  key_links:
    - "filter-etf-stocks.ts は src/portfolio/etf-exclusion.js の filterEtfStocks を import（Plan 01 依存）"
    - "書き戻した tmp/meeting-result.json が invest.md Step 2g の validate-meeting.ts・下流レポート生成・翌日注入すべてに自然に波及する"
    - "batch quote() は array 引数の単一呼び出し（market.ts の per-symbol Promise.all イディオムは流用しない — D-05）"
---

<objective>
Plan 01 の純関数 `filterEtfStocks` をネットワーク・ファイルI/O境界でラップする薄い fail-soft CLI スクリプト `src/scripts/filter-etf-stocks.ts` を構築する（D-06 の分離構成のうち CLI ラッパー側）。`urgency-history.ts` + `write-urgency-history.ts` の実証済み分離パターンを踏襲し、tmp/meeting-result.json を読み、yahoo-finance2 の単一 batch `quote([...tickers])` 呼び出し（D-05）で quoteType を照合し、ETF除外済みの highlightedStocks で同ファイルをイミュータブルに書き戻す。個別銘柄の判定不能は fail-closed（除外）、フィルタ機構全体の故障は fail-soft（元ファイル維持）という D-01/D-02/D-03 の使い分けを構造的に厳守する。

Purpose: 決定論的 ETF 除外を実パイプラインで機能させる実行層。1つの新機能の障害が既存4レポートの生成・デプロイを止めないという OPS 方針を厳守しつつ、疑わしい銘柄はウォッチリスト（Phase 28以降）汚染防止のため除外する。
Output: `filter-etf-stocks.ts` CLI・完全な単体テスト（fail-soft/fail-closed/partial継続/書き戻し検証）・stdout 監査ログ（D-13）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-etf-exclusion/27-CONTEXT.md
@.planning/phases/27-etf-exclusion/27-RESEARCH.md
@.planning/phases/27-etf-exclusion/27-PATTERNS.md

# 依存: Plan 01 が作る純関数
@.planning/phases/27-etf-exclusion/27-01-SUMMARY.md
@src/portfolio/etf-exclusion.ts
# 流用パターン（fail-soft CLI ラッパー + テストモック）
@src/scripts/write-urgency-history.ts
@src/data/market.ts
@src/meeting/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filter-etf-stocks CLI の fail-soft/fail-closed テストを作成する</name>
  <files>src/scripts/filter-etf-stocks.test.ts</files>
  <read_first>
    - src/portfolio/etf-exclusion.ts（Plan 01 の filterEtfStocks / QuoteTypeLookup — CLI テストはこの関数を実データ経由で駆動する。import する型・戻り値形状を確認）
    - src/data/market.test.ts（yahoo-finance2 のモック規約: vi.hoisted + vi.mock("yahoo-finance2", () => ({ default: ... quote: quoteMock }))。このプロジェクトの正準モック方式）
    - src/scripts/write-urgency-history.ts（fail-soft CLI の main() 構造・ENOENT 二重チェック・process.exitCode 分岐・entry-point guard の analog）
    - .planning/phases/27-etf-exclusion/27-RESEARCH.md（Code Examples: CLI wrapper skeleton と test mock convention、Pitfall 4 の「書き戻し前に故障したら元ファイルを触らない」検証）
    - src/meeting/types.ts（MeetingResult 型 — テスト fixture の meeting-result.json 形状）
  </read_first>
  <behavior>
    - fs/promises（readFile/writeFile）と yahoo-finance2 を両方 vi.mock する。quote は vi.hoisted の quoteMock で差し替える
    - Test A（正常フィルタ）: readFile が highlightedStocks=[AAPL, SPY, 1306.T, 7203.T] を含む meeting-result を返し、quoteMock が [{symbol:AAPL,quoteType:EQUITY},{symbol:SPY,quoteType:ETF},{symbol:1306.T,quoteType:ETF},{symbol:7203.T,quoteType:EQUITY}] を返すとき、writeFile が highlightedStocks=[AAPL, 7203.T] のみを含む payload で1回呼ばれる
    - Test B（partial lookup失敗で継続, Success Criterion 3）: quoteMock が一部 ticker を欠いた配列を返す（例: XYZ が応答に含まれない）とき、main() が throw せず、欠損 ticker が writeFile payload の highlightedStocks から除外される。process.exit(1) の uncaught throw が発生しない
    - Test C（batch quote() reject で fail-soft, D-02, Pitfall 4）: quoteMock.mockRejectedValue でネットワーク例外を再現。writeFile が meeting-result.json に対して呼ばれない（元ファイル維持）。process.exitCode が非0にセットされる。throw で落ちない
    - Test D（読み込み失敗で fail-soft, D-02）: readFile が meeting-result.json で reject。writeFile が呼ばれず process.exitCode 非0。throw しない
    - Test E（highlightedStocks 0件でスキップ）: highlightedStocks=[] のとき quote() を呼ばず writeFile もせず正常終了（exitCode 0）
    - Test F（D-13 監査ログ）: ETF除外時に stdout（console.log）へ "ETF除外: SPY (quoteType=ETF)" 形式、lookup失敗時に "ETF除外: <ticker> (quoteType取得失敗, fail-closed)" 形式が出力される（console.log スパイで検証）
    - Test G（single batch call, D-05）: quoteMock が配列引数で1回だけ呼ばれる（quoteMock.mock.calls.length === 1、第1引数が配列）。per-ticker の複数回呼び出しでないこと
  </behavior>
  <action>
    src/scripts/filter-etf-stocks.test.ts を新規作成する。market.test.ts の vi.hoisted + vi.mock("yahoo-finance2", ...) 規約に従い quoteMock を差し替える。fs/promises も vi.mock し readFile/writeFile を制御可能なモックにする（write-news-digest.test.ts / urgency-history.test.ts のプレーン Error による ENOENT シミュレート規約に合わせる）。テスト対象は filter-etf-stocks.ts の export された main() 関数（entry-point guard により import 時に副作用実行されない構造を前提）。beforeEach で quoteMock.mockReset、afterEach で vi.restoreAllMocks。behavior の Test A〜G を実装する。書き戻し検証（Test A）は writeFile の呼び出し引数を JSON.parse して highlightedStocks の ticker 配列を assert する。fail-soft 検証（Test C/D）は「writeFile が MEETING_RESULT_PATH に対して呼ばれていない（または元 payload と同一）」を assert（Pitfall 4 の直接検証）。single-batch 検証（Test G）は quoteMock が配列を第1引数として厳密に1回呼ばれることを assert。この時点で filter-etf-stocks.ts の main() は未実装 or スタブのため RED でよい。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/filter-etf-stocks.test.ts 2>&1 | grep -qE "Cannot find module|Failed to resolve import|No test files|FAIL|failed" && echo "RED confirmed" || echo "check output"</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/filter-etf-stocks.test.ts が存在する
    - yahoo-finance2 と fs/promises の両方が vi.mock されている（`grep -c 'vi.mock' src/scripts/filter-etf-stocks.test.ts` >= 2）
    - vi.hoisted による quoteMock 差し替えがある（`grep -c 'vi.hoisted' src/scripts/filter-etf-stocks.test.ts` >= 1）
    - behavior Test A〜G に対応する it が7個以上（`grep -c '  it(' src/scripts/filter-etf-stocks.test.ts` >= 7）
    - fail-soft 検証で writeFile が呼ばれないことの assert を含む（Test C/D に "writeFile" と "not"/"toHaveBeenCalledTimes(0)"/"not.toHaveBeenCalled" 相当が存在）
    - single-batch 検証で quoteMock の呼び出し回数を1に固定する assert を含む（`grep -cE 'toHaveBeenCalledTimes\(1\)|mock.calls.length' src/scripts/filter-etf-stocks.test.ts` >= 1）
    - この時点で `npx vitest run src/scripts/filter-etf-stocks.test.ts` は RED（main() 未実装）
  </acceptance_criteria>
  <done>CLI の fail-soft（D-02）・fail-closed（D-01）・partial継続（Success Criterion 3）・書き戻し（D-09）・D-13ログ・single-batch（D-05）を網羅するテストが存在し、実装未完で RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: filter-etf-stocks.ts fail-soft CLI ラッパーを実装しテストをGREENにする</name>
  <files>src/scripts/filter-etf-stocks.ts</files>
  <read_first>
    - src/scripts/filter-etf-stocks.test.ts（Task 1 の契約 — main() のシグネチャ・writeFile 呼び出し条件・stdout ログ形式に厳密一致させる）
    - src/scripts/write-urgency-history.ts（fail-soft CLI の正準骨格: TMP_DIR 定数、main() 構造、process.exitCode/process.exit 分岐、`if (process.argv[1] === fileURLToPath(import.meta.url))` entry-point guard、console.error での STEP ステータス出力規約）
    - src/portfolio/etf-exclusion.ts（filterEtfStocks / QuoteTypeLookup を import — 純関数に quoteTypeByTicker Map を渡し kept/excluded を受け取る）
    - src/data/market.ts（yahoo-finance2 インスタンス化規約 `new YahooFinance({ suppressNotices: ["yahooSurvey"] })`。ただし line 64 の per-symbol `quote(symbol)` は流用しない — D-05 は配列引数の単一呼び出しを要求）
    - .planning/phases/27-etf-exclusion/27-RESEARCH.md（Pattern 2 の fetchQuoteTypes 実装 + Code Examples の CLI skeleton。Array.isArray 防御的パース、欠損 ticker を暗黙の lookup 失敗として扱う扱い）
    - src/meeting/types.ts（MeetingResult 型）
  </read_first>
  <action>
    src/scripts/filter-etf-stocks.ts を新規作成する。import: node:fs/promises の readFile/writeFile、node:path の join、node:url の fileURLToPath、yahoo-finance2 のデフォルト、src/portfolio/etf-exclusion.js の filterEtfStocks、src/portfolio/etf-exclusion.js の QuoteTypeLookup 型、src/meeting/types.js の MeetingResult 型。定数 TMP_DIR = join(import.meta.dirname, "../../tmp")、MEETING_RESULT_PATH = join(TMP_DIR, "meeting-result.json")。yahoo-finance2 は `new YahooFinance({ suppressNotices: ["yahooSurvey"] })` でインスタンス化。

    fetchQuoteTypes(tickers: ReadonlyArray<string>): Promise<ReadonlyMap<string, QuoteTypeLookup>> を実装（27-RESEARCH.md Pattern 2）: tickers 空なら空 Map を返す。try 内で `await yahooFinance.quote([...tickers])` を単一 batch 呼び出し（D-05、per-ticker ループや Promise.all は禁止）。`Array.isArray(quotes) ? quotes : [quotes]` で防御的に配列化（yahoo-finance2 の overload が単一要素配列で単体オブジェクトを返すブレに備える）。各要素の symbol と quoteType を読み、両方存在すれば map.set(symbol, { status: "ok", quoteType })。応答に含まれない要求 ticker は map 未登録のまま（純関数側で lookup-failed 扱い）。この fetchQuoteTypes 内の quote() 呼び出し例外は呼び出し元 main() の try/catch で fail-soft として扱うため、fetchQuoteTypes は catch で握りつぶさず伝播させてよい（ただし研究の Pattern 2 通り batch 全体失敗時に空 Map を返す実装でも可 — その場合は全 ticker が fail-closed 除外となり D-01 と整合する。Test C の「writeFile が呼ばれない」を満たすには、batch 失敗を「メカニズム故障=fail-soft」として main() 側で書き戻しをスキップする必要があるため、fetchQuoteTypes は例外を main() へ伝播させる設計を採用すること）。

    export async function main(): (1) readFile(MEETING_RESULT_PATH) を try/catch。読み込み/JSON.parse 失敗は D-02 メカニズム故障: `console.error("[filter-etf-stocks] FAIL: ...")` を出力し process.exitCode=1 して return（writeFile 一切なし）。(2) tickers = meetingResult.highlightedStocks.map(s => s.ticker)。tickers.length===0 なら `console.error("[filter-etf-stocks] OK (skip: highlightedStocks 0件)")` して return（quote 呼ばず、書き戻しもしない）。(3) quoteTypeByTicker = await fetchQuoteTypes(tickers) を try/catch。ここでの例外は D-02 メカニズム故障: FAIL ログ + exitCode=1 + return（writeFile なし、元ファイル維持）。(4) filterEtfStocks(meetingResult.highlightedStocks, quoteTypeByTicker) を呼び kept/excluded を得る。(5) excluded を反復し D-13 監査ログ: reason==="etf" なら `console.log(\`ETF除外: ${ticker} (quoteType=${quoteType})\`)`、reason==="lookup-failed" なら `console.log(\`ETF除外: ${ticker} (quoteType取得失敗, fail-closed)\`)`。(6) updated = { ...meetingResult, highlightedStocks: kept } でイミュータブルに新オブジェクト構築（D-09: フィールド追加せず要素除去のみ）。(7) await writeFile(MEETING_RESULT_PATH, JSON.stringify(updated, null, 2), "utf-8")。(8) `console.error(\`[filter-etf-stocks] OK: ${excluded.length}件除外, ${kept.length}件残存\`)`。

    重要な構造制約（Pitfall 4）: writeFile は在メモリ計算成功後にのみ実行する。読み込み・quote 取得のいずれかが失敗した分岐では writeFile に到達しないこと（元 tmp/meeting-result.json を維持）。console.error を STEP/ステータス行に、console.log を per-除外の監査行に使い分ける（write-urgency-history.ts 規約）。entry-point guard `if (process.argv[1] === fileURLToPath(import.meta.url)) { main().catch(...) }` を write-urgency-history.ts から verbatim で流用。`[PIPELINE:FAIL]` は一切出力しないこと（D-02）。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/filter-etf-stocks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/filter-etf-stocks.test.ts` が全テスト GREEN（Task 1 の Test A〜G 全通過）
    - filter-etf-stocks.ts が `export async function main` を含む
    - batch quote() が配列引数の単一呼び出しで実装されている（`grep -cE 'quote\(\[' src/scripts/filter-etf-stocks.ts` >= 1）かつ per-symbol の `.map` 内 quote 呼び出しが存在しない（`grep -cE 'tickers.map\([^)]*quote' src/scripts/filter-etf-stocks.ts` が 0）
    - Array.isArray による防御的パースがある（`grep -c 'Array.isArray' src/scripts/filter-etf-stocks.ts` >= 1）
    - filterEtfStocks を etf-exclusion.js から import している（`grep -c 'filterEtfStocks' src/scripts/filter-etf-stocks.ts` >= 1）
    - `[PIPELINE:FAIL]` 文字列が filter-etf-stocks.ts に存在しない（`grep -c 'PIPELINE:FAIL' src/scripts/filter-etf-stocks.ts` が 0）
    - meeting-result.json 書き戻しがスプレッド構築で highlightedStocks のみ差し替え（`grep -cE '\.\.\.meetingResult' src/scripts/filter-etf-stocks.ts` >= 1）— スキーマにフィールド追加がない（D-09）
    - `npx tsc --noEmit` が filter-etf-stocks.ts で型エラーを出さない
  </acceptance_criteria>
  <done>filter-etf-stocks.ts が単一 batch quote()（D-05）で quoteType 照合し、fail-closed（D-01）/ fail-soft（D-02）を分離し、D-13 監査ログを出力し、highlightedStocks 要素除去のみで tmp/meeting-result.json を書き戻す。全テスト GREEN。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(local pipeline) → 外部API(Yahoo Finance) | filter-etf-stocks.ts が yahoo-finance2 経由でネットワーク越しに ticker 配列を送り quoteType を受け取る |
| ファイルI/O境界 → tmp/meeting-result.json | 自社生成アーティファクトを read/rewrite する。書き戻しは下流の全レポート・翌日注入に波及 |
| npm 依存(yahoo-finance2) | 既存インストール済み。本フェーズは新規パッケージ install ゼロ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Denial of Service | Yahoo Finance batch quote() 呼び出し | high | mitigate | D-05 の単一 batch `quote([...tickers])` 設計により、ticker 件数に関わらずリクエスト数を実行あたり1回に構造的に固定。per-ticker 逐次/Promise.all は禁止（research Pitfall 3）。ネットワーク障害時は D-02 fail-soft で元ファイル維持しパイプライン継続（既存4レポート・デプロイを一切ブロックしない） |
| T-27-04 | Tampering | tmp/meeting-result.json の書き戻し | high | mitigate | 書き戻しは在メモリ計算成功後にのみ実行（Pitfall 4）。読み込み/quote 取得失敗の分岐では writeFile に到達しない構造とし、故障時に元ファイルを破壊しない（D-02）。テスト Test C/D が writeFile 非呼び出しを直接 assert |
| T-27-05 | Tampering | LLM生成 ticker 文字列の Map キー使用 | medium | mitigate | quoteTypeByTicker は ReadonlyMap（plain object 不使用）で prototype pollution を構造的に回避（T-27-01 と同方針）。map 未ヒットは fail-closed 除外 |
| T-27-SC | Tampering | npm installs | high | mitigate | 本フェーズは npm install ゼロ（27-RESEARCH.md Package Legitimacy Audit: Not applicable）。yahoo-finance2@3.13.2 は既存インストール済み・既存コードで稼働中。[ASSUMED]/[SUS]/[SLOP] パッケージなし。legitimacy checkpoint 不要 |
</threat_model>

<verification>
- `npx vitest run src/scripts/filter-etf-stocks.test.ts` — Wave 内サンプリング（全 GREEN）
- `npx vitest run src/portfolio/etf-exclusion.test.ts src/scripts/filter-etf-stocks.test.ts` — Plan 01+02 の統合サンプリング
- `npx tsc --noEmit` — 型健全性
- `npx vitest run` — Wave マージ時のフルスイート回帰
- 手動（ライブ）: 翌営業日の launchd 実行後、ログに `[STEP:etf-exclusion:OK]` を確認（27-VALIDATION.md Manual-Only）
</verification>

<success_criteria>
- filter-etf-stocks.ts が単一 batch quote() で米国ETF・日本ETF を highlightedStocks から決定論的に除外する（Success Criterion 2）
- 個別銘柄 lookup 失敗時も throw せず fail-closed で除外し処理継続する（Success Criterion 3, D-01）
- フィルタ機構全体の故障時は元 tmp/meeting-result.json を維持して fail-soft で継続する（D-02, Pitfall 4）
- 除外理由が ETF判定 / lookup失敗 で区別され stdout に記録される（D-13）
- meeting-result.json はスキーマ変更なし、highlightedStocks 要素除去のみ（D-09）
- 全テスト GREEN
</success_criteria>

<artifacts>
## Artifacts this phase produces（Plan 02）

**新規ファイル:**
- `src/scripts/filter-etf-stocks.ts`
- `src/scripts/filter-etf-stocks.test.ts`

**エクスポートシンボル:**
- `export async function main(): Promise<void>`（fail-soft CLI エントリ）
- `fetchQuoteTypes(tickers: ReadonlyArray<string>): Promise<ReadonlyMap<string, QuoteTypeLookup>>`（単一 batch quote 照合、module 内 or export）

**CLI 実行パス:**
- `npx tsx src/scripts/filter-etf-stocks.ts`（invest.md Step 2g から呼ばれる — Plan 03 で wiring）

**STEP マーカー（CLI が出力、invest.md 側で echo）:**
- `[STEP:etf-exclusion:OK]` / `[STEP:etf-exclusion:FAIL:<理由>]`

**stdout 監査ログ形式（D-13）:**
- `ETF除外: <ticker> (quoteType=<type>)` / `ETF除外: <ticker> (quoteType取得失敗, fail-closed)`

**内部ステータスログ:**
- `[filter-etf-stocks] OK: <n>件除外, <m>件残存` / `[filter-etf-stocks] FAIL: <理由>`（console.error）
</artifacts>

<output>
Create `.planning/phases/27-etf-exclusion/27-02-SUMMARY.md` when done
</output>
