---
phase: 23
slug: new-candidates-section-removal
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-04
---

# Phase 23 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Phase type: REMOVAL-ONLY.** This phase adds zero new visual elements. The entire contract is:
(1) what must stop rendering, (2) what must be verified as unchanged (regression guard) in the
surrounding layout and shared dark-theme system. No new tokens, no new copy, no new components
are introduced. Source: `23-CONTEXT.md` (D-01〜D-09, all locked, `--auto` mode), `23-RESEARCH.md`
(verified exact code locations), `REQUIREMENTS.md` UI-08.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — plain TypeScript HTML-string generator (`src/scripts/generate-portfolio-report.ts` + `report-utils.ts`), not a React/Next.js/Vite app. shadcn Initialization Gate does not apply (no `components.json`, no `package.json` React/Vite dependency; confirmed via `ls components.json tailwind.config.* postcss.config.*` → no matches). |
| Preset | not applicable |
| Component library | none — hand-written HTML templates via template literals, styled by a single shared `<style>` block returned from `generateBaseStyles()` (`report-utils.ts:98-228`) |
| Icon library | none — badges use inline emoji (`⚠`) and CSS pill shapes, not an icon font/SVG library |
| Font | `'Helvetica Neue', Arial, 'Hiragino Sans', 'Hiragino Kaku Gothic ProN', Meiryo, sans-serif` (existing, unchanged, `report-utils.ts:106`) |

Source: codebase scout (Step 2). Pre-populated, not asked.

---

## Removal Contract (this phase's actual scope)

| Path | Must NOT render (verified strings) | Verification |
|------|-------------------------------------|---------------|
| Normal path (`portfolioAnalysis !== null`) | `"新規組入候補"` heading text; `"Daily Reportのアナリストミーティングで推奨された銘柄です"` paragraph text; the entire highlighted-stocks scoring `<table>` (ticker/agent-score cells/average/verdict columns) | Test 30 (inverted) — `expect(html).not.toContain(...)` using **non-empty** `validMeetingResult.highlightedStocks` (PLTR/8.2) fixture per D-05 (empty-array input would pseudo-pass even on unmodified code — Pitfall 1) |
| Fallback path (`portfolioAnalysis === null`) | Same two strings as above | Test 31 (extended) — same non-empty fixture, same `not.toContain` assertions per D-06 |
| Both paths | `formatNewCandidatesHtml()` function itself (source, not just call sites) | Full deletion per D-01 — grep/diff shows the function no longer exists in the file, not merely unreachable |
| `generate-portfolio-report.ts:1` import line | `scoreColor`, `verdictColor` identifiers | Import line pruned to `escapeHtml, generateBaseStyles, safeHref, formatPublishedAtJst` only per D-02 |

**Explicitly NOT touched (regression guard against scope creep — D-08/D-09):**

| Location | Must remain byte-for-byte identical |
|----------|--------------------------------------|
| `.claude/commands/invest.md:1746` | `"注目銘柄: [highlightedStocks 配列の全内容]"` — portfolio-analyst still receives `highlightedStocks` as prompt context. Verify via `git diff --stat .claude/commands/invest.md` → empty. |
| `src/meeting/types.ts` (`MeetingResult.highlightedStocks`) | Type definition unchanged |
| `src/meeting/schemas.ts` (`highlightedStocks` zod schema) | Schema unchanged |
| `src/scripts/report-utils.ts` (`scoreColor`, `verdictColor` function bodies) | Function definitions untouched — still consumed by `generate-daily-report.ts` (6 verified usages) |
| `generate-portfolioReportHtml(result, portfolioAnalysis, resolvedHoldingNews)` signature | Unchanged (D-03) — `result.date` still needed for title/heading |

---

## Unchanged Surrounding Layout (regression guard)

The section order below is the **existing, correct** structure. This phase must preserve it
exactly, minus the removed section:

**Normal path (`portfolioAnalysis !== null`):**
```
<h1>Portfolio Report - {date}</h1>
<p class="timestamp">生成日時: ...</p>
<h2>総括コメント</h2>                      ← formatOverallCommentHtml (unchanged)
<h2>保有銘柄 個別評価</h2>                  ← formatHoldingEvaluationsHtml (unchanged, includes
                                              urgent/decisionChanged badges from Phase 22 — must
                                              not regress)
<h2>リバランス提案</h2>                     ← formatRebalanceActionsHtml (unchanged)
[END — no 新規組入候補 section after this phase]
```

**Fallback path (`portfolioAnalysis === null`):**
```
<h1>Portfolio Report - {date}</h1>
<p class="timestamp">生成日時: ...</p>
<div class="agent-card"><p>本日のポートフォリオ分析は生成されませんでした。</p></div>
[END — no 新規組入候補 section after this phase, no replacement content added per D-04]
```

Do not reorder, do not add spacing/wrapper divs, do not add placeholder/empty-state copy in the
gap left by the removed section — D-04 explicitly forbids adding replacement content.

---

## Spacing Scale

**REGRESSION GUARD — existing values, not modified by this phase.** No new spacing introduced.

| Token | Value | Usage (existing, verified in `report-utils.ts`) |
|-------|-------|--------------------------------------------------|
| xs | 0.15rem (2.4px) | Badge/pill inline padding |
| sm | 0.4–0.5rem (6.4–8px) | Compact element spacing (news item padding, badge margins) |
| md | 0.8rem–1rem (12.8–16px) | Card padding, list-item padding, paragraph margins |
| lg | 1.2rem (19.2px) | `.agent-card` padding |
| xl | 1.5–2rem (24–32px) | h1 margin-bottom, body padding, hr margin |

Exceptions: none — all existing values already deviate from a strict 8pt px grid (project uses
`rem`-based legacy report styling, not a component-library spacing token system). This phase does
not introduce new spacing and must not "fix" this to an 8pt grid as an unrelated drive-by change.

---

## Typography

**REGRESSION GUARD — existing values from `generateBaseStyles()`, not modified by this phase.**

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (1rem, browser default) | 400 (regular, default) | 1.7 (`body` rule) |
| Label | 13.6px (0.85rem) — `.timestamp`, `.news-meta`, news source/date lines | 400 (regular) | ~1.4 (default) |
| Heading (h2/h3/h4) | 22.4px / 18.4px / 16px (1.4rem / 1.15rem / 1rem) | 700 (bold, browser default `<h2-4>`) | default (~1.2) |
| Display (h1) | 28.8px (1.8rem) | 700 (bold, browser default) | default (~1.2) |

2 weights total (400 regular body text, 700 bold headings via default browser `<h1>`–`<h4>` — no
explicit `font-weight` overrides exist in the stylesheet for these). No new sizes/weights are
introduced by this phase.

---

## Color

**REGRESSION GUARD — existing dark theme, not modified by this phase.**

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0f0f1a` (body background) | Page background |
| Secondary (30%) | `#1e1e2e` (`.agent-card`, table bg), `#1a1a28` (`.discussion-card`), `#2a2a3e` (table header row) | Cards, panels, table headers |
| Accent (10%) | `#10b981` (portfolio-report accent green, passed to `generateBaseStyles("#10b981")` in `generatePortfolioReportHtml`) | h1 border-bottom, h2 border-left/text (via `accentLight` `#34d399`), h3 text (via `accentLighter` `#6ee7b7`), `.agent-card` alt border (`#6366f1`), 保持/買増/一部売却/全売却 decision-color badges (`decisionColor()`) |
| Destructive | `#ef4444` (used for `全売却` decision color and `⚠ 緊急` urgent badge background, Phase 22) | Destructive/urgent signaling only — untouched by this phase |

Accent reserved for: portfolio-report h1/h2/h3 accent framing, decision-color badges
(`保持`/`買増`/`一部売却`/`全売却`), urgent (`⚠緊急`, red) and decision-changed (amber) badges from
Phase 22. This phase removes the `新規組入候補` table (which previously consumed `scoreColor`/
`verdictColor` for its own color coding) — no accent-color usage is added or removed elsewhere.

---

## Copywriting Contract

This phase deletes copy; it introduces none. All rows are N/A except the removal/preservation
pair, which is the actual deliverable.

| Element | Copy |
|---------|------|
| Primary CTA | N/A — no CTA in this phase (removal-only; report has no interactive CTA elements) |
| Empty state heading | N/A — no new empty state introduced |
| Empty state body | N/A — pre-existing fallback message is preserved verbatim, not newly authored: `"本日のポートフォリオ分析は生成されませんでした。"` (D-04: no replacement content added in the gap left by the removed section) |
| Error state | N/A — no error UI in this phase's scope |
| Destructive confirmation | N/A — no runtime destructive UI action; this is a build-time code deletion, not a user-facing destructive operation |
| **Removed copy (must NOT appear in output)** | `"新規組入候補（Daily Report より転載）"` (h2 heading); `"Daily Reportのアナリストミーティングで推奨された銘柄です。スコアリングマトリクスを参考に投資判断してください。"` (paragraph) |
| **Preserved copy (must remain unchanged, read-only)** | `.claude/commands/invest.md:1746` — `"注目銘柄: [highlightedStocks 配列の全内容]"` (D-08, not a rendering concern, prompt-context only) |

---

## Registry Safety

Not applicable — no shadcn, no component registry, no third-party UI blocks. This phase touches
only hand-written TypeScript template-literal HTML in `src/scripts/generate-portfolio-report.ts`
and its colocated vitest suite `src/scripts/generate-report.test.ts`.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (removal contract — no new copy; removed strings confirmed absent both paths; fallback message unchanged)
- [ ] Dimension 2 Visuals: PASS (surrounding layout order — 総括コメント → 保有銘柄個別評価 → リバランス提案 — unchanged; no replacement content in the removed section's gap)
- [ ] Dimension 3 Color: PASS (regression guard — dark theme, accent `#10b981`, decision/urgent/decision-changed badge colors untouched)
- [ ] Dimension 4 Typography: PASS (regression guard — existing sizes/weights untouched, no new type introduced)
- [ ] Dimension 5 Spacing: PASS (regression guard — existing rem-based spacing untouched, no new spacing introduced)
- [ ] Dimension 6 Registry Safety: PASS (not applicable — no registry, no shadcn, no third-party blocks)

**Approval:** pending
