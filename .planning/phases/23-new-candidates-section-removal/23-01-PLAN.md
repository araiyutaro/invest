---
phase: 23-new-candidates-section-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/generate-report.test.ts
  - src/scripts/generate-portfolio-report.ts
autonomous: true
requirements: [UI-08]

must_haves:
  truths:
    - "ポートフォリオレポート(通常パス, portfolioAnalysis !== null)のHTMLに「新規組入候補」セクションが表示されない"
    - "ポートフォリオレポート(フォールバックパス, portfolioAnalysis === null)のHTMLに「新規組入候補」セクションが表示されない"
    - "portfolio-analyst は highlightedStocks をプロンプト文脈として引き続き受け取る(invest.md Step 3d 無変更)"
    - "Daily Report の新規推奨銘柄表示(スコアリングマトリクス等)は本フェーズの変更の影響を受けず従来どおり表示される"
  artifacts:
    - path: "src/scripts/generate-portfolio-report.ts"
      provides: "formatNewCandidatesHtml 関数・両呼び出し箇所・未使用 import を削除した純関数レンダラー"
      absent: "formatNewCandidatesHtml"
    - path: "src/scripts/generate-report.test.ts"
      provides: "Test 30(非存在検証へ反転)・Test 31(フォールバックパス非存在アサーション追加)"
      contains: "not.toContain(\"新規組入候補\")"
  key_links:
    - from: "src/scripts/generate-report.test.ts"
      to: "src/scripts/generate-portfolio-report.ts"
      via: "await import + generatePortfolioReportHtml(validMeetingResult, ...) → not.toContain 検証"
      pattern: "not\\.toContain\\(\"新規組入候補\"\\)"
    - from: ".claude/commands/invest.md:1746"
      to: "portfolio-analyst prompt"
      via: "注目銘柄: [highlightedStocks 配列の全内容] (無変更・regression guard)"
      pattern: "highlightedStocks 配列の全内容"
---

<objective>
UI-08: ポートフォリオレポートから「新規組入候補（Daily Report より転載）」セクションを削除する。通常パス・フォールバックパスの両方の呼び出し箇所を除去し、レンダリングコード（`formatNewCandidatesHtml()` 関数本体・2つの呼び出し/埋め込み・未使用化する import）を完全削除する。同時に portfolio-analyst への文脈情報としての `highlightedStocks` 受け渡し（invest.md Step 3d・types.ts・schemas.ts）は一切変更せず、無変更であることを検証で担保する。

Purpose: Daily Report と重複する新規組入候補セクションを排し、ポートフォリオレポートを保有銘柄の意思決定に集中させる（Daily Report が新規発掘の専任領域）。二重目的化の再発を防ぐ（REQUIREMENTS.md 除外事項）。
Output: 反転/拡張された vitest テスト（Test 30/31）と、`formatNewCandidatesHtml` を削除した `generate-portfolio-report.ts`。

TDD 構造: Task 1 でテストを非存在検証へ反転（RED — 削除前コードに対して失敗）、Task 2 で削除実装（GREEN — テストが通る）。project CLAUDE.md の TDD 規約に従う。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-new-candidates-section-removal/23-CONTEXT.md
@.planning/phases/23-new-candidates-section-removal/23-RESEARCH.md
@.planning/phases/23-new-candidates-section-removal/23-PATTERNS.md
@.planning/phases/23-new-candidates-section-removal/23-UI-SPEC.md

<interfaces>
<!-- Executor はこれらを直接使用する。コードベース探索は不要。RESEARCH.md/PATTERNS.md で verbatim 検証済み。 -->

削除対象シグネチャ (src/scripts/generate-portfolio-report.ts):
- `function formatNewCandidatesHtml(result: MeetingResult): string` — 102-133行目（関数本体全体）
- `const newCandidatesHtml = formatNewCandidatesHtml(result);` — 143行目（呼び出し）
- `${newCandidatesHtml}` — 161行目（フォールバック branch テンプレート内埋め込み）
- `${newCandidatesHtml}` — 186行目（通常 branch テンプレート内埋め込み）

現行 import 行 (1行目):
`import { escapeHtml, scoreColor, verdictColor, generateBaseStyles, safeHref, formatPublishedAtJst } from "./report-utils.js";`
削除後の目標:
`import { escapeHtml, generateBaseStyles, safeHref, formatPublishedAtJst } from "./report-utils.js";`
（`scoreColor` / `verdictColor` は本ファイル内では `formatNewCandidatesHtml` のみが使用。`report-utils.ts` の関数本体は削除しない — generate-daily-report.ts が引き続き使用）

保持シグネチャ (D-03, 無変更):
`generatePortfolioReportHtml(result: MeetingResult, portfolioAnalysis, resolvedHoldingNews): string`
（`result.date` がタイトル/見出しで引き続き必要）

テストフィクスチャ (src/scripts/generate-report.test.ts, 既存・変更不要):
`validMeetingResult.highlightedStocks` = 非空（PLTR / averageScore 8.2 / verdict 強気 / agentScores 2件）
→ この非空フィクスチャで検証すること（空配列だと削除前コードも早期 return "" するため擬似合格になる — D-05）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test 30 を非存在検証へ反転し Test 31 にフォールバックパス非存在アサーションを追加 (RED)</name>
  <files>src/scripts/generate-report.test.ts</files>
  <read_first>
    - src/scripts/generate-report.test.ts（現行 Test 30 = 360-366行目付近、Test 31 = 368-374行目付近、および validMeetingResult フィクスチャ 13-40行目付近を確認）
    - .planning/phases/23-new-candidates-section-removal/23-PATTERNS.md（反転後のテキスト例・命名規約: it タイトル末尾に要件タグ括弧付け `(UI-08、通常パス)`）
    - .planning/phases/23-new-candidates-section-removal/23-RESEARCH.md（Pitfall 1: 空配列擬似合格の回避理由）
  </read_first>
  <behavior>
    - Test 30（通常パス, validPortfolioAnalysis 有り, validMeetingResult の非空 highlightedStocks 使用）: `expect(html).not.toContain("新規組入候補")` かつ `expect(html).not.toContain("Daily Reportのアナリストミーティングで推奨された銘柄です")`
    - Test 31（フォールバックパス, portfolioAnalysis === null, 同じ非空 validMeetingResult 使用）: 既存アサーション（`toContain("<!DOCTYPE html>")`, `toContain("Portfolio Report")`, `not.toContain("保有銘柄 個別評価")`）を維持したうえで `expect(html).not.toContain("新規組入候補")` を1行追加
    - この段階で削除前の実装に対しては両テストが FAIL（RED）すること — 非空 highlightedStocks が現状ではセクションを描画するため
  </behavior>
  <action>
    Test 30（`it("Test 30: HTML に highlightedStocks の新規組入候補セクションが含まれる (PORT-01)", ...)`）を非存在検証へ反転する。既存の3つの存在アサーション `toContain("新規組入候補")` / `toContain("PLTR")` / `toContain("8.2")` を、`not.toContain("新規組入候補")` と `not.toContain("Daily Reportのアナリストミーティングで推奨された銘柄です")` の2アサーションに置き換える。入力は `generatePortfolioReportHtml(validMeetingResult, validPortfolioAnalysis)`（既存フィクスチャをそのまま使用、空配列に差し替えない — D-05/Pitfall 1）。it タイトルは `Test 30: HTML に新規組入候補セクションが含まれない (UI-08、通常パス)` 等、既存ファイルの「Test N: 説明 (要件タグ)」規約に沿って更新（Test 33/34 の `(UI-06/D-08)` 形式に倣う。Test 番号 30 は維持）。

    Test 31（`it("Test 31: portfolioAnalysis が null の場合フォールバック HTML を返す", ...)`）は既存3アサーションを保持したまま末尾に `expect(html).not.toContain("新規組入候補");` を1行追加する（D-06。入力 `generatePortfolioReportHtml(validMeetingResult, null)` は既存のまま、非空 highlightedStocks で discriminating に保つ）。

    Test 4（Daily Report の highlightedStocks 表示テスト）は変更しない（D-07）。他のテスト・フィクスチャ・番号体系には触れない。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts -t "Test 30" 2>&1 | grep -q "FAIL\|failed\|×" && echo "RED confirmed (Test 30 fails against un-removed code)" || echo "WARN: expected RED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'not.toContain("新規組入候補")' src/scripts/generate-report.test.ts` の結果が 2（Test 30 と Test 31 の両方）
    - `grep -c 'toContain("新規組入候補")' src/scripts/generate-report.test.ts`（`not.` を含まない存在アサーション）が 0
    - Test 30 が `not.toContain("Daily Reportのアナリストミーティングで推奨された銘柄です")` を含む
    - Test 30 と Test 31 の入力が `validMeetingResult`（非空 highlightedStocks）であり、空配列フィクスチャに差し替えられていない
    - この時点で `npx vitest run src/scripts/generate-report.test.ts -t "Test 30"` が FAIL する（削除前コードに対する RED — 反転テストが discriminating である証明）
    - Test 4（Daily Report）の行は git diff に現れない
  </acceptance_criteria>
  <done>Test 30 が非存在検証へ反転、Test 31 にフォールバックパスの非存在アサーションが追加され、削除前コードに対して両者が RED。</done>
</task>

<task type="auto">
  <name>Task 2: formatNewCandidatesHtml 関数・両呼び出し箇所・未使用 import を完全削除 (GREEN)</name>
  <files>src/scripts/generate-portfolio-report.ts</files>
  <read_first>
    - src/scripts/generate-portfolio-report.ts（1行目 import、102-133行目 関数本体、143行目 呼び出し、161/186行目 埋め込み、および前後の2 branch テンプレート構造を確認）
    - .planning/phases/23-new-candidates-section-removal/23-PATTERNS.md（削除後の2 branch の目標構造・sibling formatter パターン `formatRebalanceActionsHtml`）
    - .planning/phases/23-new-candidates-section-removal/23-RESEARCH.md（Pitfall 2: report-utils.ts の関数本体は削除しない / Pitfall 3: invest.md を触らない）
  </read_first>
  <action>
    `src/scripts/generate-portfolio-report.ts` から以下を削除する（D-01/D-02/D-04、`report-utils.ts` は一切触らない）:
    1. `function formatNewCandidatesHtml(result: MeetingResult): string { ... }` 関数本体を丸ごと削除（102-133行目、呼び出しのみ除去して dead code を残す案は不採用）。
    2. `const newCandidatesHtml = formatNewCandidatesHtml(result);`（143行目）を削除。
    3. フォールバック branch（`portfolioAnalysis === null`）テンプレート内の `${newCandidatesHtml}` 埋め込み行（161行目）を削除。跡地に代替コンテンツを一切追加しない（D-04 — 既存メッセージ「本日のポートフォリオ分析は生成されませんでした。」のみ）。
    4. 通常 branch テンプレート内の `${newCandidatesHtml}` 埋め込み行（186行目）を削除。周辺のセクション順（総括コメント → 保有銘柄 個別評価 → リバランス提案）は無変更。
    5. 1行目 import を `import { escapeHtml, generateBaseStyles, safeHref, formatPublishedAtJst } from "./report-utils.js";` に変更（`scoreColor` と `verdictColor` のみ除去。`escapeHtml`/`generateBaseStyles`/`safeHref`/`formatPublishedAtJst` は他の formatter が使用中のため残す）。

    `generatePortfolioReportHtml(result, portfolioAnalysis, resolvedHoldingNews)` のシグネチャは変更しない（D-03、`result.date` はタイトル/見出しで引き続き使用）。`report-utils.ts` の `scoreColor`/`verdictColor` 関数定義・`.claude/commands/invest.md`・`src/meeting/types.ts`・`src/meeting/schemas.ts` は一切変更しない（D-08/D-09）。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "formatNewCandidatesHtml" src/scripts/generate-portfolio-report.ts` が 0（関数定義・呼び出しとも消滅）
    - `grep -c "newCandidatesHtml" src/scripts/generate-portfolio-report.ts` が 0
    - 1行目が `import { escapeHtml, generateBaseStyles, safeHref, formatPublishedAtJst } from "./report-utils.js";`（`scoreColor`/`verdictColor` を含まない）
    - `npx vitest run src/scripts/generate-report.test.ts -t "Test 30"` および `-t "Test 31"` がともに PASS（GREEN）
    - `npm run test`（vitest run 全スイート）がグリーン — 特に Test 4（Daily Report highlightedStocks 表示, D-07）が引き続き PASS
    - `npx tsc --noEmit` がエラーなし（未使用 import 除去後もコンパイル成立）
    - 【regression guard, D-09】`grep -n "highlightedStocks 配列の全内容" .claude/commands/invest.md` が 1746行目付近で1件ヒット（受け渡し維持 = Success Criteria 2）
    - 【regression guard, D-09】`git diff --stat .claude/commands/invest.md src/meeting/types.ts src/meeting/schemas.ts src/scripts/report-utils.ts` の出力が空（これらは無変更）
  </acceptance_criteria>
  <done>`formatNewCandidatesHtml` とその呼び出し/埋め込み/未使用 import が完全削除され、Test 30/31 を含む全 vitest スイートがグリーン。invest.md:1746・types.ts・schemas.ts・report-utils.ts は無変更。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

このフェーズは新たな信頼境界を導入しない。純粋なビルド時のコード削除であり、新規のユーザー入力・エンドポイント・外部データ取り込み・認証/認可/セッション面は一切追加されない。むしろ `escapeHtml` を消費する描画パス（新規組入候補テーブル）を1つ削除する（攻撃対象面の縮小）。

| Boundary | Description |
|----------|-------------|
| （新規なし） | 本フェーズは信頼境界を追加しない。既存の `MeetingResult → HTML 文字列生成` 境界から描画コードを削除するのみ。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | `generate-portfolio-report.ts` 削除に伴う巻き込み事故（report-utils.ts / invest.md / types.ts / schemas.ts の誤変更） | mitigate | Task 2 acceptance_criteria の `git diff --stat` 空確認と `grep` 保持確認で構造的に検出（D-09）。編集スコープを2ファイルに限定し repo 全体 find-and-replace を禁止（RESEARCH Pitfall 2/3）。 |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | 本フェーズは外部パッケージのインストールを行わない（RESEARCH「no new dependencies」）。install タスク不在のため Package Legitimacy Gate 適用外。 |

**Threat surface が最小である理由:** 削除のみのフェーズ。新規入力なし・新規エンドポイントなし・新規外部データ取り込みなし・暗号/認証面なし。ASVS L1 の V2/V3/V4/V5/V6 いずれも新規に該当する攻撃対象面がない（RESEARCH.md Security Domain: not applicable）。主要リスクは「削除の巻き込み事故」であり、diff/grep の regression guard（T-23-01）で対処する。
</threat_model>

<verification>
フェーズ全体の検証（Success Criteria との 1:1 対応）:

**Success Criteria 1（両パスでセクション非存在）:**
- `npx vitest run src/scripts/generate-report.test.ts -t "Test 30"` → PASS（通常パス, 非空 highlightedStocks で非表示）
- `npx vitest run src/scripts/generate-report.test.ts -t "Test 31"` → PASS（フォールバックパス, 非表示）
- `grep -c "formatNewCandidatesHtml" src/scripts/generate-portfolio-report.ts` → 0（関数完全削除, D-01）

**Success Criteria 2（highlightedStocks 受け渡し維持 — 編集ではなく検証）:**
- `grep -n "highlightedStocks 配列の全内容" .claude/commands/invest.md` → 1746行目付近で1件（D-08/D-09）
- `git diff --stat .claude/commands/invest.md src/meeting/types.ts src/meeting/schemas.ts` → 空（無変更, D-09）

**回帰:**
- `npm run test`（全スイート）グリーン — Test 4（Daily Report, D-07）を含む
- `git diff --stat src/scripts/report-utils.ts` → 空（scoreColor/verdictColor 関数本体温存, D-02/Pitfall 2）
</verification>

<success_criteria>
- [ ] Test 30 が非存在検証へ反転され PASS（非空 validMeetingResult 入力で `not.toContain("新規組入候補")`）
- [ ] Test 31 にフォールバックパスの `not.toContain("新規組入候補")` が追加され PASS
- [ ] `formatNewCandidatesHtml` 関数本体・143行目呼び出し・161/186行目埋め込みが完全削除
- [ ] 1行目 import から `scoreColor`/`verdictColor` が除去（他4シンボルは温存）
- [ ] `npm run test` 全スイートグリーン（Test 4 含む）
- [ ] `npx tsc --noEmit` エラーなし
- [ ] invest.md:1746・types.ts・schemas.ts・report-utils.ts が無変更（git diff --stat 空）
</success_criteria>

<output>
Create `.planning/phases/23-new-candidates-section-removal/23-01-SUMMARY.md` when done
</output>
