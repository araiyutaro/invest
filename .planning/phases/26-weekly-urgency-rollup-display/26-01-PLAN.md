---
phase: 26-weekly-urgency-rollup-display
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/portfolio/urgency-rollup.ts
  - src/portfolio/urgency-rollup.test.ts
autonomous: true
requirements: [HIST-03]
nyquist_compliant: true

must_haves:
  truths:
    - "computeWeeklyUrgencyRollup filters history to the 7-calendar-day window [anchor-6, anchor] inclusive, tolerating missing days"
    - "Decision changes are detected only between adjacent RECORDED dates within the window (not calendar-adjacent)"
    - "Per-symbol urgent dates are collected; symbols with zero movement are excluded"
    - "Input history object/arrays are never mutated"
    - "Malformed / __proto__ date keys are rejected via isValidDateKey before use"
    - "formatDateKeyShort turns YYYY-MM-DD into zero-padded MM/DD without a Date object"
  artifacts:
    - path: "src/portfolio/urgency-rollup.ts"
      provides: "Pure aggregation: computeWeeklyUrgencyRollup, formatDateKeyShort, WeeklyUrgencyRollup/WeeklySymbolRollup/DecisionChangeEvent types"
      exports: ["computeWeeklyUrgencyRollup", "formatDateKeyShort", "WeeklyUrgencyRollup", "WeeklySymbolRollup", "DecisionChangeEvent"]
      min_lines: 60
    - path: "src/portfolio/urgency-rollup.test.ts"
      provides: "TDD coverage for all HIST-03 aggregation behaviors"
      contains: "computeWeeklyUrgencyRollup"
  key_links:
    - from: "src/portfolio/urgency-rollup.ts"
      to: "src/portfolio/urgency-history.ts"
      via: "import isValidDateKey + UrgencyHistoryFile/HoldingUrgencySnapshot types"
      pattern: "from \"./urgency-history.js\""
---

<objective>
Build the pure, throw-free aggregation module that is the primary automated-test surface for HIST-03. It filters `data/urgency-history.json` (already loaded elsewhere) to a 7-calendar-day window anchored on `meetingResult.date`, derives per-symbol urgent-occurrence dates and decision-change events, and exposes a trivial `MM/DD` date formatter.

Purpose: All aggregation logic lives here as a deterministic (no I/O, no LLM, no throw) function so it can be exhaustively unit-tested independent of rendering and file I/O (D-11).
Output: `src/portfolio/urgency-rollup.ts` + co-located `src/portfolio/urgency-rollup.test.ts`.

LOCKED DECISION (open item #1 resolved): `formatDateKeyShort(dateKey)` lives in THIS module as an exported, unit-tested pure function — NOT as a local helper in generate-portfolio-report.ts, and NOT `formatPublishedAtJst` (which is for full ISO datetimes, adds HH:MM, no zero-padding — RESEARCH Pitfall 3). Rationale: co-located testability of a pure string transform.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-weekly-urgency-rollup-display/26-CONTEXT.md
@.planning/phases/26-weekly-urgency-rollup-display/26-RESEARCH.md
@.planning/phases/26-weekly-urgency-rollup-display/26-PATTERNS.md

<interfaces>
<!-- Contracts from Phase 25, reused as-is. Extracted from src/portfolio/urgency-history.ts. -->
export interface HoldingUrgencySnapshot {
  readonly symbol: string;
  readonly nameJa: string;
  readonly urgent: boolean;
  readonly decision: "保持" | "買増" | "一部売却" | "全売却";
}
export type UrgencyHistoryFile = Record<string, ReadonlyArray<HoldingUrgencySnapshot>>;
export function isValidDateKey(dateKey: string): boolean; // /^\d{4}-\d{2}-\d{2}$/, already hardened vs "__proto__"

<!-- New contracts THIS plan creates (downstream Plan 02 imports these): -->
export interface DecisionChangeEvent { readonly date: string; readonly from: HoldingUrgencySnapshot["decision"]; readonly to: HoldingUrgencySnapshot["decision"]; }
export interface WeeklySymbolRollup { readonly symbol: string; readonly nameJa: string; readonly urgentDates: ReadonlyArray<string>; readonly decisionChanges: ReadonlyArray<DecisionChangeEvent>; }
export interface WeeklyUrgencyRollup { readonly windowStart: string; readonly windowEnd: string; readonly daysCovered: number; readonly symbols: ReadonlyArray<WeeklySymbolRollup>; }
export function computeWeeklyUrgencyRollup(history: UrgencyHistoryFile, anchorDate: string): WeeklyUrgencyRollup;
export function formatDateKeyShort(dateKey: string): string;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define result types and write the failing test suite (RED)</name>
  <files>src/portfolio/urgency-rollup.ts, src/portfolio/urgency-rollup.test.ts</files>
  <read_first>
    - src/portfolio/urgency-history.ts (types + isValidDateKey to import; do NOT reimplement isValidDateKey)
    - src/portfolio/urgency-history.test.ts (test file structure: `import { describe, it, expect } from "vitest"`, `makeSnapshot` Partial-override builder at L~137, immutability test at L134-141, __proto__ rejection at L157-159)
    - src/portfolio/decision-diff.ts (attachDecisionChanges L19-42 — the strict enum `!==` comparison philosophy D-02 mirrors onto adjacent recorded dates)
    - .planning/phases/26-weekly-urgency-rollup-display/26-RESEARCH.md (Pattern 1 full reference algorithm L166-259; Pitfalls 1-3)
    - .planning/phases/26-weekly-urgency-rollup-display/26-VALIDATION.md (Per-Task Verification Map — the 8 behaviors to cover)
  </read_first>
  <behavior>
    Test suite (one describe block per concern, each `it` title cites the decision ID in Japanese, per urgency-history.test.ts convention):
    - window filter (D-01): keys outside [anchor-6, anchor] excluded; anchor-inclusive; anchor-6 inclusive; anchor-7 excluded; windowStart/windowEnd computed via UTC math (assert exact YYYY-MM-DD boundary strings, TZ-independent)
    - missing days (D-01): a window with gaps (e.g. only 3 of 7 days present) aggregates only present days; daysCovered === number of present valid keys
    - decision changes (D-02): change detected between adjacent RECORDED dates even when calendar days are skipped (e.g. records on 07/01 保持 then 07/04 買増, no 07/02-03 → one change dated 07/04 from 保持 to 買増); first recorded entry is baseline, never itself a change; multiple changes across window produce multiple ascending events
    - urgent (D-03/D-05): urgentDates collects only dates where urgent===true, ascending; a symbol with urgent all-false AND no decision change is EXCLUDED from symbols[]; a symbol with only urgent (no decision change) IS included; a symbol with only decision change (no urgent) IS included
    - immutable: input history object and its arrays are deep-equal to a pre-call snapshot after computeWeeklyUrgencyRollup returns (mirror urgency-history.test.ts L134-141)
    - partial (<7 days): history of 3 days → no throw, daysCovered === 3, symbols aggregated from those 3
    - 0-movement: history present but no urgent and no decision change in window → symbols === [] (not error), daysCovered reflects present days
    - malformed keys: history containing "__proto__", "not-a-date", "" keys → those keys ignored (isValidDateKey filter), daysCovered counts only valid in-window keys, no throw
    - garbage anchor: computeWeeklyUrgencyRollup(history, "not-a-date") returns { windowStart:"not-a-date", windowEnd:"not-a-date", daysCovered:0, symbols:[] } without throwing
    - corrupt snapshot entry: a date whose value is not an array, or a snapshot element missing/typeof-non-string symbol, is skipped without throwing
    - deterministic order: symbols[] sorted by symbol.localeCompare ascending
    - formatDateKeyShort: "2026-07-02" → "07/02" (zero-padded, "/" separator); "2026-12-25" → "12/25"
    Use a `makeSnapshot(Partial<HoldingUrgencySnapshot>)` builder and a small helper to assemble multi-date `UrgencyHistoryFile` fixtures.
  </behavior>
  <action>
    In urgency-rollup.ts declare (implementation bodies as `throw new Error("not implemented")` stubs OR minimal skeleton so the file type-checks): `import { isValidDateKey } from "./urgency-history.js"` and `import type { HoldingUrgencySnapshot, UrgencyHistoryFile } from "./urgency-history.js"`. Export interfaces `DecisionChangeEvent { readonly date: string; readonly from: HoldingUrgencySnapshot["decision"]; readonly to: HoldingUrgencySnapshot["decision"] }`, `WeeklySymbolRollup { readonly symbol; readonly nameJa; readonly urgentDates: ReadonlyArray<string>; readonly decisionChanges: ReadonlyArray<DecisionChangeEvent> }`, `WeeklyUrgencyRollup { readonly windowStart; readonly windowEnd; readonly daysCovered: number; readonly symbols: ReadonlyArray<WeeklySymbolRollup> }`. Export function signatures `computeWeeklyUrgencyRollup(history: UrgencyHistoryFile, anchorDate: string): WeeklyUrgencyRollup` and `formatDateKeyShort(dateKey: string): string`. Then author urgency-rollup.test.ts covering every behavior above. Run the test file and confirm it FAILS (RED) — stubs unimplemented. Use `readonly` fields and `ReadonlyArray` throughout; never mutable arrays. Do NOT import normalizeHoldingSymbol (D-04: read side does not re-normalize).
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/urgency-rollup.test.ts ; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - src/portfolio/urgency-rollup.test.ts imports `{ describe, it, expect } from "vitest"` and at least the 12 behavior groups listed in <behavior> exist as `it(...)` cases
    - src/portfolio/urgency-rollup.ts exports the three interfaces and two function signatures with the exact names above
    - `npx vitest run src/portfolio/urgency-rollup.test.ts` exits NON-zero (RED) because implementations are stubs
    - No import of `normalizeHoldingSymbol` in urgency-rollup.ts (grep: `grep -c normalizeHoldingSymbol src/portfolio/urgency-rollup.ts` returns 0)
    - `grep -c "isValidDateKey" src/portfolio/urgency-rollup.ts` >= 1 (reused, not reimplemented)
  </acceptance_criteria>
  <done>Test file authored with full HIST-03 coverage; module exports contracts; suite is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement computeWeeklyUrgencyRollup + formatDateKeyShort (GREEN)</name>
  <files>src/portfolio/urgency-rollup.ts</files>
  <read_first>
    - src/portfolio/urgency-rollup.test.ts (the spec written in Task 1 — implement to satisfy it)
    - .planning/phases/26-weekly-urgency-rollup-display/26-RESEARCH.md (Pattern 1 reference algorithm L198-257: addDaysUtc via Date.parse(`${key}T00:00:00Z`) + toISOString().slice(0,10); lexicographic string window compare on zero-padded keys; Map timeline build; localeCompare sort)
    - src/portfolio/urgency-history.ts (doc-comment discipline: Japanese comment citing decision ID + no-throw/immutability guarantee, e.g. appendUrgencySnapshot L38-48)
  </read_first>
  <action>
    Implement per RESEARCH Pattern 1. `formatDateKeyShort`: `dateKey.slice(5).replace("-", "/")` (pure string, no Date). `computeWeeklyUrgencyRollup`: guard `if (!isValidDateKey(anchorDate))` → return empty rollup echoing anchor as both bounds, daysCovered 0. Compute windowStart via UTC ms math `Date.parse(anchor + "T00:00:00Z") - 6*DAY_MS` → `toISOString().slice(0,10)`; windowEnd = anchor. Build `matchedDates` = `Object.keys(history).filter(isValidDateKey).filter(d => d >= windowStart && d <= windowEnd).sort()` (lexicographic is correct for zero-padded ISO). Build a `Map<symbol, {nameJa, entries[]}>` iterating matchedDates in order; skip when `!Array.isArray(history[date])`; skip snapshot elements where `!s || typeof s.symbol !== "string"`; keep freshest nameJa. For each symbol: urgentDates = entries where urgent, mapped to date; decisionChanges = compare entries[i].decision !== entries[i-1].decision for i>=1, push {date: entries[i].date, from: entries[i-1].decision, to: entries[i].decision}. Include symbol only if urgentDates.length>0 || decisionChanges.length>0. Return with daysCovered=matchedDates.length and symbols sorted by symbol.localeCompare. Never `.push`/`.sort` on input arrays; never `for...in`. Add a Japanese doc-comment citing D-01/D-02/D-03/D-05 and the no-throw/immutability guarantee.
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/urgency-rollup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/portfolio/urgency-rollup.test.ts` exits 0 (all behavior cases GREEN)
    - `grep -c "for (const .* in " src/portfolio/urgency-rollup.ts` returns 0 (no for...in over parsed JSON)
    - `grep -c "formatPublishedAtJst" src/portfolio/urgency-rollup.ts` returns 0 (Pitfall 3 avoided)
    - Immutability test passes: input history unchanged after call
    - Garbage-anchor and __proto__-key tests pass without throwing
  </acceptance_criteria>
  <done>All Task 1 tests pass; aggregation is deterministic, immutable, throw-free, and prototype-pollution-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-committed file → aggregation module | `data/urgency-history.json` is git-tracked but could be hand-edited/corrupted; keys and snapshot shapes are untrusted on the read side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | computeWeeklyUrgencyRollup key iteration | mitigate | Filter `Object.keys(history)` through `isValidDateKey` before use (rejects `__proto__`/malformed keys — already unit-tested in urgency-history.test.ts L157-159); use `Object.keys`, never `for...in` |
| T-26-02 | Denial of Service | computeWeeklyUrgencyRollup on corrupt/partial data | mitigate | Defensive `Array.isArray` guard per date value and `typeof s.symbol === "string"` guard per element; garbage anchor returns empty rollup — function never throws (D-14 fail-soft upheld at aggregation layer) |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — this phase installs zero new packages (RESEARCH Package Legitimacy Audit: N/A); no supply-chain surface |
</threat_model>

<verification>
- `npx vitest run src/portfolio/urgency-rollup.test.ts` green
- Full suite still green: `npx vitest run`
</verification>

<success_criteria>
computeWeeklyUrgencyRollup + formatDateKeyShort implemented and fully covered; all D-01/D-02/D-03/D-05 aggregation semantics locked by passing tests; no mutation, no throw, __proto__-safe.
</success_criteria>

<output>
Create `.planning/phases/26-weekly-urgency-rollup-display/26-01-SUMMARY.md` when done
</output>
