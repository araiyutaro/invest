---
phase: 26-weekly-urgency-rollup-display
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - src/scripts/generate-portfolio-report.ts
  - src/scripts/generate-report.test.ts
autonomous: true
requirements: [HIST-03]
nyquist_compliant: true

must_haves:
  truths:
    - "portfolio-report.html contains a section headed 今週の緊急・判断変更ロールアップ"
    - "Per-symbol cards show ⚠ 緊急フラグ: MM/DD dates (color #ef4444) and one 判断変更: MM/DD from → to line per event (color #f59e0b)"
    - "The three empty/partial tiers render exactly the locked copy and never error"
    - "The rollup section renders in BOTH the non-null and the portfolioAnalysis===null branches (fail-soft)"
    - "symbol/nameJa/decision are escaped via escapeHtml; the 4th parameter defaults to {} (backward compatible)"
    - "Implements CONTEXT.md decisions D-05, D-06, D-07, D-08, D-09, D-10, D-12 (movement-only list, badge colors, MM/DD display, placement, 3-tier empty states, heading-always, backward-compat 4th param)"
  artifacts:
    - path: "src/scripts/generate-portfolio-report.ts"
      provides: "formatWeeklyUrgencyRollupHtml renderer + formatDateKeyShort usage + 4th param urgencyHistory + both-branch insertion"
      contains: "今週の緊急・判断変更ロールアップ"
    - path: "src/scripts/generate-report.test.ts"
      provides: "Backward-compat + rollup-render + null-branch + empty-state assertions"
      contains: "今週の緊急・判断変更ロールアップ"
  key_links:
    - from: "src/scripts/generate-portfolio-report.ts"
      to: "src/portfolio/urgency-rollup.ts"
      via: "import computeWeeklyUrgencyRollup + formatDateKeyShort"
      pattern: "from \"../portfolio/urgency-rollup.js\""
    - from: "src/scripts/generate-portfolio-report.ts"
      to: "src/scripts/report-utils.ts"
      via: "escapeHtml on every dynamic string"
      pattern: "escapeHtml\\("
---

<objective>
Render the weekly rollup section into `portfolio-report.html`. Add a bespoke `formatWeeklyUrgencyRollupHtml` renderer, a backward-compatible 4th parameter (`urgencyHistory: UrgencyHistoryFile = {}`) to `generatePortfolioReportHtml`, and insert the section immediately after `overallCommentHtml` and before `holdingEvaluationsHtml` (D-08) — AND into the null-analysis branch (fail-soft).

Purpose: Turn the pure rollup (Plan 01) into the user-visible section HIST-03 requires, reusing the report's existing card/color/escaping conventions.
Output: Updated `generate-portfolio-report.ts` + new tests in `generate-report.test.ts`.

LOCKED DECISION (open item #2 resolved): The rollup renders in BOTH the non-null and `portfolioAnalysis === null` branches. Compute `weeklyRollupHtml` once (its only dependencies are `urgencyHistory` + `result.date`, both available regardless of `portfolioAnalysis`) BEFORE the null check, and interpolate it into both templates. Rationale: D-14 fail-soft — on portfolio-analysis-failure days the rollup is exactly the signal the user should still see (RESEARCH Pitfall 4). Acceptance criterion below asserts the section appears when `portfolioAnalysis === null`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-weekly-urgency-rollup-display/26-UI-SPEC.md
@.planning/phases/26-weekly-urgency-rollup-display/26-PATTERNS.md
@.planning/phases/26-weekly-urgency-rollup-display/26-RESEARCH.md
@.planning/phases/26-weekly-urgency-rollup-display/26-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (src/portfolio/urgency-rollup.ts) — consume directly, do not re-explore: -->
export interface DecisionChangeEvent { readonly date: string; readonly from: "保持"|"買増"|"一部売却"|"全売却"; readonly to: "保持"|"買増"|"一部売却"|"全売却"; }
export interface WeeklySymbolRollup { readonly symbol: string; readonly nameJa: string; readonly urgentDates: ReadonlyArray<string>; readonly decisionChanges: ReadonlyArray<DecisionChangeEvent>; }
export interface WeeklyUrgencyRollup { readonly windowStart: string; readonly windowEnd: string; readonly daysCovered: number; readonly symbols: ReadonlyArray<WeeklySymbolRollup>; }
export function computeWeeklyUrgencyRollup(history: UrgencyHistoryFile, anchorDate: string): WeeklyUrgencyRollup;
export function formatDateKeyShort(dateKey: string): string; // "2026-07-02" -> "07/02"

<!-- Existing (src/scripts/report-utils.ts): -->
export function escapeHtml(text: string): string;

<!-- Existing signature to extend (generate-portfolio-report.ts L102-106): -->
generatePortfolioReportHtml(result: MeetingResult, portfolioAnalysis: PortfolioAnalysis | null, resolvedHoldingNews = {}): string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for the rollup section + 4th-arg backward compat (RED)</name>
  <files>src/scripts/generate-report.test.ts</files>
  <read_first>
    - src/scripts/generate-report.test.ts (Test 38 backward-compat L473-479; Test 40 badge-presence L481-490; Test 31 null-branch L370-380 — mirror these shapes; note dynamic `await import("./generate-portfolio-report.js")`)
    - .planning/phases/26-weekly-urgency-rollup-display/26-UI-SPEC.md (Copywriting Contract L84-96 — EXACT locked strings; Per-Symbol Row Layout L100-114; Full section render logic decision tree L116-140)
    - .planning/phases/26-weekly-urgency-rollup-display/26-01-SUMMARY.md (final WeeklyUrgencyRollup shape + how to build urgencyHistory fixtures)
  </read_first>
  <behavior>
    New tests (mirror existing Test 38/40/31 style; build fixtures as an `UrgencyHistoryFile` passed as the 4th arg; anchor via `validMeetingResult.date`):
    - backward compat: `generatePortfolioReportHtml(validMeetingResult, validPortfolioAnalysis)` (2 args) and 3-arg form still return HTML containing `<!DOCTYPE html>` — no throw
    - Tier 1 empty: 4th arg `{}` (or omitted) → HTML contains `今週の緊急・判断変更ロールアップ` AND `まだ緊急フラグ・判断変更の履歴がありません（履歴は日次で蓄積されます）`
    - Tier 2 empty: non-empty history but zero movement in window → contains heading AND `今週は緊急フラグ・判断変更はありませんでした`
    - Tier 3 partial footnote: history with daysCovered<7 and movement → contains `（過去{N}日分の履歴に基づく）` with correct N; and NOT present when daysCovered===7
    - per-symbol render: fixture producing urgent dates 07/02,07/04 and a change 07/03 保持→一部売却 → contains `⚠ 緊急フラグ: 07/02, 07/04`, `判断変更: 07/03 保持 → 一部売却`, `#ef4444`, `#f59e0b`
    - escaping: a symbol/nameJa containing `<script>` appears escaped (contains `&lt;script&gt;`, not raw `<script>`)
    - null-branch fail-soft: `generatePortfolioReportHtml(validMeetingResult, null, {}, historyFixtureWithMovement)` → HTML contains `今週の緊急・判断変更ロールアップ` AND the per-symbol movement text (rollup present even when portfolioAnalysis is null)
    - section ordering: in the non-null render, index of `今週の緊急・判断変更ロールアップ` is greater than index of the overall-comment section and less than index of `保有銘柄 個別評価`
  </behavior>
  <action>
    Append a new `describe` block (e.g. "Weekly urgency rollup (HIST-03)") to generate-report.test.ts using the existing `await import("./generate-portfolio-report.js")` pattern and the existing `validMeetingResult` / `validPortfolioAnalysis` fixtures. Construct `UrgencyHistoryFile` fixtures inline (Record of YYYY-MM-DD → snapshot arrays) anchored so dates fall inside `[validMeetingResult.date - 6, validMeetingResult.date]`. Assert the EXACT locked strings from UI-SPEC Copywriting Contract (verbatim, not paraphrased). Run and confirm RED.
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts -t "rollup" ; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - New tests assert the verbatim strings `今週の緊急・判断変更ロールアップ`, `まだ緊急フラグ・判断変更の履歴がありません（履歴は日次で蓄積されます）`, `今週は緊急フラグ・判断変更はありませんでした`, `⚠ 緊急フラグ: `, `判断変更: `, `#ef4444`, `#f59e0b`
    - A test passes `portfolioAnalysis = null` with a movement fixture and asserts the heading IS present
    - A test asserts section ordering (rollup between overall-comment and 保有銘柄 個別評価)
    - `npx vitest run src/scripts/generate-report.test.ts -t "rollup"` exits NON-zero (RED — renderer not yet implemented)
  </acceptance_criteria>
  <done>Full render test coverage authored against locked UI-SPEC copy; suite RED.</done>
</task>

<task type="auto">
  <name>Task 2: Implement formatWeeklyUrgencyRollupHtml + 4th param + both-branch insertion (GREEN)</name>
  <files>src/scripts/generate-portfolio-report.ts</files>
  <read_first>
    - src/scripts/generate-portfolio-report.ts (current signature L102-106; null branch L110-129; non-null composition L131-152; formatUrgentBadgeHtml/formatDecisionChangedBadgeHtml L48-65 for COLOR values only; formatHoldingEvaluationsHtml L67-92 for card-map/join pattern — but do NOT copy its early `return ""` empty behavior)
    - .planning/phases/26-weekly-urgency-rollup-display/26-UI-SPEC.md (Per-Symbol Row Layout locked markup L104-114; Full section render logic decision tree L116-140; exact inline styles: `color:#ef4444;font-size:0.9rem;font-weight:bold;margin:0.2rem 0;` for urgent line, `color:#f59e0b;...` for change line, `color:#888;font-size:0.85rem;` for empty/footnote, footnote `margin-top:0.4rem`)
    - src/scripts/report-utils.ts (escapeHtml L26-33 — apply to every dynamic string)
    - .planning/phases/26-weekly-urgency-rollup-display/26-01-SUMMARY.md (imports: computeWeeklyUrgencyRollup, formatDateKeyShort)
  </read_first>
  <action>
    Add imports `import { computeWeeklyUrgencyRollup } from "../portfolio/urgency-rollup.js"` (and `formatDateKeyShort` from the same module — reuse it, do NOT redefine and do NOT use formatPublishedAtJst) plus `import type { UrgencyHistoryFile } from "../portfolio/urgency-history.js"`. Add 4th param `urgencyHistory: UrgencyHistoryFile = {}` to generatePortfolioReportHtml (default {} = backward compatible, mirrors the 3rd-param convention). Write a new `formatWeeklyUrgencyRollupHtml(rollup: WeeklyUrgencyRollup, totalHistoryEntries: number): string` implementing the UI-SPEC decision tree EXACTLY: always emit `<h2>今週の緊急・判断変更ロールアップ</h2>`; Tier 1 when totalHistoryEntries===0 (the locked history message, guard so the 過去0日 footnote never shows); Tier 2 when rollup.symbols.length===0 (the locked zero-movement message + footnote only if daysCovered<7); else one `<div class="agent-card">` per symbol in rollup.symbols order with `<h4>${escapeHtml(symbol)}${nameJa ? " -- " + escapeHtml(nameJa) : ""}</h4>`, an urgent `<p>` only if urgentDates.length>0 (`⚠ 緊急フラグ: ` + urgentDates.map(formatDateKeyShort).join(", ")), one `<p>` per decisionChange (`判断変更: ` + formatDateKeyShort(date) + " " + escapeHtml(from) + " → " + escapeHtml(to)), plus a trailing plain `<p ...margin-top:0.4rem>（過去${daysCovered}日分の履歴に基づく）</p>` only if daysCovered<7. Use exact inline styles/colors from UI-SPEC. Do NOT re-sort symbols (already ascending from Plan 01). Compute `const weeklyRollupHtml = formatWeeklyUrgencyRollupHtml(computeWeeklyUrgencyRollup(urgencyHistory, result.date), Object.keys(urgencyHistory).length)` BEFORE the `if (portfolioAnalysis === null)` check. Interpolate `${weeklyRollupHtml}` into the null-branch template (e.g. after the "本日のポートフォリオ分析は生成されませんでした" card) AND into the non-null template between `${overallCommentHtml}` and `${holdingEvaluationsHtml}`. Add a Japanese doc-comment on the renderer citing D-06/D-08/D-09/D-10/D-14. No new CSS class — reuse `.agent-card`, `h2`, `h4`, `p`.
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/generate-report.test.ts` exits 0 (all Task 1 tests GREEN, existing Tests 31/38/40 still pass)
    - `grep -c "今週の緊急・判断変更ロールアップ" src/scripts/generate-portfolio-report.ts` >= 1
    - `grep -c "formatPublishedAtJst" src/scripts/generate-portfolio-report.ts` returns 0 (uses formatDateKeyShort from urgency-rollup instead)
    - weeklyRollupHtml is computed before the `portfolioAnalysis === null` return and interpolated into both templates (grep: `${weeklyRollupHtml}` appears twice)
    - escapeHtml applied to symbol, nameJa, and from/to decision strings in the renderer
    - `npx tsc --noEmit` (or project typecheck) passes with the new 4th param and imports
  </acceptance_criteria>
  <done>Section renders in both branches with locked copy/colors/escaping; 4th param backward compatible; all render tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregated rollup data → static HTML output | symbol/nameJa/decision strings originate from a repo-committed JSON file and are rendered into HTML served via GitHub Pages (a browser executes it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Tampering / Elevation of Privilege | formatWeeklyUrgencyRollupHtml string interpolation | mitigate | `escapeHtml` on every dynamic string (symbol, nameJa, from/to decision) before interpolation — same discipline as every other section in this file; MM/DD strings are derived from isValidDateKey-validated keys and are structurally HTML-safe |
| T-26-03 | Denial of Service | rollup render on null portfolioAnalysis / empty history | mitigate | Renderer never early-returns to a broken state: 3-tier empty/partial fallback always emits valid HTML; computed before the null check so a failed portfolio analysis still yields a valid page (D-14) |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — zero new packages in this phase |
</threat_model>

<verification>
- `npx vitest run src/scripts/generate-report.test.ts` green (incl. Tests 31/38/40)
- Full suite green: `npx vitest run`
</verification>

<success_criteria>
Rollup section renders with exact locked copy, badge colors, per-symbol cards, 3-tier empty/partial states, escaped dynamic strings, in both null and non-null branches; 4th param backward compatible.
</success_criteria>

<output>
Create `.planning/phases/26-weekly-urgency-rollup-display/26-02-SUMMARY.md` when done
</output>
