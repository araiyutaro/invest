---
phase: 31-daily-report-watchlist-section
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/scripts/generate-daily-report.ts
  - src/scripts/generate-report.test.ts
autonomous: true
requirements: [UI-09, UI-10]
must_haves:
  truths:
    - "judgments 1件以上でバッジ・判定理由・会社名・as-of注記・signalsピルがHTMLに含まれる（UI-09）"
    - "judgments 空でも見出し＋空メッセージで正常描画される（Success Criteria 3）"
    - "judgmentFile が null（欠損・破損・stale）ならセクション全体が非表示（空文字列）"
    - "actionChanged === true のみ変化バッジを描画し、方向で緑/アンバーを分ける（UI-10）"
    - "actionChanged が undefined / false のときは変化バッジを描画しない（Phase 22/30 の区別）"
    - "status: skipped の銘柄はグレー系の控えめ表示で描画される"
    - "会社名が解決できない銘柄はティッカーのみにフォールバックする"
    - "既存 generateDailyReportHtml の3引数・4引数呼び出しが後方互換で動作する（D-15）"
  artifacts:
    - "generate-daily-report.ts に formatWatchlistSectionHtml と補助関数群が追加される"
    - "generate-report.test.ts にセクション描画の網羅テストが追加される（D-16: 空・1件・複数件・skipped・変化バッジ4方向・日付不整合非表示・会社名フォールバックの必須ケース）"
  key_links:
    - "formatWatchlistSectionHtml が scoringSection と webSearchSection の間に挿入される（D-01）"
    - "generateDailyReportHtml の新引数がデフォルト値付き末尾追加で後方互換を維持する（D-15）"
    - "watchlist[normalizeHoldingSymbol(ticker)] の正規化 lookup が会社名 join を成立させる（Pitfall 5）"
---

<objective>
`generate-daily-report.ts` にウォッチリストセクションの描画純関数群（`formatWatchlistSectionHtml` トップレベル3状態ディスパッチャ + カード + バッジ補助関数）を追加し、`generateDailyReportHtml` のシグネチャに2つのデフォルト値付き optional 引数を末尾追加して `scoringSection` の直後にセクションを挿入する。TDD（RED→GREEN）で描画分岐の網羅テストを先に固める。

Purpose: Phase 30 が確定した WatchlistJudgment 型を、既存 generate-portfolio-report.ts の実証済みバッジ+カードパターンの複製で daily-report.html に描画する（UI-09/UI-10）。本フェーズは一切のビジネスロジックを持たない「消費して描画するだけ」の層。
Output: `formatWatchlistSectionHtml(judgmentFile, watchlist): string`、`formatWatchlistJudgmentCardHtml`、`formatTodayActionBadgeHtml`、`formatActionChangedBadgeHtml` の4関数と、拡張された `generateDailyReportHtml` シグネチャ、および描画分岐の網羅テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-daily-report-watchlist-section/31-CONTEXT.md
@.planning/phases/31-daily-report-watchlist-section/31-RESEARCH.md
@.planning/phases/31-daily-report-watchlist-section/31-PATTERNS.md
@.planning/phases/31-daily-report-watchlist-section/31-UI-SPEC.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: セクション描画分岐の失敗テストを先に書く（RED）</name>
  <files>src/scripts/generate-report.test.ts</files>
  <!-- D-16: セクション描画純関数を単体テストする（空・1件・複数件・skipped・変化バッジ4方向・日付不整合起因の非表示・会社名フォールバックの必須ケースを網羅） -->
  <!-- D-02: 専用純関数 formatWatchlistSectionHtml を対象にテストする（HTML テンプレート直接編集ではなく関数分離をテストで担保） -->
  <read_first>
    - src/scripts/generate-report.test.ts（`describe("Daily Report", ...)` 内の generateDailyReportHtml 呼び出しパターン、Test 35 の3引数後方互換テスト:228-231、`toContain` による HTML 断片アサーション、`await import("./generate-daily-report.js")` の動的 import、`validMeetingResult` fixture）
    - src/scripts/generate-daily-report.ts:227-286（generateDailyReportHtml の現行シグネチャと section 組立・return テンプレート）
    - src/meeting/types.ts:134-170（WatchlistJudgment の全フィールドと WatchlistJudgmentFile）
    - src/portfolio/watchlist.ts:32-44（WatchlistEntry / WatchlistFile = Record<string, WatchlistEntry>、キーは normalizeHoldingSymbol 済み前提）
    - .planning/phases/31-daily-report-watchlist-section/31-UI-SPEC.md（Copywriting Contract の逐語文言・Color の色コード）
  </read_first>
  <behavior>
    - 状態①（複数件）: judgments 2件（buy 1件 + wait 1件）を渡すと HTML に見出し `ウォッチリスト 買いタイミング判定`・buy バッジ文言 `今日買うべき`・wait ラベル文言 `待ち`・両銘柄の rationale・会社名（watchlist join 済み）が含まれる
    - 状態②（空・正常系）: 有効な WatchlistJudgmentFile で judgments が空配列のとき、見出し＋空メッセージ `現在ウォッチリスト銘柄はありません` が含まれ、カード div は含まれない
    - 状態③（null）: watchlistJudgment に null を渡すと、セクション見出し文言・空メッセージのいずれも HTML に含まれない（セクション完全非表示）
    - buy バッジ色: buy 銘柄で背景色 `#10b981` を含むピルが描画される
    - wait ラベル: wait 銘柄では `#10b981` のピルではなく `#9ca3af` の控えめラベルが描画される（buy と非対称の視覚強度）
    - as-of/market 注記（D-06）: market: "US" の銘柄で `前日終値時点`、market: "JP" の銘柄で `寄付き前時点` が asOf 値とともに含まれる
    - signals ピル（D-05）: signals 配列の各要素が `.ticker-pill` クラスの span として描画される
    - addedDate メタ（D-08）: watchlist エントリに addedDate があるとき `登録日:` プレフィックス付きで含まれる
    - skipped 表示（D-07）: status: "skipped" の銘柄は `判定不能（データ不足）` 文言とグレー系スタイル（border-left-color:#4b5563 / opacity:0.7）で描画され、buy/wait バッジは出さない
    - 変化バッジ 待ち→買い（UI-10）: actionChanged: true, previousAction: "wait", todayAction: "buy" で `シグナル点灯: 待ち → 買い` 文言かつ緑系 `#10b981` バッジ
    - 変化バッジ 買い→待ち（UI-10）: actionChanged: true, previousAction: "buy", todayAction: "wait" で `買い → 待ち` 文言かつアンバー `#f59e0b` バッジ
    - 変化バッジ非表示 undefined: actionChanged 未設定（undefined）で変化バッジ文言が一切含まれない
    - 変化バッジ非表示 false: actionChanged: false で変化バッジ文言が一切含まれない
    - 会社名フォールバック（D-04）: watchlist に該当 ticker のエントリが無い銘柄は見出しがティッカーのみ（em-dash 会社名なし）
    - 会社名 join 正規化（Pitfall 5）: judgment.ticker が大文字小文字混在でも watchlist の正規化キーと join され会社名が解決される
    - 後方互換（D-15/Pitfall 1）: generateDailyReportHtml を従来どおり3引数・4引数で呼んでも throw せず HTML を返す（新引数省略時セクションは非表示）
  </behavior>
  <action>
    `describe("Daily Report", ...)` 内、または新規 `describe("Watchlist section (UI-09/UI-10)", ...)` ブロックに上記 behavior を1ケース1 it で追加する。各テストは `const { generateDailyReportHtml } = await import("./generate-daily-report.js");` の動的 import を使い、`generateDailyReportHtml(validMeetingResult, [], [], marketDataDefault, watchlistJudgmentFixture, watchlistFixture)` の6引数呼び出しで HTML を得て `expect(html).toContain(...)` / `expect(html).not.toContain(...)` でアサートする。fixture は最小形の WatchlistJudgmentFile（date は validMeetingResult.date と一致させる — セクション描画関数自体は date 不整合を見ないが、渡す判定は当日分の想定）と WatchlistFile（`{ [normalizedTicker]: { ticker, nameJa, addedDate } }`）を各テストで直接構築する。色コード・逐語文言は 31-UI-SPEC.md の Color / Copywriting Contract の値を使う（`今日買うべき` / `待ち` / `シグナル点灯: 待ち → 買い` / `買い → 待ち` / `判定不能（データ不足）` / `前日終値時点` / `寄付き前時点` / `現在ウォッチリスト銘柄はありません` / `登録日:` / `ウォッチリスト 買いタイミング判定`）。後方互換テストは既存 Test 35 と同型で3引数・4引数呼び出しが throw しないことを確認する。この時点で実装が無いため RED（型エラーまたはアサーション失敗）であることを確認する。

    注意: negative-grep 用の変化バッジ非表示テストでは、`html.not.toContain("シグナル点灯")` のように変化バッジ固有文言の不在を確認する。テスト内のコメントや文字列に、実装側ソースが negative-grep する予定のリテラルを埋め込まない（テスト文字列は HTML 出力のアサーション用であり実装ソースには影響しない）。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts 2>&1 | grep -qiE "fail|error|is not a function|expected" && echo "RED confirmed" || (echo "EXPECTED RED but passed — impl may already exist" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - generate-report.test.ts に3状態（複数件/空/null）・buy/wait バッジ・as-of・signals・addedDate・skipped・変化バッジ4方向（待ち→買い/買い→待ち/undefined非表示/false非表示）・会社名フォールバック・正規化 join・後方互換 の各テストが存在する
    - 変化バッジ非表示テストで `actionChanged: undefined` のケースと `actionChanged: false` のケースを個別に検証している（Pitfall 2 の区別）
    - この時点で `npx vitest run src/scripts/generate-report.test.ts` は RED
  </acceptance_criteria>
  <done>ウォッチリストセクション描画の全分岐テストが generate-report.test.ts に追加され、実装前のため RED になっている。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: formatWatchlistSectionHtml 群を実装しセクションを挿入して GREEN にする</name>
  <files>src/scripts/generate-daily-report.ts</files>
  <read_first>
    - src/scripts/generate-daily-report.ts（import 群:1-4、formatHighlightedStocksHtml:52-102 のカード規律、generateDailyReportHtml:227-286 のシグネチャ・section 定数・return テンプレート:273-275 挿入点）
    - src/scripts/generate-portfolio-report.ts:52-101（formatUrgentBadgeHtml/formatDecisionChangedBadgeHtml の逐語 CSS 値、formatHoldingEvaluationsHtml のカード構造 `.agent-card`+h4+p）
    - src/scripts/generate-portfolio-report.ts:116-158（formatWeeklyUrgencyRollupHtml の3状態フォールバック規律）
    - src/scripts/report-utils.ts:26（escapeHtml）、:178-204（.agent-card / .agent-card h4 / .ticker-pill CSS）
    - src/portfolio/holding-news.ts:32（normalizeHoldingSymbol export）
    - src/meeting/types.ts:134-170（WatchlistJudgment / WatchlistJudgmentFile）
    - src/portfolio/watchlist.ts:32-44（WatchlistEntry / WatchlistFile）
    - .planning/phases/31-daily-report-watchlist-section/31-UI-SPEC.md（Component Inventory の各関数シグネチャと Exact CSS values、Color、Copywriting Contract）
  </read_first>
  <action>
    import 追加: `import type { WatchlistJudgment, WatchlistJudgmentFile } from "../meeting/types.js";`、`import type { WatchlistEntry, WatchlistFile } from "../portfolio/watchlist.js";`、`import { normalizeHoldingSymbol } from "../portfolio/holding-news.js";`。

    セクションは専用の純関数 `formatWatchlistSectionHtml`（既存 `formatHighlightedStocksHtml` 等と同じ「データを受け取り HTML 文字列を返す」規律）として実装し、generate-report.ts の HTML テンプレート直接編集は行わない（D-02）。以下4つのモジュールプライベート/export 関数を追加する（バッジ関数は generate-portfolio-report.ts の非 export 関数を import できないため同型を複製する — Pitfall 4）:

    `formatTodayActionBadgeHtml(todayAction: "buy" | "wait"): string`（D-09）: buy のとき `<span>` ピルで背景 `#10b981`・文字色 `#0f0f1a`・font-size `0.8rem`・font-weight bold・padding `0.2rem 0.6rem`・margin-left `0.5rem`・border-radius `999px`、文言 `今日買うべき`。wait のときはピルではなく控えめインラインテキスト `<span style="color:#9ca3af;font-size:0.8rem;margin-left:0.5rem;">` で文言 `待ち`（背景・border-radius なし＝バッジにしない、buy との非対称強度）。

    `formatActionChangedBadgeHtml(actionChanged: boolean | undefined, previousAction: "buy" | "wait" | undefined, todayAction: "buy" | "wait"): string`（D-10/D-11/Pitfall 2）: 冒頭で `if (actionChanged !== true) return "";`（truthy チェック禁止、undefined と false を区別）。新規シグナル判定 `const isNewSignal = previousAction === "wait" && todayAction === "buy";`。isNewSignal のとき背景 `#10b981`・文字色 `#0f0f1a`・文言 `シグナル点灯: 待ち → 買い`、それ以外は背景 `#f59e0b`・文字色 `#1a1a28`・文言 `買い → 待ち`。共通スタイル: display inline-block・font-size `0.75rem`・font-weight bold・padding `0.15rem 0.5rem`・margin-left `0.5rem`・border-radius `999px`。

    `formatWatchlistJudgmentCardHtml(judgment: WatchlistJudgment, entry: WatchlistEntry | undefined): string`（D-03〜D-08）: 見出しは `const companyName = entry?.nameJa ?? entry?.name;` で解決し、companyName があれば `${escapeHtml(judgment.ticker)} — ${escapeHtml(companyName)}`（em-dash セパレータ）、なければ `escapeHtml(judgment.ticker)` のみ（D-04 フォールバック）。status === "skipped" のとき（D-07）は早期に `.agent-card` に `style="border-left-color:#4b5563;opacity:0.7;"` を付け、h4 に heading + `<span style="color:#6b7280;font-size:0.8rem;">判定不能（データ不足）</span>` のみを描画して return（buy/wait バッジ・変化バッジ・signals は出さない）。通常銘柄は h4 に heading + formatTodayActionBadgeHtml + formatActionChangedBadgeHtml + as-of注記を並べ、`<p>${escapeHtml(judgment.rationale)}</p>` を続け、signals（D-05）を `.ticker-pill` span の列挙（`judgment.signals.map((s) => \`<span class="ticker-pill">${escapeHtml(s)}</span>\`)`、0件なら空）で描画し、addedDate（D-08）があれば `<p style="color:#666;font-size:0.75rem;margin-top:0.3rem;">登録日: ${escapeHtml(entry.addedDate)}</p>` を出す。as-of注記（D-06）は `const sessionLabel = judgment.market === "JP" ? "寄付き前時点" : "前日終値時点";` で決め、judgment.asOf があるとき `<span style="color:#888;font-size:0.75rem;margin-left:0.5rem;">(${escapeHtml(judgment.asOf)} ${sessionLabel})</span>`。全動的文字列（ticker/companyName/rationale/signals/asOf/addedDate）に escapeHtml を適用する（XSS defense-in-depth）。

    `formatWatchlistSectionHtml(judgmentFile: WatchlistJudgmentFile | null, watchlist: WatchlistFile): string`（D-14 3状態）: `if (judgmentFile === null) return "";`（状態③、セクション完全非表示）。見出し定数 `const heading = ` で `<hr>` + `<h2>ウォッチリスト 買いタイミング判定</h2>`。`if (judgmentFile.judgments.length === 0)` は heading + `<p style="color: #888; font-size: 0.9rem;">現在ウォッチリスト銘柄はありません</p>` を return（状態②）。それ以外は各 judgment に対し `watchlist[normalizeHoldingSymbol(j.ticker)]` で entry を join（Pitfall 5、正規化必須）して formatWatchlistJudgmentCardHtml でカード化し、heading + サブヘッド `<p style="color: #888; font-size: 0.9rem; margin-bottom: 1rem;">ウォッチリスト銘柄の本日の買いタイミング判定です。</p>` + カード群 を return（状態①）。

    generateDailyReportHtml のシグネチャに末尾 optional 引数を2つ追加（D-15）: `watchlistJudgment: WatchlistJudgmentFile | null = null,` と `watchlist: WatchlistFile = {},`（既存 marketData のデフォルト値付き引数と同じ規律、後方互換維持）。本体で `const watchlistSection = formatWatchlistSectionHtml(watchlistJudgment, watchlist);` を scoringSection と webSearchSection の定義の間に追加し、return テンプレートリテラルの `${scoringSection}` と `${webSearchSection}` の間に `${watchlistSection}` を挿入する（D-01）。既存の generateBaseStyles("#3b82f6") 呼び出しは変更しない（アクセント確定済み、二重呼び出し禁止）。`.agent-card` / `.ticker-pill` は既存 CSS を再利用し新規 CSS ブロックは追加しない。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/generate-report.test.ts` が GREEN（新規セクションテスト＋既存 Test 35 後方互換テストの両方パス）
    - generate-daily-report.ts に `formatWatchlistSectionHtml` / `formatWatchlistJudgmentCardHtml` / `formatTodayActionBadgeHtml` / `formatActionChangedBadgeHtml` の4関数が存在する
    - formatActionChangedBadgeHtml が `if (actionChanged !== true) return "";` を含む（truthy チェックではない、Pitfall 2）: `grep -c 'actionChanged !== true' src/scripts/generate-daily-report.ts` が1以上
    - 会社名 join で `normalizeHoldingSymbol` を使用している（Pitfall 5）: `grep -c 'normalizeHoldingSymbol' src/scripts/generate-daily-report.ts` が1以上
    - generateDailyReportHtml のシグネチャに `watchlistJudgment` と `watchlist` の2つのデフォルト値付き引数が末尾追加されている（D-15）
    - return テンプレートで `${watchlistSection}` が `${scoringSection}` の後・`${webSearchSection}` の前に出現する（D-01 挿入順序）
    - buy バッジ・wait ラベル・変化バッジの色コード（`#10b981` / `#9ca3af` / `#f59e0b`）が 31-UI-SPEC.md の Color 表と一致する
  </acceptance_criteria>
  <done>セクション描画4関数が実装され、セクションが scoringSection 直後に挿入され、generate-report.test.ts が全て GREEN（後方互換含む）。</done>
</task>

</tasks>

<artifacts_this_phase_produces>
このプランが新規作成するシンボル（drift 検証の対象外・新規作成として扱う）:

- `formatWatchlistSectionHtml` — `src/scripts/generate-daily-report.ts` の新規関数。`(judgmentFile: WatchlistJudgmentFile | null, watchlist: WatchlistFile): string`
- `formatWatchlistJudgmentCardHtml` — 同ファイル新規関数。`(judgment: WatchlistJudgment, entry: WatchlistEntry | undefined): string`
- `formatTodayActionBadgeHtml` — 同ファイル新規関数。`(todayAction: "buy" | "wait"): string`
- `formatActionChangedBadgeHtml` — 同ファイル新規関数。`(actionChanged: boolean | undefined, previousAction: "buy" | "wait" | undefined, todayAction: "buy" | "wait"): string`
- `generateDailyReportHtml` の拡張シグネチャ — 末尾に `watchlistJudgment: WatchlistJudgmentFile | null = null, watchlist: WatchlistFile = {}` の2引数を追加（既存 export の後方互換拡張）

参照する既存シンボル（本プランでは作成しない）: `escapeHtml`, `generateBaseStyles`, `.agent-card` / `.ticker-pill` CSS（report-utils.ts）, `normalizeHoldingSymbol`（holding-news.ts）, `WatchlistJudgment` / `WatchlistJudgmentFile`（types.ts）, `WatchlistEntry` / `WatchlistFile`（watchlist.ts）, `formatHighlightedStocksHtml`（同ファイル既存）。
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 判定データ（LLM 由来 rationale/signals）→ HTML 出力 | rationale と signals は Phase 30 の LLM 生成テキスト。GitHub Pages に公開される静的 HTML に埋め込まれる信頼境界 |
| watchlist データ（ファイル由来 ticker/会社名）→ HTML 出力 | ticker/nameJa/name/addedDate も HTML に埋め込まれる |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 | Tampering | formatWatchlistJudgmentCardHtml（LLM生成 rationale/signals への任意HTML混入 XSS） | high | mitigate | 全動的文字列（ticker/companyName/rationale/signals[]/asOf/addedDate）に report-utils.ts の escapeHtml を適用。既存 formatHoldingEvaluationsHtml と同一規律（v2.6 WR-03 の defense-in-depth 方針）。テストで escapeHtml 適用を間接検証 |
| T-31-05 | Information Disclosure | formatActionChangedBadgeHtml（undefined/false 区別崩壊による誤バッジ表示） | medium | mitigate | `actionChanged !== true` 早期 return で undefined（比較不能）と false（変化なし）を非表示に統一しつつ、truthy チェックを禁止。テストで undefined/false 両ケースを個別検証（Pitfall 2） |
| T-31-06 | Tampering | formatWatchlistSectionHtml（正規化漏れによる会社名 join 失敗） | low | accept | normalizeHoldingSymbol 適用で join を成立させる。失敗してもティッカーのみ表示にフォールバックし機能は壊れない（UX 劣化のみ、Pitfall 5） |
| T-31-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本フェーズは新規パッケージを一切導入しない（RESEARCH §Installation）。install タスク不在のため対象なし |
</threat_model>

<verification>
- `npx vitest run src/scripts/generate-report.test.ts` が GREEN（新規セクションテスト＋既存後方互換テスト）
- 3状態（複数件/空/null）・skipped・変化バッジ4方向・会社名フォールバック・正規化 join が全てテストで検証されている
- ワーブ完了時: `npm run test`（フルスイート）が GREEN で既存テストに回帰がない
</verification>

<success_criteria>
- judgments 1件以上で各銘柄にバッジ・判定理由・会社名・as-of注記・signals が描画される（UI-09）
- 前日からの判定変化が actionChanged === true のときのみ方向別の色で区別表示される（UI-10）
- 空・null 状態でも正常描画（fail-soft、既存レポート生成への影響なし、Success Criteria 3）
- generateDailyReportHtml の既存呼び出しが後方互換で動作する（D-15）
</success_criteria>

<output>
Create `.planning/phases/31-daily-report-watchlist-section/31-02-SUMMARY.md` when done
</output>
