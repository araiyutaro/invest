---
phase: 31-daily-report-watchlist-section
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - src/scripts/generate-report.ts
autonomous: false
requirements: [UI-09, UI-10, OPS-06]
must_haves:
  truths:
    - "generate-report.ts の main() が loadWatchlistJudgment(meetingResult.date) と loadWatchlist() を Promise.all で並列実行する"
    - "generateDailyReportHtml 呼び出しが判定データとウォッチリストを渡すよう拡張される"
    - "ローダー失敗（null/{}）が他レポート生成・デプロイを止めない（fail-soft、OPS-06）"
    - "生成された daily-report.html にウォッチリストセクションが実描画される"
  artifacts:
    - "generate-report.ts の import に loadWatchlistJudgment / loadWatchlist が追加される"
    - "generate-report.ts の Promise.all に2ローダー呼び出しが追加される"
  key_links:
    - "loadWatchlistJudgment に meetingResult.date を渡すことで D-13 stale ガードが有効化される"
    - "generateDailyReportHtml の6引数呼び出しが Plan 02 のセクション描画を起動する"
---

<objective>
`generate-report.ts` の main() の `Promise.all` ローダー群に Plan 01 の2ローダーを追加し、`generateDailyReportHtml` 呼び出しに判定データとウォッチリストを渡すよう配線して、ウォッチリストセクションを Daily Report に実描画する。invest.md への新ステップ追加は不要（Step 3-J は Phase 30 で Step 3c より前に配置済み）。

Purpose: Plan 01 の fail-soft ローダーと Plan 02 のセクション描画関数を統合し、実パイプラインで daily-report.html にウォッチリストセクションが出力されるようにする。ローダー失敗が他3+1レポート生成を止めない fail-soft 配線を維持する（OPS-06、D-15）。
Output: 拡張された generate-report.ts の import・Promise.all・generateDailyReportHtml 呼び出し、および生成 HTML の live 目視確認（human-verify）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-daily-report-watchlist-section/31-CONTEXT.md
@.planning/phases/31-daily-report-watchlist-section/31-RESEARCH.md
@.planning/phases/31-daily-report-watchlist-section/31-PATTERNS.md
@.planning/phases/31-daily-report-watchlist-section/31-01-SUMMARY.md
@.planning/phases/31-daily-report-watchlist-section/31-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate-report.ts の Promise.all と generateDailyReportHtml 呼び出しを配線する</name>
  <files>src/scripts/generate-report.ts</files>
  <read_first>
    - src/scripts/generate-report.ts（import:6/9、generateHtml ラッパー:84-91、main() の meetingResult parse:119-120、Promise.all ローダー群:122-134、generateDailyReportHtml 呼び出し:151）
    - src/scripts/report-data-loaders.ts（Plan 01 で追加された loadWatchlistJudgment / loadWatchlist の正確なシグネチャ）
    - src/scripts/generate-daily-report.ts（Plan 02 で拡張された generateDailyReportHtml の6引数シグネチャ）
    - .planning/phases/31-daily-report-watchlist-section/31-01-SUMMARY.md（Plan 01 の実装結果）
    - .planning/phases/31-daily-report-watchlist-section/31-02-SUMMARY.md（Plan 02 の実装結果）
  </read_first>
  <action>
    line 9 の `report-data-loaders.js` からの import リストに `loadWatchlistJudgment, loadWatchlist` を追加する。

    main() の `Promise.all`（122-134）の配列末尾に `loadWatchlistJudgment(meetingResult.date)` と `loadWatchlist()` の2呼び出しを追加し、分割代入の左辺（`const [webSearchResults, ..., urgencyHistory]`）末尾に対応する変数 `watchlistJudgment` と `watchlist` を追加する。`meetingResult` は既に119-120で parse 済みのため `meetingResult.date` を loadWatchlistJudgment に渡せる（D-13 stale ガードの入力）。

    line 151 の `generateDailyReportHtml(meetingResult, webSearchResults, reevalResults, marketData)` 呼び出しに末尾2引数を追加して `generateDailyReportHtml(meetingResult, webSearchResults, reevalResults, marketData, watchlistJudgment, watchlist)` にする。

    generateHtml ラッパー（84-91）は変更しない — Plan 02 の新引数はデフォルト値付き optional であり、既存の4引数呼び出しは後方互換で動作するため（Pitfall 1）。invest.md も変更しない（Step 3-J → Step 3c の順序は Phase 30 で確立済み、本フェーズは描画層のみ）。

    ローダーは Plan 01 で throw-free に実装済みのため、追加配線が既存の fail-soft 特性を壊さない（判定欠損時 null、watchlist 欠損時 {} が渡り、セクションは非表示または会社名フォールバックになる — OPS-06）。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/generate-report.test.ts src/scripts/report-data-loaders.test.ts && grep -c 'loadWatchlistJudgment(meetingResult.date)' src/scripts/generate-report.ts</automated>
  </verify>
  <acceptance_criteria>
    - generate-report.ts の import に `loadWatchlistJudgment` と `loadWatchlist` が含まれる（`grep -c` で各1以上）
    - Promise.all に `loadWatchlistJudgment(meetingResult.date)` と `loadWatchlist()` が存在する
    - line 151 相当の generateDailyReportHtml 呼び出しが `watchlistJudgment` と `watchlist` を末尾引数に渡す（6引数呼び出し）
    - generateHtml ラッパー（84-91）は無変更（後方互換維持）
    - `npx vitest run src/scripts/generate-report.test.ts src/scripts/report-data-loaders.test.ts` が GREEN（既存＋新規テスト全パス）
    - `npm run test`（フルスイート）が GREEN で回帰なし
  </acceptance_criteria>
  <done>generate-report.ts が2ローダーを Promise.all で並列読込し generateDailyReportHtml に判定データを渡す。全テスト GREEN。ローダー失敗は他レポート生成を止めない。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: daily-report.html のウォッチリストセクション実描画を目視確認する</name>
  <action>これは human-verify チェックポイントである。Task 1 完了後、以下の <how-to-verify> 手順に従って生成された daily-report.html をブラウザで開き、ウォッチリストセクションの実描画（バッジ色・as-of注記・変化バッジ・skipped表示・挿入順序・他レポートの継続生成）を目視確認する。live 実行検証が保留の場合は 31-HUMAN-UAT.md に記録して続行する（Phase 21/22/24/29/30 前例踏襲）。</action>
  <what-built>
    Daily Report（daily-report.html）へのウォッチリストセクションの実描画配線。Plan 01 の fail-soft ローダー2件 + Plan 02 のセクション描画4関数 + 本プランのオーケストレーター配線が統合され、tmp/watchlist-judgment.json + data/watchlist.json を入力に daily-report.html にセクションが出力される。バッジ色・as-of注記・変化バッジ・skipped 表示・空/null 状態は単体テストで検証済みだが、ブラウザでの実描画（色味・レイアウト・バッジの視認性）は静的解析では確認できない。
  </what-built>
  <how-to-verify>
    1. パイプラインを実行して当日レポートを生成する（`/invest` の Step 3c までを実行、または直近の docs/{date}/daily-report.html を利用）。tmp/watchlist-judgment.json に judgments が存在する日のレポートを対象にする。
    2. 生成された `docs/{date}/daily-report.html` をブラウザで開く。
    3. 「注目銘柄スコアリングマトリクス」セクションの直後・「WebSearch リサーチ結果」の前に「ウォッチリスト 買いタイミング判定」セクションが表示されることを確認する（D-01 挿入順序）。
    4. buy 銘柄に緑（#10b981）の「今日買うべき」バッジ、wait 銘柄に控えめなグレー（#9ca3af）の「待ち」ラベルが表示され、buy バッジだけが目に飛び込むこと（強度の非対称、D-09）を確認する。
    5. 各銘柄に as-of 注記（US=「前日終値時点」/ JP=「寄付き前時点」）・判定理由の段落・signals ピル・登録日メタが表示されることを確認する（D-05/D-06/D-08）。
    6. 前日から変化した銘柄があれば、待ち→買いは緑「シグナル点灯: 待ち → 買い」、買い→待ちはアンバー（#f59e0b）「買い → 待ち」バッジが表示されることを確認する（UI-10）。
    7. skipped 銘柄があればグレー系（opacity 0.7）の「判定不能（データ不足）」表示になっていることを確認する（D-07）。
    8. 会社名が「ティッカー — 会社名」形式で表示され、watchlist に無い銘柄はティッカーのみになることを確認する（D-04）。
    9. 他の3+1レポート（meeting-minutes / portfolio-report / news-digest）が従来どおり生成・表示されていることを確認する（fail-soft、OPS-06）。
  </how-to-verify>
  <resume-signal>「approved」と入力するか、視認上の問題（色・レイアウト・文言・順序）を報告してください。live 実行検証が保留の場合は 31-HUMAN-UAT.md に記録して続行します（Phase 21/22/24/29/30 前例踏襲）。</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
このプランが変更する既存シンボル（新規シンボル作成なし — 配線のみ）:

- `generate-report.ts` の `main()` — Promise.all に2ローダー呼び出しを追加し generateDailyReportHtml 呼び出しを6引数に拡張（既存関数の内部配線変更）
- `generate-report.ts` の import 文 — `loadWatchlistJudgment` / `loadWatchlist` を report-data-loaders.js から追加 import

参照する既存/前プラン作成シンボル: `loadWatchlistJudgment` / `loadWatchlist`（Plan 01）, `generateDailyReportHtml` の拡張シグネチャ（Plan 02）, `meetingResult`（同ファイル既存）。
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ローダー出力 → generateDailyReportHtml | Plan 01 の fail-soft ローダーが返す null/{} を含む値が描画関数に渡る。配線ミスで fail-soft 特性が壊れると全レポート生成が停止するリスク境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-07 | Denial of Service | Promise.all 配線（1ローダーの reject で全レポート生成停止） | high | mitigate | Plan 01 のローダーは throw-free（catch で null/{} 返却）のため Promise.all が reject しない。配線後も既存テストで他レポート生成継続を検証（OPS-06、D-15） |
| T-31-08 | Tampering | loadWatchlistJudgment への date 誤伝播（stale ガード無効化） | medium | mitigate | main() の meetingResult.date（既に parse 済み line 119-120）を明示的に渡す。誤って別の date や undefined を渡さないことをコードレビューと grep で確認 |
| T-31-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本フェーズは新規パッケージを一切導入しない（RESEARCH §Installation）。install タスク不在のため対象なし |
</threat_model>

<verification>
- `npx vitest run src/scripts/generate-report.test.ts src/scripts/report-data-loaders.test.ts` が GREEN
- `npm run test`（フルスイート）が GREEN で既存テストに回帰がない
- Promise.all に loadWatchlistJudgment(meetingResult.date) と loadWatchlist() が配線されている
- human-verify: 生成 daily-report.html にウォッチリストセクションが scoringSection 直後に描画され、他レポート生成が継続している
</verification>

<success_criteria>
- generate-report.ts が判定データとウォッチリストを描画層に渡し、daily-report.html にセクションが出力される（UI-09/UI-10）
- ローダー失敗が他レポート生成・デプロイを止めない（fail-soft、OPS-06/D-15）
- invest.md / generateHtml ラッパーは無変更で後方互換が保たれる
</success_criteria>

<output>
Create `.planning/phases/31-daily-report-watchlist-section/31-03-SUMMARY.md` when done
</output>
