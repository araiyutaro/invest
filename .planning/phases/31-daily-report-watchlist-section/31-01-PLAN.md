---
phase: 31-daily-report-watchlist-section
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/scripts/report-data-loaders.ts
  - src/scripts/report-data-loaders.test.ts
autonomous: true
requirements: [UI-09]
must_haves:
  truths:
    - "loadWatchlistJudgment は当日 date のファイルのみ返し、前日残留ファイルは null を返す（D-12/D-13）"
    - "loadWatchlist は data/watchlist.json を throw-free で読み、欠損・破損時は {} を返す（D-12）"
    - "両ローダーは欠損・破損・形状不整合で throw せずフォールバック値を返す（D-12: loadUrgencyHistory / loadPortfolioAnalysis の ENOENT 二重チェック様式踏襲）"
  artifacts:
    - "src/scripts/report-data-loaders.ts に loadWatchlistJudgment / loadWatchlist が export される"
    - "src/scripts/report-data-loaders.test.ts に両ローダーの分岐網羅テストが存在する"
  key_links:
    - "loadWatchlistJudgment の date 不整合ガード（file.date !== meetingResultDate → null）が D-13 の stale 防止を担う"
    - "loadWatchlist の {} フォールバックが会社名 join のフェイルソフト起点になる"
---

<objective>
`report-data-loaders.ts` に throw-free の fail-soft ローダー2件（`loadWatchlistJudgment` / `loadWatchlist`）を新設し、TDD（RED→GREEN）で単体テストを先に固める。判定ファイルの日付不整合ガード（D-13）を含む。

Purpose: Phase 31 の描画層（Plan 02）とオーケストレーター（Plan 03）が消費する fail-soft データ入力を、既存 `loadUrgencyHistory` 様式の踏襲で提供する。前日残留ファイルの誤表示（TIME-05 のルックアヘッド防止思想の表示側適用）を構造的に防ぐ。
Output: `loadWatchlistJudgment(meetingResultDate: string): Promise<WatchlistJudgmentFile | null>` と `loadWatchlist(): Promise<WatchlistFile>` の2 export、および両者の分岐網羅テスト。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-daily-report-watchlist-section/31-CONTEXT.md
@.planning/phases/31-daily-report-watchlist-section/31-RESEARCH.md
@.planning/phases/31-daily-report-watchlist-section/31-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: loadWatchlistJudgment / loadWatchlist の失敗テストを先に書く（RED）</name>
  <files>src/scripts/report-data-loaders.test.ts</files>
  <read_first>
    - src/scripts/report-data-loaders.test.ts（既存モック規約 `vi.mock("node:fs/promises", ...)`、`describe("loadUrgencyHistory", ...)` の CR-02 root-shape テスト、`vi.mocked(readFile).mockResolvedValueOnce` / `mockRejectedValueOnce` の per-case パターン、`vi.spyOn(console, "warn")` の使い方）
    - src/scripts/report-data-loaders.ts（既存 loadUrgencyHistory:143-159 と loadHoldingNews:127-135、TMP_DIR/DATA_DIR 定数、import 群）
    - src/meeting/types.ts:134-170（WatchlistJudgment / WatchlistJudgmentFile の正確なフィールド）
    - src/portfolio/watchlist.ts:32-44（WatchlistEntry / WatchlistFile = Record<string, WatchlistEntry>）
  </read_first>
  <behavior>
    - loadWatchlistJudgment 正常系: file.date === meetingResultDate かつ judgments が配列なら、その WatchlistJudgmentFile を返す
    - loadWatchlistJudgment stale 系（D-13）: file.date が渡した meetingResultDate と異なる場合は null を返し console.warn が1回呼ばれる（新規ケース、既存 analog なし）
    - loadWatchlistJudgment ENOENT 系: readFile が `new Error("ENOENT")` で reject されたら throw せず null を返し console.warn が呼ばれる
    - loadWatchlistJudgment パース失敗系: 不正 JSON 文字列なら throw せず null を返す
    - loadWatchlistJudgment root-shape 系: 内容が `"null"` / `"[1,2,3]"`（typeof !== object または Array）のとき null を返す
    - loadWatchlistJudgment 形状不整合系: judgments が配列でない、または date が string でないとき null を返す
    - loadWatchlist 正常系: data/watchlist.json を読めたら JSON.parse 結果を WatchlistFile として返す
    - loadWatchlist ENOENT 系: throw せず {} を返し console.warn が呼ばれる
    - loadWatchlist パース失敗系: throw せず {} を返す
    - loadWatchlist root-shape 系: `"null"` / `"[1,2,3]"` で {} を返す
  </behavior>
  <action>
    既存の `describe("loadUrgencyHistory", ...)` ブロックの直後に、`describe("loadWatchlistJudgment", ...)` と `describe("loadWatchlist", ...)` を追加する。import 文の loader 名リストに `loadWatchlistJudgment` と `loadWatchlist` を追加し、型 import `WatchlistJudgmentFile`（"../meeting/types.js"）を必要に応じて追加する。各テストは既存の `vi.mocked(readFile).mockResolvedValueOnce(...)` / `mockRejectedValueOnce(new Error("ENOENT"))` パターンと `vi.spyOn(console, "warn")` を逐語的に踏襲する。loadWatchlistJudgment のテストは第1引数に meetingResultDate を渡す形（例: 正常系は fixture の date と一致、stale 系は不一致）で呼ぶ。stale 系テストは `expect(result).toBeNull()` と console.warn 呼び出しの両方をアサートする。判定 fixture は最小形（date/generatedAt/judgments の3フィールド、judgments は空配列または1件）で作る。実装は Task 2 で行うため、この時点でテストは RED（未実装/型エラーで落ちる）であることを確認する。ローダーが存在しない状態でのテスト失敗は許容される RED 状態である。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/report-data-loaders.test.ts 2>&1 | grep -qiE "fail|error|is not a function|has no exported member" && echo "RED confirmed (tests fail before impl)" || (echo "EXPECTED RED but tests passed — impl may already exist" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/report-data-loaders.test.ts に `describe("loadWatchlistJudgment"` と `describe("loadWatchlist"` の両文字列が存在する（`grep -c` で各1以上）
    - loadWatchlistJudgment の stale ケース（file.date ≠ meetingResultDate → null）を検証するテストが1件以上ある（date 不一致 fixture + `toBeNull()` アサーション）
    - loadWatchlistJudgment の ENOENT・パース失敗・root-shape（null/array）・judgments 非配列の各分岐テストが存在する
    - loadWatchlist の 正常・ENOENT・パース失敗・root-shape の各分岐テストが存在する
    - この時点で `npx vitest run src/scripts/report-data-loaders.test.ts` は RED（新規テストが未実装により失敗）
  </acceptance_criteria>
  <done>新規2ローダーの分岐網羅テストが report-data-loaders.test.ts に追加され、実装前のため RED になっている。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: loadWatchlistJudgment / loadWatchlist を実装しテストを GREEN にする</name>
  <files>src/scripts/report-data-loaders.ts</files>
  <read_first>
    - src/scripts/report-data-loaders.ts（loadUrgencyHistory:143-159 を逐語クローンの雛形にする。TMP_DIR = `join(import.meta.dirname, "../../tmp")`、DATA_DIR = `join(import.meta.dirname, "../../data")`。console.warn チャネル規約: 欠損は正常条件のため console.warn（console.error ではない））
    - src/scripts/report-data-loaders.test.ts（Task 1 で確定したテスト期待値）
    - src/meeting/types.ts:134-170（WatchlistJudgmentFile.date は string、judgments は readonly WatchlistJudgment[]）
    - src/portfolio/watchlist.ts:32-44（WatchlistFile 型の import 元）
  </read_first>
  <action>
    ファイル冒頭の import に型追加: `import type { WatchlistJudgmentFile } from "../meeting/types.js";` と `import type { WatchlistFile } from "../portfolio/watchlist.js";`。次に `loadUrgencyHistory` の直後に2関数を追加する。

    `loadWatchlistJudgment(meetingResultDate: string): Promise<WatchlistJudgmentFile | null>`: `readFile(join(TMP_DIR, "watchlist-judgment.json"), "utf-8")` を try で読み、`JSON.parse` 後に root-shape ガード（`parsed === null || typeof parsed !== "object" || Array.isArray(parsed)` → console.warn + return null）、形状ガード（`!Array.isArray(file.judgments) || typeof file.date !== "string"` → console.warn + return null）、そして D-13 の stale ガード（`file.date !== meetingResultDate` → console.warn に file.date と meetingResultDate の両方を含めて出力 + return null）を順に適用し、全て通過したら file を返す。catch は console.warn（第1実行時の欠損を含む fail-soft 想定）+ return null。ファイルは TMP_DIR 直下（tmp/watchlist-judgment.json）。

    `loadWatchlist(): Promise<WatchlistFile>`: `readFile(join(DATA_DIR, "watchlist.json"), "utf-8")`（DATA_DIR、TMP_DIR ではない）を try で読み、root-shape ガード（`parsed === null || typeof parsed !== "object" || Array.isArray(parsed)` → console.warn + return {}）を適用し、通過したら `parsed as WatchlistFile` を返す。catch は console.warn + return {}。フォールバック値は `{}`（WatchlistFile = Record<string, WatchlistEntry> の空）。

    どちらのローダーも `console.error` は使わない（欠損は正常条件のため console.warn を使う既存 loadUrgencyHistory / loadPrevPortfolioAnalysis 規約に合わせる）。zod は使わず型アサーションのみ（自社TS生成物のため、loadHoldingNews / loadUrgencyHistory と同区分）。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/report-data-loaders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/report-data-loaders.test.ts` が GREEN（全テストパス）
    - report-data-loaders.ts に `export async function loadWatchlistJudgment` と `export async function loadWatchlist` が存在する
    - loadWatchlistJudgment 内に `file.date !== meetingResultDate` の比較が存在する（D-13 stale ガード）— `grep -c 'meetingResultDate' src/scripts/report-data-loaders.ts` が2以上（引数 + 比較）
    - loadWatchlist は `DATA_DIR` と `"watchlist.json"` を参照する（`data/` 配下読込）
    - 両ローダーで `console.error` を使っていない（欠損は console.warn チャネル）: `grep -n 'console.error' src/scripts/report-data-loaders.ts` の出力に新規2関数の行が含まれない
  </acceptance_criteria>
  <done>両ローダーが実装され、report-data-loaders.test.ts が全て GREEN。judgment ファイルの date 不整合は null に落ち、watchlist 欠損は {} に落ちる。</done>
</task>

</tasks>

<artifacts_this_phase_produces>
このプランが新規作成するシンボル（drift 検証の対象外・新規作成として扱う）:

- `loadWatchlistJudgment` — `src/scripts/report-data-loaders.ts` の新規 export 関数。シグネチャ `(meetingResultDate: string): Promise<WatchlistJudgmentFile | null>`
- `loadWatchlist` — `src/scripts/report-data-loaders.ts` の新規 export 関数。シグネチャ `(): Promise<WatchlistFile>`

参照する既存シンボル（本プランでは作成しない）: `WatchlistJudgmentFile`（src/meeting/types.ts）, `WatchlistFile`（src/portfolio/watchlist.ts）, `TMP_DIR` / `DATA_DIR`（同ファイル内既存定数）。
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ファイルシステム → ローダー | tmp/watchlist-judgment.json（Phase 30 の自社生成物）と data/watchlist.json（Phase 28 の自社生成物）を読み込む。破損・欠損・前日残留の可能性がある信頼境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | loadWatchlistJudgment（前日残留ファイルの当日誤表示） | medium | mitigate | D-13 stale ガード（`file.date !== meetingResultDate` → null）で前日判定の当日誤表示を構造的に防止。テストで date 不一致ケースを検証 |
| T-31-02 | Denial of Service | 両ローダー（破損ファイルによる throw でレポート生成停止） | high | mitigate | try/catch + root-shape ガードで throw-free 化。欠損・破損・形状不整合いずれも null / {} フォールバック。既存 loadUrgencyHistory 様式を踏襲 |
| T-31-03 | Tampering | loadWatchlist（JSON.parse された data/watchlist.json のキーが __proto__ 等） | low | accept | 読み取り専用 lookup であり書き込みは発生しないため実害は限定的（RESEARCH Security Domain 判断）。root-shape 検証は適用するが ReadonlyMap 化までは本フェーズでは不要 |
| T-31-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本フェーズは新規パッケージを一切導入しない（RESEARCH §Installation）。install タスク不在のため対象なし |
</threat_model>

<verification>
- `npx vitest run src/scripts/report-data-loaders.test.ts` が GREEN
- loadWatchlistJudgment の stale ガード・root-shape・形状ガードの各分岐がテストで網羅されている
- loadWatchlist の 正常・欠損・破損・root-shape の各分岐がテストで網羅されている
- ワーブ完了時: `npm run test`（フルスイート）が GREEN で既存テストに回帰がない
</verification>

<success_criteria>
- loadWatchlistJudgment が当日 date のファイルのみ返し、前日残留・欠損・破損は null を返す（D-12/D-13）
- loadWatchlist が data/watchlist.json を fail-soft 読込し欠損・破損時は {} を返す（D-12）
- 両ローダーが throw-free で、既存 loadUrgencyHistory の console.warn チャネル規約に従う
</success_criteria>

<output>
Create `.planning/phases/31-daily-report-watchlist-section/31-01-SUMMARY.md` when done
</output>
