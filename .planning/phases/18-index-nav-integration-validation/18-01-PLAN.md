---
phase: 18-index-nav-integration-validation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/scripts/update-index.test.ts
  - src/scripts/update-index.ts
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "news-digest.html が実在する日付の index.html エントリに 'News Digest' リンクが末尾（4番目）に表示される（D-04: ラベルは英語 'News Digest' / D-05: 配置は末尾4番目）"
    - "news-digest.html が無い日付のエントリには News Digest リンクが無い（404リンクなし）（D-01: 実行のたびに全エントリへ fs 実在チェック、forward-only 不採用）"
    - "パース済み HTML に残っていた古い News Digest リンクは、ファイル不在時に出力から除去される（D-02: リンクは毎回 fs から完全導出、パース結果を信用せず上書き）"
    - "既存3リンク（daily-report / meeting-minutes / portfolio-report）は全エントリで保持され回帰しない（D-03: 実在チェックを既存3リンクへ広げない / D-07: index.html への CSS 追加・変更なし）"
    - "ヒーローブロック（最新レポート枠）にも News Digest リンクが表示される（D-06: renderEntryLinks 共有により専用分岐なしで成立）"
    - "D-08: TDD で update-index.test.ts に4ケース（実在→付与 / 不在→なし / stale除去 / 既存3リンク保持）を先に追加し RED→GREEN で実装する"
  artifacts:
    - path: "src/scripts/update-index.ts"
      provides: "withNewsDigestLink 正規化パスと newsDigestFileExists fs 実在チェック、updateIndexHtml への配線"
      contains: "withNewsDigestLink"
    - path: "src/scripts/update-index.test.ts"
      provides: "access モックを含む vi.mock ファクトリと D-08 の4テストケース"
      contains: "access"
  key_links:
    - from: "src/scripts/update-index.ts:updateIndexHtml"
      to: "withNewsDigestLink"
      via: "mergeEntry 後・buildRegion 前の Promise.all(merged.map(...))"
      pattern: "Promise\\.all\\(merged\\.map\\(withNewsDigestLink\\)\\)"
    - from: "src/scripts/update-index.ts:withNewsDigestLink"
      to: "docs/{date}/news-digest.html"
      via: "access() from node:fs/promises"
      pattern: "access\\(join\\(DOCS_DIR"
---

<objective>
`src/scripts/update-index.ts` を拡張し、index.html の全エントリ（当日 + 約118件の過去エントリ）に対して毎回 fs 実在チェックを行い、`docs/{date}/news-digest.html` が実在する日付のみ "News Digest" リンクを末尾に導出する。パース済みリンクは信用せず毎回 strip→再導出する（D-01/D-02）。既存3リンク・CSS・レンダリング関数・invest.md 呼び出しは無変更（D-03/D-07）。

D-08 の TDD 契約に従い、テストを先に書いて RED を確認してから実装する。

Purpose: UI-04 を満たし、欠落日の 404 リンクを構造的に排除する自己修復的・冪等な設計を確立する。
Output: `withNewsDigestLink` / `newsDigestFileExists` ヘルパー、`updateIndexHtml` への1行配線、`update-index.test.ts` の access モック拡張 + 4テストケース。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-index-nav-integration-validation/18-CONTEXT.md
@.planning/phases/18-index-nav-integration-validation/18-RESEARCH.md
@.planning/phases/18-index-nav-integration-validation/18-PATTERNS.md
@.planning/phases/18-index-nav-integration-validation/18-VALIDATION.md

<interfaces>
<!-- update-index.ts の既存契約。executor はこれらを直接使用し、コードベース探索は不要。 -->

src/scripts/update-index.ts:
- interface ReportLink { readonly href: string; readonly label: string }
- interface ReportEntry { readonly date: string; readonly links: ReadonlyArray<ReportLink> }
- const DOCS_DIR = join(import.meta.dirname, "../../docs")
- import { readFile, writeFile } from "node:fs/promises"  ← ここに access を追加する
- function buildStandardLinks(date): ReportLink[]  ← 変更禁止（D-03）、3リンク固定
- export function mergeEntry(existing, newEntry): ReportEntry[]  ← 変更禁止、new date wins
- function renderEntryLinks(entry): string  ← 変更禁止、escapeHtml を href/label に適用済み
- 配線点（現状）: updateIndexHtml 内 `const merged = mergeEntry(existingEntries, newEntry);` の直後・`const regionHtml = buildRegion(merged);` の直前

src/scripts/update-index.test.ts:
- 現行 vi.mock("node:fs/promises", () => ({ readFile, writeFile, mkdir, readdir }))  ← access が欠落（Pitfall 4）
- HEAD_SCAFFOLD: 2026-07-01（3リンク）と 2026-06-10（2リンク）の2エントリ、どちらも News Digest リンク無し
- NEW_DATE = "2026-07-02"
- 既存アサーションパターン: writeFile.mock.calls から index.html 書き込みを find → regex で report-links ブロック抽出 → substring assert
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — access モック拡張と D-08 の4テストケース追加</name>
  <files>src/scripts/update-index.test.ts</files>
  <read_first>
    - src/scripts/update-index.test.ts（現行の vi.mock ファクトリ、HEAD_SCAFFOLD、beforeEach、既存 "preserves the 2-link entry" テストの assert パターンを踏襲する）
    - .planning/phases/18-index-nav-integration-validation/18-PATTERNS.md（"src/scripts/update-index.test.ts" 節: モック拡張・fixture・per-test access 設定・assert パターンの手本）
    - .planning/phases/18-index-nav-integration-validation/18-RESEARCH.md（Pitfall 4 = access モック欠落の gotcha、Code Examples 節）
  </read_first>
  <behavior>
    - Test 1 "adds News Digest link": access が `2026-07-02/news-digest.html`（=NEW_DATE、hero + accordion 両方に出る最新日）に対し resolve する設定で updateIndexHtml(NEW_DATE) を実行 → 出力の当該日 report-links ブロックに `<a href="2026-07-02/news-digest.html">News Digest</a>` が含まれ、既存3リンクの後（末尾）に位置する
    - Test 2 "omits News Digest link": access が全パスに対し reject（デフォルト）で実行 → 2026-06-10 エントリに `news-digest.html` および `News Digest` ラベルが一切含まれない
    - Test 3 "removes stale News Digest link": HEAD_SCAFFOLD の複製に 2026-07-01 エントリへ `<a href="2026-07-01/news-digest.html">News Digest</a>` を手で挿入した fixture を用い、access がそのパスに対し reject する設定で実行 → 出力の 2026-07-01 ブロックに News Digest リンクが残っていない（strip 検証、Pitfall 2）
    - Test 4 "preserves 3 links alongside News Digest": access が NEW_DATE に resolve する設定で実行 → 当該日エントリに daily-report / meeting-minutes / portfolio-report の3 href が全て残存しつつ News Digest が4本目として追加される（回帰なし = 成功基準3）
  </behavior>
  <action>
    現行の `vi.mock("node:fs/promises", ...)` ファクトリオブジェクトに `access: vi.fn().mockRejectedValue(new Error("ENOENT"))` を追加する（デフォルト = ファイル不在。これにより既存5テストは access を設定せず News Digest リンクを期待しないため無改修で pass し続ける）。Pitfall 4 のとおり vi.mock ファクトリは exhaustive replacement のため access を省くと実装側で `TypeError: access is not a function` になる。
    D-08 の4ケースを新規 `it(...)` として追加する。各ケースは per-test で `(fsMock.access as ReturnType<typeof vi.fn>).mockImplementation((path) => ...)` を設定し、対象日付の `news-digest.html` パスにのみ resolve/reject を分岐させる（共有 beforeEach には access の日付固有実装を置かない。beforeEach では既存の readFile 設定のみ維持）。
    Test 3 用に、HEAD_SCAFFOLD をベースにした派生 fixture 文字列（2026-07-01 の report-links 内に News Digest アンカーを1行挿入したもの）をバッククォートテンプレートリテラルで用意し、その beforeEach 内 readFile を派生 fixture に差し替える（temp ディレクトリ fixture は使わない — モジュールモック継続、Claude's Discretion 既存慣例に従う）。
    assert は既存 "preserves the 2-link entry" テストと同型: writeFile.mock.calls から index.html 書き込みを find → 対象日付の `<div class="report-links">([\s\S]*?)</div>` を regex 抽出 → substring の presence/absence を expect する。この時点では実装が無いためテストは RED（失敗）で正しい。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/update-index.test.ts 2>&1 | grep -Eq "(adds News Digest link|omits News Digest link|removes stale News Digest link)" && echo "new tests present"</automated>
  </verify>
  <acceptance_criteria>
    - `src/scripts/update-index.test.ts` の vi.mock ファクトリに `access:` が含まれる（`grep -c 'access:' src/scripts/update-index.test.ts` >= 1）
    - 新規 `it(` が4件追加されている（"adds News Digest link" / "omits News Digest link" / "removes stale News Digest link" / 3リンク保持ケースの4名称に対応）
    - この時点で `npx vitest run src/scripts/update-index.test.ts` を実行すると新規4ケースが FAIL する（RED 確認、実装未着手のため）
    - 既存5テストは access 未設定でも pass する（News Digest を期待しないため回帰なし）
    - RED 確認後にコミット: `test(18-01): add failing News Digest link tests + access mock`
  </acceptance_criteria>
  <done>vi.mock に access 追加、D-08 の4新規テストが存在し RED、既存テストは維持。RED 状態でコミット済み。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — withNewsDigestLink 導出パスの実装と配線</name>
  <files>src/scripts/update-index.ts</files>
  <read_first>
    - src/scripts/update-index.ts（imports 行、DOCS_DIR、ReportEntry/ReportLink、buildStandardLinks、mergeEntry、updateIndexHtml の現行構造。変更点は import への access 追加・新規ヘルパー2関数・updateIndexHtml 内の1行配線のみ）
    - src/scripts/update-index.test.ts（Task 1 で追加した4ケースの期待挙動 — これを green にする）
    - .planning/phases/18-index-nav-integration-validation/18-PATTERNS.md（"新規ヘルパー" と "配線" のコードシェイプ、イミュータブル慣例 `{ ...entry, links }`）
    - src/scripts/write-news-digest.ts（date プロヴィナンス / パストラバーサル対策コメント T-17-03。date ソースを広げないことの根拠）
  </read_first>
  <action>
    line 1 の import を `import { access, readFile, writeFile } from "node:fs/promises";` に変更する（node:fs の同期 existsSync は使わない — src/ 全体で同期 fs はゼロ、単一 fs API サーフェスを維持）。
    定数 `const NEWS_DIGEST_LABEL = "News Digest";` を追加する（D-04 のラベル、Phase 16 D-13 のページタイトルと一致）。
    `async function newsDigestFileExists(date: string): Promise<boolean>` を追加する: `await access(join(DOCS_DIR, date, "news-digest.html"))` を try し、成功で true、catch で false を返す（ENOENT は例外でなく通常の不在ブランチ。ここでは boolean を返す設計で、write-news-digest.ts のパイプラインレベル try/catch とは別レイヤー）。パスは必ず DOCS_DIR 起点で構築する。
    `async function withNewsDigestLink(entry: ReportEntry): Promise<ReportEntry>` を追加する: まず `entry.links.filter((l) => l.label !== NEWS_DIGEST_LABEL)` で既存 News Digest リンクを無条件に除去（D-02、パース結果を信用しない）、次に newsDigestFileExists(entry.date) が true なら `{ href: ` + `${entry.date}/news-digest.html` + `, label: NEWS_DIGEST_LABEL }` を末尾に append（D-05 の4番目配置）、false なら base のまま。返り値は `{ ...entry, links }` の新規オブジェクト（イミュータブル、mutation 禁止）。
    updateIndexHtml 内、`const merged = mergeEntry(existingEntries, newEntry);` の直後に `const withDigestLinks = await Promise.all(merged.map(withNewsDigestLink));` を追加し、`const regionHtml = buildRegion(merged);` を `buildRegion(withDigestLinks)` に変更する。これが D-01（全エントリ毎回導出）と D-06（renderHero/renderAccordion 共有の renderEntryLinks 経由で hero にも自動反映）を無改修で満たす唯一の変更点。
    buildStandardLinks・renderEntryLinks・renderHero・renderAccordion・parseExistingEntries・invest.md 呼び出しには一切触れない（D-03/D-07、Pitfall 1/3 回避）。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/update-index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'access' src/scripts/update-index.ts` の import 行に access が含まれ、`node:fs`（同期）の import は存在しない（`grep -c 'from "node:fs"' src/scripts/update-index.ts` は 0）
    - `withNewsDigestLink` と `newsDigestFileExists` が定義され、updateIndexHtml が `Promise.all(merged.map(withNewsDigestLink))` を呼び buildRegion にその結果を渡す
    - `buildStandardLinks` は3リンク固定のまま変更なし（daily-report/meeting-minutes/portfolio-report の3 href、News Digest push 無し）
    - `npx vitest run src/scripts/update-index.test.ts` で新規4ケース含む全テストが green
    - `npm test` フルスイートが green（report-utils.ts 他コンシューマへの回帰なし）
    - `npx tsc --noEmit` で型エラーが出ない
    - GREEN 確認後にコミット: `feat(18-01): derive News Digest link from fs existence per entry`
  </acceptance_criteria>
  <done>実装完了、D-08 の4ケース含む update-index.test.ts が green、npm test フルスイート green、buildStandardLinks 無変更。GREEN でコミット済み。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/meeting-result.json → update-index.ts | 当日 date の供給元。上流（Phase 14.1）で `^\d{4}-\d{2}-\d{2}$` 検証済み |
| 既存 docs/index.html → parseExistingEntries | 過去エントリ date の供給元。自スクリプトの過去自作出力のみ（外部入力ではない）、regex capture `([\d-]+)` |
| docs/{date}/ ファイルシステム → newsDigestFileExists | 実在チェック対象。別パイプラインステップ（write-news-digest.ts）が書き出す可能性のあるファイル |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | newsDigestFileExists の `join(DOCS_DIR, date, "news-digest.html")` パス構築 | accept | date は検証済み meeting-result.json（当日）または自作 index.html の regex capture（過去）のみに由来。外部/LLM 入力を新規に受け付けない。date ソースを広げないことをレビュー時に確認（T-17-03 のパストラバーサル対策を継承） |
| T-18-02 | Tampering / Spoofing | News Digest リンクの href/label の HTML 出力 | mitigate | 新リンクオブジェクトは entry.links に append され既存 renderEntryLinks 経由でレンダされる。renderEntryLinks は href/label 双方に escapeHtml を適用済み。新規エスケープロジック不要、専用レンダ分岐を作らないことで既存の XSS 対策を再利用 |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | 本フェーズはパッケージインストールなし（node:fs/promises の access は Node 組込み、既存 import に追加のみ）。RESEARCH.md「Package Legitimacy Audit: Not applicable」により slopcheck 不要 |
</threat_model>

<verification>
- `npx vitest run src/scripts/update-index.test.ts` — D-08 の4ケース + 既存5ケース全 green
- `npm test` — フルスイート green（回帰なし）
- `npx tsc --noEmit` — 型エラーなし
- ソース確認: buildStandardLinks / renderEntryLinks / renderHero / renderAccordion / parseExistingEntries が無変更
</verification>

<success_criteria>
- news-digest.html 実在日付のエントリに News Digest リンクが末尾に付与される（成功基準1）
- news-digest.html 不在日付には News Digest リンクが無い（成功基準2、404なし）
- 既存3リンクが全エントリで保持される（成功基準3、回帰なし）
- パース済み古 News Digest リンクが fs 不在時に除去される（自己修復、D-02）
- TDD 順序（RED→GREEN）で2コミット
</success_criteria>

<output>
Create `.planning/phases/18-index-nav-integration-validation/18-01-SUMMARY.md` when done
</output>
