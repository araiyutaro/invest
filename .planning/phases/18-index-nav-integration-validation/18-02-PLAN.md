---
phase: 18-index-nav-integration-validation
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - docs/index.html
  - docs/portfolio.html
autonomous: false
requirements: [UI-04]
must_haves:
  truths:
    - "update-index.ts 単体ライブ実行で docs/index.html の 2026-07-03 エントリに News Digest リンクが付与される（D-10: npx tsx src/scripts/update-index.ts の単体実行のみ、フル /invest 再実行なし）"
    - "news-digest.html を持たない過去日付（例 2026-06-10）のエントリには News Digest リンクが付かない（D-09: 既存の約108日分の過去エントリで自然検証、失敗注入なし）"
    - "既存3レポートリンクは全エントリで保持される（回帰なし）（D-08: ユニットテスト + ライブ実行の2段構えのライブ検証レグ）"
  artifacts:
    - path: "docs/index.html"
      provides: "実機反映後の index.html（2026-07-03 のみ News Digest リンク付与、~117 過去日は無し）"
      contains: "2026-07-03/news-digest.html"
  key_links:
    - from: "docs/index.html:2026-07-03 エントリ"
      to: "docs/2026-07-03/news-digest.html"
      via: "News Digest アンカーリンク"
      pattern: "2026-07-03/news-digest\\.html"
---

<objective>
D-08/D-10 のライブ検証レグを実施する。`npx tsx src/scripts/update-index.ts` を `tmp/meeting-result.json` 存在下で単体実行し、実 docs/ ツリーに対して条件付きリンク導出が正しく機能することを確認する。実環境グラウンドトゥルース: 118 日付ディレクトリ中 news-digest.html を持つのは 2026-07-03 のみ。フル /invest 再実行はしない（D-10、パイプラインは Phase 17 で実証済み）。

Purpose: 成功基準1・2をユニットテストではなく実 fs 状態で自然検証し（D-09）、UI-04 を実機で確定する。
Output: 実機反映された docs/index.html（+ portfolio.html 副次更新）、およびデプロイ/コミット判断。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-index-nav-integration-validation/18-CONTEXT.md
@.planning/phases/18-index-nav-integration-validation/18-VALIDATION.md
@.planning/phases/18-index-nav-integration-validation/18-01-SUMMARY.md

Live ground truth (研究時点で確認済み):
- docs/2026-07-03/news-digest.html は実在（唯一）
- 他 ~117 日付ディレクトリは news-digest.html を持たない
- tmp/meeting-result.json は存在（date フィールドあり）
- docs/index.html は OPS-02 SHA256 チェックサム保護対象・直接編集禁止（更新は update-index.ts 経由のみ）
</context>

<tasks>

<task type="auto">
  <name>Task 1: ライブ単体実行と自動アサーション</name>
  <files>docs/index.html, docs/portfolio.html</files>
  <read_first>
    - .planning/phases/18-index-nav-integration-validation/18-01-SUMMARY.md（実装内容の確認 — withNewsDigestLink 配線が済んでいること）
    - .planning/phases/18-index-nav-integration-validation/18-CONTEXT.md（D-09 自然検証・D-10 単体実行の制約）
    - src/scripts/update-index.ts（実行対象スクリプト。REPORT_ENTRIES マーカー間のみ書き換える設計の確認）
  </read_first>
  <action>
    `npx tsx src/scripts/update-index.ts` を実行する（tmp/meeting-result.json が存在する状態、フル /invest は起動しない）。実行後、生成された docs/index.html に対して自動アサーションを行う: (1) 2026-07-03 の report-links / hero ブロックに `2026-07-03/news-digest.html` と `News Digest` ラベルが含まれること、(2) news-digest.html を持たない過去日付（少なくとも 2026-06-10 をサンプルに）のブロックに `news-digest.html` が含まれないこと、(3) 2026-07-03 エントリに daily-report / meeting-minutes / portfolio-report の3 href が保持されていること。
    アサーションは grep ベースで行う（例: 2026-07-03 ブロックに news-digest リンク在り = 期待マッチ数 >= 1、他日付ブロックに無し = マッチ 0）。マーカー外の領域（title/style/footer）が不変であることも確認する。
    OPS-02 チェックサム保護下の docs HTML への差分は News Digest リンク追加分に限定されること（既存3リンク・CSS への差分が無いこと）を `git diff docs/index.html` で確認する。
  </action>
  <verify>
    <automated>grep -q "2026-07-03/news-digest.html" docs/index.html && echo "OK: 2026-07-03 has News Digest link"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx src/scripts/update-index.ts` が exit 0 で完了する
    - `grep -c "2026-07-03/news-digest.html" docs/index.html` >= 1（成功基準1: 実在日にリンク付与）
    - 2026-06-10 の report-links ブロック内に `news-digest.html` が現れない（成功基準2: 欠落日は404リンク無し）
    - `grep -c "news-digest.html" docs/index.html` が実在日数（=1 相当）に一致し、~117 過去日に余分なリンクが無い
    - `git diff docs/index.html` の差分が News Digest リンク追加行に限定され、既存3リンク・CSS の変更行が無い
  </acceptance_criteria>
  <done>ライブ実行成功、2026-07-03 のみ News Digest リンク付与、過去日は無し、既存3リンク保持、差分は追加分に限定。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 実機ブラウザ確認とデプロイ方針決定</name>
  <action>実行を一時停止し、下記 how-to-verify の手順に沿って人間が docs/index.html を実機ブラウザで確認する。2026-07-03 のヒーロー枠に News Digest リンクが4番目に表示され、過去日（2026-06-10 等）にはリンクが無く、既存3リンクが全日付で保持されていることを目視確認したうえで、デプロイ/コミット方針（既存 deploy 慣例に従うか launchd 自動実行に委ねるか）を承認・指示するまでブロックする。</action>
  <what-built>update-index.ts 単体ライブ実行により docs/index.html を実機反映。2026-07-03 エントリに News Digest リンクが末尾に追加され、news-digest.html を持たない過去 ~117 日付にはリンクが付かない（404リンク無し）。既存3レポートリンクは全エントリで保持。</what-built>
  <how-to-verify>
    1. docs/index.html をブラウザで開く（またはローカルサーブ）。
    2. 最新レポート（ヒーロー枠 = 2026-07-03）に「News Digest」リンクが Daily Report / Meeting Minutes / Portfolio Report の後（4番目）に表示され、クリックで 2026-07-03/news-digest.html が開くことを確認。
    3. アコーディオンを展開し、2026-07-03 より前の日付（例 2026-06-10）に「News Digest」リンクが無いことを確認（404リンクが生成されていない = 成功基準2）。
    4. 既存3リンク（Daily Report / Meeting Minutes / Portfolio Report）が全日付で従来通り表示されることを確認（回帰なし = 成功基準3）。
    5. 見た目が他リンクと同一で、CSS の追加/強調が無いことを確認（D-07）。
    6. デプロイ/コミット方針: 承認後、既存 deploy 慣例（invest.md Step 4 の git ベースデプロイ、OPS-02 チェックサム更新を含む）に従って docs/index.html を反映するか、翌朝の launchd 自動実行に委ねるかを指示してください（Claude's Discretion 既存慣例に従う）。
  </how-to-verify>
  <resume-signal>"approved" と入力するか、問題点・デプロイ方針を記述してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/meeting-result.json → update-index.ts | ライブ実行時の当日 date 供給元。上流検証済み `^\d{4}-\d{2}-\d{2}$` |
| update-index.ts → docs/index.html（書き込み） | OPS-02 SHA256 チェックサム保護対象。単一 readFile + 単一 writeFile、REPORT_ENTRIES マーカー間のみ書換 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | docs/index.html への書き込み（チェックサム保護領域） | accept | update-index.ts はマーカー間のみ書換・単一 write。差分は News Digest リンク追加に限定され `git diff` で検証。既存 deploy 慣例が OPS-02 チェックサム更新を扱う |
| T-18-04 | Denial of Service / Info | ライブ実行の副作用範囲 | accept | フル /invest を起動せず update-index.ts 単体のみ実行（D-10）。副次更新は docs/portfolio.html のホールディングスリストのみ（既存挙動、無変更コード経路） |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | パッケージインストールなし。実行のみ（既存 tsx トゥルチェーン） |
</threat_model>

<verification>
- `npx tsx src/scripts/update-index.ts` exit 0
- docs/index.html: 2026-07-03 に News Digest リンク在り、過去日に無し（自然検証 D-09）
- `git diff docs/index.html` 差分が追加分に限定、既存3リンク・CSS 不変
- 人間による実機ブラウザ確認（ヒーロー + アコーディオン、回帰なし、デプロイ方針決定）
</verification>

<success_criteria>
- 成功基準1: 実在日（2026-07-03）に News Digest リンク付与（実機確認）
- 成功基準2: 欠落日に404リンク無し（自然検証、D-09）
- 成功基準3: 既存3リンク回帰なし（実機確認）
- D-10: update-index.ts 単体実行のみ、翌朝 launchd が自然な E2E
</success_criteria>

<output>
Create `.planning/phases/18-index-nav-integration-validation/18-02-SUMMARY.md` when done
</output>
