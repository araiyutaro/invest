# Phase 11 Plan 02: Cross-Language Dedup

**Requirements:** NEWS-03
**Depends on:** 11-01-PLAN.md (types.ts の `ticker?` フィールド追加が前提)
**Estimated tasks:** 7

## Goal

英語と日本語で同内容の記事（例: Reuters英語版とロイター日本語版）がフィルターパイプラインの新Pass（Pass 2.5）で1件に集約され、`NewsFilterStats.afterCrossLangDedup` に集約件数が記録される。

## Tasks

### Task 1: filter.test.ts — `extractProperNouns` テストを先行作成（TDD RED）

**File:** `src/data/news/filter.test.ts`
**Action:** modify (append)
**TDD:** テスト先行 → 実装は Task 2

ファイル末尾に以下を追加（既存の imports に `extractProperNouns`, `deduplicateCrossLanguage` を追加する必要あり — Task 2 実装後に import が通る）:

```typescript
// import 行（ファイル先頭の import を更新: Task 2 実装後に有効化）
// import { ..., extractProperNouns, deduplicateCrossLanguage } from "./filter.js";

describe("extractProperNouns (NEWS-03 / D-07, D-10)", () => {
  it("4桁年を抽出する", () => {
    // import 後に実行可能
    const { extractProperNouns } = require("./filter.js");
    const result = extractProperNouns("Apple Q3 2026 results beat estimates");
    expect(result.has("2026")).toBe(true);
  });

  it("大文字始まりの固有名詞を抽出する", () => {
    const { extractProperNouns } = require("./filter.js");
    const result = extractProperNouns("Apple Q3 2026 results beat estimates");
    expect(result.has("Apple")).toBe(true);
  });

  it("Q1-Q4パターンを抽出する", () => {
    const { extractProperNouns } = require("./filter.js");
    const result = extractProperNouns("Apple Q3 2026 results");
    expect(result.has("Q3")).toBe(true);
  });

  it("大文字のみのティッカー（2-5文字）を抽出する", () => {
    const { extractProperNouns } = require("./filter.js");
    const result = extractProperNouns("MRNA Q3 2026 earnings miss analyst");
    expect(result.has("MRNA")).toBe(true);
  });

  it("日本語タイトルから年数を抽出する（ASCII部分のみ）", () => {
    const { extractProperNouns } = require("./filter.js");
    const result = extractProperNouns("モデルナが2026年第3四半期の決算を発表");
    expect(result.has("2026")).toBe(true);
  });

  it("日本語タイトルにASCIIティッカーが含まれる場合は抽出する", () => {
    const { extractProperNouns } = require("./filter.js");
    const result = extractProperNouns("MRNA 2026年3Q 決算ミス");
    expect(result.has("MRNA")).toBe(true);
    expect(result.has("2026")).toBe(true);
  });

  it("空文字列は空Setを返す", () => {
    const { extractProperNouns } = require("./filter.js");
    const result = extractProperNouns("");
    expect(result.size).toBe(0);
  });

  it("短い一般語（'at', 'in'等）は含まれない", () => {
    const { extractProperNouns } = require("./filter.js");
    const result = extractProperNouns("Stocks rise in the US at market open");
    expect(result.has("in")).toBe(false);
    expect(result.has("at")).toBe(false);
  });
});
```

> **注:** vitest では `require()` が使えないため、実際のテストは `import` を使う。Task 2 実装後に import 文をファイル先頭で更新する。上記は実装すべき仕様を示したプレースホルダーである。

**実際のテストコード（Task 2 実装後にファイルへ追加するもの）:**

```typescript
import {
  filterNewsArticles,
  extractProperNouns,
  deduplicateCrossLanguage,
} from "./filter.js";

describe("extractProperNouns (NEWS-03 / D-07, D-10)", () => {
  it("4桁年を抽出する", () => {
    const result = extractProperNouns("Apple Q3 2026 results beat estimates");
    expect(result.has("2026")).toBe(true);
  });

  it("大文字始まりの固有名詞（2文字以上）を抽出する", () => {
    const result = extractProperNouns("Apple Q3 2026 results beat estimates");
    expect(result.has("Apple")).toBe(true);
  });

  it("Q1-Q4パターンを大文字で抽出する", () => {
    const result = extractProperNouns("Apple Q3 2026 results");
    expect(result.has("Q3")).toBe(true);
  });

  it("全大文字のティッカー（2-5文字）を抽出する", () => {
    const result = extractProperNouns("MRNA Q3 2026 earnings miss analyst");
    expect(result.has("MRNA")).toBe(true);
  });

  it("日本語タイトルから年数を抽出する", () => {
    const result = extractProperNouns("モデルナが2026年第3四半期の決算を発表");
    expect(result.has("2026")).toBe(true);
  });

  it("日本語タイトルにASCIIティッカーが含まれる場合は抽出する", () => {
    const result = extractProperNouns("MRNA 2026年3Q 決算ミス");
    expect(result.has("MRNA")).toBe(true);
    expect(result.has("2026")).toBe(true);
  });

  it("空文字列は空Setを返す", () => {
    const result = extractProperNouns("");
    expect(result.size).toBe(0);
  });

  it("2文字未満のトークンは含まれない", () => {
    const result = extractProperNouns("AI is up");
    expect(result.has("is")).toBe(false);
  });
});
```

---

### Task 2: filter.ts — `extractProperNouns` 実装 + export（TDD GREEN）

**File:** `src/data/news/filter.ts`
**Action:** modify
**TDD:** Task 1 のテストをパスさせる実装

現 `isJapaneseTitle` (L55-59) の直後、`deduplicateByUrl` (L64) の前に以下を挿入:

```typescript
/**
 * タイトルから固有名詞セットを抽出する (D-07, D-10)。
 * 抽出対象: 4桁年, Q1-Q4, 大文字始まりの語(2文字以上), 全大文字ティッカー(2-5文字)。
 * 日本語タイトルではASCII部分のみ処理されるため年数・ティッカーが主な抽出結果となる。
 */
export function extractProperNouns(title: string): Set<string> {
  const tokens = new Set<string>();

  // 4桁年: 2020-2029 の範囲（一般的な投資記事の年）
  const years = title.match(/\b20[2-9]\d\b/g) ?? [];
  for (const y of years) tokens.add(y);

  // Q1-Q4 表記（大文字小文字混在を正規化）
  const quarters = title.match(/\bQ[1-4]\b/gi) ?? [];
  for (const q of quarters) tokens.add(q.toUpperCase());

  // 全大文字 2-5 文字（ティッカー記号 / 略語）
  const allCaps = title.match(/\b[A-Z]{2,5}\b/g) ?? [];
  for (const t of allCaps) tokens.add(t);

  // 大文字始まり + 小文字が続く語（固有名詞: Apple, Moderna, Reuters 等）2文字以上
  const properNouns = title.match(/\b[A-Z][a-z][a-zA-Z]*\b/g) ?? [];
  for (const n of properNouns) tokens.add(n);

  return tokens;
}
```

**Task 1 テストの import 文更新（filter.test.ts 先頭の import を更新）:**

```typescript
// 現 L2 を以下に変更:
import {
  filterNewsArticles,
  extractProperNouns,
  deduplicateCrossLanguage,
} from "./filter.js";
```

> `deduplicateCrossLanguage` は Task 4 実装後に有効になる。それまでは import エラーになるため、Task 3 テストと Task 4 実装を続けて行うこと。

---

### Task 3: filter.test.ts — `deduplicateCrossLanguage` テストを先行作成（TDD RED）

**File:** `src/data/news/filter.test.ts`
**Action:** modify (append)
**TDD:** テスト先行 → 実装は Task 4

ファイル末尾に以下を追加:

```typescript
describe("deduplicateCrossLanguage (NEWS-03 / D-07, D-08, D-09)", () => {
  const baseTime = new Date(Date.now() - 2 * 60 * 60 * 1000); // 2h ago

  it("共通固有名詞2つ以上かつ6h以内のEN/JP記事ペアが1件に集約される", () => {
    const articles = [
      makeArticle({
        url: "https://reuters.com/mrna-q3",
        title: "MRNA Q3 2026 earnings miss analyst estimates",
        summary: "Short EN summary.",
        source: "Reuters",
        publishedAt: baseTime,
      }),
      makeArticle({
        url: "https://jp.reuters.com/mrna-q3",
        title: "MRNA 2026年第3四半期の決算が予想を下回る",
        summary: "こちらの方が長い日本語の本文サマリー",
        source: "ロイター",
        publishedAt: new Date(baseTime.getTime() + 30 * 60 * 1000), // 30min後
      }),
    ];
    const result = deduplicateCrossLanguage(articles);
    expect(result).toHaveLength(1);
  });

  it("summary が長い方の記事が残る (D-09)", () => {
    const articles = [
      makeArticle({
        url: "https://reuters.com/mrna-q3",
        title: "MRNA Q3 2026 earnings miss analyst",
        summary: "Short.",
        source: "Reuters",
        publishedAt: baseTime,
      }),
      makeArticle({
        url: "https://jp.reuters.com/mrna-q3",
        title: "MRNA 2026年3Q 決算ミス",
        summary: "こちらの方が長い日本語サマリーです",
        source: "ロイター",
        publishedAt: new Date(baseTime.getTime() + 10 * 60 * 1000),
      }),
    ];
    const result = deduplicateCrossLanguage(articles);
    expect(result).toHaveLength(1);
    expect(result[0].summary).toBe("こちらの方が長い日本語サマリーです");
  });

  it("6時間超の時間差がある記事ペアは集約されない (D-07: 6h window)", () => {
    const articles = [
      makeArticle({
        url: "https://reuters.com/mrna-old",
        title: "MRNA Q3 2026 earnings miss analyst estimates",
        summary: "EN summary",
        source: "Reuters",
        publishedAt: new Date(Date.now() - 10 * 60 * 60 * 1000), // 10h ago
      }),
      makeArticle({
        url: "https://jp.reuters.com/mrna-new",
        title: "MRNA 2026年3Q 決算ミス",
        summary: "JP summary",
        source: "ロイター",
        publishedAt: new Date(Date.now() - 1 * 60 * 60 * 1000), // 1h ago
      }),
    ];
    const result = deduplicateCrossLanguage(articles);
    expect(result).toHaveLength(2);
  });

  it("共通固有名詞が1つだけの場合は集約されない (D-08: conservative)", () => {
    const articles = [
      makeArticle({
        url: "https://reuters.com/market-2026",
        title: "Market outlook for 2026 remains positive",
        summary: "EN summary",
        publishedAt: baseTime,
      }),
      makeArticle({
        url: "https://jp.example.com/market-2026",
        title: "2026年の市場見通しはポジティブ",
        summary: "JP summary",
        publishedAt: baseTime,
      }),
    ];
    // "2026" だけでは共通名詞1個 → 集約しない
    const result = deduplicateCrossLanguage(articles);
    expect(result).toHaveLength(2);
  });

  it("異なる銘柄のEN/JP記事ペアは集約されない", () => {
    const articles = [
      makeArticle({
        url: "https://reuters.com/mrna",
        title: "MRNA Q3 2026 earnings miss",
        summary: "MRNA EN",
        publishedAt: baseTime,
      }),
      makeArticle({
        url: "https://jp.reuters.com/joby",
        title: "JOBY 2026年3Q 決算",
        summary: "JOBY JP",
        publishedAt: baseTime,
      }),
    ];
    // MRNA と JOBY は別ティッカー、2026 は共通だが1個のみ
    const result = deduplicateCrossLanguage(articles);
    expect(result).toHaveLength(2);
  });

  it("EN/EN または JP/JP ペアには適用されない（同言語は既存dedupが対応）", () => {
    const articles = [
      makeArticle({
        url: "https://reuters.com/a",
        title: "MRNA Q3 2026 earnings miss analyst estimates",
        summary: "Reuters EN",
        publishedAt: baseTime,
      }),
      makeArticle({
        url: "https://bloomberg.com/a",
        title: "MRNA Q3 2026 results disappoint analysts",
        summary: "Bloomberg EN",
        publishedAt: baseTime,
      }),
    ];
    // 両方英語 → cross-language dedup は適用しない
    const result = deduplicateCrossLanguage(articles);
    expect(result).toHaveLength(2);
  });

  it("元の配列を変更しない（immutable）", () => {
    const articles = [
      makeArticle({ url: "https://a.com", title: "MRNA Q3 2026 earnings" }),
      makeArticle({ url: "https://b.com", title: "MRNA 2026年3Q 決算" }),
    ];
    const originalLength = articles.length;
    deduplicateCrossLanguage(articles);
    expect(articles).toHaveLength(originalLength);
  });
});
```

---

### Task 4: filter.ts — `deduplicateCrossLanguage` 実装

**File:** `src/data/news/filter.ts`
**Action:** modify
**TDD:** Task 3 のテストをパスさせる実装

現 `deduplicateByTitle` (L83-117) の直後に以下を挿入:

```typescript
/**
 * 英語と日本語で同内容の記事を集約する (NEWS-03 / D-07, D-08, D-09)。
 * EN記事の固有名詞セットとJP記事の固有名詞セットを比較し、
 * 共通トークン >= 2 かつ時間差 <= 6h の場合に summary が長い方を残す。
 * 保守的アプローチ: 偽陽性より偽陰性を許容 (D-08)。
 */
export function deduplicateCrossLanguage(
  articles: ReadonlyArray<RawNewsArticle>,
): RawNewsArticle[] {
  const SIX_HOURS_MS = 6 * 60 * 60 * 1000;
  const excluded = new Set<number>();

  const enEntries = articles
    .map((a, i) => ({ a, i }))
    .filter(({ a }) => !isJapaneseTitle(a.title));

  const jpEntries = articles
    .map((a, i) => ({ a, i }))
    .filter(({ a }) => isJapaneseTitle(a.title));

  for (const { a: enArticle, i: enIdx } of enEntries) {
    if (excluded.has(enIdx)) continue;

    const enNouns = extractProperNouns(enArticle.title);
    if (enNouns.size === 0) continue;

    for (const { a: jpArticle, i: jpIdx } of jpEntries) {
      if (excluded.has(jpIdx)) continue;

      const timeDiff = Math.abs(
        enArticle.publishedAt.getTime() - jpArticle.publishedAt.getTime(),
      );
      if (timeDiff > SIX_HOURS_MS) continue;

      const jpNouns = extractProperNouns(jpArticle.title);
      const sharedCount = [...enNouns].filter((n) => jpNouns.has(n)).length;

      // D-08: 保守的。共通トークン2個以上でのみ集約する
      if (sharedCount < 2) continue;

      // D-09: summary が長い方を残す
      if (enArticle.summary.length >= jpArticle.summary.length) {
        excluded.add(jpIdx);
      } else {
        excluded.add(enIdx);
        break; // enIdx を除外したのでこの EN 記事のループを終了
      }
    }
  }

  return articles.filter((_, idx) => !excluded.has(idx));
}
```

---

### Task 5: types.ts — `afterCrossLangDedup` を `NewsFilterStats` に追加

**File:** `src/data/news/types.ts`
**Action:** modify
**TDD:** 型変更 → filter.ts の実装変更で使用 (Task 6)

現 L23-29 の `NewsFilterStats` を以下に置き換える:

```typescript
export interface NewsFilterStats {
  readonly raw: number;
  readonly afterUrlDedup: number;
  readonly afterTitleDedup: number;
  readonly afterCrossLangDedup: number;
  readonly afterRelevance: number;
  readonly final: number;
}
```

---

### Task 6: filter.ts — パイプラインに `deduplicateCrossLanguage` を挿入 + stats 更新

**File:** `src/data/news/filter.ts`
**Action:** modify
**TDD:** 既存テストがすべて PASS し続けること + Task 3 テストが PASS すること

現 `filterNewsArticles` 関数全体（L179-197 相当、Task 4-01 実施後の版）を以下に置き換える:

```typescript
/**
 * ニュース記事をフィルタリングしてスコア順にソートする。
 * Pass 1: URL dedup (DEDUP-01)
 * Pass 2: Title Jaccard dedup — 同言語内 (DEDUP-02)
 * Pass 2.5: Cross-language dedup — EN/JP 間 (NEWS-03)
 * Pass 3: Relevance filter — denylist + 投資キーワード例外 (FILT-01)
 * Pass 4: 24h time filter (FILT-02)
 * Pass 5: Priority score sort (NEWS-02)
 */
export function filterNewsArticles(
  articles: ReadonlyArray<RawNewsArticle>,
  portfolioTickers: ReadonlyArray<string> = [],
): NewsFilterResult {
  const afterUrlDedup = deduplicateByUrl(articles);
  const afterTitleDedup = deduplicateByTitle(afterUrlDedup);
  const afterCrossLangDedup = deduplicateCrossLanguage(afterTitleDedup);
  const afterRelevance = filterByRelevance(afterCrossLangDedup);
  const afterTime = filterByTime(afterRelevance);
  const finalArticles = sortByPriorityScore(afterTime, portfolioTickers);

  return {
    articles: finalArticles,
    stats: {
      raw: articles.length,
      afterUrlDedup: afterUrlDedup.length,
      afterTitleDedup: afterTitleDedup.length,
      afterCrossLangDedup: afterCrossLangDedup.length,
      afterRelevance: afterRelevance.length,
      final: afterTime.length,
    },
  };
}
```

---

### Task 7: filter.test.ts — 統合テストの stats 期待値を更新 + クロスランゲージ統合テスト追加

**File:** `src/data/news/filter.test.ts`
**Action:** modify
**TDD:** 既存統合テストが stats の新フィールドで失敗するため期待値を更新

**7-a: 既存統合テスト `filterNewsArticles integration` の期待値に `afterCrossLangDedup` を追加 (L274-329)**

現テスト内の `expect(result.stats.raw)` ブロックに `afterCrossLangDedup` を追加:

```typescript
    // 既存の検証に追加:
    expect(result.stats.afterCrossLangDedup).toBe(5); // cross-lang dedup による変化なし（EN/JPペアなし）
```

> 既存テストデータにはEN/JP同内容ペアが含まれていないため `afterCrossLangDedup === afterTitleDedup` になる。

**7-b: 統合テスト — クロスランゲージ dedupがパイプラインに組み込まれていることを確認**

```typescript
describe("filterNewsArticles cross-language integration (NEWS-03)", () => {
  it("EN/JP同内容記事ペアがパイプライン全体で1件に集約される", () => {
    const baseTime = new Date(Date.now() - 1 * 60 * 60 * 1000);
    const articles = [
      makeArticle({
        url: "https://reuters.com/mrna-q3-en",
        title: "MRNA Q3 2026 earnings miss analyst estimates forecast",
        summary: "Short EN.",
        source: "Reuters",
        publishedAt: baseTime,
      }),
      makeArticle({
        url: "https://jp.reuters.com/mrna-q3-jp",
        title: "MRNA 2026年3Q 決算ミス アナリスト予想下回る",
        summary: "こちらの方が長い日本語本文サマリーです",
        source: "ロイター",
        publishedAt: new Date(baseTime.getTime() + 15 * 60 * 1000),
      }),
    ];
    const result = filterNewsArticles(articles);
    expect(result.articles).toHaveLength(1);
    expect(result.articles[0].summary).toBe("こちらの方が長い日本語本文サマリーです");
    expect(result.stats.afterTitleDedup).toBe(2);    // 同言語dedupでは除外されない
    expect(result.stats.afterCrossLangDedup).toBe(1); // クロスランゲージで集約
  });

  it("statsにafterCrossLangDedupフィールドが含まれる", () => {
    const articles = [makeArticle({ url: "https://example.com/1" })];
    const result = filterNewsArticles(articles);
    expect(result.stats).toHaveProperty("afterCrossLangDedup");
    expect(typeof result.stats.afterCrossLangDedup).toBe("number");
  });
});
```

---

## Verification

- [ ] `npx vitest run src/data/news/` が全件 PASS
- [ ] `extractProperNouns("MRNA Q3 2026 earnings miss")` → Set に `"MRNA"`, `"Q3"`, `"2026"` を含む
- [ ] `extractProperNouns("MRNA 2026年3Q 決算ミス")` → Set に `"MRNA"`, `"2026"` を含む
- [ ] EN "MRNA Q3 2026..." と JP "MRNA 2026年3Q..." を `deduplicateCrossLanguage` に渡すと1件に集約される
- [ ] 共通名詞1個のペアは集約されない（保守的アプローチの確認）
- [ ] `filterNewsArticles` の stats に `afterCrossLangDedup` フィールドが存在する
- [ ] 既存の URL/Title dedup / relevance / time filter のテストがすべて PASS し続ける（リグレッションなし）
- [ ] `collect-data.ts` でのログ出力が変わらず動作する（stats フィールドは未参照のため影響なし）

## Rollback

1. `git revert HEAD` で 11-02 関連コミットを戻す
2. types.ts から `afterCrossLangDedup` を削除し、filterNewsArticles の stats オブジェクトから同フィールドを削除する
3. filter.ts から `extractProperNouns`, `deduplicateCrossLanguage` を削除し、パイプラインを Pass 1→2→3→4 に戻す
4. filter.test.ts の import 文から `extractProperNouns`, `deduplicateCrossLanguage` を削除する
5. 11-01 の変更（`filterNewsArticles` の `portfolioTickers` 引数）は独立しているため影響なし
