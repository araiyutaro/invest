---
phase: 03-report-builder-websearch-research
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/scripts/generate-report.ts
  - src/scripts/generate-report.test.ts
autonomous: true
requirements:
  - RPT-01
  - RPT-02
must_haves:
  truths:
    - "npx tsx src/scripts/generate-report.ts を実行すると reports/YYYY-MM-DD/daily-report.html が生成される"
    - "daily-report.html が Bloomberg 風ダークテーマ CSS (#0f0f1a 背景、.agent-card、.discussion-card クラス) を含んでいる"
    - "daily-report.html にモデレーターの統合見解（marketOverview.summary）が含まれている"
    - "daily-report.html に5アナリストのスコアリングマトリクスが含まれている"
    - "daily-report.html に WebSearch リサーチ結果セクションが含まれている"
    - "daily-report.html に再評価ラウンド結果セクションが含まれている"
    - "reports/YYYY-MM-DD/ ディレクトリが自動作成される"
    - "npm run test が全テスト PASS する"
  artifacts:
    - path: "src/scripts/generate-report.ts"
      provides: "Bloomberg風HTMLレポートジェネレータ"
      exports: ["main (CLI entry)"]
    - path: "src/scripts/generate-report.test.ts"
      provides: "レポートジェネレータのユニットテスト (Vitest)"
    - path: "reports/YYYY-MM-DD/daily-report.html"
      provides: "日次投資レポート (実行時生成)"
  key_links:
    - from: "src/scripts/generate-report.ts"
      to: "src/meeting/schemas.ts"
      via: "validateMeetingResult import"
      pattern: "import.*validateMeetingResult.*from.*meeting/schemas"
    - from: "src/scripts/generate-report.ts"
      to: "tmp/meeting-result.json"
      via: "fs/promises readFile"
      pattern: "readFile.*meeting-result\\.json"
    - from: "src/scripts/generate-report.ts"
      to: "tmp/websearch/"
      via: "fs/promises readdir + readFile"
      pattern: "readdir.*websearch"
    - from: "src/scripts/generate-report.ts"
      to: "tmp/reeval/"
      via: "fs/promises readdir + readFile"
      pattern: "readdir.*reeval"
    - from: "src/scripts/generate-report.ts"
      to: "reports/"
      via: "fs/promises writeFile"
      pattern: "writeFile.*reports"
    - from: "src/scripts/generate-report.ts"
      to: "src/meeting/schemas.ts"
      via: "Zod validation for websearch/reeval JSON"
      pattern: "import.*validateWebSearchResult.*validateReevaluationOutput.*from.*meeting/schemas"
---

<objective>
Bloomberg風ダークテーマHTMLレポートジェネレータ `src/scripts/generate-report.ts` をTDDで実装する。`tmp/meeting-result.json`、`tmp/websearch/*.json`、`tmp/reeval/*.json` を読み込み、全分析結果を統合した HTML レポートを `reports/YYYY-MM-DD/daily-report.html` に出力する。

Purpose: RPT-01 (Bloomberg風HTMLレポート出力) と RPT-02 (reports/YYYY-MM-DD/ 保存) を実現する。v1.0 generator.ts の CSS・HTML ユーティリティを流用しつつ、v2.0 MeetingResult 型に対応した新スクリプトを作成する（D-08, D-09）
Output: src/scripts/generate-report.ts, src/scripts/generate-report.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/arai/invest/.planning/PROJECT.md
@/Users/arai/invest/.planning/ROADMAP.md
@/Users/arai/invest/.planning/STATE.md
@/Users/arai/invest/.planning/phases/03-report-builder-websearch-research/03-CONTEXT.md
@/Users/arai/invest/.planning/phases/03-report-builder-websearch-research/03-RESEARCH.md
@/Users/arai/invest/.planning/phases/03-report-builder-websearch-research/03-PATTERNS.md
@/Users/arai/invest/.planning/phases/03-report-builder-websearch-research/03-VALIDATION.md

<interfaces>
From src/meeting/types.ts:
```typescript
export interface MeetingResult {
  readonly date: string;
  readonly generatedAt: string;
  readonly marketOverview: {
    readonly summary: string;
    readonly trend: "上昇" | "下降" | "混合";
    readonly keyIndices: ReadonlyArray<{ readonly name: string; readonly changePercent: number; }>;
  };
  readonly sectorRecommendations: ReadonlyArray<{
    readonly rank: number; readonly sector: string; readonly rationale: string; readonly outlook: "強気" | "中立" | "弱気";
  }>;
  readonly highlightedStocks: ReadonlyArray<{
    readonly ticker: string; readonly averageScore: number; readonly verdict: "強気" | "中立" | "弱気";
    readonly summary: string;
    readonly agentScores: ReadonlyArray<{ readonly agentRole: string; readonly score: number; readonly reason: string; }>;
    readonly nominatedBy: ReadonlyArray<string>;
  }>;
  readonly riskWarnings: ReadonlyArray<{ readonly severity: "高" | "中" | "低"; readonly description: string; }>;
  readonly actionItems: ReadonlyArray<string>;
  readonly weeklyEvents: ReadonlyArray<{ readonly date: string; readonly event: string; readonly impact: "高" | "中" | "低"; }>;
  readonly indexInvestorAdvice: string;
  readonly roundSummary: {
    readonly round1Count: number; readonly round2Count: number; readonly round3Count: number;
    readonly scoredTickers: ReadonlyArray<string>;
  };
}

export interface WebSearchResult {
  readonly ticker: string;
  readonly researchSummary: string;
  readonly positiveFindings: ReadonlyArray<string>;
  readonly negativeFindings: ReadonlyArray<string>;
  readonly keyArticles: ReadonlyArray<{ readonly title: string; readonly summary: string; }>;
  readonly researchedAt: string;
}

export interface ReevaluationOutput {
  readonly agentId: string;
  readonly agentRole: string;
  readonly reevaluations: ReadonlyArray<{
    readonly ticker: string; readonly originalScore: number; readonly revisedScore: number;
    readonly comment: string; readonly changed: boolean;
  }>;
}
```

From src/report/generator.ts (v1.0, 流用元):
```typescript
const HTML_STYLES = `<style>/* Bloomberg dark theme CSS */</style>`;
function escapeHtml(text: string): string;
function markdownToHtml(md: string): string;
function formatScoringHtml(record: MeetingRecord): string;
function formatResearchHtml(record: MeetingRecord): string;
```

From src/scripts/collect-data.ts (エントリポイントパターン):
```typescript
const TMP_DIR = join(import.meta.dirname, "../../tmp");
main().catch((error) => { console.error("Fatal error:", error); process.exit(1); });
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: generate-report テスト作成 (RED フェーズ)</name>
  <files>src/scripts/generate-report.test.ts</files>
  <read_first>
    - /Users/arai/invest/src/scripts/collect-data.test.ts — テストパターン参照（vi.mock, beforeEach/afterEach 構成、fs/promises モックパターン）
    - /Users/arai/invest/src/scripts/validate-meeting.test.ts — 別のテストファイルのパターン確認
    - /Users/arai/invest/src/meeting/types.ts — MeetingResult, WebSearchResult, ReevaluationOutput の型定義を確認
    - /Users/arai/invest/src/report/generator.ts — v1.0 HTML_STYLES, escapeHtml, markdownToHtml, formatScoringHtml の実装を確認
  </read_first>
  <behavior>
    - Test 1: generateHtml が MeetingResult + WebSearchResult[] + ReevaluationOutput[] を受け取り HTML 文字列を返す
    - Test 2: HTML 出力に Bloomberg 風ダークテーマの CSS class が含まれる（"agent-card" と "0f0f1a"）
    - Test 3: HTML 出力に marketOverview.summary の内容が含まれる
    - Test 4: HTML 出力に highlightedStocks のティッカーとスコアが含まれる
    - Test 5: HTML 出力に WebSearch リサーチ結果セクション（researchSummary）が含まれる
    - Test 6: HTML 出力に再評価ラウンド結果セクション（revisedScore）が含まれる
    - Test 7: WebSearchResult が空配列の場合でも HTML が正常に生成される
    - Test 8: ReevaluationOutput が空配列の場合でも HTML が正常に生成される
    - Test 9: main() が reports/YYYY-MM-DD/daily-report.html にファイルを書き出す（fs mock 経由で確認）
    - Test 10: escapeHtml が HTML 特殊文字 (&, <, >) をエスケープする

    モック方針:
    - vi.mock("node:fs/promises") で readFile, writeFile, mkdir, readdir をモックする
    - readFile モックは meeting-result.json, websearch/*.json, reeval/*.json の内容を返す
    - readdir モックは websearch/ と reeval/ のファイルリストを返す
    - テスト対象: generateHtml は純粋関数としてテスト（ファイルI/Oなし）、main は fs モック経由でテスト
  </behavior>
  <action>
    src/scripts/generate-report.test.ts を新規作成する:

    1. import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
    2. テストデータ定義:
       - validMeetingResult: MeetingResult 型のフルデータ（date: "2026-06-24", marketOverview, sectorRecommendations, highlightedStocks (ticker: "PLTR", averageScore: 8.2, verdict: "強気"), riskWarnings, actionItems, weeklyEvents, indexInvestorAdvice, roundSummary）
       - validWebSearchResults: WebSearchResult[] のデータ（ticker: "PLTR", researchSummary: "AI需要拡大で成長加速", positiveFindings, negativeFindings, keyArticles）
       - validReevalResults: ReevaluationOutput[] のデータ（agentId: "fundamentals", reevaluations: [{ticker: "PLTR", originalScore: 8, revisedScore: 9, comment: "Web調査で成長確認", changed: true}]）

    3. describe("generate-report") 内に上記 Test 1〜10 を記述
    4. vi.mock でファイルI/Oをモック
    5. afterEach で vi.restoreAllMocks()

    テストが RED で終わることを確認:
    npm run test -- src/scripts/generate-report.test.ts が失敗
    commit: test(03-02): add failing tests for generate-report script
  </action>
  <verify>
    <automated>npm run test -- src/scripts/generate-report.test.ts 2>&1; test $? -ne 0 && echo "RED_CONFIRMED: tests fail as expected" || echo "ERROR: tests should fail in RED phase"</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/generate-report.test.ts が存在する
    - テストファイルに10個の it() ブロックが含まれる (grep -c "it(" = 10)
    - テストデータに MeetingResult, WebSearchResult, ReevaluationOutput の型に準拠したオブジェクトが定義されている
    - npm run test -- src/scripts/generate-report.test.ts が「Cannot find module」等のエラーで失敗する（RED確認）
    - git commit が作成されている
  </acceptance_criteria>
  <done>テストファイルが存在し全テストが失敗（RED）状態。</done>
</task>

<task type="tdd">
  <name>Task 2: generate-report.ts 実装 (GREEN フェーズ)</name>
  <files>src/scripts/generate-report.ts</files>
  <read_first>
    - /Users/arai/invest/src/scripts/generate-report.test.ts — Task 1 で作成したテストの期待値を確認してから実装する
    - /Users/arai/invest/src/report/generator.ts — v1.0 の HTML_STYLES (lines 55-158), escapeHtml (lines 7-12), markdownToHtml (lines 14-53), formatScoringHtml (lines 161-210), formatResearchHtml (lines 251-270) を流用元として確認する
    - /Users/arai/invest/src/scripts/validate-meeting.ts — エントリポイントパターン参照（import.meta.dirname, main().catch）
    - /Users/arai/invest/src/meeting/schemas.ts — validateMeetingResult の API 確認
    - /Users/arai/invest/.planning/phases/03-report-builder-websearch-research/03-PATTERNS.md — generate-report.ts のパターン詳細を確認
    - /Users/arai/invest/.planning/phases/03-report-builder-websearch-research/03-RESEARCH.md — Pattern 3 (HTMLレポートジェネレータ) と Common Pitfalls を確認
  </read_first>
  <behavior>
    実装すべき動作（テスト通過基準）:
    - escapeHtml(text): &, <, > を HTML エンティティに変換する
    - markdownToHtml(md): v1.0 generator.ts の実装をそのまま流用する
    - generateHtml(meetingResult, webSearchResults, reevalResults): MeetingResult + WebSearchResult[] + ReevaluationOutput[] を受け取り完全な HTML 文字列を返す
    - HTML に Bloomberg 風 CSS (#0f0f1a, .agent-card, .discussion-card) を含む
    - HTML に marketOverview.summary, highlightedStocks のスコアリングテーブル, WebSearch 結果, 再評価結果を含む
    - WebSearchResult が空配列の場合はリサーチセクションを省略する
    - ReevaluationOutput が空配列の場合は再評価セクションを省略する
    - main() は tmp/meeting-result.json, tmp/websearch/*.json, tmp/reeval/*.json を読み込み、reports/{date}/daily-report.html に書き出す
    - reports/{date}/ ディレクトリを mkdir({ recursive: true }) で自動作成する
  </behavior>
  <action>
    src/scripts/generate-report.ts を新規作成する:

    ファイル先頭:
    - import { readFile, writeFile, mkdir, readdir } from "node:fs/promises"
    - import { join } from "node:path"
    - import { validateMeetingResult } from "../meeting/schemas.js"
    - import type { MeetingResult, WebSearchResult, ReevaluationOutput } from "../meeting/types.js"

    定数定義:
    - const TMP_DIR = join(import.meta.dirname, "../../tmp")
    - const REPORTS_DIR = join(import.meta.dirname, "../../reports")

    ユーティリティ関数（v1.0 generator.ts から流用）:
    - escapeHtml(): src/report/generator.ts lines 7-12 と同一ロジック（&, <, > の3文字をエスケープ）
    - markdownToHtml(): src/report/generator.ts lines 14-53 と同一ロジック
    - HTML_STYLES: src/report/generator.ts lines 55-158 と同一の CSS 定数

    HTML セクション生成関数群:
    - formatMarketOverviewHtml(result: MeetingResult): string — marketOverview.summary, trend, keyIndices をカード表示
    - formatSectorRecommendationsHtml(result: MeetingResult): string — sectorRecommendations をランク順テーブル表示
    - formatHighlightedStocksHtml(result: MeetingResult): string — highlightedStocks のスコアリングマトリクス。v1.0 formatScoringHtml のフィールドマッピングを変更: scoreSummaries -> highlightedStocks, agentScores -> agentScores。scoreColor: >=8 #10b981, >=6 #60a5fa, >=4 #f59e0b, else #ef4444。verdictColor: 強気 #10b981, 弱気 #ef4444, else #f59e0b
    - formatWebSearchHtml(results: ReadonlyArray<WebSearchResult>): string — results.length === 0 なら空文字列を返す。各銘柄を agent-card (border-left-color: #10b981) で表示、researchSummary + positiveFindings/negativeFindings リスト + keyArticles リスト
    - formatReevalHtml(reevals: ReadonlyArray<ReevaluationOutput>): string — reevals.length === 0 なら空文字列を返す。各アナリストの再評価を agent-card (border-left-color: #f59e0b) で表示、changed === true の銘柄のみ originalScore -> revisedScore の変化を表示
    - formatRiskWarningsHtml(result: MeetingResult): string — riskWarnings を severity の色分けで表示（高: #ef4444, 中: #f59e0b, 低: #10b981）
    - formatActionItemsHtml(result: MeetingResult): string — actionItems をリスト表示
    - formatWeeklyEventsHtml(result: MeetingResult): string — weeklyEvents をテーブル表示

    メイン HTML 組み立て関数:
    - export function generateHtml(result: MeetingResult, webSearchResults: ReadonlyArray<WebSearchResult>, reevalResults: ReadonlyArray<ReevaluationOutput>): string — 上記セクションを統合した完全な HTML ドキュメントを返す。セクション順序: タイトル → 市場概況 → セクター推奨 → 注目銘柄スコアリング → WebSearch リサーチ → 再評価ラウンド → リスク警告 → アクションアイテム → 週間イベント → インデックス投資アドバイス

    ファイル I/O 関数群:
    - loadWebSearchResults(): Promise<ReadonlyArray<WebSearchResult>> — tmp/websearch/ ディレクトリの *.json を readdir + readFile で読み込み。各ファイルを validateWebSearchResult() で Zod バリデーションし、失敗したエントリはスキップ（空配列にフォールバック）。ディレクトリ不在時も空配列を返す
    - loadReevalResults(): Promise<ReadonlyArray<ReevaluationOutput>> — tmp/reeval/ ディレクトリの *.json を readdir + readFile で読み込み。各ファイルを validateReevaluationOutput() で Zod バリデーションし、失敗したエントリはスキップ。フォールバック同上
    - import { validateMeetingResult, validateWebSearchResult, validateReevaluationOutput } from "../meeting/schemas.js" で3つのバリデーション関数をインポートする

    main() 関数:
    1. console.log("レポート生成開始...")
    2. const raw = await readFile(join(TMP_DIR, "meeting-result.json"), "utf-8")
    3. const meetingResult = validateMeetingResult(JSON.parse(raw) as unknown)
    4. const webSearchResults = await loadWebSearchResults()
    5. const reevalResults = await loadReevalResults()
    6. const html = generateHtml(meetingResult, webSearchResults, reevalResults)
    7. const dateDir = join(REPORTS_DIR, meetingResult.date)
    8. await mkdir(dateDir, { recursive: true })
    9. const reportPath = join(dateDir, "daily-report.html")
    10. await writeFile(reportPath, html, "utf-8")
    11. console.log("レポート生成完了: reports/" + meetingResult.date + "/daily-report.html")

    ファイル末尾:
    - main().catch((error) => { console.error("Fatal error:", error); process.exit(1); })

    禁止事項:
    - v1.0 の generator.ts を import しない（依存を避け独立スクリプトにする）
    - docs/ への出力はしない（D-09: reports/ に変更）
    - チャート画像関連のコードは含めない（v2.0でテキストベースに統一）
    - meeting-minutes.html は生成しない（v2.0では daily-report.html のみ出力。v1.0のsaveReports()が2ファイル出力していたパターンとは異なる）

    実装後にテスト GREEN 確認:
    npm run test -- src/scripts/generate-report.test.ts が全テスト PASS すること
    commit: feat(03-02): implement Bloomberg-style HTML report generator
  </action>
  <verify>
    <automated>npm run test -- src/scripts/generate-report.test.ts</automated>
    <automated>npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/generate-report.ts が存在する
    - npm run test -- src/scripts/generate-report.test.ts が全テスト PASS する (0 failures)
    - src/scripts/generate-report.ts に generateHtml がエクスポートされている (grep -c "export function generateHtml" = 1)
    - src/scripts/generate-report.ts に const REPORTS_DIR が含まれる (grep -c "REPORTS_DIR" >= 1)
    - src/scripts/generate-report.ts に "../../reports" が含まれる (v1.0 の docs/ ではない)
    - src/scripts/generate-report.ts に "0f0f1a" が含まれる (Bloomberg ダークテーマ背景色)
    - src/scripts/generate-report.ts に "agent-card" が含まれる (CSS class)
    - src/scripts/generate-report.ts に validateMeetingResult import が含まれる
    - src/scripts/generate-report.ts に readdir が含まれる (websearch/reeval ディレクトリ読み込み)
    - src/scripts/generate-report.ts に process.exit(1) が含まれる
    - src/scripts/generate-report.ts に "docs/" パスが含まれない (grep -c "docs/" = 0)
    - npx tsc --noEmit でエラーなし
    - git commit が作成されている
  </acceptance_criteria>
  <done>全テストが GREEN。generate-report.ts が Bloomberg 風 HTML レポートを reports/YYYY-MM-DD/ に出力する。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/meeting-result.json → generate-report.ts | Phase 2 出力の JSON を読み込み。Zod バリデーション済みだが再検証する |
| tmp/websearch/*.json → generate-report.ts | WebSearch Agent 出力を読み込み。不正 JSON はスキップ |
| tmp/reeval/*.json → generate-report.ts | 再評価 Agent 出力を読み込み。不正 JSON はスキップ |
| generate-report.ts → reports/ HTML | 生成した HTML をファイルシステムに書き出し |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | HTML injection via JSON content | mitigate | escapeHtml() で全テキストコンテンツを HTML エスケープしてから埋め込む。XSS 防止 |
| T-03-07 | Information Disclosure | reports/ ディレクトリ | accept | ローカルファイル。公開サーバーにデプロイしない前提。個人利用限定 |
| T-03-08 | Denial of Service | 巨大 meeting-result.json | accept | 実際のデータサイズは通常 50KB 未満。個人利用限定 |
| T-03-SC | Tampering | npm installs | accept | 新規パッケージインストールなし |
</threat_model>

<verification>
Phase 3 Plan 02 完了確認:

1. `npm run test -- src/scripts/generate-report.test.ts` → 全テスト PASS
2. `npx tsc --noEmit` → エラーなし
3. `grep -c "export function generateHtml" /Users/arai/invest/src/scripts/generate-report.ts` → 1
4. `grep -c "REPORTS_DIR" /Users/arai/invest/src/scripts/generate-report.ts` → 1 以上
5. `grep -c "0f0f1a" /Users/arai/invest/src/scripts/generate-report.ts` → 1 以上 (Bloomberg ダークテーマ)
6. `grep -c "agent-card" /Users/arai/invest/src/scripts/generate-report.ts` → 1 以上
7. `grep -v "^//" /Users/arai/invest/src/scripts/generate-report.ts | grep -c "docs/"` → 0 (v1.0 出力先を使っていない)
</verification>

<success_criteria>
- src/scripts/generate-report.ts が存在し、Vitest テストが全 PASS する
- generateHtml() が MeetingResult + WebSearchResult[] + ReevaluationOutput[] を統合した HTML を返す
- Bloomberg風ダークテーマ CSS が含まれている
- レポートに全セクション（市場概況、セクター推奨、注目銘柄スコアリング、WebSearchリサーチ、再評価、リスク警告、アクションアイテム、週間イベント、インデックス投資アドバイス）が含まれている
- 出力先が reports/YYYY-MM-DD/daily-report.html（D-09）
- D-08（TSスクリプト実装）と D-09（reports/ 出力先）の決定事項が反映されている
- RPT-01（Bloomberg風HTMLレポート）と RPT-02（reports/YYYY-MM-DD/ 保存）の要件が満たされている
</success_criteria>

<output>
完了後に `.planning/phases/03-report-builder-websearch-research/03-02-SUMMARY.md` を作成する
</output>
