---
phase: 25-urgency-history-persistence
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/scripts/write-urgency-history.ts
  - src/scripts/write-urgency-history.test.ts
  - .claude/commands/invest.md
autonomous: true
requirements: [HIST-01, HIST-02]

must_haves:
  truths:
    - "Running write-urgency-history.ts with a valid tmp/portfolio-analysis.json + tmp/meeting-result.json writes data/urgency-history.json under the meeting-result date key with all holdings' snapshots"
    - "mkdir(DATA_DIR, {recursive:true}) runs as the first statement in main() so data/ exists on disk regardless of any downstream skip/fail branch"
    - "Missing or 0-holding tmp/portfolio-analysis.json skips the write, leaves existing history untouched, exits 0 (D-13)"
    - "A corrupted/unparseable existing data/urgency-history.json is preserved (not overwritten) and the script exits 1 (D-14)"
    - "An invalid dateKey (fails isValidDateKey) causes no write and exit 1 (D-06)"
    - "invest.md contains a new Step 3f that runs write-urgency-history.ts and emits [STEP:urgency-history:OK] / [STEP:urgency-history:FAIL:...], never [PIPELINE:FAIL]"
    - "invest.md Step 4 stages both docs/ and data/ (git add docs/ data/) after guarding data/ existence"
  artifacts:
    - path: "src/scripts/write-urgency-history.ts"
      provides: "Fail-soft CLI wrapper: reads tmp inputs, merges via pure functions, writes data/urgency-history.json"
      exports: ["main"]
      min_lines: 40
    - path: "src/scripts/write-urgency-history.test.ts"
      provides: "vi.mock('node:fs/promises') tests for normal/skip/corrupted/invalid-date paths"
      contains: "vi.mock(\"node:fs/promises\""
    - path: ".claude/commands/invest.md"
      provides: "Step 3f pipeline step + Step 4 git add extension"
      contains: "[STEP:urgency-history:OK]"
  key_links:
    - from: "src/scripts/write-urgency-history.ts"
      to: "src/portfolio/urgency-history.ts"
      via: "import extract/append/isValidDateKey"
      pattern: "from \"../portfolio/urgency-history.js\""
    - from: "src/scripts/write-urgency-history.ts"
      to: "tmp/meeting-result.json"
      via: "dateKey source (Step 4 alignment)"
      pattern: "meeting-result\\.json"
    - from: ".claude/commands/invest.md"
      to: "data/"
      via: "git add docs/ data/"
      pattern: "git add docs/ data/"
---

<objective>
Wire the Plan 01 pure functions into the daily pipeline: a thin fail-soft CLI wrapper `src/scripts/write-urgency-history.ts` persists the daily urgency snapshot to `data/urgency-history.json`, and `.claude/commands/invest.md` gains a dedicated Step 3f (runs the script, emits `[STEP:urgency-history:*]`) plus a Step 4 `git add docs/ data/` extension so the history is committed and pushed alongside the reports. This completes HIST-01 (persistence + git) and HIST-02 (same-day guard reaching disk).

Purpose: Closes the persistence loop with strict fail-soft isolation — no failure here may block the existing 4 reports or the docs/ deploy. The unconditional `mkdir(DATA_DIR)` first-statement ordering is the single highest-impact correctness detail (verified pitfall: `git add` on a non-existent pathspec exits 128 and would crash the whole deploy).
Output: `src/scripts/write-urgency-history.ts` + `src/scripts/write-urgency-history.test.ts` (green) and the two edits to `.claude/commands/invest.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-urgency-history-persistence/25-CONTEXT.md
@.planning/phases/25-urgency-history-persistence/25-RESEARCH.md
@.planning/phases/25-urgency-history-persistence/25-PATTERNS.md

<interfaces>
<!-- Consumed from Plan 01 (src/portfolio/urgency-history.ts). Use these directly. -->
```typescript
export interface HoldingUrgencySnapshot { readonly symbol: string; readonly nameJa: string; readonly urgent: boolean; readonly decision: "保持" | "買増" | "一部売却" | "全売却"; }
export type UrgencyHistoryFile = Record<string, ReadonlyArray<HoldingUrgencySnapshot>>;
export function extractUrgencySnapshots(analysis: PortfolioAnalysis): ReadonlyArray<HoldingUrgencySnapshot>;
export function appendUrgencySnapshot(history: UrgencyHistoryFile, dateKey: string, snapshots: ReadonlyArray<HoldingUrgencySnapshot>): UrgencyHistoryFile;
export function isValidDateKey(dateKey: string): boolean;
```

<!-- CLI-wrapper analog: src/scripts/write-news-digest.ts main() shape (mkdir-first, meeting-result date, fail-soft, exit-code-as-signal). -->
<!-- Read style analog: src/scripts/report-data-loaders.ts loadHoldingNews (plain JSON.parse + try/catch, no zod for self-generated artifacts). -->
<!-- Test analog: src/scripts/write-news-digest.test.ts (vi.mock node:fs/promises, path-substring readFile routing, process.exit spy). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail-soft CLI wrapper write-urgency-history.ts + tests</name>
  <files>src/scripts/write-urgency-history.ts, src/scripts/write-urgency-history.test.ts</files>
  <read_first>
    - src/scripts/write-news-digest.ts (primary analog: main() with mkdir-first, meeting-result.json date read, try/catch fail-soft, process.exit(1) signal, `if (process.argv[1] === fileURLToPath(import.meta.url))` guard)
    - src/scripts/write-news-digest.test.ts (test analog: vi.mock("node:fs/promises"), beforeEach/afterEach mock clearing + process.exit spy, path-substring readFile routing, exit-code assertions)
    - src/scripts/report-data-loaders.ts (loadHoldingNews L121-133 — plain JSON.parse + try/catch for self-generated artifacts, no zod)
    - src/portfolio/urgency-history.ts (Plan 01 output — the functions being wired)
    - .planning/phases/25-urgency-history-persistence/25-RESEARCH.md (Pattern 3 full main() reference impl + Pitfall 1 mkdir-first + Pitfall 2 dateKey source + Pitfall 4 no-zod read)
    - .planning/phases/25-urgency-history-persistence/25-PATTERNS.md (write-urgency-history.ts section)
  </read_first>
  <behavior>
    - 正常系: given a valid tmp/portfolio-analysis.json (12 holdings) and tmp/meeting-result.json ({date:"2026-07-04"}) and no existing history, main() calls writeFile on a path including "urgency-history.json" with JSON whose "2026-07-04" key holds all 12 snapshots; process.exit NOT called with 1
    - 同日上書き: given an existing history already containing "2026-07-04", main() writes a history where that key holds ONLY the new snapshots (single key, no duplication) — HIST-02 reaching disk
    - skip (D-13): tmp/portfolio-analysis.json missing (readFile rejects ENOENT) OR holdings length 0 → NO writeFile of urgency-history.json, process.exit NOT called with 1, exits 0
    - corrupted (D-14): existing data/urgency-history.json unparseable (readFile resolves but JSON.parse throws) → existing file NOT overwritten (no writeFile to urgency-history.json), process.exit called with 1
    - 不正なdate (D-06): meeting-result.json date is "2026-7-4" (fails isValidDateKey) → NO writeFile, process.exit called with 1
    - mkdir(DATA_DIR) is called on every invocation before any skip/fail branch
  </behavior>
  <action>
    Create src/scripts/write-urgency-history.ts mirroring write-news-digest.ts's main() shape. Imports: `readFile, writeFile, mkdir` from "node:fs/promises"; `join` from "node:path"; `fileURLToPath` from "node:url"; `extractUrgencySnapshots, appendUrgencySnapshot, isValidDateKey` and `type UrgencyHistoryFile` from "../portfolio/urgency-history.js"; `type PortfolioAnalysis` from "../meeting/types.js". Constants: `TMP_DIR = join(import.meta.dirname, "../../tmp")`, `DATA_DIR = join(import.meta.dirname, "../../data")`, `HISTORY_PATH = join(DATA_DIR, "urgency-history.json")`. In main(), ordering is strict: (1) `await mkdir(DATA_DIR, { recursive: true })` as the UNCONDITIONAL FIRST STATEMENT (Pitfall 1 — data/ must exist on disk before Step 4's git add docs/ data/, regardless of any later branch); (2) load existing history via a plain readFile+JSON.parse in try/catch — on ENOENT treat as empty object (first run, normal), on any OTHER parse/read error treat as corrupted → console.error a `[urgency-history] FAIL:` line and process.exit(1) WITHOUT writing (D-14 preserve); (3) read tmp/portfolio-analysis.json via plain readFile+JSON.parse in its own try/catch (NOT loadPortfolioAnalysis / no zod — matches loadHoldingNews self-generated-artifact style, Pitfall 4); if null OR holdings is not a non-empty array → console.error a `[urgency-history] OK (skip: ...D-13)` line and return (exit 0, no write); (4) read tmp/meeting-result.json and take `.date` as dateKey (D-05 — this exact field aligns with Step 4's docs/{date}/ and commit message, Pitfall 2 — do NOT use portfolioAnalysis.date and do NOT re-derive JST); (5) if `!isValidDateKey(dateKey)` → console.error `[urgency-history] FAIL: 不正なdateキー形式: ${dateKey}` and process.exit(1), no write (D-06); (6) `const snapshots = extractUrgencySnapshots(analysis)`; `const updated = appendUrgencySnapshot(existingHistory, dateKey, snapshots)`; `await writeFile(HISTORY_PATH, JSON.stringify(updated, null, 2), "utf-8")`; console.error a `[urgency-history] OK: ...` line. Use console.error (never console.log) for all STEP-marker-adjacent signal lines, matching write-news-digest.ts's `[digest-crossref] OK/FAIL` convention. Add the `if (process.argv[1] === fileURLToPath(import.meta.url)) { main().catch((error) => { console.error("Fatal error:", error); process.exit(1); }); }` guard. Then create src/scripts/write-urgency-history.test.ts using vi.mock("node:fs/promises") with the beforeEach/afterEach mock-clear + process.exit spy shape from write-news-digest.test.ts, route mocked readFile by path substring (meeting-result.json / portfolio-analysis.json / urgency-history.json), and cover all six behavior cases. Use toEqual not toBe. Run both files; they MUST pass. Commit: `feat(25-02): add fail-soft write-urgency-history CLI wrapper + tests`.
  </action>
  <verify>
    <automated>npx vitest run src/scripts/write-urgency-history.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/scripts/write-urgency-history.ts` first statement inside `main()` (after any leading comments) is `await mkdir(DATA_DIR, { recursive: true });` — appears before any readFile/skip/fail branch
    - `src/scripts/write-urgency-history.ts` contains `from "../portfolio/urgency-history.js"` and reads the dateKey from a file whose path includes `meeting-result.json`
    - `src/scripts/write-urgency-history.ts` contains `isValidDateKey(` gating before `writeFile(HISTORY_PATH`
    - `src/scripts/write-urgency-history.ts` does NOT import zod and does NOT call loadPortfolioAnalysis (grep: no `zod`, no `loadPortfolioAnalysis`)
    - `src/scripts/write-urgency-history.test.ts` contains `vi.mock("node:fs/promises"` and asserts `process.exit` NOT called with 1 for the skip case and called with 1 for corrupted + invalid-date cases
    - `npx vitest run src/scripts/write-urgency-history.test.ts` exits 0
    - `npx vitest run` (full suite) exits 0
    - No `console.log` in either file (`grep -v '^\s*//' src/scripts/write-urgency-history.ts | grep -c 'console.log'` returns 0)
  </acceptance_criteria>
  <done>CLI wrapper implemented with mkdir-first ordering and all four fail-soft branches; test suite green; dateKey sourced from meeting-result.json.</done>
</task>

<task type="auto">
  <name>Task 2: invest.md Step 3f insertion + Step 4 git add extension</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md §Step 3e (news-digest step template — the structure to copy for Step 3f: run script, "終了コードに関わらず Step 4 へ", OK/FAIL echo, `[PIPELINE:FAIL]` 禁止 note)
    - .claude/commands/invest.md §Step 4 deploy block (the `execSync('git add docs/'` line, the `const fs = require('fs')` at the top of the node -e block, the `/^\d{4}-\d{2}-\d{2}$/` date validation, git diff --staged --quiet / commit / push)
    - .planning/phases/25-urgency-history-persistence/25-RESEARCH.md (Code Examples: invest.md new step + Step 4 deploy script extension diff; Pitfall 1)
    - .planning/phases/25-urgency-history-persistence/25-PATTERNS.md (invest.md Step 3f + Step 4 extension section, exact diff targets)
  </read_first>
  <action>
    Two edits to .claude/commands/invest.md, no renumbering of existing steps:
    (1) Insert a NEW `### Step 3f: 緊急度履歴の追記` block immediately AFTER the end of the existing Step 3e (news-digest) block and BEFORE the `## Step 4` boundary. Structure it as a direct substitution of the Step 3e template: instruct running `cd /Users/arai/invest && npx tsx src/scripts/write-urgency-history.ts`; then "スクリプトの終了コードに関わらず、Step 4 へ進んでください（fail-soft, D-09）"; on exit code 0 echo `[STEP:urgency-history:OK]`; on non-zero exit echo `[STEP:urgency-history:FAIL:<短い理由>]` (summarizing the `[urgency-history] FAIL:` stderr line); and the bold note `**[PIPELINE:FAIL] は絶対に出力しないこと**` explaining this failure does not block the existing 3 reports, news-digest, or deploy (HIST-01/HIST-02 persistence failure must not halt the pipeline).
    (2) In the Step 4 deploy `node -e` block, replace the single `execSync('git add docs/', { stdio: 'inherit' });` line with a two-line sequence: first `if (!fs.existsSync('data')) fs.mkdirSync('data', { recursive: true });` (defense-in-depth belt-and-suspenders guard — `fs` is already required at the top of this same block, no new import), then `execSync('git add docs/ data/', { stdio: 'inherit' });` (D-11 — stage both dirs into the one existing commit). Add a short comment noting data/ is guaranteed to exist by Step 3f's mkdir but is guarded here anyway (Pitfall 1: `git add` on a non-existent pathspec exits 128 and would crash the whole deploy). Do NOT change the git diff --staged --quiet check, the `report: {date} daily update` commit message, or the `git push origin master` line — they operate on whatever is staged. Commit: `feat(25-02): add invest.md Step 3f + stage data/ in Step 4 deploy`.
  </action>
  <verify>
    <automated>grep -q "\[STEP:urgency-history:OK\]" .claude/commands/invest.md && grep -q "write-urgency-history.ts" .claude/commands/invest.md && grep -q "git add docs/ data/" .claude/commands/invest.md && grep -q "existsSync('data')" .claude/commands/invest.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/commands/invest.md` contains a `### Step 3f: 緊急度履歴の追記` heading located after Step 3e and before `## Step 4`
    - `.claude/commands/invest.md` contains `npx tsx src/scripts/write-urgency-history.ts`
    - `.claude/commands/invest.md` contains both `[STEP:urgency-history:OK]` and `[STEP:urgency-history:FAIL:` markers
    - `.claude/commands/invest.md` Step 3f contains the bold `[PIPELINE:FAIL] は絶対に出力しないこと` note
    - `.claude/commands/invest.md` Step 4 contains `git add docs/ data/` and `existsSync('data')`
    - `.claude/commands/invest.md` still contains exactly one `git push origin master` and the unchanged `report:` commit message (no accidental deploy-logic edits)
  </acceptance_criteria>
  <done>Step 3f runs the writer and emits dedicated OK/FAIL markers without [PIPELINE:FAIL]; Step 4 stages docs/ and data/ into the existing single commit with a data/ existence guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/meeting-result.json → dateKey → object key + persisted file | The date value flows from an internal pipeline artifact into a JSON object key and sits adjacent to the git deploy step; it must be format-validated before use |
| tmp/portfolio-analysis.json → data/urgency-history.json | Self-generated artifact read then persisted to a git-tracked file; a corrupt read must not destroy the existing audit history |
| write step → git add/commit/push (Step 4) | A crash in the new step or a missing data/ dir must not break the pre-existing docs/ deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | dateKey validation in write-urgency-history.ts | mitigate | `isValidDateKey(dateKey)` gate (`/^\d{4}-\d{2}-\d{2}$/`, D-06) runs before extractUrgencySnapshots/appendUrgencySnapshot/writeFile; malformed or prototype-pollution-style keys ('__proto__', arbitrary text) are rejected and cause exit 1 with no write. Unit test asserts the invalid-date path performs no writeFile and exits 1. |
| T-25-02 | Tampering / Integrity | loadExistingHistory (D-14) | mitigate | On a non-ENOENT read/parse error the existing data/urgency-history.json is preserved (never overwritten): the script emits `[urgency-history] FAIL:` and exits 1 without calling writeFile. The file is git-tracked, so even a hypothetical bad overwrite is recoverable via git history. Unit test asserts corrupted-existing-file case does NOT writeFile and exits 1. |
| T-25-03 | Denial of Service (deploy) | invest.md Step 4 `git add docs/ data/` | mitigate | `write-urgency-history.ts` runs `mkdir(DATA_DIR, {recursive:true})` as the unconditional first statement of main(), and Step 4 additionally guards `if (!fs.existsSync('data')) fs.mkdirSync('data', {recursive:true})` before the git add — preventing the verified exit-128 `git add` crash on a non-existent pathspec that would otherwise break the existing docs/ deploy. |
| T-25-04 | Information Disclosure | data/urgency-history.json contents | accept | The file persists only symbol/nameJa/urgent/decision — no secrets, credentials, or PII. It is committed to the private data/ directory (not public docs/), consistent with Success Criteria #2. No sensitive data leaks. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | Phase introduces ZERO new packages (RESEARCH.md Package Legitimacy Audit: not applicable). No install task exists; nothing to slopcheck. |
</threat_model>

<verification>
- `npx vitest run src/scripts/write-urgency-history.test.ts` green (normal, same-day overwrite, skip, corrupted, invalid-date)
- `npx vitest run` full suite green — no regression
- `.claude/commands/invest.md` contains Step 3f markers and `git add docs/ data/` with a data/ existence guard
- No `console.log` in the new script
- Manual (pipeline run, tracked in 25-HUMAN-UAT if generated): `[STEP:urgency-history:OK]` appears in pipeline logs and `git log` shows data/urgency-history.json in the report commit
</verification>

<success_criteria>
- src/scripts/write-urgency-history.ts exports main(), mkdir-first, dateKey from meeting-result.json, all four fail-soft branches
- Test suite green covering HIST-01 (persistence) and HIST-02 (same-day overwrite reaching disk)
- invest.md Step 3f emits dedicated OK/FAIL markers, never [PIPELINE:FAIL]
- invest.md Step 4 stages docs/ and data/ into the existing commit/push flow
</success_criteria>

<output>
Create `.planning/phases/25-urgency-history-persistence/25-02-SUMMARY.md` when done
</output>
