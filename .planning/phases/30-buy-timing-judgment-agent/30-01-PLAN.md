---
phase: 30-buy-timing-judgment-agent
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/meeting/types.ts
  - src/meeting/schemas.ts
  - src/meeting/schemas.test.ts
  - src/portfolio/watchlist-judgment.ts
  - src/portfolio/watchlist-judgment.test.ts
autonomous: true
requirements:
  - TIME-02
  - TIME-03
  - TIME-04
  - TIME-05
must_haves:
  truths:
    - "watchlistJudgmentSchema がフィールド名ゆらぎ（action/verdict/buyToday/日本語値）を todayAction の二値 enum に正準化し、不正入力でも throw しない"
    - "todayAction=buy かつ signals<2件 の判定は applyConfluenceGate が決定論的に wait へ降格しログを出す"
    - "attachActionChanges が前日判定との todayAction 等値比較のみで previousAction/actionChanged を付与し、prev なし銘柄にはプロパティ自体を付与しない"
    - "deriveMarket が .T サフィックスから US/JP を決定論導出し、buildSkippedJudgment がテクニカル欠落銘柄を status:skipped の陽性レコードにする"
    - "asOf/market/previousAction/actionChanged は raw スキーマに存在せず、LLM がエコーしても正準形に混入しない"
  artifacts:
    - src/meeting/types.ts
    - src/meeting/schemas.ts
    - src/portfolio/watchlist-judgment.ts
    - src/portfolio/watchlist-judgment.test.ts
    - src/meeting/schemas.test.ts
  key_links:
    - "watchlistJudgmentSchema.transform → applyConfluenceGate（post-transform signals.length のみを読む順序契約）"
    - "attachActionChanges → normalizeHoldingSymbol（銘柄キー正規化の共有）"
---

<objective>
判定パイプラインの決定論コア（LLM 出力契約スキーマ・純関数群・単体テスト）を実装する。LLM を一切呼ばず、TS 側だけで検証・降格・変化検出・market/asOf 導出・skip 記録を完結させる層を先に固め、後続の CLI（Plan 02）とオーケストレーション（Plan 03）が信頼できる型と関数に依存できるようにする。

Purpose: 「LLM 自己申告を信用せず TS で決定できるものは TS 側決定論」というこのプロジェクトの中核ドクトリン（D-07/D-08/D-11/D-13/D-15）を、テスト可能な純関数と二段階 alias 硬化スキーマとして具現化する。スキーマは既存 `src/meeting/schemas.ts` に追加し `rawHoldingSchema`→`holdingEvaluationSchema` の二段階パターンを踏襲する（D-04、第2の契約ファイルを作らない）。正準判定フィールドは `todayAction: "buy" | "wait"` 二値 enum で alias 硬化する（D-05）。判定理由は `rationale`（日本語散文）＋`signals`（合致シグナル配列）の2フィールド構成にする（D-06）。
Output: `WatchlistJudgment` 型系（types.ts）、`rawWatchlistJudgmentSchema`→`watchlistJudgmentSchema`（schemas.ts）、純関数モジュール `watchlist-judgment.ts`、およびそれらの単体テスト。

Scope 補足（D-12: 本フェーズでは実装しない）: TS 側の表示デバウンス（2日連続同一判定までバッジ切替を抑制するヒステリシス）は本フェーズで実装しない。前日注入（D-10, Plan 03）＋決定論的変化検出（D-11, 本 Plan）を launch 時の緩和機構とし、`actionChanged` フィールドが振動観測データを供給する。デバウンスは判定履歴の日付キー永続化（WLST-F2）が前提のため将来フェーズへ明示的に見送る（research flag への回答、silently omitting ではない）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-buy-timing-judgment-agent/30-CONTEXT.md
@.planning/phases/30-buy-timing-judgment-agent/30-RESEARCH.md
@.planning/phases/30-buy-timing-judgment-agent/30-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces

このフェーズが新規に導入するシンボル・ファイル・マーカー（下流のドリフト検証はこれらを新規作成物として除外してよい）:

**型（src/meeting/types.ts）:**
- `WatchlistJudgment` — 正準判定型（`ticker`, `todayAction: "buy" | "wait"`, `rationale`, `signals: readonly string[]`, `asOf?: string`, `market?: "US" | "JP"`, `previousAction?: "buy" | "wait"`, `actionChanged?: boolean`, `status?: "skipped"`）
- `WatchlistJudgmentFile` — 最終出力ファイル形状（`date`, `generatedAt`, `judgments: readonly WatchlistJudgment[]`）

**スキーマ（src/meeting/schemas.ts）:**
- `rawWatchlistJudgmentSchema`（非 export、内部）
- `watchlistJudgmentSchema`（export、`.passthrough().transform()` 二段階）
- `normalizeTodayAction`（alias 解決ヘルパ、モジュール内）

**純関数（src/portfolio/watchlist-judgment.ts）:**
- `applyConfluenceGate(judgment): { todayAction, downgraded }` — D-07 降格ゲート
- `attachActionChanges(judgments, prevJudgments)` — D-11 変化検出
- `deriveMarket(ticker): "US" | "JP"` — D-13 市場導出
- `buildSkippedJudgment(ticker): WatchlistJudgment` — D-20 skip レコード生成

**テスト（新規）:** `src/portfolio/watchlist-judgment.test.ts`、`src/meeting/schemas.test.ts` への追記 describe ブロック

このフェーズ全体（Plan 02/03 含む）が導入する他シンボル: CLI `src/scripts/write-watchlist-judgment.ts`、invest.md 内 `Step 3-J` 見出しと `[STEP:watchlist-judgment:OK]` / `[STEP:watchlist-judgment:FAIL:...]` マーカー、tmp ファイルパス `tmp/watchlist-judgment.json` / `tmp/prev-watchlist-judgment.json` / `tmp/watchlist-judgment-raw/{ticker}.json`。
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WatchlistJudgment 型定義と二段階 alias 硬化スキーマ（TIME-02）</name>
  <files>src/meeting/types.ts, src/meeting/schemas.ts, src/meeting/schemas.test.ts</files>
  <read_first>
    - src/meeting/types.ts（既存 `HoldingEvaluation`（L110-132）の readonly フィールド規約・`previousDecision?`/`decisionChanged?` の「TS側付与・LLM生出力には存在しない」JSDoc パターンを踏襲する。追加先はこのファイル）
    - src/meeting/schemas.ts（L1-9 の import 規約、L191 `decisionEnum`、L196-198 `lenientBoolean`、L200-227 `rawHoldingSchema`→`holdingEvaluationSchema` の二段階 passthrough/transform テンプレート。これを複製する）
    - src/meeting/schemas.test.ts（`holdingEvaluationSchema` の describe ブロック規約: 「エイリアス受理」「strip: LLMが誤って...出力してもtransform後には存在しない」「寛容boolean」の it 命名パターン。同じ構造で新 describe を追記する）
    - .planning/phases/30-buy-timing-judgment-agent/30-RESEARCH.md（Pattern 1 の適用形状 L280-300、Pitfall 4 L473-478 の「market/asOf を raw スキーマに置かない」禁止事項）
    - .planning/phases/30-buy-timing-judgment-agent/30-PATTERNS.md（schemas.ts セクション L121-176 の Applied shape と Anti-pattern warning）
  </read_first>
  <action>
    types.ts に `WatchlistJudgment` インターフェース（readonly フィールド: `ticker: string`, `todayAction: "buy" | "wait"`, `rationale: string`, `signals: readonly string[]`, `asOf?: string`, `market?: "US" | "JP"`, `previousAction?: "buy" | "wait"`, `actionChanged?: boolean`, `status?: "skipped"`）と `WatchlistJudgmentFile`（`date: string`, `generatedAt: string`, `judgments: readonly WatchlistJudgment[]`）を追加する。`asOf`/`market`/`previousAction`/`actionChanged` の各フィールドには「TS側で決定論的に付与（D-08/D-13/D-15）、LLMの生出力には存在しない」旨の JSDoc を `HoldingEvaluation.previousDecision` の JSDoc 規約に沿って付す。`actionChanged?` には「undefined=比較不能、false=変化なし」の区別を明記する（D-11）。

    schemas.ts に `rawWatchlistJudgmentSchema`（非 export）を `z.object({...}).passthrough()` で定義する。正準フィールド `ticker: z.string()` は必須、`todayAction: z.enum(["buy","wait"]).optional()` と alias 群（`action` 同 enum optional / `verdict: z.string().optional()`（日本語値「買い」「待ち」を運ぶ可能性）/ `buyToday` は既存 `lenientBoolean` を再利用）、`rationale: z.string().optional()`、`signals: z.array(z.string()).optional()` を持たせる。**`asOf`/`market`/`previousAction`/`actionChanged`/`status` は raw スキーマに定義しない**（`holdingEvaluationSchema` が `previousDecision`/`decisionChanged` を raw に持たないのと同じ構造的排除。D-08）。alias 解決は `normalizeTodayAction(raw)` ヘルパに分離し、優先順位 `todayAction` → `action` → `verdict`（「買い」→buy / 「待ち」→wait の日本語マッピング含む）→ `buyToday`（true→buy / false→wait）→ デフォルト `"wait"`（fail-closed: 不明時は買いを出さない、D-07 思想）で解決する。`watchlistJudgmentSchema = rawWatchlistJudgmentSchema.transform(...)` を export し、返却オブジェクトは `{ ticker, todayAction: normalizeTodayAction(raw), rationale: raw.rationale ?? "", signals: raw.signals ?? [] }` のみ（asOf/market/previousAction/actionChanged は transform で参照しない）。**`raw.market ?? deriveMarket(...)` のような `??` フォールバックを market/asOf に対して書かないこと**（Pitfall 4）。

    schemas.test.ts に `describe("watchlistJudgmentSchema (TIME-02)")` を追記し、alias 受理（`action`/`verdict`/`buyToday` 各経路が todayAction に正準化）、日本語値受理（「買い」→buy）、passthrough 寛容（未知フィールドで throw しない）、strip 検証（`market`/`asOf`/`previousAction`/`actionChanged` を raw に入れても transform 後の結果に存在しない — `.not.toHaveProperty` で検証）、不明値のデフォルト降格（todayAction 相当フィールド全欠 → `"wait"`）を `holdingEvaluationSchema` の既存 it 命名規約に沿って追加する。
  </action>
  <behavior>
    - Test: `watchlistJudgmentSchema.parse({ ticker: "AAPL", action: "buy", signals: ["a","b"] })` → `.todayAction === "buy"`（action alias 解決）
    - Test: `parse({ ticker: "7203.T", verdict: "待ち" })` → `.todayAction === "wait"`（日本語値解決）
    - Test: `parse({ ticker: "X", buyToday: true })` → `.todayAction === "buy"`（lenientBoolean 経路）
    - Test: `parse({ ticker: "X", buyToday: "false" })` → `.todayAction === "wait"`（文字列boolean矯正）
    - Test: `parse({ ticker: "X", garbageField: 1 })` は throw せず `.todayAction === "wait"`（passthrough + fail-closed デフォルト）
    - Test: `parse({ ticker: "X", todayAction: "buy", market: "JP", asOf: "2026-07-14", previousAction: "wait", actionChanged: true })` の結果は `market`/`asOf`/`previousAction`/`actionChanged` を **持たない**（strip 検証）
  </behavior>
  <verify>
    <automated>npx vitest run src/meeting/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/meeting/schemas.test.ts` が全て green（新 describe ブロック含む）
    - `grep -n "watchlistJudgmentSchema" src/meeting/schemas.ts` が export 定義行にヒットする
    - `grep -n "WatchlistJudgment" src/meeting/types.ts` が `WatchlistJudgment` と `WatchlistJudgmentFile` の2型定義にヒットする
    - Source assertion: `rawWatchlistJudgmentSchema` の定義ブロック内に `market`/`asOf`/`previousAction`/`actionChanged` フィールドが存在しない（`sed -n '/rawWatchlistJudgmentSchema/,/passthrough/p' src/meeting/schemas.ts | grep -v '^#' | grep -c -E '\basOf\b|\bmarket\b|\bpreviousAction\b|\bactionChanged\b'` が 0）
    - Source assertion: transform 内に market/asOf への `??` フォールバックが無い（`watchlistJudgmentSchema` の transform ブロックが `deriveMarket` を呼ばない）
    - Behavior assertion: strip 検証テスト（raw に TS専用フィールドを入れても transform 後に存在しない）が green
  </acceptance_criteria>
  <done>
    watchlistJudgmentSchema が全 alias 経路を todayAction 二値 enum に正準化し、TS専用4フィールドを構造的に strip し、不正・未知フィールドでも throw せず fail-closed で wait をデフォルトにする。schemas.test.ts の新 describe が全 green。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 決定論純関数群（confluence ゲート・変化検出・market/skip）とテスト（TIME-03/04/05）</name>
  <files>src/portfolio/watchlist-judgment.ts, src/portfolio/watchlist-judgment.test.ts</files>
  <read_first>
    - src/portfolio/decision-diff.ts（全42行。`attachDecisionChanges` の構造を逐語的に複製する: prevHoldings===null 早期 return、`prevBySymbol` Map、today 配列を primary ループ、prev は Map.get のみ、match 時に両フィールド付与。import 規約 `import { normalizeHoldingSymbol } from "./holding-news.js";`）
    - src/portfolio/decision-diff.test.ts（全66行。`makeHolding` ファクトリ・`describe`/`it` 命名・`.not.toHaveProperty` アサーション規約。同型の `makeJudgment` ファクトリを作る）
    - src/portfolio/holding-news.ts（`normalizeHoldingSymbol` の trim+toUpperCase 実装。銘柄キー照合に流用）
    - src/scripts/extract-tickers.ts（L71-72 付近の `.T` サフィックス正規表現規約。`deriveMarket` はこの慣習に準拠）
    - src/meeting/types.ts（Task 1 で追加した `WatchlistJudgment` 型を import する）
    - .planning/phases/30-buy-timing-judgment-agent/30-RESEARCH.md（Code Examples L489-545 の applyConfluenceGate/deriveMarket/attachActionChanges 図示実装、Pitfall 3 L466-471 の「confluence ゲートは transform 後の signals.length のみを読む・スキーマとゲートを分離」、Pitfall 5 L480-485 の skip=陽性レコード契約）
    - .planning/phases/30-buy-timing-judgment-agent/30-PATTERNS.md（watchlist-judgment.ts セクション L20-70 の Discipline to replicate と No Analog Found）
  </read_first>
  <action>
    `src/portfolio/watchlist-judgment.ts` を新規作成し、以下の純関数を実装する（全て同期・I/O なし・try/catch なし。decision-diff.ts の純度規律に一致）。

    `applyConfluenceGate(judgment: { readonly todayAction: "buy" | "wait"; readonly signals: readonly string[] }): { readonly todayAction: "buy" | "wait"; readonly downgraded: boolean }` — `todayAction === "buy"` かつ `signals.length < 2` の場合のみ `{ todayAction: "wait", downgraded: true }` を返し `console.warn` で降格理由（signals 件数を含む日本語ログ）を出す。それ以外は `{ todayAction: judgment.todayAction, downgraded: false }`。**必ず transform 後の正準 `signals` を読む**（raw の alias キーを読まない。Pitfall 3）。スキーマ変換とは別関数として分離する。

    `attachActionChanges(judgments, prevJudgments)` — `attachDecisionChanges` の逐語的構造クローン。ジェネリック制約 `T extends { readonly ticker: string; readonly todayAction: "buy" | "wait" }`、戻り値要素は `T & { previousAction?: "buy" | "wait"; actionChanged?: boolean }`。`prevJudgments === null` → 各要素をプレーン spread（previousAction/actionChanged キーを付与しない）。それ以外は `normalizeHoldingSymbol(j.ticker)` をキーにした `prevByTicker` Map を作り、today 配列を primary ループ・prev は `Map.get` のみ。match しない銘柄はプレーン spread（キー非付与）、match した銘柄は `previousAction: prev.todayAction, actionChanged: prev.todayAction !== j.todayAction` を付与（enum 等値比較のみ）。

    `deriveMarket(ticker: string): "US" | "JP"` — `/\.T$/.test(ticker) ? "JP" : "US"`（extract-tickers.ts の `.T` 規約準拠）。

    `buildSkippedJudgment(ticker: string): WatchlistJudgment` — テクニカルスナップショット欠落銘柄の陽性 skip レコード（D-20）。`{ ticker, todayAction: "wait", rationale: "テクニカルデータ欠落のため判定不能", signals: [], market: deriveMarket(ticker), status: "skipped" }` を返す（asOf は付与しない = undefined）。**省略ではなく陽性レコード**（Pitfall 5）。

    `src/portfolio/watchlist-judgment.test.ts` を新規作成し、`makeJudgment(overrides)` ファクトリ（decision-diff.test.ts の `makeHolding` 同型）を用いて以下を網羅する。
  </action>
  <behavior>
    - applyConfluenceGate: buy + signals 1件 → `{ todayAction: "wait", downgraded: true }`（confluence 未達降格、D-07 必須ケース）
    - applyConfluenceGate: buy + signals 2件 → `{ todayAction: "buy", downgraded: false }`（充足）
    - applyConfluenceGate: wait + signals 0件 → `{ todayAction: "wait", downgraded: false }`（wait は降格対象外）
    - attachActionChanges: prevJudgments===null → 結果に `previousAction`/`actionChanged` プロパティが**存在しない**（`.not.toHaveProperty`、D-11 必須ケース「prev なし→プロパティ非付与」）
    - attachActionChanges: prev に同銘柄・同 action → `actionChanged: false`
    - attachActionChanges: prev に同銘柄・異 action（wait→buy）→ `actionChanged: true`, `previousAction: "wait"`
    - attachActionChanges: today のみ存在（prev に無い銘柄）→ プロパティ非付与（undefined 値ではなくキー欠如）
    - attachActionChanges: 銘柄キー照合が空白/大小文字許容（` aapl ` と `AAPL` が normalizeHoldingSymbol でマッチ）
    - deriveMarket: `"7203.T"` → `"JP"`, `"AAPL"` → `"US"`
    - buildSkippedJudgment: `"MSFT"` → `status: "skipped"`, `market: "US"`, `asOf` プロパティ非付与
  </behavior>
  <verify>
    <automated>npx vitest run src/portfolio/watchlist-judgment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/portfolio/watchlist-judgment.test.ts` が全て green
    - `grep -c "export function" src/portfolio/watchlist-judgment.ts` が 4 以上（applyConfluenceGate/attachActionChanges/deriveMarket/buildSkippedJudgment）
    - Source assertion: `attachActionChanges` が `prevJudgments === null` 早期 return を持つ（`grep -n "=== null" src/portfolio/watchlist-judgment.ts` がヒット）
    - Source assertion: today 配列を primary ループしている（`prevJudgments.map` で反復せず `judgments.map` を使う — `grep -c "prevJudgments.map" src/portfolio/watchlist-judgment.ts` が Map 構築の1回のみ、`.map` 反復本体では today 配列を使う）
    - Behavior assertion: 「buy で signals 1件 → wait 降格」テストが green（D-23 必須ケース）
    - Behavior assertion: 「prev なし → プロパティ非付与」テストが `.not.toHaveProperty` で green（D-23 必須ケース）
    - Source assertion: `watchlist-judgment.ts` に try/catch と await が存在しない（純関数モジュール、`grep -c -E 'try \{|await ' src/portfolio/watchlist-judgment.ts` が 0）
  </acceptance_criteria>
  <done>
    4純関数が全て実装され、confluence 降格・変化検出（undefined vs false 区別）・market 導出・skip 陽性レコードが単体テストで green。全関数が同期・純粋で I/O・try/catch を含まない。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM Agent 出力 → TS スキーマ | 信頼できない LLM 生 JSON が `watchlistJudgmentSchema` を通過する境界（本 Plan で防御を定義、消費は Plan 02） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Spoofing | watchlistJudgmentSchema（TS専用フィールドの偽装注入） | high | mitigate | `asOf`/`market`/`previousAction`/`actionChanged` を raw スキーマから構造的に排除。`.passthrough()` は余剰キーを許容するが `.transform()` 出力リテラルが参照しないため正準形に生存不可（D-08） |
| T-30-02 | Tampering | applyConfluenceGate（LLM 自己申告 buy の無検証採用） | high | mitigate | confluence ≥2 を TS 側決定論ゲートで強制。signals<2 の buy は wait へ降格。プロンプト契約（Plan 03）と二層防御（D-07） |
| T-30-03 | Tampering | normalizeTodayAction（不明値の buy 誤昇格） | medium | mitigate | 不明・欠落時のデフォルトを `"wait"` に固定（fail-closed）。買いシグナルは疑わしきは出さない |
| T-30-SC | Tampering | npm/pip/cargo installs | high | accept | 本フェーズはゼロ新規パッケージ（RESEARCH.md Package Legitimacy Audit: N/A）。install タスク無し |
</threat_model>

<verification>
- `npx vitest run src/meeting/schemas.test.ts src/portfolio/watchlist-judgment.test.ts` が全 green
- `npx vitest run` 全スイート回帰 green（既存テストへの副作用なし）
- `npx tsc --noEmit`（型エラー無し。新型・スキーマが既存 import と整合）
</verification>

<success_criteria>
- watchlistJudgmentSchema が alias 硬化・TS専用フィールド strip・fail-closed デフォルトを満たす（TIME-02）
- attachActionChanges が undefined/false を区別して変化検出する（TIME-03 の TS 決定論部分）
- applyConfluenceGate が buy+signals<2 を wait 降格する（TIME-04 の TS ゲート部分）
- deriveMarket/buildSkippedJudgment が market 導出と skip 陽性記録を満たす（TIME-05 の market 部分・D-20）
- 全純関数が単体テスト green、TS 型チェック通過
</success_criteria>

<output>
Create `.planning/phases/30-buy-timing-judgment-agent/30-01-SUMMARY.md` when done
</output>
