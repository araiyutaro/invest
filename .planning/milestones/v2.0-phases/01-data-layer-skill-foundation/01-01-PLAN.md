---
phase: 01-data-layer-skill-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/scripts/collect-data.ts
  - src/scripts/collect-data.test.ts
  - .gitignore
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - SKILL-03
must_haves:
  truths:
    - "npx tsx src/scripts/collect-data.ts を実行すると tmp/market.json, tmp/news.json, tmp/portfolio.json が生成される"
    - "市場データ取得失敗時はプロセスが exit(1) で終了する（必須データ）"
    - "ニュース・ポートフォリオ取得失敗時は空配列JSONが書き込まれ処理が続行される（任意データ）"
    - "各収集ステップで「〜収集中...」「〜収集完了 (N件)」の進捗メッセージがコンソールに出力される"
    - "tmp/ ディレクトリが .gitignore に含まれておりポートフォリオデータがgit追跡されない"
  artifacts:
    - path: "src/scripts/collect-data.ts"
      provides: "データ収集スクリプト"
      exports: ["main (default via CLI)"]
    - path: "src/scripts/collect-data.test.ts"
      provides: "ユニットテスト (Vitest)"
    - path: "tmp/market.json"
      provides: "市場指数・セクターデータ (実行時生成)"
      contains: "indices, sectors"
    - path: "tmp/news.json"
      provides: "生ニュース記事データ (実行時生成)"
    - path: "tmp/portfolio.json"
      provides: "ポートフォリオ銘柄データ (実行時生成)"
  key_links:
    - from: "src/scripts/collect-data.ts"
      to: "src/data/market.ts"
      via: "fetchAllMarketData() import"
      pattern: "import.*fetchAllMarketData.*from.*data/market"
    - from: "src/scripts/collect-data.ts"
      to: "tmp/market.json"
      via: "fs/promises writeFile"
      pattern: "writeFile.*market\\.json"
    - from: ".gitignore"
      to: "tmp/"
      via: "gitignore entry"
      pattern: "^tmp/"
---

<objective>
データ収集スクリプト `src/scripts/collect-data.ts` を TDD で実装する。既存の fetch 関数（市場・ニュース・ポートフォリオ）を集約し、`tmp/market.json`, `tmp/news.json`, `tmp/portfolio.json` を出力する。市場データは必須（失敗時 exit(1)）、ニュースとポートフォリオはオプション（失敗時は空配列を書き込み続行）。

Purpose: Phase 1 の根幹となるデータハンドオフ境界（TS → Claude）を確立する（D-01, D-03, D-04, D-06, D-08, D-09, D-10）
Output: src/scripts/collect-data.ts, src/scripts/collect-data.test.ts, tmp/ gitignore エントリ
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/arai/invest/.planning/ROADMAP.md
@/Users/arai/invest/.planning/phases/01-data-layer-skill-foundation/01-CONTEXT.md
@/Users/arai/invest/.planning/phases/01-data-layer-skill-foundation/01-RESEARCH.md
@/Users/arai/invest/.planning/phases/01-data-layer-skill-foundation/01-PATTERNS.md

<interfaces>
From src/data/market.ts:
```typescript
export interface MarketIndex {
  readonly name: string;
  readonly symbol: string;
  readonly price: number;
  readonly change: number;
  readonly changePercent: number;
}

export interface SectorPerformance {
  readonly sector: string;
  readonly symbol: string;
  readonly changePercent: number;
}

export async function fetchAllMarketData(): Promise<{
  readonly indices: ReadonlyArray<MarketIndex>;
  readonly sectors: ReadonlyArray<SectorPerformance>;
}>;
```

From src/data/news/types.ts:
```typescript
export interface RawNewsArticle {
  readonly title: string;
  readonly summary: string;
  readonly source: string;
  readonly url: string;
  readonly publishedAt: Date;  // JSON.stringify で ISO文字列化される (PITFALL-2)
  readonly category: string;
}
```

From src/data/news/finnhub.ts:
```typescript
export interface FinnhubNews {
  general: ReadonlyArray<RawNewsArticle>;
  merger: ReadonlyArray<RawNewsArticle>;
}
export async function fetchAllFinnhubNews(): Promise<FinnhubNews>;
```

From src/data/news/google-news.ts:
```typescript
export async function fetchGoogleNewsJapan(): Promise<ReadonlyArray<RawNewsArticle>>;
```

From src/data/news/rss-sources.ts:
```typescript
export async function fetchAllRssNews(): Promise<ReadonlyArray<RawNewsArticle>>;
```

From src/portfolio/holdings.ts:
```typescript
export const PORTFOLIO_HOLDINGS: ReadonlyArray<PortfolioHolding>;
```

From src/portfolio/data.ts:
```typescript
export interface StockData {
  readonly symbol: string;
  readonly name: string;
  readonly nameJa: string;
  readonly sector: string;
  readonly price: number;
  readonly change: number;
  readonly changePercent: number;
  readonly marketCap: number | null;
  readonly peRatio: number | null;
  readonly volume: number | null;
  readonly fiftyTwoWeekHigh: number | null;
  readonly fiftyTwoWeekLow: number | null;
  readonly averageVolume: number | null;
}
export async function fetchPortfolioData(
  holdings: ReadonlyArray<PortfolioHolding>
): Promise<ReadonlyArray<StockData>>;
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: テストスタブ作成 + .gitignore 更新 (RED フェーズ)</name>
  <files>src/scripts/collect-data.test.ts, .gitignore</files>
  <read_first>
    - /Users/arai/invest/.gitignore — tmp/ エントリが既に存在するか確認してから追記
    - /Users/arai/invest/src/index.ts — 既存エントリポイントのパターンと import.meta.dirname の使い方を確認
    - /Users/arai/invest/src/data/market.ts — MarketIndex / SectorPerformance / fetchAllMarketData の実装を確認
    - /Users/arai/invest/src/data/news/types.ts — RawNewsArticle の型定義を確認
    - /Users/arai/invest/src/portfolio/data.ts — StockData の型定義を確認
  </read_first>
  <behavior>
    - Test 1: collect-data モジュールを import してから `main()` を実行すると tmp/market.json が作成される
    - Test 2: tmp/market.json を JSON.parse すると `indices` (配列) と `sectors` (配列) のキーを持つオブジェクトが返る
    - Test 3: tmp/news.json を JSON.parse すると配列が返る（空配列も含む）
    - Test 4: tmp/portfolio.json を JSON.parse すると配列が返る（空配列も含む）
    - Test 5: fetchAllMarketData が reject したとき process.exit(1) が呼ばれる
    - Test 6: fetchAllFinnhubNews が reject したとき tmp/news.json に "[]" が書き込まれ処理が続行される
    - Test 7: main() 実行時にコンソールに「市場データ収集中...」を含むメッセージが出力される

    外部 API モック方針:
    - vi.mock で `src/data/market`, `src/data/news/finnhub`, `src/data/news/google-news`, `src/data/news/rss-sources`, `src/portfolio/data`, `src/portfolio/holdings` をモックする
    - fs/promises は vi.mock で `writeFile` と `mkdir` をスパイし、実際のファイル書き込みを防ぐ
    - テスト前に vi.spyOn(console, 'log') でログをキャプチャする
    - プロセス終了テストは vi.spyOn(process, 'exit') を使う
  </behavior>
  <action>
    1. .gitignore を読んで `tmp/` エントリが存在しない場合のみ `tmp/` を追記する（セキュリティ要件: ポートフォリオデータをgit管理外にする）

    2. src/scripts/collect-data.test.ts を新規作成する:
       - `import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"` でインポート
       - `vi.mock("node:fs/promises", ...)` で mkdir・writeFile を no-op スタブ化
       - `vi.mock("../data/market.js", ...)` で fetchAllMarketData がデフォルトの marketData オブジェクト（indices: [{name: "S&P 500", symbol: "^GSPC", price: 5000, change: 10, changePercent: 0.2}], sectors: []）を返すよう設定
       - `vi.mock("../data/news/finnhub.js", ...)` で fetchAllFinnhubNews がデフォルト `{ general: [], merger: [] }` を返すよう設定
       - `vi.mock("../data/news/google-news.js", ...)` で fetchGoogleNewsJapan がデフォルト `[]` を返すよう設定
       - `vi.mock("../data/news/rss-sources.js", ...)` で fetchAllRssNews がデフォルト `[]` を返すよう設定
       - `vi.mock("../portfolio/data.js", ...)` で fetchPortfolioData がデフォルト `[]` を返すよう設定
       - `vi.mock("../portfolio/holdings.js", ...)` で PORTFOLIO_HOLDINGS が `[]` を返すよう設定
       - テストケース上記 Test 1〜7 を記述（この時点で collect-data.ts が存在しないため全テストが失敗=RED）
       - afterEach で vi.restoreAllMocks() を呼ぶ

    3. テストが RED（失敗）で終わることを確認:
       `npm run test -- src/scripts/collect-data.test.ts` が失敗することを確認してコミット
       commit: `test(01-01): add failing tests for collect-data script`
  </action>
  <verify>
    <automated>npm run test -- src/scripts/collect-data.test.ts 2>&1 | grep -E "FAIL|Cannot find module|collect-data"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore に `tmp/` エントリが存在する (`grep -c "^tmp/" /Users/arai/invest/.gitignore` が 1 以上)
    - src/scripts/collect-data.test.ts が存在する
    - `npm run test -- src/scripts/collect-data.test.ts` が「Cannot find module」等のエラーで失敗する（RED確認）
    - git commit が作成されている
  </acceptance_criteria>
  <done>テストファイルが存在し全テストが失敗（RED）状態。.gitignore に tmp/ エントリあり。</done>
</task>

<task type="tdd">
  <name>Task 2: collect-data.ts 実装 (GREEN フェーズ)</name>
  <files>src/scripts/collect-data.ts</files>
  <read_first>
    - /Users/arai/invest/src/scripts/collect-data.test.ts — Task 1 で作成したテストの期待値を確認してから実装する
    - /Users/arai/invest/src/index.ts — import.meta.dirname パターン、main().catch パターンを確認
    - /Users/arai/invest/src/data/news/finnhub.ts lines 1-70 — FinnhubNews 型と fetchAllFinnhubNews の実装を確認
    - /Users/arai/invest/.planning/phases/01-data-layer-skill-foundation/01-RESEARCH.md — Pattern 1: データ収集スクリプトの全コード例を確認（PITFALL 1〜6 も参照）
  </read_first>
  <behavior>
    実装すべき動作（テスト通過基準）:
    - main() を実行すると await mkdir(TMP_DIR, { recursive: true }) が呼ばれる
    - fetchAllMarketData() で取得したデータを JSON.stringify(data, null, 2) して tmp/market.json に writeFile する
    - fetchAllFinnhubNews(), fetchGoogleNewsJapan(), fetchAllRssNews() を Promise.all で並列実行し結果をフラット化して tmp/news.json に書き込む
    - fetchPortfolioData(PORTFOLIO_HOLDINGS) を呼び結果を tmp/portfolio.json に書き込む
    - ニュース/ポートフォリオ収集が例外を投げた場合は catch して "[]" を該当 JSON ファイルに書き込み続行する
    - 市場データ取得が例外を投げた場合は catch せず main().catch で process.exit(1) に至る
    - console.log("市場データ収集中...") → console.log(`市場データ収集完了 (指数: ${n}件, セクター: ${m}件)`) の進捗メッセージを出力する
    - console.log("ニュース収集中...") → console.log(`ニュース収集完了 (${n}件)`) の進捗メッセージを出力する
    - console.log("ポートフォリオデータ収集中...") → console.log(`ポートフォリオデータ収集完了 (${n}銘柄)`) の進捗メッセージを出力する
    - D-10 に従い最後にサマリーを表示する: 指数変動率, ニュース件数, ポートフォリオ銘柄数
  </behavior>
  <action>
    src/scripts/collect-data.ts を新規作成する:

    ファイル先頭:
    - `import "dotenv/config";` (FINNHUB_API_KEY 読み込み)
    - `import { writeFile, mkdir } from "node:fs/promises";`
    - `import { join } from "node:path";`
    - `import { fetchAllMarketData } from "../data/market.js";`
    - `import { fetchAllFinnhubNews } from "../data/news/finnhub.js";`
    - `import { fetchGoogleNewsJapan } from "../data/news/google-news.js";`
    - `import { fetchAllRssNews } from "../data/news/rss-sources.js";`
    - `import { PORTFOLIO_HOLDINGS } from "../portfolio/holdings.js";`
    - `import { fetchPortfolioData } from "../portfolio/data.js";`

    TMP_DIR 定義:
    - `const TMP_DIR = join(import.meta.dirname, "../../tmp");`
    - (src/scripts/ から2階層上がって /tmp を指す — PATTERNS.md 参照)

    main() 関数:
    1. console.log("データ収集開始...");
    2. await mkdir(TMP_DIR, { recursive: true }); // tmp/ 冪等作成
    3. 市場データ必須ブロック: console.log("市場データ収集中..."); → const marketData = await fetchAllMarketData(); → await writeFile(join(TMP_DIR, "market.json"), JSON.stringify(marketData, null, 2), "utf-8"); → console.log(`市場データ収集完了 (指数: ${marketData.indices.length}件, セクター: ${marketData.sectors.length}件)`);
    4. ニュース任意ブロック (try-catch): console.log("ニュース収集中..."); → const [finnhubNews, googleNews, rssNews] = await Promise.all([fetchAllFinnhubNews(), fetchGoogleNewsJapan().catch(() => []), fetchAllRssNews().catch(() => [])]); → const allArticles = [...finnhubNews.general, ...finnhubNews.merger, ...googleNews, ...rssNews]; → await writeFile(join(TMP_DIR, "news.json"), JSON.stringify(allArticles, null, 2), "utf-8"); → console.log(`ニュース収集完了 (${allArticles.length}件)`); catch(e): console.error("ニュース収集失敗（続行）:", e); → await writeFile(join(TMP_DIR, "news.json"), "[]", "utf-8");
    5. ポートフォリオ任意ブロック (try-catch): console.log("ポートフォリオデータ収集中..."); → const portfolioStocks = await fetchPortfolioData(PORTFOLIO_HOLDINGS); → await writeFile(join(TMP_DIR, "portfolio.json"), JSON.stringify(portfolioStocks, null, 2), "utf-8"); → console.log(`ポートフォリオデータ収集完了 (${portfolioStocks.length}銘柄)`); catch(e): console.error("ポートフォリオ収集失敗（続行）:", e); → await writeFile(join(TMP_DIR, "portfolio.json"), "[]", "utf-8");
    6. サマリー表示 (D-10): const topIndex = marketData.indices[0]; のような形で最初の指数変動率を表示。console.log で「=== データ収集サマリー ===」と各件数を出力する
    7. console.log("データ収集完了");

    ファイル末尾:
    - `main().catch((error) => { console.error("Fatal error:", error); process.exit(1); });`

    禁止事項 (per D-03, D-06):
    - `fetchMarketNews()` (src/data/news.ts) は import しない (Gemini呼び出しを含む)
    - `generateAllAnalyses()` は import しない
    - `generateSectorChart()` / `generateMarketOverviewChart()` は import しない
    - `news.ts` の `fetchMarketNews` は import しない

    実装後にテスト GREEN 確認:
    `npm run test -- src/scripts/collect-data.test.ts` が全テスト PASS すること
    commit: `feat(01-01): implement collect-data script`
  </action>
  <verify>
    <automated>npm run test -- src/scripts/collect-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/scripts/collect-data.test.ts` が 0 failures で終了する
    - src/scripts/collect-data.ts に `import.*fetchMarketNews` が存在しない (`grep -c "fetchMarketNews" /Users/arai/invest/src/scripts/collect-data.ts` = 0)
    - src/scripts/collect-data.ts に `generateAllAnalyses\|generateSectorChart\|generateMarketOverviewChart` が存在しない
    - src/scripts/collect-data.ts に `const TMP_DIR = join(import.meta.dirname` が存在する
    - src/scripts/collect-data.ts に `mkdir(TMP_DIR, { recursive: true })` が存在する
    - src/scripts/collect-data.ts に `writeFile.*market\.json` パターンが存在する
    - src/scripts/collect-data.ts に `writeFile.*news\.json` パターンが存在する
    - src/scripts/collect-data.ts に `writeFile.*portfolio\.json` パターンが存在する
    - src/scripts/collect-data.ts に `process.exit(1)` が存在する（市場データ失敗時の終了）
    - src/scripts/collect-data.ts に `"市場データ収集中..."` 文字列が存在する
  </acceptance_criteria>
  <done>全テストが GREEN。collect-data.ts が D-01〜D-10 の制約を満たして実装されている。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance API → Node.js | 外部APIからの数値データ（未検証の型） |
| Finnhub API → Node.js | 外部APIからのニュースデータ（未検証のコンテンツ） |
| tmp/*.json → .claude/commands/invest.md | TSスクリプトの出力をClaudeエージェントが読み込む境界 |
| .env → Node.js process.env | APIキーの環境変数注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | tmp/portfolio.json | mitigate | tmp/ を .gitignore に追加し、ポートフォリオ保有銘柄・価格がgitに残らないようにする |
| T-01-02 | Information Disclosure | .env (FINNHUB_API_KEY) | accept | .gitignore に .env が既に含まれている。ローカル個人利用のため追加制御は不要 |
| T-01-03 | Tampering | tmp/*.json | accept | ローカル専用、Claudeセッション内のみ使用。改ざんリスクは低い |
| T-01-04 | Denial of Service | Yahoo Finance API calls | mitigate | fetchQuoteSafe() の try-catch パターンで個別失敗を吸収。市場データは必須のため全失敗時は exit(1) で明示的に停止 |
| T-01-05 | Information Disclosure | console.log 出力 | accept | 株価・変動率は公開情報。個人識別情報・秘密鍵は出力しない |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし。Package Legitimacy Audit 確認済み（RESEARCH.md） |
</threat_model>

<verification>
Phase 1 Plan 01 完了確認:

1. `npm run test -- src/scripts/collect-data.test.ts` → 全テスト PASS
2. `grep -c "^tmp/" /Users/arai/invest/.gitignore` → 1 以上
3. `grep -c "fetchMarketNews" /Users/arai/invest/src/scripts/collect-data.ts` → 0 (Gemini依存コードを呼ばない)
4. `grep -rn "generateAllAnalyses\|generateSectorChart" /Users/arai/invest/src/scripts/` → 0件
5. TypeScriptコンパイル確認: `npx tsc --noEmit` でエラーなし
</verification>

<success_criteria>
- src/scripts/collect-data.ts が存在し、Vitest テストが全 PASS する
- .gitignore に tmp/ が含まれる
- Gemini依存関数 (fetchMarketNews, generateAllAnalyses, generateSectorChart) を import していない
- 市場データ必須・ニュース/ポートフォリオ任意のグレースフルデグラデーション実装済み
- D-01, D-03, D-04, D-06, D-08, D-09, D-10 のすべての決定事項が実装に反映されている
</success_criteria>

<output>
完了後に `.planning/phases/01-data-layer-skill-foundation/01-01-SUMMARY.md` を作成する
</output>
