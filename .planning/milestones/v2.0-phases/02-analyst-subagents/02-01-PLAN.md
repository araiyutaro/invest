---
phase: 02-analyst-subagents
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/meeting/types.ts
  - src/meeting/schemas.ts
  - src/scripts/validate-meeting.ts
  - src/scripts/validate-meeting.test.ts
autonomous: true
requirements:
  - AGENT-07
must_haves:
  truths:
    - "src/meeting/types.ts に全ミーティング出力型（StockPick, AnalystRound1Output, AnalystRound2Output, AnalystRound3Output, StockScore, MeetingResult）が readonly フィールドで定義されている"
    - "src/meeting/schemas.ts に meetingResultSchema (Zod) と validateMeetingResult 関数がエクスポートされている"
    - "validateMeetingResult(data) が不正データに対して ZodError を throw する"
    - "src/scripts/validate-meeting.ts が tmp/meeting-result.json を読み込み、スキーマ検証してサマリーを表示する"
    - "npm run test -- src/scripts/validate-meeting.test.ts が全テスト PASS する"
    - "npx tsc --noEmit でエラーなし"
  artifacts:
    - path: "src/meeting/types.ts"
      provides: "ミーティング出力 TypeScript インターフェース定義"
      exports: ["StockPick", "AnalystRound1Output", "AnalystRound2Output", "AnalystRound3Output", "StockScore", "MeetingResult"]
    - path: "src/meeting/schemas.ts"
      provides: "Zod バリデーションスキーマ + validate 関数"
      exports: ["meetingResultSchema", "validateMeetingResult"]
    - path: "src/scripts/validate-meeting.ts"
      provides: "meeting-result.json 検証 CLI スクリプト"
      exports: ["validate (named)"]
    - path: "src/scripts/validate-meeting.test.ts"
      provides: "バリデーションスクリプトのユニットテスト (Vitest)"
  key_links:
    - from: "src/meeting/schemas.ts"
      to: "src/meeting/types.ts"
      via: "型インポート"
      pattern: "import.*from.*meeting/types"
    - from: "src/scripts/validate-meeting.ts"
      to: "src/meeting/schemas.ts"
      via: "validateMeetingResult import"
      pattern: "import.*validateMeetingResult.*from.*meeting/schemas"
    - from: "src/scripts/validate-meeting.ts"
      to: "tmp/meeting-result.json"
      via: "fs/promises readFile"
      pattern: "readFile.*meeting-result\\.json"
---

<objective>
ミーティング出力の TypeScript インターフェースと Zod バリデーションスキーマを TDD で実装する。対象は Round 1/2/3 アナリスト出力と meeting-result.json のフルスキーマ。さらに tmp/meeting-result.json をスキーマ検証する CLI スクリプトを作成する。これにより Phase 2（ミーティングオーケストレーション）と Phase 3（レポート生成）の間のコントラクトを確立する。

Purpose: RESEARCH.md Section 3 のスキーマ設計を型安全な実装に落とし込み、Phase 間のデータ境界を厳密に定義する（AGENT-07, Pitfall 5 対策）
Output: src/meeting/types.ts, src/meeting/schemas.ts, src/scripts/validate-meeting.ts, src/scripts/validate-meeting.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/arai/invest/.planning/ROADMAP.md
@/Users/arai/invest/.planning/phases/02-analyst-subagents/02-CONTEXT.md
@/Users/arai/invest/.planning/phases/02-analyst-subagents/02-RESEARCH.md
@/Users/arai/invest/.planning/phases/02-analyst-subagents/02-PATTERNS.md

<interfaces>
From src/agents/types.ts (既存型定義 — 参照のみ、変更しない):
```typescript
export interface StockScoreSummary {
  readonly ticker: string;
  readonly averageScore: number;
  readonly verdict: "強気" | "中立" | "弱気";
  readonly agentScores: ReadonlyArray<{
    readonly agentRole: string;
    readonly score: number;
    readonly reason: string;
  }>;
}
```

From RESEARCH.md Section 3.5 (meeting-result.json スキーマ — 実装対象):
```typescript
interface MeetingResult {
  date: string;              // "YYYY-MM-DD"
  generatedAt: string;       // ISO 8601 timestamp
  marketOverview: { summary: string; trend: "上昇" | "下降" | "混合"; keyIndices: Array<{ name: string; changePercent: number; }>; };
  sectorRecommendations: Array<{ rank: number; sector: string; rationale: string; outlook: "強気" | "中立" | "弱気"; }>;
  highlightedStocks: Array<{ ticker: string; averageScore: number; verdict: "強気" | "中立" | "弱気"; summary: string; agentScores: Array<{ agentRole: string; score: number; reason: string; }>; nominatedBy: string[]; }>;
  riskWarnings: Array<{ severity: "高" | "中" | "低"; description: string; }>;
  actionItems: string[];
  weeklyEvents: Array<{ date: string; event: string; impact: "高" | "中" | "低"; }>;
  indexInvestorAdvice: string;
  roundSummary: { round1Count: number; round2Count: number; round3Count: number; scoredTickers: string[]; };
}
```

From src/scripts/collect-data.ts (スクリプトパターン参照):
- import.meta.dirname ベースのパス解決
- main().catch → process.exit(1) パターン
- コンソール進捗メッセージ
</interfaces>

<zod_version>
プロジェクトは zod ^4.3.6 を使用。Zod 4.x は Zod 3.x とは API が異なる可能性あり。
実装前に以下を確認すること:
1. `cat /Users/arai/invest/node_modules/zod/package.json | grep version` で実際のインストール済みバージョンを確認
2. Zod 4.x の import パスと API パターンを確認（Context7 MCP または node_modules/zod の型定義を参照）
3. z.object / z.string / z.number / z.array / z.enum は v3/v4 共通だが、z.infer の使い方やエラー型が異なる可能性あり
</zod_version>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: テストスタブ作成 (RED フェーズ)</name>
  <files>src/scripts/validate-meeting.test.ts</files>
  <read_first>
    - /Users/arai/invest/src/agents/types.ts — 既存型定義の確認（StockScoreSummary の verdict 型等）
    - /Users/arai/invest/src/scripts/collect-data.test.ts — テストパターン参照（vi.mock, beforeEach/afterEach 構成）
    - /Users/arai/invest/.planning/phases/02-analyst-subagents/02-RESEARCH.md Section 3.5 — MeetingResult スキーマ定義の全フィールド確認
    - /Users/arai/invest/node_modules/zod/package.json — Zod の実際のバージョン確認
  </read_first>
  <behavior>
    - Test 1: 全必須フィールドが揃った有効な meeting-result.json データが validateMeetingResult を通過する
    - Test 2: 必須フィールド (date) が欠けたデータで validateMeetingResult が ZodError を throw する
    - Test 3: verdict に無効な値（"超強気" 等）を入れると validateMeetingResult が ZodError を throw する
    - Test 4: score が範囲外 (score: 11) のデータで validateMeetingResult が ZodError を throw する
    - Test 5: highlightedStocks が空配列の場合でも validateMeetingResult が正常に通過する（有効ケース）
    - Test 6: validate-meeting スクリプトモジュールが validate 関数をエクスポートしている

    モック方針:
    - vi.mock("node:fs/promises") で readFile をモック（実際のファイルI/Oを防ぐ）
    - validate-meeting モジュールの import は動的 import (await import) を使用
    - validateMeetingResult は src/meeting/schemas.ts から直接 import してテストする（スクリプトの実行テストとは分離）
  </behavior>
  <action>
    1. src/scripts/validate-meeting.test.ts を新規作成する:
       - `import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"` でインポート
       - vi.mock("node:fs/promises") で readFile をモックスタブ化
       - テストデータとして validMeetingResult オブジェクトを定義（RESEARCH.md Section 3.5 の全フィールドを網羅した有効データ）:
         ```typescript
         const validMeetingResult = {
           date: "2026-06-24",
           generatedAt: "2026-06-24T08:00:00.000Z",
           marketOverview: {
             summary: "市場は上昇基調",
             trend: "上昇" as const,
             keyIndices: [{ name: "S&P 500", changePercent: 0.5 }],
           },
           sectorRecommendations: [
             { rank: 1, sector: "テクノロジー", rationale: "AI需要拡大", outlook: "強気" as const },
           ],
           highlightedStocks: [
             {
               ticker: "PLTR",
               averageScore: 8.2,
               verdict: "強気" as const,
               summary: "AI関連で成長期待",
               agentScores: [{ agentRole: "テクニカル", score: 8, reason: "上昇トレンド" }],
               nominatedBy: ["tenbagger"],
             },
           ],
           riskWarnings: [{ severity: "中" as const, description: "金利上昇リスク" }],
           actionItems: ["ポートフォリオリバランス検討"],
           weeklyEvents: [{ date: "2026-06-26", event: "FOMC議事要旨", impact: "高" as const }],
           indexInvestorAdvice: "積立継続推奨",
           roundSummary: {
             round1Count: 5,
             round2Count: 5,
             round3Count: 5,
             scoredTickers: ["PLTR"],
           },
         };
         ```
       - describe("validate-meeting schemas") 内に Test 1〜5 を記述
       - describe("validate-meeting script") 内に Test 6 を記述
       - afterEach で vi.restoreAllMocks() を呼ぶ

    2. テストが RED（失敗）で終わることを確認:
       `npm run test -- src/scripts/validate-meeting.test.ts` が「Cannot find module」等のエラーで失敗することを確認
       commit: `test(02-01): add failing tests for meeting schema validation`
  </action>
  <verify>
    <automated>npm run test -- src/scripts/validate-meeting.test.ts 2>&1 | grep -E "FAIL|Cannot find module|validate-meeting"</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/validate-meeting.test.ts が存在する
    - テストファイルに6つの it() ブロックが含まれる (`grep -c "it(" /Users/arai/invest/src/scripts/validate-meeting.test.ts` が 6)
    - `npm run test -- src/scripts/validate-meeting.test.ts` が「Cannot find module」等のエラーで失敗する（RED確認）
    - git commit が作成されている
  </acceptance_criteria>
  <done>テストファイルが存在し全テストが失敗（RED）状態。</done>
</task>

<task type="tdd">
  <name>Task 2: 型定義 + Zod スキーマ (GREEN フェーズ)</name>
  <files>src/meeting/types.ts, src/meeting/schemas.ts</files>
  <read_first>
    - /Users/arai/invest/src/scripts/validate-meeting.test.ts — Task 1 で作成したテストの期待値を確認してから実装する
    - /Users/arai/invest/src/agents/types.ts — 既存型定義との名前衝突・整合性確認
    - /Users/arai/invest/.planning/phases/02-analyst-subagents/02-RESEARCH.md Section 3.1〜3.5 — 全スキーマ定義
    - /Users/arai/invest/node_modules/zod/package.json — Zod 実バージョン確認
    - Zod 4.x の API を Context7 MCP で確認する（CRITICAL: Zod 4 は breaking changes あり）
  </read_first>
  <behavior>
    src/meeting/types.ts:
    - StockPick: { ticker, direction ("強気"|"中立"|"弱気"), rationale } — 全フィールド readonly
    - AnalystRound1Output: { agentId, agentRole, summary, highlights (string[]), risks (string[]), picks (StockPick[]), sectorView } — 全フィールド readonly, ReadonlyArray
    - AnalystRound2Output: { agentId, comment, agreements (string[]), disagreements (string[]) } — 全フィールド readonly
    - AnalystRound3Output: { agentId, agentRole, scores (StockScore[]) } — 全フィールド readonly
    - StockScore: { ticker, score (number), reason } — 全フィールド readonly
    - MeetingResult: RESEARCH.md Section 3.5 の全フィールドを readonly で定義

    src/meeting/schemas.ts:
    - `import { z } from "zod"` (v3/v4 共通パターン)
    - stockPickSchema, analystRound1OutputSchema, analystRound2OutputSchema, analystRound3OutputSchema, stockScoreSchema を定義
    - meetingResultSchema を定義（MeetingResult の全フィールドを Zod スキーマで表現）
    - score フィールドに z.number().int().min(1).max(10) 制約を付与
    - averageScore フィールドに z.number().min(1).max(10) 制約を付与
    - verdict フィールドに z.enum(["強気", "中立", "弱気"]) を使用
    - trend フィールドに z.enum(["上昇", "下降", "混合"]) を使用
    - severity / impact フィールドに z.enum(["高", "中", "低"]) を使用
    - outlook フィールドに z.enum(["強気", "中立", "弱気"]) を使用
    - validateMeetingResult(data: unknown): MeetingResult 関数をエクスポート
      - 内部で meetingResultSchema.parse(data) を呼ぶ
      - 成功時: パースされた MeetingResult を返す
      - 失敗時: ZodError を throw する（Zod のデフォルト挙動）

    設計判断（RESEARCH.md / CONTEXT.md 由来）:
    - アナリスト固有フィールド（FundamentalsExtra 等）は Zod スキーマに含めない（D-01 簡略化方針）。固有データは summary テキストフィールドに含める
    - 確信度スコアは含めない（RESEARCH.md Section 3.1 の決定）
    - スコア閾値ロジック（強気>=7, 中立>=4, 弱気<4）はスキーマではなくモデレーターAgent側で実装する（Plan 02以降）
  </behavior>
  <action>
    1. src/meeting/types.ts を新規作成（既存の runner.ts は変更しない）:
       - 全インターフェースを readonly フィールドで定義
       - 配列フィールドは ReadonlyArray<T> を使用
       - export する型: StockPick, AnalystRound1Output, AnalystRound2Output, AnalystRound3Output, StockScore, MeetingResult

    2. src/meeting/schemas.ts を新規作成:
       - Zod 4.x の API を確認した上で実装する（CRITICAL）
       - 型定義は src/meeting/types.ts から import type する
       - Zod スキーマと TypeScript 型の整合性を保つ
       - meetingResultSchema と validateMeetingResult をエクスポート

    3. テスト GREEN 確認:
       `npm run test -- src/scripts/validate-meeting.test.ts` が Test 1〜5 で PASS することを確認
       （Test 6 は validate-meeting.ts が未作成のため、この時点では FAIL のままでもよい）
       commit: `feat(02-01): add meeting output types and zod schemas`
  </action>
  <verify>
    <automated>npm run test -- src/scripts/validate-meeting.test.ts 2>&1 | grep -E "pass|fail|Tests"</automated>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/meeting/types.ts が存在し、6つの型がエクスポートされている (`grep -c "export interface\|export type" /Users/arai/invest/src/meeting/types.ts` が 6 以上)
    - src/meeting/schemas.ts が存在する
    - src/meeting/schemas.ts に `meetingResultSchema` がエクスポートされている (`grep -c "export.*meetingResultSchema" /Users/arai/invest/src/meeting/schemas.ts` が 1)
    - src/meeting/schemas.ts に `validateMeetingResult` がエクスポートされている (`grep -c "export.*validateMeetingResult" /Users/arai/invest/src/meeting/schemas.ts` が 1)
    - 全フィールドが readonly である (`grep -c "readonly" /Users/arai/invest/src/meeting/types.ts` が 30 以上)
    - Test 1〜5 が PASS する
    - `npx tsc --noEmit` でエラーなし
    - git commit が作成されている
  </acceptance_criteria>
  <done>型定義とZodスキーマが実装され、バリデーションテスト (Test 1-5) が GREEN。</done>
</task>

<task type="tdd">
  <name>Task 3: バリデーションスクリプト (GREEN フェーズ)</name>
  <files>src/scripts/validate-meeting.ts</files>
  <read_first>
    - /Users/arai/invest/src/scripts/validate-meeting.test.ts — テストの期待値を確認（特に Test 6: validate 関数のエクスポート確認）
    - /Users/arai/invest/src/scripts/collect-data.ts — スクリプトパターン参照（import.meta.dirname, main().catch, コンソール出力）
    - /Users/arai/invest/src/meeting/schemas.ts — Task 2 で作成した validateMeetingResult の API を確認
  </read_first>
  <behavior>
    実装すべき動作（テスト通過基準）:
    - validate 関数をnamed exportする（Test 6）
    - validate() は tmp/meeting-result.json を readFile で読み込む
    - 読み込んだ JSON を JSON.parse → validateMeetingResult() でスキーマ検証する
    - 検証成功時: サマリーを console.log で表示する:
      - "Validation passed"
      - 注目銘柄数: highlightedStocks.length
      - リスク警告数: riskWarnings.length
      - スコア対象ティッカー数: roundSummary.scoredTickers.length
    - 検証失敗時: エラーメッセージを console.error で表示し process.exit(1) で終了
    - ファイル未存在時: エラーメッセージを console.error で表示し process.exit(1) で終了
    - main().catch パターンで CLI エントリポイントとして動作する
  </behavior>
  <action>
    src/scripts/validate-meeting.ts を新規作成する:

    ファイル先頭:
    - `import { readFile } from "node:fs/promises";`
    - `import { join } from "node:path";`
    - `import { validateMeetingResult } from "../meeting/schemas.js";`

    TMP_DIR 定義:
    - `const TMP_DIR = join(import.meta.dirname, "../../tmp");`

    validate() 関数（named export）:
    1. const filePath = join(TMP_DIR, "meeting-result.json");
    2. const raw = await readFile(filePath, "utf-8");
    3. const data = JSON.parse(raw);
    4. const result = validateMeetingResult(data);
    5. console.log("Validation passed");
    6. console.log(`  注目銘柄: ${result.highlightedStocks.length}件`);
    7. console.log(`  リスク警告: ${result.riskWarnings.length}件`);
    8. console.log(`  スコア対象: ${result.roundSummary.scoredTickers.length}銘柄`);
    9. console.log(`  アクションアイテム: ${result.actionItems.length}件`);
    10. return result;

    ファイル末尾:
    - `validate().catch((error) => { console.error("Validation failed:", error); process.exit(1); });`

    禁止事項:
    - dotenv/config は不要（API 呼び出しなし）
    - writeFile は使わない（読み取り専用スクリプト）

    実装後にテスト GREEN 確認:
    `npm run test -- src/scripts/validate-meeting.test.ts` が全テスト PASS すること
    commit: `feat(02-01): add meeting-result validation script`
  </action>
  <verify>
    <automated>npm run test -- src/scripts/validate-meeting.test.ts</automated>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/validate-meeting.ts が存在する
    - `npm run test -- src/scripts/validate-meeting.test.ts` が全 6 テスト PASS する (0 failures)
    - src/scripts/validate-meeting.ts に `export async function validate` が存在する (`grep -c "export async function validate" /Users/arai/invest/src/scripts/validate-meeting.ts` が 1)
    - src/scripts/validate-meeting.ts に `validateMeetingResult` import が存在する (`grep -c "validateMeetingResult" /Users/arai/invest/src/scripts/validate-meeting.ts` が 1 以上)
    - src/scripts/validate-meeting.ts に `readFile` import が存在する
    - src/scripts/validate-meeting.ts に `process.exit(1)` が存在する
    - src/scripts/validate-meeting.ts に `"Validation passed"` が存在する
    - `npx tsc --noEmit` でエラーなし
    - git commit が作成されている
  </acceptance_criteria>
  <done>全テストが GREEN。validate-meeting.ts がスキーマ検証スクリプトとして動作する。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/meeting-result.json → validate-meeting.ts | Phase 2 オーケストレーション出力のバリデーション境界 |
| meeting-result.json → Phase 3 (report generator) | Phase 間データ受け渡し — スキーマ不一致を防止するコントラクト |
| LLM生成JSON → Zod schema | アナリストAgent出力（構造化されていない可能性）の型安全化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tmp/meeting-result.json | mitigate | Zod スキーマで全フィールドをバリデーション。不正なデータは ZodError として拒否 |
| T-02-02 | Information Disclosure | validate-meeting.ts エラー出力 | accept | ZodError メッセージにはスキーマ構造のみ含まれ、機密情報なし |
| T-02-03 | Denial of Service | 巨大 JSON ファイル | accept | ローカル個人利用。meeting-result.json のサイズは通常 50KB 未満 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 新規パッケージインストールなし。zod 4.3.6 は既存依存 |
</threat_model>

<verification>
Phase 2 Plan 01 完了確認:

1. `npm run test -- src/scripts/validate-meeting.test.ts` → 全テスト PASS (6/6)
2. `npx tsc --noEmit` → エラーなし
3. `grep -c "export interface\|export type" /Users/arai/invest/src/meeting/types.ts` → 6 以上
4. `grep -c "meetingResultSchema" /Users/arai/invest/src/meeting/schemas.ts` → 1 以上
5. `grep -c "validateMeetingResult" /Users/arai/invest/src/meeting/schemas.ts` → 1 以上
6. `grep -c "readonly" /Users/arai/invest/src/meeting/types.ts` → 30 以上（全フィールド readonly）
7. `grep -c "export async function validate" /Users/arai/invest/src/scripts/validate-meeting.ts` → 1
</verification>

<success_criteria>
- src/meeting/types.ts に全ミーティング出力型が readonly フィールドで定義されている
- src/meeting/schemas.ts に meetingResultSchema と validateMeetingResult がエクスポートされている
- src/scripts/validate-meeting.ts が tmp/meeting-result.json を読み込みスキーマ検証を実行する
- 全テスト（6件）が PASS する
- TypeScript コンパイルエラーなし
- RESEARCH.md Section 3.5 の全フィールドがスキーマでカバーされている
- アナリスト固有フィールドはスキーマに含めない（簡略化方針）
- コーディングスタイル（readonly, イミュータブル）が準拠している
</success_criteria>

<output>
完了後に `.planning/phases/02-analyst-subagents/02-01-SUMMARY.md` を作成する
</output>
