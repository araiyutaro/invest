---
phase: 28-watchlist-persistence
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/scripts/write-watchlist.ts
  - src/scripts/write-watchlist.test.ts
autonomous: true
requirements: [WLST-01, WLST-02, WLST-03, WLST-04, WLST-05]

must_haves:
  truths:
    - "write-watchlist.ts が tmp/meeting-result.json（ETF除外・検証済み）+ data/watchlist.json（前日状態）+ PORTFOLIO_HOLDINGS を入力に prune→admit を実行し data/watchlist.json を更新する (WLST-01〜05)"
    - "batch quote() はラッパー側で1回だけ実行し、純関数には lookup 結果を引数で渡す (D-22, D-23)"
    - "既存 data/watchlist.json が欠損（ENOENT）なら空状態で初期化し正常続行、破損なら上書きせず [STEP:watchlist:FAIL:corrupted] でスキップ (D-18)"
    - "成功時 [STEP:watchlist:OK]・失敗時 [STEP:watchlist:FAIL:<理由>] を出力し、[PIPELINE:FAIL] は絶対に出さない (D-16, OPS-06分担)"
    - "active 件数・removed 件数を console.log でサイズ計測ログに出力する (D-10)"
  artifacts:
    - "src/scripts/write-watchlist.ts (fail-soft CLI ラッパー: I/O・batch quote()・prune→admit 合成・STEP マーカー)"
    - "src/scripts/write-watchlist.test.ts (ENOENT/破損/dateKey不正/STEP マーカーのテスト)"
  key_links:
    - "write-watchlist.ts → watchlist.ts の admitBullishStocks/pruneWatchlist/getActiveWatchlistEntries/型（Plan 01 成果物）"
    - "write-watchlist.ts → filter-etf-stocks.ts の fetchQuoteTypes パターンを流用し longName/shortName も取得"
    - "write-watchlist.ts → write-urgency-history.ts の loadExistingHistory 二段フェイル（ENOENT vs 破損）を verbatim 流用"
    - "write-watchlist.ts → tmp/meeting-result.json の date フィールドを dateKey に使う（JST再導出しない, D-13）"
---

<objective>
`src/scripts/write-watchlist.ts` を fail-soft CLI ラッパーとして実装する。すべての I/O（readFile/writeFile/mkdir）と単一 batch quote() 呼び出しと process.exit をこのファイルに集約し、純関数 `watchlist.ts`（Plan 01）を合成して `data/watchlist.json` を更新する。`urgency-history.ts`＋`write-urgency-history.ts` の実証済みテンプレート（D-14）と `filter-etf-stocks.ts` の batch quote() 流用（D-22）の合成。同時に `src/scripts/write-watchlist.test.ts` で ENOENT/破損/dateKey不正/STEP マーカーを検証する。

Purpose: Plan 01 の純関数を実パイプラインに接続し、破損時の履歴保全（D-18）・fail-soft ステップ（D-16）・1コール2役の社名取得（D-04）を担保する薄いラッパーを確立する。
Output: `src/scripts/write-watchlist.ts`（CLI エントリポイント）と `src/scripts/write-watchlist.test.ts`（I/O ブランチの単体テスト）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-watchlist-persistence/28-CONTEXT.md
@.planning/phases/28-watchlist-persistence/28-RESEARCH.md
@.planning/phases/28-watchlist-persistence/28-PATTERNS.md
@src/portfolio/watchlist.ts
@src/scripts/write-urgency-history.ts
@src/scripts/filter-etf-stocks.ts
</context>

<artifacts_this_phase_produces>
以下のシンボルは本フェーズが新規作成する（下流ドリフト検証の対象外）:

- `src/scripts/write-watchlist.ts`（新規ファイル）
  - `main`（async function, export）— fail-soft CLI エントリ
  - `loadExistingWatchlist`（async function）— ENOENT vs 破損の二段フェイルローダー
  - `fetchQuoteTypesAndNames`（async function）— batch quote() で quoteType + longName/shortName を1回取得
  - `WATCHLIST_PATH` / `DATA_DIR` / `TMP_DIR`（path 定数）
  - STEP マーカー文字列 `[STEP:watchlist:OK]` / `[STEP:watchlist:FAIL:<reason>]`
- `src/scripts/write-watchlist.test.ts`（新規ファイル）
- `data/watchlist.json`（実行時に生成される成果物 — コミット対象。ファイル自体はスクリプト実行で作られる）

再利用（本フェーズが Plan 01 で作成）: `admitBullishStocks`, `pruneWatchlist`, `getActiveWatchlistEntries`, `WatchlistFile`, `WatchlistCandidate`, `WatchlistEntry`（watchlist.ts）。
再利用（既存）: `QuoteTypeLookup`（etf-exclusion.ts）; `isValidDateKey`（urgency-history.ts）; `PORTFOLIO_HOLDINGS`（holdings.ts）; `MeetingResult`（meeting/types.ts）; `YahooFinance`（yahoo-finance2）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: I/O ローダーと batch quote()（quoteType + 社名）を実装する</name>
  <files>src/scripts/write-watchlist.ts, src/scripts/write-watchlist.test.ts</files>
  <read_first>
    - src/scripts/write-urgency-history.ts（path 定数・loadExistingHistory の ENOENT vs 破損二段フェイル・mkdir(DATA_DIR)・isValidDateKey ガード・エントリポイントガード — すべて流用元）
    - src/scripts/filter-etf-stocks.ts（YahooFinance インスタンス化 `new YahooFinance({ suppressNotices: ["yahooSurvey"] })`・fetchQuoteTypes の batch quote() ループ・Array.isArray 防御的パース・batch 例外の try/catch 様式）
    - src/portfolio/watchlist.ts（WatchlistFile 型・WatchlistCandidate 型・純関数シグネチャ）
    - src/portfolio/etf-exclusion.ts（QuoteTypeLookup の判別可能ユニオン: {status:"ok",quoteType} | {status:"failed"}）
  </read_first>
  <behavior>
    - `loadExistingWatchlist()` は ENOENT（`error.code==="ENOENT"` または `error.message.includes("ENOENT")` の両方チェック）なら `{ watchlist: {}, corrupted: false }`、それ以外の catch（JSON 破損等）なら `{ watchlist: {}, corrupted: true }` を返す（D-18）
    - `fetchQuoteTypesAndNames(tickers)` は batch quote() 1回で `ReadonlyMap<symbol, QuoteTypeLookup>` と `ReadonlyMap<symbol, {name?, nameJa?}>` を同時に構築する（longName を name、日本語名が取れなければ nameJa は undefined, D-04/D-22）
    - tickers.length === 0 のとき quote() を呼ばず空 Map を返す（0候補ショートサーキット）
  </behavior>
  <action>
    `src/scripts/write-watchlist.ts` を新規作成する。ES module 拡張子付き import（`.js`）で `readFile, writeFile, mkdir` を `node:fs/promises` から、`join` を `node:path` から、`fileURLToPath` を `node:url` から、`YahooFinance` を `yahoo-finance2` から、`admitBullishStocks, pruneWatchlist, getActiveWatchlistEntries` と型 `WatchlistFile, WatchlistCandidate` を `../portfolio/watchlist.js` から、`isValidDateKey` を `../portfolio/urgency-history.js` から、型 `QuoteTypeLookup` を `../portfolio/etf-exclusion.js` から、`PORTFOLIO_HOLDINGS` を `../portfolio/holdings.js` から、型 `MeetingResult` を `../meeting/types.js` から import する。

    path 定数を write-urgency-history.ts と同一様式で定義する: `TMP_DIR = join(import.meta.dirname, "../../tmp")`, `DATA_DIR = join(import.meta.dirname, "../../data")`, `WATCHLIST_PATH = join(DATA_DIR, "watchlist.json")`。

    `loadExistingWatchlist(): Promise<{ watchlist: WatchlistFile; corrupted: boolean }>` を write-urgency-history.ts の `loadExistingHistory` を verbatim ベースに実装する（型名を WatchlistFile に、PATH を WATCHLIST_PATH に変えるだけ）。ENOENT 判定は `(error as NodeJS.ErrnoException).code === "ENOENT" || (error instanceof Error && error.message.includes("ENOENT"))` の二段チェックを踏襲する（D-18、Phase 25 のテストモック規約）。パースは `JSON.parse(raw) as WatchlistFile` の防御的パース様式（loadUrgencyHistory と同じ throw-しない防御）で行い zod は使わない（D-19: 自社生成ファイルのため alias 硬化不要）。

    `yahooFinance` インスタンスを filter-etf-stocks.ts と同一の `new YahooFinance({ suppressNotices: ["yahooSurvey"] })` で作る。`fetchQuoteTypesAndNames(tickers: ReadonlyArray<string>): Promise<{ quoteTypeByTicker: ReadonlyMap<string, QuoteTypeLookup>; nameByTicker: ReadonlyMap<string, { name?: string; nameJa?: string }> }>` を実装する。filter-etf-stocks.ts の fetchQuoteTypes の batch quote() ループ（`yahooFinance.quote([...tickers])` → `Array.isArray(quotes) ? quotes : [quotes]`）を流用し、同一ループ内で `(q as { symbol?, quoteType?, longName?, shortName? })` を読み、symbol&quoteType があれば quoteTypeByTicker に `{status:"ok",quoteType}`、symbol があれば nameByTicker に `{ name: longName ?? shortName, nameJa: undefined }` をセットする（1コール2役, D-04/D-22。追加 API 呼び出しゼロ）。tickers.length===0 なら空 Map ペアを返す。batch 例外は呼び出し元（main）の try/catch へ伝播させる（filter-etf-stocks.ts と同じ「メカニズム故障」扱い）。

    テストを先に書く（RED→GREEN）: loadExistingWatchlist の ENOENT ケース（`{ watchlist:{}, corrupted:false }`）・破損 JSON ケース（`{ watchlist:{}, corrupted:true }`）を、write-urgency-history.ts のテスト規約（プレーンな `Error("ENOENT")` で欠損シミュレート）で検証する。readFile を vi.mock でモックする。fetchQuoteTypesAndNames は yahoo-finance2 の quote をモックし、longName が name に入ること・quoteType が拾えることを検証する。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/write-watchlist.test.ts -t "ENOENT|corrupted|quote"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "message.includes(\"ENOENT\")" src/scripts/write-watchlist.ts` がヒットする（二段フェイル, D-18）
    - `grep -n "new YahooFinance" src/scripts/write-watchlist.ts` が `suppressNotices` 付きでヒットする（filter-etf-stocks.ts 規約）
    - `grep -cE "yahooFinance\.quote\(" src/scripts/write-watchlist.ts` が 1（batch 1回のみ, D-22。per-ticker ループ内 quote 呼び出しなし）
    - `grep -n "longName\|shortName" src/scripts/write-watchlist.ts` がヒットする（社名 1コール2役, D-04）
    - ENOENT テストが corrupted:false、破損 JSON テストが corrupted:true を検証している（D-18）
    - `npx vitest run src/scripts/write-watchlist.test.ts -t "ENOENT|corrupted|quote"` が pass
  </acceptance_criteria>
  <done>loadExistingWatchlist（二段フェイル）と fetchQuoteTypesAndNames（batch 1回で quoteType+社名）が実装され、ENOENT/破損/quote モックの各テストが green。</done>
</task>

<task type="auto">
  <name>Task 2: main() で prune→admit を合成し STEP マーカー・サイズログ・破損保全を配線する</name>
  <files>src/scripts/write-watchlist.ts, src/scripts/write-watchlist.test.ts</files>
  <read_first>
    - src/scripts/write-watchlist.ts（Task 1 のローダー・quote 取得関数・path 定数）
    - src/scripts/write-urgency-history.ts（main() の全体骨格: mkdir 先行 → corrupted 分岐 → meeting-result 読み込み → dateKey 検証 → 純関数合成 → writeFile → OK ログ・エントリポイントガード）
    - src/scripts/filter-etf-stocks.ts（batch 取得の try/catch fail-soft・0件ショートサーキット・除外理由ログ様式）
    - src/portfolio/watchlist.ts（admitBullishStocks/pruneWatchlist/getActiveWatchlistEntries のシグネチャ）
    - .planning/phases/28-watchlist-persistence/28-RESEARCH.md（Pitfall 3: prune を admit より先に実行して purchased/downgraded を優先。System Architecture Diagram の処理順 5→6）
  </read_first>
  <behavior>
    - mkdir(DATA_DIR,{recursive:true}) を全分岐より先に実行（git add data/ が exit 128 で失敗しないため, write-urgency-history.ts Pitfall 1 対策）
    - corrupted===true なら writeFile に到達せず `[STEP:watchlist:FAIL:corrupted]` 相当を出力し既存ファイルを保全（D-18）
    - dateKey は tmp/meeting-result.json の date フィールド由来・JST再導出しない・isValidDateKey で検証（D-13）
    - 処理順は prune（PORTFOLIO_HOLDINGS + 当日 highlighted + today）→ admit（当日 verdict 強気 + quote 結果 + today）。purchased/downgraded が「今日も強気」に優先される（Pitfall 3）
    - batch quote() 失敗は fail-soft: writeFile に到達させず既存ファイル維持・[STEP:watchlist:FAIL:quote] 相当（社名なしで登録＝ETF未検証登録を避ける, D-18/Pitfall 1）
    - 成功時 `[STEP:watchlist:OK]` と `[watchlist] active=N件, removed=M件` を出力（D-16/D-10）
    - どの失敗分岐でもパイプライン全体停止マーカー（invest.md の全レポート生成を止める種類）を出さず、watchlist スコープの FAIL マーカーのみ出力する（D-16）
  </behavior>
  <action>
    `main(): Promise<void>` を実装する。write-urgency-history.ts の main 骨格を土台にする。

    処理順: (1) `await mkdir(DATA_DIR, { recursive: true })` を最初に実行（Pitfall 1）。(2) `loadExistingWatchlist()` を呼び、`corrupted` なら日本語で「既存の data/watchlist.json が破損しています。保全のため今回の書き込みをスキップします。」を console.error し、`[STEP:watchlist:FAIL:corrupted]` を console.error で出力して return（writeFile に到達しない, D-18）。(3) tmp/meeting-result.json を読み `{ date, highlightedStocks }` を取り出す。読み込み/パース失敗は fail-soft: console.error + `[STEP:watchlist:FAIL:meeting-read]` で return（元ファイル維持）。(4) `isValidDateKey(date)` が false なら console.error + `[STEP:watchlist:FAIL:invalid-date]` で return（D-13）。(5) 当日 highlightedStocks から verdict==="強気" の銘柄を新規候補 ticker として抽出し、既存 watchlist に active でない ticker のみを batch quote() 対象にする（D-22: 既存登録済みは再検証不要）。(6) `fetchQuoteTypesAndNames(candidateTickers)` を try/catch で呼び、例外なら console.error + `[STEP:watchlist:FAIL:quote]` で return（fail-soft、既存ファイル維持、社名/ETF未検証のまま登録しない, D-18/Pitfall 1）。(7) `pruneWatchlist(existingWatchlist, highlightedStocks, PORTFOLIO_HOLDINGS, date)` を先に実行し、その結果に対し `admitBullishStocks(pruned, 強気stocks, quoteTypeByTicker, nameByTicker, date)` を実行する（prune→admit 順、Pitfall 3）。(8) `writeFile(WATCHLIST_PATH, JSON.stringify(result, null, 2), "utf-8")`。(9) `getActiveWatchlistEntries(result).length` を active 件数、`Object.keys(result).length - active` 相当を removed 件数として `console.log(\`[watchlist] active=${active}件, removed=${removed}件\`)` を出力（D-10 サイズ計測、console.log=監査チャネル規約）。(10) `console.error("[STEP:watchlist:OK]")`（D-16、console.error=STEP ステータスチャネル規約）。

    どの FAIL 分岐でもパイプライン全体停止マーカー（invest.md の全レポート生成を止める種類のマーカー）を出力しない（D-16）。出力するのは watchlist スコープの `[STEP:watchlist:FAIL:<reason>]` のみに限定する。fail-soft 分岐では process.exit を使わず return で抜ける（invest.md 側が終了コードに関わらず継続するため、write-urgency-history.ts の corrupted 分岐が process.exit(1) を使う点と異なり、本ステップは D-16 で「終了コード非0でもパイプライン継続」なので exit コードよりも STEP マーカー文字列で状態を伝える。process.exit を使う場合も invest.md が握り潰す設計だが、テスト容易性のため return を優先する）。

    エントリポイントガードは write-urgency-history.ts の `if (process.argv[1] === fileURLToPath(import.meta.url)) { main().catch(...) }` を verbatim 流用する。

    テスト（RED→GREEN）を追加する: 破損時に writeFile が呼ばれず `[STEP:watchlist:FAIL:corrupted]` が出ることを検証（vi.spyOn(console,"error") + writeFile モックが未呼び出し）。dateKey 不正時に FAIL マーカーで return することを検証。成功時に `[STEP:watchlist:OK]` と `active=…件, removed=…件` ログが出ることを検証。全 FAIL 分岐でパイプライン全体停止マーカー（D-16 で禁止された種類）を含む出力が一度も無いことを、キャプチャした console 出力に対するアサーションで検証する（アサーションの検索文字列はテストコード側で定義する）。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/write-watchlist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mkdir(DATA_DIR" src/scripts/write-watchlist.ts` が main 冒頭に存在する（Pitfall 1）
    - `grep -n "\[STEP:watchlist:OK\]" src/scripts/write-watchlist.ts` と `grep -c "\[STEP:watchlist:FAIL:" src/scripts/write-watchlist.ts` が 1以上（D-16 マーカー）
    - `grep -c "\[PIPELINE:FAIL\]" src/scripts/write-watchlist.ts` が 0（D-16: 絶対に出さない）
    - `grep -n "active=" src/scripts/write-watchlist.ts` が console.log のサイズ計測行にヒットする（D-10）
    - main 内で pruneWatchlist が admitBullishStocks より前に呼ばれている（Pitfall 3、prune→admit 順）
    - 破損テストで writeFile が呼ばれないことを検証している（D-18 履歴保全）
    - 成功テストで `[STEP:watchlist:OK]` が出ることを検証している
    - `[PIPELINE:FAIL]` がどのテストの出力にも現れないことを検証している（D-16）
    - `npx vitest run src/scripts/write-watchlist.test.ts` 全体が pass
  </acceptance_criteria>
  <done>main() が prune→admit 合成・STEP マーカー・サイズログ・破損保全・fail-soft 全分岐を配線し、write-watchlist.test.ts 全体が green。data/watchlist.json が生成される。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmp/meeting-result.json（LLM由来）→ write-watchlist.ts | date/highlightedStocks が untrusted。dateKey は正規表現検証、ticker は純関数側で正規化 |
| yahoo-finance2 quote() 応答（外部API）→ ETF第2ゲート | ネットワーク障害・部分応答が fail-open を招く危険 |
| 既存 data/watchlist.json（蓄積履歴）→ 上書き | 破損ファイルを空で上書きすると全履歴喪失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-04 | Tampering (integrity) | loadExistingWatchlist の破損処理 | high | mitigate | ENOENT（欠損=空初期化）と破損（上書きせず FAIL）を二段フェイルで厳密に分離（D-18）。破損時 writeFile に到達しないことをテストで検証（Pitfall 4） |
| T-28-05 | Tampering / Repudiation | batch quote() 失敗時の ETF ゲート fail-open | high | mitigate | quote() 例外時は writeFile に到達させず既存ファイル維持・[STEP:watchlist:FAIL:quote]（社名/ETF未検証のまま登録しない, Pitfall 1）。lookup 失敗銘柄は純関数側 filterEtfStocks が fail-closed で除外 |
| T-28-06 | Input Validation | dateKey をオブジェクトキー・パス生成に使用 | medium | mitigate | isValidDateKey（`/^\d{4}-\d{2}-\d{2}$/`）で検証してから使用（D-13）。JST 再導出せず meeting-result.json の date を単一情報源にする |
| T-28-SC | Tampering | npm/外部パッケージ導入 | low | accept | 本フェーズは新規パッケージを導入しない（yahoo-finance2 は既存・Phase 27 で使用実績）。Package Legitimacy Gate はトリガー条件外でスキップ（RESEARCH.md 明記） |
</threat_model>

<verification>
- `npx vitest run src/scripts/write-watchlist.test.ts` が全ケース green
- `grep -c "\[PIPELINE:FAIL\]" src/scripts/write-watchlist.ts` が 0（D-16）
- batch quote() が1回のみ（D-22）: `grep -cE "yahooFinance\.quote\(" src/scripts/write-watchlist.ts` が 1
- スモーク: fixture tmp/meeting-result.json を用意し `npx tsx src/scripts/write-watchlist.ts` を実行、stderr に `[STEP:watchlist:OK]` が出て data/watchlist.json が生成されることを確認（手動/Wave merge 時）
- `npm test`（フルスイート）が green（Plan 01 のテストと合わせて回帰なし）
</verification>

<success_criteria>
- write-watchlist.ts が Plan 01 の純関数を prune→admit 順で合成し data/watchlist.json を更新
- batch quote() 1回で quoteType（ETF第2ゲート）と社名を取得（D-04/D-22）
- ENOENT=空初期化・破損=上書きスキップの二段フェイル（D-18）
- [STEP:watchlist:OK/FAIL] マーカー・サイズ計測ログ・[PIPELINE:FAIL] 非出力（D-16/D-10）
</success_criteria>

<output>
Create `.planning/phases/28-watchlist-persistence/28-02-SUMMARY.md` when done
</output>
