---
phase: 28-watchlist-persistence
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - .claude/commands/invest.md
autonomous: false
requirements: [WLST-01, WLST-02, WLST-03, WLST-04, WLST-05]

must_haves:
  truths:
    - "invest.md の Step 2g（filter-etf-stocks → validate-meeting）完了直後・Step 3 開始前に write-watchlist.ts の新ステップが挿入される (D-15)"
    - "新ステップは終了コードに関わらず次へ進む fail-soft で、[STEP:watchlist:OK] / [STEP:watchlist:FAIL:<理由>] を出力し [PIPELINE:FAIL] を出さない (D-16)"
    - "Step 4 の git add は既に data/ を含むため watchlist.json は追加変更なしで自動コミットされる（実装時に確認のみ）(D-20)"
  artifacts:
    - ".claude/commands/invest.md（Step 2g 直後に write-watchlist 新ステップ追記）"
  key_links:
    - "invest.md 新ステップ → src/scripts/write-watchlist.ts（Plan 02 成果物）を npx tsx で実行"
    - "invest.md 新ステップ → tmp/meeting-result.json（Step 2g で ETF除外・検証済み）を入力とする"
    - "invest.md Step 4 → git add docs/ docs_old/ data/（既存行が data/watchlist.json を包含, D-20）"
---

<objective>
`.claude/commands/invest.md` を編集し、ウォッチリスト更新ステップ（`npx tsx src/scripts/write-watchlist.ts`）を Step 2g の `validate-meeting.ts` 実行完了直後・Step 3 開始前に挿入する（D-15）。既存の `[STEP:etf-exclusion:*]` ブロックと同一の fail-soft 様式（終了コード非依存・STEP マーカー・`[PIPELINE:FAIL]` 非出力）を踏襲する（D-16）。Step 4 の `git add docs/ docs_old/ data/` は既に data/ を含むため変更不要だが、実装時に該当行を確認して data/watchlist.json が自動コミット対象であることを検証する（D-20）。

Purpose: Plan 01/02 の成果物を実パイプラインに接続し、ETF除外済み・バリデーション済みの meeting-result を入力にウォッチリスト状態が Phase 29/30 より前に確定する順序を構造的に保証する。
Output: 編集済み `.claude/commands/invest.md`（Step 2g と Step 3 の間に新ステップ）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-watchlist-persistence/28-CONTEXT.md
@.planning/phases/28-watchlist-persistence/28-PATTERNS.md
@.claude/commands/invest.md
</context>

<artifacts_this_phase_produces>
本プランは既存ファイル `.claude/commands/invest.md` を編集するのみで、新規シンボルは作成しない。挿入する新ステップは既存の Step 2g / Step 3f の STEP マーカー様式（`[STEP:<name>:OK]` / `[STEP:<name>:FAIL:<reason>]`）を踏襲する。参照する `write-watchlist.ts` は Plan 02 の成果物。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: invest.md の Step 2g 直後に write-watchlist の新ステップを挿入する</name>
  <files>.claude/commands/invest.md</files>
  <read_first>
    - .claude/commands/invest.md（Step 2g 全体: 行1154〜1248 付近。filter-etf-stocks.ts 実行ブロック（1170〜1194）の `[STEP:etf-exclusion:OK]` / `[STEP:etf-exclusion:FAIL:<短い理由>]` / 「`[PIPELINE:FAIL]` は絶対に出力しないこと」の fail-soft 文言様式、validate-meeting.ts 実行（1197）、`[STEP:round-3:OK]`（1234）、validationEnd 記録（1237〜1247）、そして Step 3 開始（1251〜）の境界を確認する）
    - .claude/commands/invest.md（Step 3f: 行1948〜1958 付近。write-urgency-history.ts の fail-soft ステップ記述様式 — 「終了コードに関わらず次のステップへ進んでください（fail-soft, D-xx）」文言の雛形）
    - .claude/commands/invest.md（Step 4 の git add 行: 行2172〜2178 付近。`git add docs/ docs_old/ data/` が data/ を包含していることを確認 — D-20）
    - src/scripts/write-watchlist.ts（Plan 02 成果物: 実行コマンド・出力する STEP マーカー文字列 `[STEP:watchlist:OK]` / `[STEP:watchlist:FAIL:<reason>]` を確認）
  </read_first>
  <action>
    D-15 に従い、Step 2g の `validate-meeting.ts` 実行と Step 2 完了サマリー表示が済んだ**後**、`## Step 3: WebSearch リサーチ & レポート生成`（行1251 付近）の**前**に、新しいステップ `### Step 2h: ウォッチリスト更新` を挿入する。挿入は既存記述を置換せず追記する（Step 2g の filter-etf-stocks/validate-meeting/round-3 マーカー・validationEnd 記録は一切変更しない）。

    新ステップの内容は Step 2g の `[STEP:etf-exclusion:*]` ブロック（行1170〜1194）と Step 3f の write-urgency-history 記述を雛形にして構成する:
    - ユーザー向け表示文言（例: 「ウォッチリストを更新中...」）
    - 実行コマンド: `cd /Users/arai/invest && npx tsx src/scripts/write-watchlist.ts`
    - fail-soft 指示: 「終了コードに関わらず、次のステップ（Step 3）へ進んでください（fail-soft, D-16）」。write-watchlist.ts は tmp/meeting-result.json（ETF除外・検証済みの highlightedStocks）+ data/watchlist.json（前日状態）+ PORTFOLIO_HOLDINGS を入力に、当日強気銘柄を登録し降格/購入/失効を除外して data/watchlist.json を更新する旨を1〜2文で説明する。**必ず filter-etf-stocks.ts / validate-meeting.ts より後に実行する**旨を明記（D-15 の入力前提）。
    - STEP マーカー出力: write-watchlist.ts 自身が stderr に `[STEP:watchlist:OK]` / `[STEP:watchlist:FAIL:<reason>]` を出すため、Step 2g の etf-exclusion ブロックと同様に「スクリプトの stderr に出た `[STEP:watchlist:*]` 行をそのまま尊重し、追加で echo する場合は同じ形式を使う」方針にする（filter-etf-stocks.ts は invest.md 側で echo するが、write-watchlist.ts はスクリプト自身がマーカーを出す設計 = Plan 02 のため、二重出力を避けるならスクリプト出力を採用する。実装時に既存 Step 2g がどちらの方式かを確認し整合させる）。
    - 「このステップの失敗は既存4レポートの生成・デプロイを一切ブロックしない（fail-soft, D-16）」旨を明記する。パイプライン全体停止マーカー（invest.md 全体を止める種類のマーカー文字列）を新ステップの記述に echo 命令として書き込まないこと — 新ステップが出力するのは write-watchlist.ts 自身の watchlist スコープの STEP マーカーのみ。既存 Step 2g の当該禁止マーカー文言は変更・複製しない（invest.md 内の当該マーカー文字列の総出現数を編集前後で不変に保つため）。

    Step 4 の `git add docs/ docs_old/ data/` 行（行2178 付近）は変更しない。data/ が既に含まれ data/watchlist.json が自動コミットされることを read_first で確認済みであることを SUMMARY に記録する（D-20: 変更不要・確認のみ）。
  </action>
  <verify>
    <automated>grep -c "write-watchlist.ts" .claude/commands/invest.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "npx tsx src/scripts/write-watchlist.ts" .claude/commands/invest.md` が 1（新ステップ挿入）
    - `grep -n "Step 2h\|ウォッチリスト更新" .claude/commands/invest.md` がヒットする（新ステップ見出し）
    - 新ステップが `validate-meeting.ts`（行1197 付近）より後・`## Step 3`（行1251 付近）より前に位置する: `awk '/npx tsx src\/scripts\/validate-meeting.ts/{v=NR} /npx tsx src\/scripts\/write-watchlist.ts/{w=NR} /^## Step 3:/{s=NR} END{print (v<w && w<s) ? "OK" : "BAD"}' .claude/commands/invest.md` が `OK`（D-15 順序）
    - `grep -c "\[STEP:watchlist:" .claude/commands/invest.md` が 1以上（マーカー言及）
    - 新ステップ近傍に fail-soft 文言（「fail-soft」または「終了コードに関わらず」）が存在する
    - `grep -c "git add docs/ docs_old/ data/" .claude/commands/invest.md` が既存どおり 1（Step 4 未変更, D-20）— 既存の filter-etf / validate-meeting / round-3 マーカーの件数が編集前後で不変
    - `[PIPELINE:FAIL]` を write-watchlist 新ステップの記述が echo で出力していない（D-16）
  </acceptance_criteria>
  <done>invest.md の Step 2g 直後に write-watchlist の fail-soft 新ステップが挿入され、順序（validate-meeting 後・Step 3 前）・STEP マーカー・[PIPELINE:FAIL] 非出力・Step 4 未変更が満たされている。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: launchd 実行での watchlist ステップ・data/watchlist.json 生成を人手確認する</name>
  <files>.claude/commands/invest.md</files>
  <action>Task 1 で挿入した Step 2h の実パイプライン挙動を人手で検証する。手動スモークまたは翌朝の launchd 実行ログで `[STEP:watchlist:OK]` 出力・data/watchlist.json 生成・自動コミット・既存4レポート無影響を確認する（静的解析で確認不能な項目、28-VALIDATION.md の Manual-Only）。</action>
  <what-built>invest.md に Step 2h（ウォッチリスト更新）を fail-soft ステップとして挿入した。write-watchlist.ts が日次パイプラインで実行され data/watchlist.json を生成・更新する配線。静的解析（grep/awk による順序・マーカー検証）は Task 1 で完了しているが、実 launchd 実行での STEP マーカー出力・data/watchlist.json 実生成・自動コミットは静的検証では確認できない（28-VALIDATION.md の Manual-Only 項目）。</what-built>
  <how-to-verify>
    1. 手動スモーク（任意・即時確認）: fixture として tmp/meeting-result.json に verdict:"強気" を含む highlightedStocks と有効な date（YYYY-MM-DD）を用意し、`cd /Users/arai/invest && npx tsx src/scripts/write-watchlist.ts` を実行する。期待: stderr に `[STEP:watchlist:OK]`、stdout に `[watchlist] active=N件, removed=M件`、data/watchlist.json が生成され強気銘柄が active エントリで入っている。`[PIPELINE:FAIL]` が出ないこと。
    2. ライブ検証（翌朝の launchd 日次実行後）: 実行ログで Step 2h 相当の `[STEP:watchlist:OK]` が filter-etf-stocks / validate-meeting の後に出ていること、data/watchlist.json が更新・`git add docs/ docs_old/ data/` でコミットされていることを確認する。
    3. 既存4レポート（daily/portfolio/meeting-minutes/news-digest）の生成・デプロイが本ステップ追加後も一切影響を受けていないこと（fail-soft, OPS-06 分担）。
  </how-to-verify>
  <resume-signal>「approved」と入力するか、観測された問題（マーカー未出力・watchlist.json 未生成・レポート影響など）を記述してください</resume-signal>
  <verify><human-check>手動スモークまたは launchd 実行ログで `[STEP:watchlist:OK]`・data/watchlist.json 生成・既存4レポート無影響を確認</human-check></verify>
  <done>実行時に data/watchlist.json が生成・コミットされ、[STEP:watchlist:OK] が出力され、既存4レポートの生成・デプロイが影響を受けないことを人手で確認した</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invest.md パイプライン定義 → write-watchlist.ts 実行 | ステップ順序の誤りが Phase 29/30 の入力（確定済み watchlist）契約を壊す |
| write-watchlist.ts 終了コード → パイプライン継続判断 | fail-soft を守らないと1ステップ失敗が既存4レポートのデプロイを止める |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-07 | Denial of Service (可用性) | 新ステップの fail-soft 逸脱 | high | mitigate | Step 2g の etf-exclusion ブロックと同一の「終了コード非依存で次へ進む」文言・`[PIPELINE:FAIL]` 非出力を明記（D-16）。human-verify で既存4レポートへの無影響を確認 |
| T-28-08 | Tampering (順序) | write-watchlist を validate-meeting より前に配置 | medium | mitigate | grep/awk で validate-meeting 後・Step 3 前の順序を acceptance_criteria で構造的に検証（D-15）。ETF除外・検証済み meeting-result を入力とする前提を保証 |
</threat_model>

<verification>
- `grep -c "npx tsx src/scripts/write-watchlist.ts" .claude/commands/invest.md` が 1
- 順序検証 awk（validate-meeting < write-watchlist < Step 3）が OK（D-15）
- `grep -c "\[PIPELINE:FAIL\]" .claude/commands/invest.md` が編集前後で不変（新ステップが増やさない, D-16）
- Step 4 の `git add docs/ docs_old/ data/` が未変更（D-20）
- human-verify チェックポイントで実 launchd 実行のマーカー・watchlist.json 生成・レポート無影響を確認
</verification>

<success_criteria>
- write-watchlist ステップが Step 2g 直後・Step 3 前に fail-soft で挿入されている（D-15/D-16）
- 既存 Step 2g（filter-etf/validate-meeting/round-3）と Step 4（git add）が未変更（D-20）
- 実行時に data/watchlist.json が生成・コミットされ、既存4レポートに影響しない（human-verify 確認）
</success_criteria>

<output>
Create `.planning/phases/28-watchlist-persistence/28-03-SUMMARY.md` when done
</output>
