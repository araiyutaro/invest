---
phase: 28-watchlist-persistence
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/portfolio/watchlist.ts
  - src/portfolio/watchlist.test.ts
autonomous: true
requirements: [WLST-01, WLST-02, WLST-03, WLST-04, WLST-05]

must_haves:
  truths:
    - "当日 verdict 強気 の highlightedStocks 銘柄（ETF除外後）が ticker キーで watchlist の active エピソードとして登録される (WLST-01)"
    - "既にアクティブな銘柄が当日再度強気なら lastVerdictDate が today に更新される（addedDate は初回値保持, 冪等）(WLST-01)"
    - "verdict が中立/弱気に転落した active 銘柄は removedReason=downgraded で除外され、レコードは history に残る (WLST-02, WLST-05)"
    - "PORTFOLIO_HOLDINGS に含まれる active 銘柄は removedReason=purchased で除外される (WLST-03)"
    - "lastVerdictDate から EXPIRY_CALENDAR_DAYS 超過の active 銘柄は removedReason=expired で失効する (WLST-04)"
    - "除外・失効しても removedReason/removedDate が history に保持され、再追加は過去の除外記録を破壊せず新エピソードを作る (WLST-05, D-05)"
  artifacts:
    - "src/portfolio/watchlist.ts (純関数モジュール: 型定義・admitBullishStocks・pruneWatchlist・EXPIRY_CALENDAR_DAYS・getActiveWatchlistEntries)"
    - "src/portfolio/watchlist.test.ts (WLST-01〜05 の単体テスト)"
  key_links:
    - "watchlist.ts → etf-exclusion.ts の filterEtfStocks（第2ゲート, D-21）を admitBullishStocks 内で呼ぶ"
    - "watchlist.ts → holding-news.ts の normalizeHoldingSymbol を ticker キー正規化に再利用"
    - "watchlist.ts → urgency-history.ts の isValidDateKey と同一正規表現を日付文字列検証に使う（D-03）"
    - "getActiveWatchlistEntries は Phase 29/30 が消費する安定シグネチャ（D-06）"
---

<objective>
`src/portfolio/watchlist.ts` を純関数モジュールとして実装し、ウォッチリストの admit（強気銘柄登録）/ prune（3トリガー除外）/ アクティブ銘柄導出のコアロジックを提供する。すべて I/O 非依存・throw-free。同時に `src/portfolio/watchlist.test.ts` を先に書いて RED→GREEN で駆動する（TDD, workflow.tdd_mode は無効だが本モジュールは入出力が明確なため TDD 適用）。

Purpose: 本フェーズの全ビジネスロジック（WLST-01〜05）を LLM 非関与の決定論的 TS として一箇所に集約し、Wave 2 の CLI ラッパー・Phase 29/30 の下流消費者が依存する安定した純関数境界を確立する。
Output: `src/portfolio/watchlist.ts`（型定義・純関数群・EXPIRY_CALENDAR_DAYS 定数）と `src/portfolio/watchlist.test.ts`（admit/prune/失効/再追加/冪等性の単体テスト）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-watchlist-persistence/28-CONTEXT.md
@.planning/phases/28-watchlist-persistence/28-RESEARCH.md
@.planning/phases/28-watchlist-persistence/28-PATTERNS.md
</context>

<artifacts_this_phase_produces>
以下のシンボルは本フェーズが新規作成する（下流ドリフト検証の対象外）:

- `src/portfolio/watchlist.ts`（新規ファイル）
  - `WatchlistRemovalEpisode`（interface）— addedDate, lastVerdictDate, removedReason, removedDate フィールド
  - `WatchlistEntry`（interface）— ticker, name?, nameJa?, addedDate?, lastVerdictDate?, history フィールド
  - `WatchlistFile`（type）— `Record<string, WatchlistEntry>`
  - `WatchlistCandidate`（type/interface）— admit に渡す新規候補の社名付きシェイプ（ticker, name?, nameJa?）
  - `EXPIRY_CALENDAR_DAYS`（const = 30）
  - `admitBullishStocks`（function）
  - `pruneWatchlist`（function）
  - `getActiveWatchlistEntries`（function）
  - `isActive`（function, entry のアクティブ判定）
- `src/portfolio/watchlist.test.ts`（新規ファイル）

再利用（本フェーズが作成しない既存シンボル）: `filterEtfStocks`, `QuoteTypeLookup`, `EtfExclusionResult`（etf-exclusion.ts）; `normalizeHoldingSymbol`（holding-news.ts）; `isValidDateKey`（urgency-history.ts）; `PORTFOLIO_HOLDINGS`, `PortfolioHolding`（holdings.ts）; `MeetingResult`（meeting/types.ts）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 型定義と定数・アクティブ導出関数を実装する（TDD RED→GREEN）</name>
  <files>src/portfolio/watchlist.ts, src/portfolio/watchlist.test.ts</files>
  <read_first>
    - src/portfolio/urgency-history.ts（純関数モジュールの構造・isValidDateKey 正規表現・immutable spread 規約・日本語 JSDoc 慣行の雛形）
    - src/portfolio/etf-exclusion.ts（QuoteTypeLookup 判別可能ユニオン型・readonly 型の書式・fail-closed 規約）
    - src/portfolio/etf-exclusion.test.ts（makeHighlightedStock / makeQuoteLookup フィクスチャビルダー規約・describe("fn (D-xx)")・日本語 it 記述の命名規約）
    - src/portfolio/holding-news.ts（normalizeHoldingSymbol の trim+toUpperCase 契約: " 8522.t " → "8522.T"）
    - src/portfolio/holdings.ts（PortfolioHolding 型・PORTFOLIO_HOLDINGS の .T サフィックス保持）
    - src/meeting/types.ts（MeetingResult.highlightedStocks の要素型: ticker/averageScore/verdict("強気"|"中立"|"弱気")/summary/agentScores/nominatedBy）
  </read_first>
  <behavior>
    - `WatchlistEntry` の `isActive(entry)` は `entry.addedDate !== undefined` のとき true、除外済み（addedDate が undefined で history のみ）のとき false を返す
    - `getActiveWatchlistEntries(watchlist)` は `isActive` が true のエントリのみを配列で返す（Phase 29/30 が依存する安定シグネチャ, D-06）
    - `EXPIRY_CALENDAR_DAYS` は 30（named constant, D-08/D-09）
    - 日付文字列（addedDate/lastVerdictDate/removedDate）の検証は urgency-history.ts の `isValidDateKey`（`/^\d{4}-\d{2}-\d{2}$/`）と同一（import 再利用, D-03）
  </behavior>
  <action>
    `src/portfolio/watchlist.ts` を新規作成する。ES module 拡張子付き import（`.js`）規約に従い、`normalizeHoldingSymbol` を `./holding-news.js` から、`filterEtfStocks` と型 `QuoteTypeLookup` を `./etf-exclusion.js` から、`isValidDateKey` を `./urgency-history.js` から、型 `MeetingResult` を `../meeting/types.js` から、型 `PortfolioHolding` を `./holdings.js` から import する（isValidDateKey は再定義せず再利用する — D-03 が同一正規表現の再利用を明示的に要求）。

    D-06 のスキーマ形状として「active フィールド + history 配列」方式を採用する（episode 配列方式ではなく）。理由: `isActive` が `addedDate !== undefined` の単純判定で済み、Phase 29/30 の active 導出が軽い（Open Question 1 の推奨解）。

    型を定義する: `WatchlistRemovalEpisode`（readonly: addedDate, lastVerdictDate, removedReason("downgraded"|"purchased"|"expired" — D-02 の正準 enum、REQUIREMENTS.md の値。research ARCHITECTURE の verdict-downgrade/entered-portfolio は採用しない), removedDate — すべて YYYY-MM-DD 文字列）。`WatchlistEntry`（readonly: ticker; name?; nameJa?; addedDate?; lastVerdictDate?; history: `ReadonlyArray<WatchlistRemovalEpisode>`）。`WatchlistFile = Record<string, WatchlistEntry>`（キーは normalizeHoldingSymbol 済み ticker）。admit に渡す新規候補型 `WatchlistCandidate`（readonly: ticker; name?; nameJa? — CLI ラッパーが batch quote() の longName/shortName から埋める, D-04）。

    定数 `EXPIRY_CALENDAR_DAYS = 30` を名前付きで一箇所に定義する（D-09、マジックナンバー分散禁止）。

    純関数 `isActive(entry: WatchlistEntry): boolean` を `entry.addedDate !== undefined` で実装する。`getActiveWatchlistEntries(watchlist: WatchlistFile): ReadonlyArray<WatchlistEntry>` を `Object.values(watchlist).filter(isActive)` で実装する（D-06 の下流安定シグネチャ）。両関数を export する。

    すべての型に readonly を付け、関数は入力を mutate しない（グローバル coding-style: イミュータブル）。JSDoc は日本語で、駆動する決定 ID（D-01〜D-06）を明記する（既存モジュールの慣行）。

    先に `src/portfolio/watchlist.test.ts` に `isActive` / `getActiveWatchlistEntries` / `EXPIRY_CALENDAR_DAYS` のテストを書いて RED を確認してから実装で GREEN にする。テストは etf-exclusion.test.ts の import 書式（`from "./watchlist.js"`）・`describe("fn (D-xx)", ...)`・日本語 it 記述を踏襲する。`makeWatchlistEntry` オーバーライドビルダーをこのファイルで定義する（active エントリ・除外済みエントリ両方を作れるように）。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/watchlist.test.ts -t "isActive|getActiveWatchlistEntries|EXPIRY"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EXPIRY_CALENDAR_DAYS = 30" src/portfolio/watchlist.ts` が1件ヒットする（named constant, D-09）
    - `grep -c 'removedReason.*"downgraded".*"purchased".*"expired"\|"downgraded" | "purchased" | "expired"' src/portfolio/watchlist.ts` が1以上（D-02 正準 enum）
    - `grep -n "isValidDateKey" src/portfolio/watchlist.ts` が import 行を含む（再定義ではなく再利用, D-03）
    - `grep -n "export function getActiveWatchlistEntries" src/portfolio/watchlist.ts` がヒットする（D-06 下流シグネチャ）
    - `import.*from "yahoo-finance2"` および `readFile\|writeFile\|process.exit` が watchlist.ts に出現しない（純関数・I/O 非依存, D-14）— `grep -vE '^\s*(//|\*)' src/portfolio/watchlist.ts | grep -cE 'yahoo-finance2|readFile|writeFile|process\.exit'` が 0
    - `npx vitest run src/portfolio/watchlist.test.ts -t "isActive|getActiveWatchlistEntries|EXPIRY"` が pass
  </acceptance_criteria>
  <done>型定義・EXPIRY_CALENDAR_DAYS 定数・isActive/getActiveWatchlistEntries が実装され、対応テストが green。watchlist.ts に I/O・ネットワーク依存が一切ない。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admitBullishStocks を実装する（強気登録・冪等・ETF第2ゲート・lastVerdictDate 更新）</name>
  <files>src/portfolio/watchlist.ts, src/portfolio/watchlist.test.ts</files>
  <read_first>
    - src/portfolio/watchlist.ts（Task 1 で作った型・定数・アクティブ導出関数）
    - src/portfolio/urgency-history.ts（appendUrgencySnapshot の spread-merge 冪等パターン: `{ ...history, [key]: value }`）
    - src/portfolio/etf-exclusion.ts（filterEtfStocks の返り値 `{ kept, excluded }` と QuoteTypeLookup, 第2ゲート再利用, D-21）
    - src/portfolio/holding-news.ts（normalizeHoldingSymbol）
    - src/meeting/types.ts（highlightedStocks 要素型と verdict enum）
  </read_first>
  <behavior>
    - 新規強気銘柄（active でない ticker）は addedDate=today, lastVerdictDate=today で active エピソードとして登録される（WLST-01）
    - 既にアクティブな ticker が当日強気で再登場したら lastVerdictDate=today に更新、addedDate は初回値を保持（冪等, Pitfall 2 対策 — reconfirm ケース）
    - 同日2回 admit を呼んでも結果が同一（addedDate 不変, 重複登録が構造的に不可能, D-17）
    - filterEtfStocks の第2ゲートで quoteType !== "EQUITY" または lookup 失敗の候補は登録されない（fail-closed, D-21/D-22, Pitfall 1 対策）
    - 除外済み（history あり・addedDate なし）の ticker が再度強気なら新 active エピソードを作り、既存 history は破壊しない（D-05, 再追加）
  </behavior>
  <action>
    `admitBullishStocks(watchlist: WatchlistFile, bullishStocks: MeetingResult["highlightedStocks"], quoteTypeByTicker: ReadonlyMap<string, QuoteTypeLookup>, nameByTicker: ReadonlyMap<string, { name?: string; nameJa?: string }>, today: string): WatchlistFile` を実装する。純関数・throw-free・入力を mutate しない。

    処理順: (1) 入力 `bullishStocks` は呼び出し側で verdict==="強気" に絞られている前提だが、防御的に verdict==="強気" のみを対象にする。(2) それらに対し `filterEtfStocks(bullishStocks, quoteTypeByTicker)` を呼び、`kept` のみを登録候補とする（D-21 第2ゲート、fail-closed は filterEtfStocks が担保）。filterEtfStocks は既存の純関数をそのまま呼ぶ（変更しない — 変更が必要になったら設計逸脱としてフラグ）。(3) kept 各銘柄について `normalizeHoldingSymbol(stock.ticker)` でキーを作り、既存エントリがあれば `{ ...existing, addedDate: existing.addedDate ?? today, lastVerdictDate: today, name/nameJa は既存優先で nameByTicker 補完, history: existing.history }` で更新（既にアクティブなら addedDate 保持で lastVerdictDate だけ today に前進 = Pitfall 2 の reconfirm）。除外済み（addedDate undefined, history あり）だった場合は addedDate=today で再アクティブ化しつつ `history` を保持する（D-05 再追加）。既存エントリが無ければ `{ ticker, name, nameJa, addedDate: today, lastVerdictDate: today, history: [] }` を新規作成。(4) すべて `{ ...watchlist, [key]: entry }` の spread-merge で構築（urgency-history.ts の冪等パターン, D-17）。

    社名は nameByTicker（CLI ラッパーが batch quote() の longName/shortName から作る, D-04/D-22）から補完し、無ければ name/nameJa を undefined のままにする（下流は ticker フォールバック）。today は引数で受け取り、関数内で Date.now() や JST 再導出をしない（決定論・テスト容易性、D-13 の思想）。純関数は quote() を一切呼ばず、quoteTypeByTicker と nameByTicker を引数で受け取る（D-23: ネットワーク非依存を維持し単体テスト容易性を保つ。quote() 呼び出しは Plan 02 のラッパー側の責務）。watchlist.json は TS 自己生成ファイルであり LLM 出力ではないため zod は使わない（D-19: alias 硬化不要）。

    テストを先に書く（RED→GREEN）: 新規登録（addedDate/lastVerdictDate=today）、既存アクティブ再確認（addedDate 不変・lastVerdictDate 前進）、冪等（同一入力2回で結果一致）、ETF 候補が第2ゲートで拒否、lookup 失敗候補が fail-closed 除外、再追加（除外済み ticker が history 保持のまま再アクティブ化）。フィクスチャは makeHighlightedStock（etf-exclusion.test.ts 由来）+ makeWatchlistEntry（Task 1）+ Map ベースの quoteTypeByTicker/nameByTicker を使う。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/watchlist.test.ts -t "admitBullishStocks|idempotent|ETF|lookup|reconfirm|re-admission"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function admitBullishStocks" src/portfolio/watchlist.ts` がヒットする
    - `grep -n "filterEtfStocks" src/portfolio/watchlist.ts` がヒットする（第2ゲート再利用, D-21）
    - admit 新規登録テストで addedDate と lastVerdictDate が両方 today になることを検証している
    - 既存アクティブ再確認テストで addedDate が初回値・lastVerdictDate が today に更新されることを検証している（Pitfall 2）
    - 冪等テストで同一入力を2回適用した結果が deepEqual であることを検証している（D-17）
    - ETF 候補（quoteType!=="EQUITY"）が active に登録されないことをテストで検証している（D-21）
    - 再追加テストで除外済みエントリの history 配列が新 active エピソード作成後も残ることを検証している（D-05）
    - `npx vitest run src/portfolio/watchlist.test.ts -t "admitBullishStocks|idempotent|ETF|lookup|reconfirm|re-admission"` が pass
  </acceptance_criteria>
  <done>admitBullishStocks が新規登録・reconfirm・冪等・ETF第2ゲート・lookup fail-closed・再追加を正しく処理し、対応テストが全て green。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pruneWatchlist を実装する（downgraded/purchased/expired の3トリガー・履歴保持）</name>
  <files>src/portfolio/watchlist.ts, src/portfolio/watchlist.test.ts</files>
  <read_first>
    - src/portfolio/watchlist.ts（Task 1/2 の型・定数・admitBullishStocks）
    - src/portfolio/holdings.ts（PORTFOLIO_HOLDINGS: purchased トリガーの正準ソース, D-12。.T サフィックス保持を確認）
    - src/portfolio/holding-news.ts（normalizeHoldingSymbol: purchased 照合の正規化）
    - src/meeting/types.ts（highlightedStocks 要素型と verdict enum: 中立/弱気 の判定に使う）
    - .planning/phases/28-watchlist-persistence/28-RESEARCH.md（Pitfall 3 の prune-then-admit 優先順位, Open Question 2 の推奨）
  </read_first>
  <behavior>
    - active 銘柄が当日 highlightedStocks に verdict 中立/弱気 で登場 → removedReason=downgraded, removedDate=today で除外、history にエピソード追加、active フィールド（addedDate/lastVerdictDate）を消す（WLST-02）
    - active 銘柄が当日 highlightedStocks に一切登場しない → 現状維持（lastVerdictDate 据え置き、失効カウント進行, D-11）
    - active 銘柄が PORTFOLIO_HOLDINGS の symbol に一致 → removedReason=purchased で除外（WLST-03, D-12）
    - active 銘柄の lastVerdictDate から today までの暦日数 > EXPIRY_CALENDAR_DAYS → removedReason=expired で除外（WLST-04）
    - 境界: 経過日数がちょうど EXPIRY_CALENDAR_DAYS のとき失効しない、EXPIRY_CALENDAR_DAYS+1 のとき失効する（D-08 境界）
    - 除外時、addedDate/lastVerdictDate/removedReason/removedDate を1エピソードとして history に append し、active フィールドは undefined にする（レコード削除ではない, WLST-05/D-05）
  </behavior>
  <action>
    `pruneWatchlist(watchlist: WatchlistFile, todaysHighlighted: MeetingResult["highlightedStocks"], holdings: ReadonlyArray<PortfolioHolding>, today: string): WatchlistFile` を実装する。純関数・throw-free・入力を mutate しない。

    トリガー優先順位（Pitfall 3 / Open Question 2）: purchased > downgraded > expired。RESEARCH.md の推奨に従い、パイプライン統合（Wave 2）では prune を admit より前に実行して「購入済み・降格が『今日も強気』より優先される」順序を保証するが、本純関数内でも同一銘柄が複数トリガーに該当したときの優先順位を上記に固定し、テストで検証する。

    実装: active な各エントリ（isActive===true）について順に判定する。(1) purchased: `holdings.map(h => normalizeHoldingSymbol(h.symbol))` に正規化済み ticker キーが含まれれば除外（D-12: PORTFOLIO_HOLDINGS が正準ソース、tmp/portfolio.json は使わない。.T 完全一致）。(2) downgraded: 当日 todaysHighlighted に同 ticker（normalizeHoldingSymbol 照合）が verdict==="中立" または verdict==="弱気" で存在すれば除外（D-11）。当日 highlightedStocks に verdict==="強気" で登場する銘柄は admit 側が扱うので prune 対象外。当日一切登場しない銘柄は現状維持（D-11、除外しない・lastVerdictDate 据え置き）。(3) expired: lastVerdictDate と today の暦日差を計算し `> EXPIRY_CALENDAR_DAYS` なら除外（D-07 暦日ベース）。暦日差は Date オブジェクトのミリ秒差から算出する軽量ロジックにする（営業日/祝日カレンダー依存は不採用, D-07。`Math.round((Date.parse(today) - Date.parse(lastVerdictDate)) / 86400000)` 相当の単純計算）。

    除外は「レコード削除」ではなく、現 active エピソードを `WatchlistRemovalEpisode`（addedDate, lastVerdictDate, removedReason, removedDate=today）として `history` 末尾に append し、entry の addedDate/lastVerdictDate を除去（undefined 化）した新エントリを `{ ...watchlist, [key]: prunedEntry }` で構築する（WLST-05/D-05 履歴保持）。active でないエントリ（既に除外済み）はそのまま保持する。

    テストを先に書く（RED→GREEN）: downgraded（中立で除外・弱気で除外）、no-mention（当日未登場は active 維持・lastVerdictDate 不変）、purchased（PORTFOLIO_HOLDINGS 一致で除外）、expired（EXPIRY_CALENDAR_DAYS+1 日で失効）、boundary（ちょうど EXPIRY_CALENDAR_DAYS 日は失効しない）、history（除外後 removedReason/removedDate が history に残る・レコード削除されない）、precedence（同銘柄が purchased かつ downgraded に該当したとき purchased が勝つ）。日付フィクスチャは today と過去日を文字列で与える。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/watchlist.test.ts -t "pruneWatchlist|downgraded|purchased|expired|boundary|no mention|history|precedence"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function pruneWatchlist" src/portfolio/watchlist.ts` がヒットする
    - `grep -n "PORTFOLIO_HOLDINGS\|holdings" src/portfolio/watchlist.ts` が purchased 判定の入力に PORTFOLIO_HOLDINGS 系（holdings 引数）を使っていることを示す（D-12: tmp/portfolio.json を使わない — `grep -c "portfolio.json" src/portfolio/watchlist.ts` が 0）
    - downgraded テストで中立・弱気の両方で removedReason=downgraded になることを検証している（WLST-02）
    - no-mention テストで当日未登場の active 銘柄が lastVerdictDate 不変・active 維持であることを検証している（D-11）
    - expired 境界テストで経過日数 == EXPIRY_CALENDAR_DAYS は非失効・== EXPIRY_CALENDAR_DAYS+1 は失効することを検証している（D-08）
    - history テストで除外後に removedReason と removedDate が history 配列に保持され、entry がオブジェクトから削除されていないことを検証している（WLST-05/D-05）
    - precedence テストで purchased が downgraded/expired より優先されることを検証している（Pitfall 3）
    - `npx vitest run src/portfolio/watchlist.test.ts` 全体が pass
  </acceptance_criteria>
  <done>pruneWatchlist が3トリガー（downgraded/purchased/expired）を正準ソースから決定論的に判定し、除外時に履歴を保持し、境界・優先順位が正しい。watchlist.test.ts 全体が green。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meeting-result.json（LLM生成由来）→ watchlist.ts | highlightedStocks の ticker 文字列が untrusted。オブジェクトキーとして使う前に正規化・検証が必要 |
| watchlist.json 既存状態 → merge 構築 | 既存 ticker キーが prototype pollution ペイロード（`__proto__` 等）を含む可能性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | admitBullishStocks の ticker キー merge | medium | mitigate | normalizeHoldingSymbol で正規化した ticker のみをキーに使う。日付文字列は isValidDateKey（`/^\d{4}-\d{2}-\d{2}$/`）で検証。`__proto__` 等のキーは正規化後も spread-merge のみ（`Object.assign` の連鎖代入や `[key] =` の直接代入で prototype を汚染しない書式）で扱い、urgency-history.ts の既存 prototype-pollution 防御様式を踏襲する |
| T-28-02 | Tampering / Repudiation | filterEtfStocks 第2ゲートの fail-open | high | mitigate | admitBullishStocks 内の ETF 第2ゲートは filterEtfStocks の fail-closed（lookup 失敗＝除外）に委譲する。lookup 失敗銘柄を「不明だから登録」してはならない（Pitfall 1）。テストで lookup 失敗候補が active に入らないことを検証する |
| T-28-03 | Tampering | 再追加時の history 破壊 | medium | mitigate | admit の再追加分岐で既存 history を必ず保持する（D-05）。テストで再追加後に過去 removedReason が残ることを検証する |
</threat_model>

<verification>
- `npx vitest run src/portfolio/watchlist.test.ts` が全ケース green（WLST-01〜05 を網羅）
- watchlist.ts に I/O・ネットワーク・process.exit が存在しない（純関数境界, D-14）: `grep -vE '^\s*(//|\*)' src/portfolio/watchlist.ts | grep -cE 'yahoo-finance2|readFile|writeFile|process\.exit'` が 0
- filterEtfStocks / normalizeHoldingSymbol / isValidDateKey が再定義ではなく import 再利用（D-03/D-21）
- `npx tsc --noEmit` 相当（既存 build 設定）で型エラーなし
</verification>

<success_criteria>
- admitBullishStocks: 新規登録・reconfirm・冪等・ETF第2ゲート・fail-closed・再追加が全て動作
- pruneWatchlist: downgraded/purchased/expired の3トリガー・境界・優先順位・履歴保持が全て動作
- getActiveWatchlistEntries が Phase 29/30 向けの安定シグネチャとして export されている
- WLST-01〜05 に対応する単体テストが green
</success_criteria>

<output>
Create `.planning/phases/28-watchlist-persistence/28-01-SUMMARY.md` when done
</output>
