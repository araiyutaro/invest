---
phase: 30-buy-timing-judgment-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/scripts/write-watchlist-judgment.ts
  - src/scripts/write-watchlist-judgment.test.ts
autonomous: true
requirements:
  - TIME-01
  - TIME-02
  - TIME-03
  - TIME-05
must_haves:
  truths:
    - "write-watchlist-judgment.ts が tmp/watchlist-judgment-raw/{ticker}.json 群を1銘柄ずつ独立に zod 検証し、1銘柄の不正 JSON が他銘柄の検証を止めない"
    - "検証済み判定に applyConfluenceGate・attachActionChanges・market/asOf 付与・skip 記録を適用し tmp/watchlist-judgment.json を常に有効 JSON で書く"
    - "前日ファイル読込が ENOENT（.code と .message 両チェック）と破損を区別し、欠損は no-prior-data として扱う"
    - "アクティブ0件・全滅・破損のいずれでも throw せず有効 JSON を書き stderr に適切な [STEP:watchlist-judgment:*] マーカーを出す"
    - "asOf/market は TS が入力データ（TechnicalSnapshot.asOf・ティッカーサフィックス）から決定論再付与し LLM エコーを採らない"
  artifacts:
    - src/scripts/write-watchlist-judgment.ts
    - src/scripts/write-watchlist-judgment.test.ts
  key_links:
    - "write-watchlist-judgment.ts → watchlistJudgmentSchema / applyConfluenceGate / attachActionChanges / deriveMarket / buildSkippedJudgment（Plan 01 の公開 API を消費）"
    - "検証順序: schema.parse → applyConfluenceGate → attachActionChanges → market/asOf 付与（Pitfall 3 のゲート順序契約）"
---

<objective>
Plan 01 の決定論コアを組み込む fail-soft CLI `src/scripts/write-watchlist-judgment.ts` を実装する。銘柄別 raw Agent 出力（`tmp/watchlist-judgment-raw/{ticker}.json`）を1件ずつ独立検証し、confluence ゲート・前日比較・market/asOf 決定論付与・skip 記録を経て最終 `tmp/watchlist-judgment.json` を生成する。全ての I/O とフェイルソフト分岐を CLI 境界に閉じ込め、既存4レポートの生成・デプロイを一切ブロックしない。

Purpose: TIME-01（銘柄ごと判定出力）・TIME-02（zod 検証でパイプライン非停止）・TIME-03（前日比較の TS 決定論・同日再実行ガード）・TIME-05（asOf/market の決定論再付与）を、`write-watchlist.ts`/`collect-watchlist-data.ts`/`validate-portfolio-research.ts` の実証済み fail-soft CLI 形状で満たす。
Output: `write-watchlist-judgment.ts`（fail-soft CLI）とその単体/統合テスト。最終出力 `tmp/watchlist-judgment.json` は常に有効 JSON（D-18/D-19）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-buy-timing-judgment-agent/30-CONTEXT.md
@.planning/phases/30-buy-timing-judgment-agent/30-RESEARCH.md
@.planning/phases/30-buy-timing-judgment-agent/30-PATTERNS.md
@.planning/phases/30-buy-timing-judgment-agent/30-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

このプランが新規に導入するシンボル・ファイル・マーカー:

**CLI（src/scripts/write-watchlist-judgment.ts）:**
- `loadPrevJudgmentDefensive()`（前日ファイルの ENOENT-vs-破損二段判定リーダー、`write-watchlist.ts` の `loadExistingWatchlist` 同型）
- `writeEmptyOutput()`（空の有効 JSON 書き出しヘルパ、`collect-watchlist-data.ts` の `writeEmptyOutputs` 同型）
- `main()`（fail-soft オーケストレーション本体）
- STEP マーカー文字列: `[STEP:watchlist-judgment:OK]` / `[STEP:watchlist-judgment:FAIL:corrupted]` / `[STEP:watchlist-judgment:FAIL:{N}/{M}銘柄失敗（{ティッカー}）]`

**入力ファイル契約（Phase 29/本プランで確定・消費）:**
- `tmp/watchlist-judgment-raw/{ticker}.json`（Plan 03 のオーケストレータが生成する raw Agent 出力。本プランが読込・検証）
- `tmp/watchlist-technicals.json`（Phase 29 供給、`TechnicalSnapshot[]` — asOf/market 導出の入力源）
- `tmp/watchlist-judgment.json`（**本プランが生成する最終ファイル**。同時に翌日実行の前日ソースにもなる）
- `tmp/prev-watchlist-judgment.json`（Plan 03 が退避生成、本プランが前日比較で読込）

**テスト（新規）:** `src/scripts/write-watchlist-judgment.test.ts`

Plan 01 の型・スキーマ・純関数（`WatchlistJudgment`/`watchlistJudgmentSchema`/`applyConfluenceGate`/`attachActionChanges`/`deriveMarket`/`buildSkippedJudgment`）はこのプランの依存として消費する（Plan 01 の Artifacts 節参照）。
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fail-soft CLI 本体（raw 検証ループ・confluence ゲート・market/asOf 付与・前日比較・STEP マーカー）</name>
  <files>src/scripts/write-watchlist-judgment.ts</files>
  <read_first>
    - src/scripts/write-watchlist.ts（L29-50 の `loadExistingWatchlist` ENOENT-vs-破損二段判定を逐語で流用。**`.code === "ENOENT"` と `.message.includes("ENOENT")` の両チェック必須** — テストモックがプレーン `Error("ENOENT")` を使うため。L1-28 の import/定数規約・エントリポイントガード）
    - src/scripts/collect-watchlist-data.ts（`main()` 構造チェックリスト: 冒頭 `await mkdir(TMP_DIR, { recursive: true })` 無条件実行、corrupted 時は空出力+FAIL マーカーで return、アクティブ0件は空 judgments+OK マーカー、データ分岐ごと独立 try/catch、`if (process.argv[1] === fileURLToPath(import.meta.url))` トップレベルガード + `.catch()` で最終手段の空出力書き+`process.exitCode = 1`、STEP マーカーは `console.error`、監査ログは `console.log`。`writeEmptyOutputs` の空有効 JSON 形状）
    - src/scripts/validate-portfolio-research.ts（全63行。銘柄別 raw ファイルを readdir → 期待リストと突合 → 1件ずつ zod parse を try/catch で独立検証 → 失敗件数集計するループ構造。本 CLI の raw 検証ループの直接雛形。ファイル名 `/`→`-` 置換規約とファイル名 ticker と JSON 内 ticker の突合も踏襲）
    - src/portfolio/watchlist-judgment.ts（Plan 01。`applyConfluenceGate`/`attachActionChanges`/`deriveMarket`/`buildSkippedJudgment` を import。純関数の順序契約に従う）
    - src/meeting/schemas.ts（Plan 01 の `watchlistJudgmentSchema` を import して各 raw を parse）
    - src/meeting/types.ts（Plan 01 の `WatchlistJudgment`/`WatchlistJudgmentFile` 型）
    - src/data/technicals.ts（L11-27 `TechnicalSnapshot` 型。`asOf` フィールドを判定への asOf 付与ソースにする。`symbol` で銘柄照合）
    - .planning/phases/30-buy-timing-judgment-agent/30-RESEARCH.md（Pattern 4 L382-416 の CLI 構造チェックリスト、Pitfall 3 L466-471 のゲート順序、Pitfall 4/5、Security Domain L628-635 の per-ticker try/catch DoS 対策）
    - .planning/phases/30-buy-timing-judgment-agent/30-PATTERNS.md（write-watchlist-judgment.ts セクション L74-117 の全チェックリストと STEP マーカー exact format）
  </read_first>
  <action>
    `src/scripts/write-watchlist-judgment.ts` を新規作成する。定数は `TMP_DIR`、`RAW_DIR = join(TMP_DIR, "watchlist-judgment-raw")`、`OUTPUT_PATH = join(TMP_DIR, "watchlist-judgment.json")`、`PREV_PATH = join(TMP_DIR, "prev-watchlist-judgment.json")`、`TECHNICALS_PATH = join(TMP_DIR, "watchlist-technicals.json")`、`MEETING_RESULT_PATH = join(TMP_DIR, "meeting-result.json")` を名前付きで1箇所定義する（D-09 準拠、CHUNK 定数の単一定義規約と同方針）。

    `loadPrevJudgmentDefensive()`: `write-watchlist.ts` L29-50 の形状を流用し `PREV_PATH` を読む。JSON parse・型チェック（object かつ非配列かつ非 null）を行い、失敗時は ENOENT（`.code === "ENOENT"` **または** `.message.includes("ENOENT")` の両チェック）を「欠損 = no-prior-data、`{ prev: null, corrupted: false }`」、それ以外を「破損 = `{ prev: null, corrupted: true }`」に分類する。欠損と破損はどちらも前日比較をスキップする（prev=null で attachActionChanges に渡す）が、破損は監査ログに残す。

    `writeEmptyOutput(date)`: `{ date, generatedAt: new Date().toISOString(), judgments: [] }` を `OUTPUT_PATH` に書く（D-18/D-19、常に有効 JSON）。**`OUTPUT_PATH` と `PREV_PATH` は絶対に `rm -rf` 対象にしない**（D-22。この CLI はそもそも削除を行わない）。

    `main()`: (1) 冒頭で `await mkdir(TMP_DIR, { recursive: true })`。(2) `MEETING_RESULT_PATH` から `date` を読む（JST 再導出しない、D-09/Pitfall 2）。読めない場合はフォールバック date で空出力+`[STEP:watchlist-judgment:FAIL:meeting-result読込失敗]`+return。(3) `RAW_DIR` を `readdir`。存在しない/空なら、アクティブ0件正常系として空 judgments+`[STEP:watchlist-judgment:OK]`+return（D-19）。(4) `TECHNICALS_PATH` を読み `TechnicalSnapshot[]` を得て `Map<normalizeHoldingSymbol(symbol), snapshot>` を構築（asOf/技術データ照合用）。(5) raw 検証ループ: 各 `{ticker}.json` を1件ずつ独立 try/catch で `readFile`→`JSON.parse`→`watchlistJudgmentSchema.parse`。**1銘柄の失敗が他銘柄を止めない**（Security T-30-04 DoS 対策・D-18）。失敗銘柄は件数集計。(6) 検証成功した各判定に順序契約で後処理: まず `applyConfluenceGate`（signals<2 の buy を wait 降格）→ その結果を判定に反映 → その後 `deriveMarket(ticker)` と technicals Map からの `asOf` を**決定論的に付与**（LLM エコー不採用、D-08/D-13/D-15。technicals に該当銘柄が無ければ asOf は付与しない）。(7) technicals にスナップショットがあるのに raw ファイルが欠落/全滅した銘柄、および raw 検証失敗銘柄は判定欠落として扱う。technicals にスナップショット自体が無い銘柄は `buildSkippedJudgment(ticker)` で status:skipped の陽性レコードを合成する（D-20、Pitfall 5）。(8) `loadPrevJudgmentDefensive()` の prev を使い `attachActionChanges(judgments, prev?.judgments ?? null)` で previousAction/actionChanged を付与（D-11、TIME-03）。(9) 最終 `WatchlistJudgmentFile`（date/generatedAt/judgments）を `OUTPUT_PATH` に書く。(10) STEP マーカーを stderr（`console.error`）へ: 全成功→`[STEP:watchlist-judgment:OK]`、部分失敗→`[STEP:watchlist-judgment:FAIL:{N}/{M}銘柄失敗（{失敗ティッカー}）]`（M=アクティブ銘柄数の動的値、Step 3-P の固定/12 と異なる、D-17）。`[PIPELINE:FAIL]` は絶対に出さない。監査ログ（skip 件数・降格件数等）は `console.log`。

    トップレベル: `if (process.argv[1] === fileURLToPath(import.meta.url))` ガード内で `main().catch(...)` を呼び、catch では最終手段として空出力を書き `process.exitCode = 1`（`main()` 内で `process.exit()` を直接呼ばない）。
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` が型エラー無しで通過する
    - Source assertion: ENOENT 二段チェックが存在する（`grep -c 'includes("ENOENT")' src/scripts/write-watchlist-judgment.ts` が 1 以上、かつ `.code === "ENOENT"` の比較も存在）
    - Source assertion: 冒頭無条件 mkdir が存在する（`grep -c 'mkdir' src/scripts/write-watchlist-judgment.ts` が 1 以上、`recursive: true` 付き）
    - Source assertion: 全 STEP マーカーが stderr へ出る（`[STEP:watchlist-judgment:` を含む出力は全て `console.error` 経由。`console.log` 行に `[STEP:` が含まれない — `grep -n 'console.log' src/scripts/write-watchlist-judgment.ts | grep -c '\[STEP:'` が 0）
    - Source assertion: `main()` 本体に `process.exit(` の直接呼び出しが無い（`sed -n '/async function main/,/^}/p' src/scripts/write-watchlist-judgment.ts | grep -c 'process.exit('` が 0）
    - Source assertion: OUTPUT_PATH/PREV_PATH への `rm` や `rmdir`/`unlink` が無い（`grep -c -E 'rm -rf|rmSync|unlink' src/scripts/write-watchlist-judgment.ts` が 0、D-22）
    - Source assertion: market/asOf の LLM エコー採用が無い（`grep -c -E 'raw\.market|raw\.asOf|\.market \?\?|\.asOf \?\?' src/scripts/write-watchlist-judgment.ts` が 0、Pitfall 4）
    - Source assertion: Plan 01 API を import している（`grep -c -E 'applyConfluenceGate|attachActionChanges|watchlistJudgmentSchema' src/scripts/write-watchlist-judgment.ts` が 3 以上）
  </acceptance_criteria>
  <done>
    write-watchlist-judgment.ts が銘柄別 raw を独立検証し、confluence 降格→market/asOf 決定論付与→前日比較→最終ファイル書き出しの順序契約を満たし、全フェイルソフト分岐（corrupted/0件/全滅/部分失敗）で throw せず有効 JSON と適切な STEP マーカーを出す。型チェック通過。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fail-soft CLI 統合テスト（同日再実行ガード・ENOENT・0件・部分失敗・降格）</name>
  <files>src/scripts/write-watchlist-judgment.test.ts</files>
  <read_first>
    - src/scripts/write-watchlist-judgment.ts（Task 1 で実装した本体。export された `main`/`loadPrevJudgmentDefensive` 等をテスト対象にする）
    - src/portfolio/watchlist.test.ts（L1-40 付近のモック規約: プレーン `Error("ENOENT")`（`.code` なし）で欠損シミュレート、fs モックの差し替え方。`describe`/`it` 命名）
    - src/scripts/collect-watchlist-data.ts（テスト可能な形の `main(overrides)` 引数注入や tmp ディレクトリ差し替えの前例があれば踏襲。無ければ実 tmp をテスト用サブディレクトリに向ける方式）
    - .planning/phases/30-buy-timing-judgment-agent/30-RESEARCH.md（Validation Architecture L578-612 の Test Map と Wave 0 Gaps、Pitfall 1 L452-457 の ENOENT モック規約）
    - .planning/phases/30-buy-timing-judgment-agent/30-PATTERNS.md（テストセクション L242-280 の makeJudgment ファクトリと必須ケース、D-23 必須ケース）
  </read_first>
  <action>
    `src/scripts/write-watchlist-judgment.test.ts` を新規作成する。既存 CLI テストのモック規約（プレーン `Error("ENOENT")`・plain-object fixture・`vi.mock` を避け引数注入や一時ディレクトリ差し替えを優先）に従い、以下を網羅する。テストが本体を呼べるよう Task 1 で `main` および補助関数を export しておく（もしくはテスト用に `RAW_DIR` 等を引数注入可能にする）。

    必須ケース（D-23 + RESEARCH.md Test Map）: (1) **同日再実行ガード**: 既存 `tmp/watchlist-judgment.json` の date が meeting-result.json の date と一致する場合、前日退避はオーケストレータ（Plan 03）の責務だが、CLI 側の `loadPrevJudgmentDefensive` が同日データを壊さないこと・prev 読込が期待どおり動くことを検証（TIME-03c）。(2) **ENOENT 分類**: prev ファイルをプレーン `Error("ENOENT")`（`.code` なし）で欠損シミュレート → `{ prev: null, corrupted: false }`（no-prior-data 扱い、Pitfall 1）。(3) **破損分類**: prev が非 JSON/配列/null → `corrupted: true` かつ前日比較スキップ。(4) **アクティブ0件正常系**: raw ディレクトリ空 → 空 judgments + `[STEP:watchlist-judgment:OK]`（D-19）。(5) **部分失敗**: 3銘柄中1銘柄の raw が不正 JSON → 残り2銘柄は判定される・`[STEP:watchlist-judgment:FAIL:1/3銘柄失敗（...）]` 形式マーカー・throw しない（D-18）。(6) **confluence 降格の統合**: raw に buy+signals 1件 → 最終出力で todayAction=wait（Task 1 のゲートが CLI 経路で効くこと）。(7) **skip 記録**: technicals にスナップショットが無い銘柄 → 最終 judgments に status:skipped エントリが存在（D-20）。(8) **market/asOf 決定論付与**: raw が market="US"（誤）を出しても最終出力の market は deriveMarket 由来・asOf は technicals 由来（LLM エコー不採用検証、D-08/D-15）。

    STEP マーカーの検証は `console.error` のスパイ（またはキャプチャ）で行い、`[STEP:watchlist-judgment:` プレフィックスと期待サフィックスを assert する。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/write-watchlist-judgment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/write-watchlist-judgment.test.ts` が全て green
    - Behavior assertion: プレーン `Error("ENOENT")`（`.code` なし）モックが「欠損」に分類されるテストが green（Pitfall 1 回帰防止）
    - Behavior assertion: 部分失敗テストで残存銘柄が判定され続け throw しないことが green（D-18）
    - Behavior assertion: アクティブ0件で `[STEP:watchlist-judgment:OK]` が出るテストが green（D-19）
    - Behavior assertion: raw が market を誤申告しても最終出力の market が deriveMarket 由来になるテストが green（D-08/D-15）
    - Behavior assertion: buy+signals 1件 の raw が最終出力で wait に降格するテストが green（D-07 の CLI 経路統合）
    - `npx vitest run` 全スイート回帰 green
  </acceptance_criteria>
  <done>
    CLI の全フェイルソフト分岐（ENOENT/破損/0件/部分失敗）・confluence 降格・skip 記録・market/asOf 決定論付与が統合テストで green。ENOENT プレーン Error モック規約に準拠。全スイート回帰 green。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw Agent 出力ファイル → CLI | 信頼できない LLM 生 JSON（`tmp/watchlist-judgment-raw/{ticker}.json`）が CLI に読み込まれ zod 検証される境界 |
| 前日ファイル → CLI | `tmp/prev-watchlist-judgment.json`（前実行の自己生成物、欠損/破損しうる）を読む境界 |
| 埋め込みニュース本文 → 最終判定 | ニュース記事本文は Plan 03 のプロンプト組立で埋め込まれる（本 CLI は ID 解決済みデータのみ扱い、外部テキストを直接実行しない） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Denial of Service | raw 検証ループ（悪意/不正 JSON による CLI 全体 abort） | high | mitigate | 銘柄別 raw ごとに独立 try/catch。1銘柄の parse 失敗が他銘柄検証を止めない。全滅時も有効 JSON を書く（D-18、`fetchChunked` の per-ticker try/catch 同型） |
| T-30-05 | Spoofing | market/asOf の LLM エコー混入 | high | mitigate | market は `deriveMarket`、asOf は `TechnicalSnapshot.asOf` から TS が決定論再付与。raw の同名フィールドを一切採用しない（D-08/D-13/D-15、Pitfall 4） |
| T-30-06 | Tampering | 前日ファイル破損による誤った変化検出 | medium | mitigate | 破損は `corrupted: true` として前日比較をスキップ（prev=null）。欠損（ENOENT 二段チェック）と破損を区別し、いずれも no-prior-data で安全側 |
| T-30-07 | Denial of Service | 判定失敗によるパイプライン停止 | high | mitigate | `[PIPELINE:FAIL]` を絶対に出さず、全分岐で throw せず return。既存4レポートの生成・デプロイを一切ブロックしない（OPS-06 の本フェーズ分担、D-17/D-18） |
| T-30-SC | Tampering | npm/pip/cargo installs | high | accept | ゼロ新規パッケージ（RESEARCH.md Package Legitimacy Audit: N/A） |
</threat_model>

<verification>
- `npx vitest run src/scripts/write-watchlist-judgment.test.ts` 全 green
- `npx tsc --noEmit` 型エラー無し
- `npx vitest run` 全スイート回帰 green（Plan 01 のテスト含め副作用なし）
- CLI を手動実行（raw ディレクトリ空 / technicals 空）→ `tmp/watchlist-judgment.json` が有効 JSON・stderr に `[STEP:watchlist-judgment:OK]`
</verification>

<success_criteria>
- 銘柄別 raw を独立検証し1銘柄失敗が他を止めない（TIME-01/TIME-02/D-18）
- zod 検証でパイプライン非停止・全分岐で有効 JSON（TIME-02/D-18/D-19）
- 前日比較の TS 決定論・同日データ非破壊（TIME-03）
- asOf/market を TS が決定論再付与し LLM エコー不採用（TIME-05/D-08/D-13/D-15）
- 全テスト green・型チェック通過・全スイート回帰 green
</success_criteria>

<output>
Create `.planning/phases/30-buy-timing-judgment-agent/30-02-SUMMARY.md` when done
</output>
