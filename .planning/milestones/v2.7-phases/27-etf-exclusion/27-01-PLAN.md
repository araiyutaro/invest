---
phase: 27-etf-exclusion
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/portfolio/etf-exclusion.ts
  - src/portfolio/etf-exclusion.test.ts
autonomous: true
requirements: [ETF-02]
must_haves:
  truths:
    - "filterEtfStocks が quoteType が EQUITY 以外の銘柄（米国ETF・日本ETF・投信・INDEX等）を kept から除外する"
    - "filterEtfStocks が quoteType lookup 失敗（map未登録 or status failed）の銘柄を fail-closed で除外する（D-01）"
    - "filterEtfStocks が米国個別株（AAPL: EQUITY）・日本個別株（7203.T: EQUITY）を kept に残す"
    - "filterEtfStocks が除外理由を etf / lookup-failed で区別して excluded に記録する（D-13 の監査ログの根拠）"
    - "filterEtfStocks が入力 stocks 配列を mutate しない（イミュータブル・新配列構築）"
  artifacts:
    - "src/portfolio/etf-exclusion.ts（純関数モジュール: filterEtfStocks + QuoteTypeLookup + EtfExclusionResult 型）"
    - "src/portfolio/etf-exclusion.test.ts（D-15 の5分類 + イミュータビリティ検証テスト）"
  key_links:
    - "filterEtfStocks のシグネチャが Plan 02（filter-etf-stocks.ts CLI）から import され再利用される — kept/excluded の形状が下流の書き戻し・監査ログに直結"
    - "quoteTypeByTicker は ReadonlyMap<string, QuoteTypeLookup>（プロトタイプ汚染対策で plain object ではなく Map）"
---

<objective>
ETF除外の第2層（決定論的フィルタ）の中核となる純関数モジュール `src/portfolio/etf-exclusion.ts` を、TDD（RED→GREEN）で構築する。ネットワーク・ファイルI/Oを一切持たず、事前取得済みの quoteType lookup 結果を引数で受け取り、`quoteType === "EQUITY"` の allowlist（D-04）と lookup失敗時の fail-closed（D-01）に基づいて highlightedStocks を決定論的にフィルタする。

Purpose: 「LLM自己申告を信用せず、TSで決定できるものはTS側決定論」というv2.5/2.6中核方針の直接適用。純関数を独立させることで単体テスト容易性（Success Criteria 4）を確保し、Phase 28 のウォッチリスト admission 側からも同じ関数を再利用可能にする。本プランは D-06（純関数モジュール `etf-exclusion.ts` ＋ 薄い CLI ラッパー `filter-etf-stocks.ts` の分離構成）のうち純関数側を担い、CLI ラッパー側は Plan 02 が担う。
Output: `filterEtfStocks` 純関数・`QuoteTypeLookup` / `EtfExclusionResult` 型・完全な単体テストスイート。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-etf-exclusion/27-CONTEXT.md
@.planning/phases/27-etf-exclusion/27-RESEARCH.md
@.planning/phases/27-etf-exclusion/27-PATTERNS.md

# 流用パターン（純関数 + テストの実証済みテンプレート）
@src/portfolio/urgency-history.ts
@src/portfolio/urgency-history.test.ts
# 型参照元（highlightedStocks の要素形状）
@src/meeting/types.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: filterEtfStocks 純関数の失敗テストを先に作成する（RED）</name>
  <files>src/portfolio/etf-exclusion.test.ts</files>
  <read_first>
    - src/portfolio/urgency-history.test.ts（テスト構造・fixture factory・イミュータビリティ検証イディオムの正準analog: describe-per-function、Partial<T> overrides ファクトリ、"capture original, call, assert unchanged" パターン）
    - src/meeting/types.ts（MeetingResult["highlightedStocks"] の要素型: ticker/averageScore/verdict/summary/agentScores/nominatedBy — fixture はこの形状に一致させる）
    - .planning/phases/27-etf-exclusion/27-RESEARCH.md（Pattern 1: filterEtfStocks の目標シグネチャと QuoteTypeLookup / EtfExclusionResult 型の確定形状）
    - .planning/phases/27-etf-exclusion/27-PATTERNS.md（etf-exclusion.test.ts セクション: fixture factory と describe/it の日本語命名規約）
  </read_first>
  <behavior>
    - makeHighlightedStock(overrides): MeetingResult["highlightedStocks"][number] を返す Partial<T> ファクトリ（デフォルト ticker="TEST", averageScore=50, verdict="中立", summary="デフォルト要約", agentScores=[], nominatedBy=[]）
    - makeQuoteLookup(overrides): QuoteTypeLookup を返すファクトリ（デフォルト { status: "ok", quoteType: "EQUITY" }）
    - Test 1（米国ETF, D-04）: quoteType="ETF" の SPY は excluded に入り reason="etf" quoteType="ETF" を持つ。kept に含まれない
    - Test 2（日本ETF, D-04, サフィックス非依存）: quoteType="ETF" の 1306.T が excluded に入る。7203.T（EQUITY）は kept に残る（.T サフィックスで判別していないことの証明）
    - Test 3（米国個別株通過）: quoteType="EQUITY" の AAPL は kept に残り excluded に入らない
    - Test 4（日本個別株通過）: quoteType="EQUITY" の 7203.T は kept に残る
    - Test 5（lookup失敗 fail-closed, D-01）: quoteTypeByTicker に該当エントリ無しの銘柄は excluded に入り reason="lookup-failed" を持つ（quoteType フィールドは付かない）
    - Test 6（status:"failed" fail-closed, D-01）: { status: "failed" } の lookup を持つ銘柄も excluded reason="lookup-failed"
    - Test 7（非EQUITYの各種, D-04 allowlist）: quoteType が "MUTUALFUND" / "INDEX" の銘柄はすべて excluded reason="etf"（EQUITY以外は全除外、denylistではないことの証明）
    - Test 8（イミュータビリティ）: 呼び出し後、入力 stocks 配列が toEqual で不変
    - Test 9（混在ケース）: EQUITY + ETF + lookup失敗 を混ぜた入力で kept が EQUITY のみ・excluded が正しい reason 分けになる
  </behavior>
  <action>
    urgency-history.test.ts の構造規約に従い etf-exclusion.test.ts を新規作成する。vitest の describe/it/expect を import。describe は "filterEtfStocks (D-01/D-04)" 単一ブロック、各 it タイトルは日本語で検証対象の decision ID を明示する。fixture は makeHighlightedStock / makeQuoteLookup の2ファクトリで組み立てる（Partial overrides + デフォルト値、urgency-history.test.ts の makeHoldingEvaluation と同方式）。quoteTypeByTicker は new Map<string, QuoteTypeLookup>() で構築し ticker をキーにする（plain object は使わない — プロトタイプ汚染対策の Map 選択、27-PATTERNS.md validation-guard セクション参照）。import 対象はまだ存在しない src/portfolio/etf-exclusion.js の filterEtfStocks / QuoteTypeLookup 型 / EtfExclusionResult 型。このタスクの時点でテストは RED（モジュール未実装のため）であること。イミュータビリティ検証は入力 stocks を [...stocks] でコピー捕捉し呼び出し後 toEqual で照合する urgency-history.test.ts のイディオムを流用。behavior に列挙した Test 1〜9 をすべて実装する。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/etf-exclusion.test.ts 2>&1 | grep -qE "Cannot find module|Failed to resolve import|No test files|error" && echo "RED confirmed (module missing)" || echo "check output"</automated>
  </verify>
  <acceptance_criteria>
    - src/portfolio/etf-exclusion.test.ts が存在し、`describe("filterEtfStocks` を含む（`grep -c 'describe("filterEtfStocks' src/portfolio/etf-exclusion.test.ts` が 1 以上）
    - テストファイルが `from "./etf-exclusion.js"` を import している
    - behavior の Test 1〜9 に対応する it ブロックが9個以上存在する（`grep -c '  it(' src/portfolio/etf-exclusion.test.ts` >= 9）
    - quoteTypeByTicker が `new Map` で構築されている（`grep -c 'new Map' src/portfolio/etf-exclusion.test.ts` >= 1）
    - この時点で `npx vitest run src/portfolio/etf-exclusion.test.ts` は import 解決失敗で RED（etf-exclusion.ts 未実装のため）
  </acceptance_criteria>
  <done>etf-exclusion.test.ts が D-15 の5分類（米国ETF/日本ETF/米国株/日本株/lookup失敗）+ 非EQUITY allowlist + イミュータビリティを網羅し、モジュール未実装により RED であること。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: etf-exclusion.ts 純関数を実装しテストをGREENにする</name>
  <files>src/portfolio/etf-exclusion.ts</files>
  <read_first>
    - src/portfolio/etf-exclusion.test.ts（Task 1 で作成した契約 — import する型名・関数名・期待する kept/excluded 形状に厳密に一致させる）
    - src/portfolio/urgency-history.ts（純関数モジュールの正準スタイル: readonly interface + decision-ID を引用した日本語JSDoc、immutable-spread 構築、try/catch と throw を一切持たない）
    - src/meeting/types.ts（MeetingResult 型を import type する — highlightedStocks の要素型を再利用）
    - .planning/phases/27-etf-exclusion/27-RESEARCH.md（Pattern 1: filterEtfStocks の確定実装形状。ALLOWED_QUOTE_TYPE="EQUITY" allowlist、lookup無し/failed→lookup-failed、非EQUITY→etf の分岐）
  </read_first>
  <action>
    src/portfolio/etf-exclusion.ts を新規作成する。`import type { MeetingResult } from "../meeting/types.js";` のみを project import とする（ゼロI/O純関数のため他の依存なし）。エクスポートする型: `QuoteTypeLookup`（判別可能ユニオン: `{ readonly status: "ok"; readonly quoteType: string } | { readonly status: "failed" }`）と `EtfExclusionResult`（`readonly kept: ReadonlyArray<MeetingResult["highlightedStocks"][number]>` と `readonly excluded: ReadonlyArray<{ readonly ticker: string; readonly reason: "etf" | "lookup-failed"; readonly quoteType?: string }>`）。定数 `ALLOWED_QUOTE_TYPE` を値 "EQUITY" で定義。エクスポート関数 `filterEtfStocks(stocks: MeetingResult["highlightedStocks"], quoteTypeByTicker: ReadonlyMap<string, QuoteTypeLookup>): EtfExclusionResult` を実装。ロジックは for ループで各 stock について: (1) map.get(stock.ticker) が undefined または status==="failed" なら excluded に reason="lookup-failed" で push して continue（D-01 fail-closed）、(2) lookup.quoteType が ALLOWED_QUOTE_TYPE と不一致なら excluded に reason="etf", quoteType=lookup.quoteType で push して continue（D-04 allowlist、非EQUITYは全除外）、(3) それ以外は kept に push。入力 stocks は一切 mutate せず新しい kept/excluded 配列を構築して返す。JSDoc は urgency-history.ts の日本語・decision-ID引用スタイルに合わせ D-01/D-04 を明記する。allowlist は必ず `!== ALLOWED_QUOTE_TYPE` 判定で実装し、非EQUITY型のハードコード denylist 配列は作らないこと（未知の将来 quoteType に対しても fail-closed を保つ）。try/catch・throw は書かない。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/etf-exclusion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/portfolio/etf-exclusion.test.ts` が全テスト GREEN（Task 1 の9テスト全通過）
    - src/portfolio/etf-exclusion.ts が `export function filterEtfStocks` を含む
    - allowlist が `ALLOWED_QUOTE_TYPE` 定数（値 "EQUITY"）との比較で実装されている（`grep -c 'ALLOWED_QUOTE_TYPE' src/portfolio/etf-exclusion.ts` >= 2）
    - 純関数モジュールに try/catch・throw が存在しない（`grep -v '^#' src/portfolio/etf-exclusion.ts | grep -cE 'try \{|throw '` が 0）
    - quoteTypeByTicker パラメータの型が `ReadonlyMap` である（`grep -c 'ReadonlyMap' src/portfolio/etf-exclusion.ts` >= 1）
    - `npx tsc --noEmit` が etf-exclusion.ts で型エラーを出さない
  </acceptance_criteria>
  <done>filterEtfStocks が実装され、米国ETF・日本ETF・米国株・日本株・lookup失敗・非EQUITYの全分類テストが GREEN。純関数は throw/try/catch/I/O を持たず入力を mutate しない。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM出力(meeting-result.json) → TS純関数 | highlightedStocks の ticker 文字列は LLM 生成の非検証データ。純関数の Map キーとして扱われる |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | filterEtfStocks の quoteTypeByTicker ルックアップ | medium | mitigate | ticker をキーにするストアは `ReadonlyMap`（plain object ではない）を必須とする（D-01整合・27-PATTERNS.md validation-guard）。`"__proto__"` 等の敵対的 ticker が prototype pollution を引き起こせない。map.get 未ヒットは fail-closed で除外されるため、汚染由来の未定義 quoteType も安全側に倒れる |
| T-27-02 | Information Disclosure | filterEtfStocks の返り値 excluded | low | accept | excluded は ticker/reason/quoteType のみを含み機微情報を持たない。ログ出力は Plan 02 の CLI 側で行い、純関数はデータを返すのみ |
</threat_model>

<verification>
- `npx vitest run src/portfolio/etf-exclusion.test.ts` — Wave 内サンプリング（全 GREEN）
- `npx tsc --noEmit` — 型健全性
- `npx vitest run` — Wave マージ時のフルスイート回帰
</verification>

<success_criteria>
- filterEtfStocks が米国ETF（SPY: ETF）・日本ETF（1306.T: ETF）を除外し、米国株（AAPL）・日本株（7203.T）を通過させる（Success Criterion 4 の中核）
- lookup失敗銘柄を fail-closed で除外する（D-01, Success Criterion 3 の決定論部分）
- excluded の reason が etf / lookup-failed で区別される（D-13 監査ログの根拠）
- 純関数は throw せず I/O を持たず入力を mutate しない
- 単体テストスイートが D-15 の全分類を GREEN で検証
</success_criteria>

<artifacts>
## Artifacts this phase produces（Plan 01）

**新規ファイル:**
- `src/portfolio/etf-exclusion.ts`
- `src/portfolio/etf-exclusion.test.ts`

**エクスポートシンボル（下流の Plan 02 / Phase 28 が import する）:**
- `function filterEtfStocks(stocks: MeetingResult["highlightedStocks"], quoteTypeByTicker: ReadonlyMap<string, QuoteTypeLookup>): EtfExclusionResult`
- `type QuoteTypeLookup = { readonly status: "ok"; readonly quoteType: string } | { readonly status: "failed" }`
- `interface EtfExclusionResult`（`kept: ReadonlyArray<...>`, `excluded: ReadonlyArray<{ ticker, reason: "etf"|"lookup-failed", quoteType? }>`）
- 定数 `ALLOWED_QUOTE_TYPE = "EQUITY"`（module-internal）

**STEP マーカー / CLI パス:** なし（このプランは純関数のみ。CLI と STEP マーカーは Plan 02）
</artifacts>

<output>
Create `.planning/phases/27-etf-exclusion/27-01-SUMMARY.md` when done
</output>
