---
phase: 29-daily-tracking-data-supply
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - src/scripts/collect-watchlist-data.ts
  - src/scripts/collect-watchlist-data.test.ts
autonomous: true
requirements: [TRAC-01, TRAC-02, TRAC-03, OPS-06]
tags: [watchlist, technicals, news-matching, fail-soft, step-marker, cli]

must_haves:
  truths:
    - "アクティブなウォッチリスト各銘柄について、当日株価・テクニカル指標が tmp/watchlist-technicals.json に供給される（同日キャッシュ再利用 + 欠落分のみチャンク取得, TRAC-01）"
    - "アクティブ各銘柄の関連ニュースが tmp/news.json から buildHoldingNewsMap 無改変流用で抽出され tmp/watchlist-news.json に供給される（全銘柄キー保証, TRAC-02）"
    - "1銘柄の取得失敗（レート制限含む）が他銘柄処理やパイプライン全体を止めない（銘柄単位 fail-soft skip, TRAC-03）"
    - "スクリプト自身が [STEP:watchlist-data:OK] / [STEP:watchlist-data:FAIL:<reason>] を stderr に出力し、[PIPELINE:FAIL] は絶対に出さない（OPS-06/D-03）"
    - "空ウォッチリスト・watchlist.json 破損・news.json 欠損のいずれの異常系でも両出力ファイルが常に有効JSONになる（D-04/D-19/D-20）"
  artifacts:
    - "src/scripts/collect-watchlist-data.ts — fail-soft CLI（fs I/O + チャンク取得 + STEP マーカー）"
    - "src/scripts/collect-watchlist-data.test.ts — CLI レベル fail-soft の単体テスト（vi.mock fs/promises + yahoo-finance2）"
    - "tmp/watchlist-technicals.json / tmp/watchlist-news.json（実行時生成の下流供給契約）"
  key_links:
    - "テクニカルブランチとニュースブランチが独立 try/catch で、片系の失敗が他系を道連れにしない（D-20/Pitfall 4）"
    - "buildHoldingNewsMap に toPortfolioHoldingShape(activeEntries) を渡すのみで holding-news.ts を無改変流用（D-13）"
    - "fetchChunked の atomic unit は fetchTechnicalSnapshot 単数形（fetchTechnicalSnapshots 複数形は不使用, Pitfall 1）"
---

<objective>
Phase 29 の唯一の CLI `src/scripts/collect-watchlist-data.ts` を fail-soft ラッパーとして実装する（D-02: 単一 CLI がテクニカルとニュース両方を収集し2ファイルを出力、1 CLI = 1 STEP マーカー）。Plan 01 の純関数（toPortfolioHoldingShape / mergeWithCache / fetchChunked）と既存の無改変流用モジュール（fetchTechnicalSnapshot 単数形 / buildHoldingNewsMap / getActiveWatchlistEntries）を組み合わせ、全 I/O・ネットワーク・STEP マーカー出力をこの1ファイルに集約する。

処理の流れ:
1. `data/watchlist.json` を read-only・防御的に読込（ENOENT-vs-corrupted 二段フェイル, D-19）→ getActiveWatchlistEntries でアクティブ銘柄導出
2. **テクニカルブランチ**（独立 try/catch）: 同日キャッシュ tmp/technicals.json 読込（fail-soft, D-11/D-12）→ mergeWithCache でヒット/欠落を分離 → 欠落分のみ fetchChunked(fetchTechnicalSnapshot) でチャンク取得（D-09/D-10）→ キャッシュヒット + 新規取得をマージ → tmp/watchlist-technicals.json 書込（D-05, `{generatedAt, snapshots}` 形状）
3. **ニュースブランチ**（独立 try/catch）: tmp/news.json 読込（fail-soft, D-20）→ toPortfolioHoldingShape(activeEntries) → buildHoldingNewsMap で銘柄別ニュースマップ生成（D-13/D-18）→ tmp/watchlist-news.json 書込（D-05/D-06, HoldingNewsFile 形状）
4. `[STEP:watchlist-data:OK]` を stderr 出力（異常系は `[STEP:watchlist-data:FAIL:<reason>]`）

Purpose: TRAC-01/02/03 + OPS-06 を1ステップで満たす。write-watchlist.ts（fail-soft CLI + STEP マーカー + ENOENT-vs-corrupted load）と collect-technicals.ts（catch-and-write-empty パターン）を合成する。

Output: `src/scripts/collect-watchlist-data.ts`（CLI）と `src/scripts/collect-watchlist-data.test.ts`（単体テスト）。
</objective>

<artifacts_this_phase_produces>
**このプラン（29-02）で新規作成:**
- 新ファイル: `src/scripts/collect-watchlist-data.ts`
- 新ファイル: `src/scripts/collect-watchlist-data.test.ts`
- 関数 `loadWatchlistDefensive()` — data/watchlist.json の ENOENT-vs-corrupted 二段フェイル読込（write-watchlist.ts の loadExistingWatchlist アナログ）
- 関数 `loadSameDayCache(path)` — tmp/technicals.json の fail-soft 読込（D-12、欠損/破損/形状不整合は空配列フォールバック）
- 関数 `writeEmptyOutputs()` — 両出力ファイルに有効な空JSONを書込（D-04/D-19）
- 関数 `main()` — fail-soft CLI エントリポイント
- STEP マーカー文字列 `[STEP:watchlist-data:OK]` / `[STEP:watchlist-data:FAIL:<reason>]`（D-03）
- 出力ファイル `tmp/watchlist-technicals.json`（`{generatedAt, snapshots: TechnicalSnapshot[]}`, D-05）
- 出力ファイル `tmp/watchlist-news.json`（`HoldingNewsFile`, D-05/D-06）

**既存流用（無改変・新規シンボルではない）:** fetchTechnicalSnapshot / TechnicalSnapshot（technicals.ts）、buildHoldingNewsMap / HoldingNewsFile / normalizeHoldingSymbol（holding-news.ts）、getActiveWatchlistEntries / WatchlistFile（watchlist.ts）、toPortfolioHoldingShape / mergeWithCache / fetchChunked（29-01 で作成）、NewsArticleWithId（article-id.ts）。
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-daily-tracking-data-supply/29-CONTEXT.md
@.planning/phases/29-daily-tracking-data-supply/29-RESEARCH.md
@.planning/phases/29-daily-tracking-data-supply/29-PATTERNS.md
@src/portfolio/watchlist-data.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fail-soft CLI の骨格・防御的読込・両出力書込・STEP マーカーを実装する</name>
  <files>src/scripts/collect-watchlist-data.ts, src/scripts/collect-watchlist-data.test.ts</files>
  <read_first>
    - src/scripts/write-watchlist.ts（正準アナログ: loadExistingWatchlist の ENOENT-vs-corrupted 二段フェイル、main() の STEP マーカー early-return 分岐、fileURLToPath 直接実行ガード。TMP_DIR/DATA_DIR の join(import.meta.dirname, ...) 規約）
    - src/scripts/collect-technicals.ts（catch-and-write-empty パターン: fatal 時も有効な空 `{generatedAt, snapshots:[]}` を書く。⚠取得失敗ログ様式）
    - src/portfolio/watchlist.ts（getActiveWatchlistEntries / WatchlistFile の消費シグネチャ）
    - src/portfolio/watchlist-data.ts（29-01 成果物: toPortfolioHoldingShape / mergeWithCache / fetchChunked / TechnicalsCacheFile。本 CLI が消費する）
    - src/data/technicals.ts（fetchTechnicalSnapshot 単数形 — import して fetchChunked に注入。TechnicalSnapshot 形状）
    - src/scripts/write-watchlist.test.ts（vi.hoisted モック規約: readFileMock/writeFileMock/mkdirMock、yahoo-finance2 default mock、beforeEach reset、console spy。プレーン Error("ENOENT") モック）
    - .planning/phases/29-daily-tracking-data-supply/29-RESEARCH.md（CLI skeleton コード例、Pitfall 4/5、writeEmptyOutputs 契約）
  </read_first>
  <behavior>
    - 空ウォッチリスト（アクティブ0件, D-04）: `[STEP:watchlist-data:OK]` を出し、両ファイルに有効な空JSON（technicals: `{generatedAt, snapshots:[]}`、news: `{}`）を書く。0件ログを出す。
    - watchlist.json ENOENT（初回実行, D-19 正常系）: 空ウォッチリストとして D-04 正常系パスに合流（FAIL マーカーは出さない）。
    - watchlist.json 破損（JSON パース失敗 or 非オブジェクト形状, D-19）: `[STEP:watchlist-data:FAIL:corrupted]` を出し、両ファイルに有効な空JSONを書いて return。watchlist.json 自体には一切書き込まない。
    - `[PIPELINE:FAIL]` はいかなる分岐でも一切出力しない（OPS-06/D-03）。
    - fatal catch（main 全体の catch）: 両出力に空JSONを書き、process.exitCode=1（exit code は invest.md 側が無視）。[PIPELINE:FAIL] は出さない。
  </behavior>
  <action>
`src/scripts/collect-watchlist-data.ts` を新規作成する。imports: `readFile, writeFile, mkdir` from `node:fs/promises`、`join` from `node:path`、`fileURLToPath` from `node:url`、`fetchTechnicalSnapshot, type TechnicalSnapshot` from `../data/technicals.js`、`getActiveWatchlistEntries, type WatchlistFile` from `../portfolio/watchlist.js`、`buildHoldingNewsMap, type HoldingNewsFile` from `../portfolio/holding-news.js`、`type NewsArticleWithId` from `../data/news/article-id.js`、`toPortfolioHoldingShape, mergeWithCache, fetchChunked, type TechnicalsCacheFile` from `../portfolio/watchlist-data.js`。**fetchTechnicalSnapshots 複数形は import しない**（Pitfall 1）。

パス定数を定義: `TMP_DIR = join(import.meta.dirname, "../../tmp")`、`DATA_DIR = join(import.meta.dirname, "../../data")`、`WATCHLIST_PATH = join(DATA_DIR, "watchlist.json")`、`TECHNICALS_CACHE_PATH = join(TMP_DIR, "technicals.json")`、`NEWS_POOL_PATH = join(TMP_DIR, "news.json")`、`OUT_TECHNICALS_PATH = join(TMP_DIR, "watchlist-technicals.json")`、`OUT_NEWS_PATH = join(TMP_DIR, "watchlist-news.json")`。

`loadWatchlistDefensive(): Promise<{ watchlist: WatchlistFile; corrupted: boolean }>` を write-watchlist.ts の loadExistingWatchlist から verbatim ベースで実装する（WATCHLIST_PATH を読む）。ENOENT 判定は `.code === "ENOENT"` と `.message.includes("ENOENT")` の両方（プレーン Error モック規約）。JSON valid でも非オブジェクト/null/配列は corrupted=true。ENOENT は `{ watchlist: {}, corrupted: false }`（D-19 正常系）。

`writeEmptyOutputs(): Promise<void>` を実装する（D-04/D-19）: OUT_TECHNICALS_PATH に `JSON.stringify({ generatedAt: new Date().toISOString(), snapshots: [] })`、OUT_NEWS_PATH に `JSON.stringify({})` を書く。

`main(): Promise<void>` を実装する: `await mkdir(TMP_DIR, { recursive: true })` を最初に呼ぶ（後続分岐に関わらず tmp/ を保証）。loadWatchlistDefensive を呼び、corrupted なら破損ログ + writeEmptyOutputs + `console.error("[STEP:watchlist-data:FAIL:corrupted]")` + return。`getActiveWatchlistEntries(watchlist)` でアクティブ銘柄を導出する。アクティブ0件なら 0件ログ + writeEmptyOutputs + `console.error("[STEP:watchlist-data:OK]")` + return（D-04 正常系）。それ以外は Task 2 で実装する2ブランチへ進む（本タスクではブランチ本体は TODO プレースホルダ、末尾で `console.error("[STEP:watchlist-data:OK]")` を出す骨格まで）。

直接実行ガード: `if (process.argv[1] === fileURLToPath(import.meta.url))` で `main().catch(async (error) => { console.error("Fatal error:", error); try { await writeEmptyOutputs(); } catch {} process.exitCode = 1; })`。**process.exit ではなく process.exitCode を使い、[PIPELINE:FAIL] は出さない**（D-03）。channel 規約: console.error=STEP ステータス、console.log=監査ログ（write-urgency-history.ts 踏襲）。日本語ログ規約。イミュータブル構築。

テストファイル `collect-watchlist-data.test.ts` を write-watchlist.test.ts の vi.hoisted モック規約で作成する。yahoo-finance2 モックは `chart` メソッド（quote ではない）。fs/promises モック（readFile/writeFile/mkdir）。本タスクのテストケース: (a) アクティブ0件 → 両ファイルに空JSON書込 + [STEP:watchlist-data:OK] + [PIPELINE:FAIL] 未出力、(b) watchlist.json ENOENT → 空扱い正常系（FAIL 未出力）、(c) watchlist.json 破損 → [STEP:watchlist-data:FAIL:corrupted] + 両ファイル空JSON + [PIPELINE:FAIL] 未出力。ENOENT シミュレートは `readFileMock.mockRejectedValue(new Error("ENOENT"))`。STEP/PIPELINE アサートは console.error spy の呼び出し引数を検査する（write-watchlist.test.ts の「全FAIL分岐で[PIPELINE:FAIL]が一度も出力されない」パターン踏襲）。

コミット: `feat(29-02): fail-soft CLI skeleton with defensive load and STEP markers`。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/collect-watchlist-data.test.ts -t "empty"</automated>
    <automated>npx vitest run src/scripts/collect-watchlist-data.test.ts -t "corrupted"</automated>
    <automated>npx vitest run src/scripts/collect-watchlist-data.test.ts -t "STEP"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/collect-watchlist-data.test.ts -t "empty"` が pass（アクティブ0件 → 両ファイル空JSON + OK マーカー）
    - `npx vitest run src/scripts/collect-watchlist-data.test.ts -t "corrupted"` が pass（破損 → FAIL:corrupted + 両ファイル空JSON）
    - source assertion: OK マーカー文字列が存在する — `grep -c '\[STEP:watchlist-data:OK\]' src/scripts/collect-watchlist-data.ts` が 1 以上
    - source assertion: FAIL マーカー文字列が存在する — `grep -c '\[STEP:watchlist-data:FAIL' src/scripts/collect-watchlist-data.ts` が 1 以上
    - source assertion: [PIPELINE:FAIL] を console 出力する行が存在しない — `grep -E 'console\.(error|log)\(' src/scripts/collect-watchlist-data.ts | grep -c 'PIPELINE:FAIL'` が 0（OPS-06/D-03。※コメント/ドキュメント文字列内での言及は許容し、実際の出力呼び出しのみを禁止する。この判定は console 呼び出し行だけを対象にするためコメント自己無効化を構造的に回避する）
    - source assertion: process.exit を直接呼ばず exitCode を使う — `grep -c 'process.exitCode' src/scripts/collect-watchlist-data.ts` が 1 以上
    - source assertion: fetchTechnicalSnapshots 複数形を import しない — `grep -c 'fetchTechnicalSnapshots' src/scripts/collect-watchlist-data.ts` が 0（Pitfall 1）
    - behavior assertion: 全 FAIL 分岐テストで [PIPELINE:FAIL] が console.error に一度も渡らない（テストで検証）
  </acceptance_criteria>
  <done>CLI 骨格・防御的 watchlist 読込・writeEmptyOutputs・STEP マーカー分岐が実装され、空ウォッチリスト・ENOENT・破損の3異常系テストが GREEN。[PIPELINE:FAIL] はどの分岐でも出力されない。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: テクニカルブランチとニュースブランチを独立 fail-soft で実装する</name>
  <files>src/scripts/collect-watchlist-data.ts, src/scripts/collect-watchlist-data.test.ts</files>
  <read_first>
    - src/scripts/collect-watchlist-data.ts（Task 1 で作成した骨格。2ブランチのプレースホルダを実装で置換）
    - src/portfolio/watchlist-data.ts（mergeWithCache / fetchChunked / toPortfolioHoldingShape / loadSameDayCache は使わず TechnicalsCacheFile 型を使う。fetchChunked に fetchTechnicalSnapshot を渡す）
    - src/data/technicals.ts（fetchTechnicalSnapshot 単数形 = fetchChunked の fetchOne 引数。既に per-ticker try/catch で null 返却）
    - src/portfolio/holding-news.ts（buildHoldingNewsMap のシグネチャ: `(articles: NewsArticleWithId[], holdings: PortfolioHolding[]) => HoldingNewsFile`。全銘柄キー保証 D-18。無改変流用 D-13）
    - src/scripts/collect-data.ts（tmp/news.json は NewsArticleWithId[] の JSON 配列であること、buildHoldingNewsMap 呼び出し例、失敗時 [] フォールバック）
    - src/scripts/collect-technicals.ts（⚠取得失敗ログ様式 `⚠ 取得失敗: <tickers>`、キャッシュ + 新規のマージで snapshots を作る際の omit 契約 D-17）
    - .planning/phases/29-daily-tracking-data-supply/29-RESEARCH.md（Pattern 3 loadSameDayCache コード例、Pitfall 4 の2ブランチ独立 try/catch、Pitfall 5 の tmp クリーンアップ scope）
  </read_first>
  <behavior>
    - テクニカルブランチ: アクティブ銘柄のうち同日キャッシュヒット分は再取得せずコピー、欠落分のみチャンク取得（D-11）。キャッシュ読込失敗（欠損/破損/形状不整合）は警告ログのみで全銘柄新規取得にフォールバック（D-12）。取得失敗銘柄は snapshots から omit（null 埋めしない, D-17）+ `⚠ 取得失敗: <tickers>` ログ（D-10）。tmp/watchlist-technicals.json に `{generatedAt, snapshots}` を書く（D-05）。
    - ニュースブランチ: tmp/news.json を NewsArticleWithId[] として読込。読込失敗は全銘柄空配列にフォールバック（D-20、テクニカルは巻き込まない）。toPortfolioHoldingShape(activeEntries) を buildHoldingNewsMap に渡す（D-13）。結果は全アクティブ銘柄のキーを持つ（D-18）。tmp/watchlist-news.json に HoldingNewsFile を書く（D-05/D-06、ID参照方式）。
    - 2ブランチ独立: news.json 欠損時もテクニカル収集は完走する（Pitfall 4 — 片系失敗が他系を道連れにしない, D-20）。
    - 1銘柄の fetch 失敗（reject/null）が他銘柄の snapshot を失わせない（TRAC-03 / Success Criteria 3 必須ケース）。
  </behavior>
  <action>
Task 1 の main() の2ブランチプレースホルダを実装する。

**テクニカルブランチ**（独立 try/catch でラップ）: `loadSameDayCache(TECHNICALS_CACHE_PATH)` を実装する（29-RESEARCH.md Pattern 3）— tmp/technicals.json を読み、`{ generatedAt, snapshots }` 形状（TechnicalsCacheFile）を検証。TechnicalSnapshot 形状（asOf を含む）はそのまま保持・出力し、削らない（D-07: 銘柄ごとの asOf と両ファイルの generatedAt を Phase 30 の TIME-05 基盤として保存する契約）。欠損（ENOENT）・パース失敗・snapshots が配列でない場合は空配列を返す（D-12 fail-soft、警告ログのみ）。アクティブ銘柄の ticker 配列を作り `mergeWithCache(activeTickers, cachedSnapshots)` でヒット/欠落を分離。キャッシュヒット snapshots（cachedTickers に含まれる cached 要素）はそのままコピー。`fetchChunked(missingTickers, fetchTechnicalSnapshot)` で欠落分のみチャンク取得。コピー + 新規取得を1配列にマージ（omit で欠落表現, D-17）。`failed = activeTickers.filter((t) => !merged.some((s) => s.symbol === t))` を計算し 0件でなければ `console.log(\`⚠ 取得失敗: ${failed.join(", ")}\`)`（D-10）。`writeFile(OUT_TECHNICALS_PATH, JSON.stringify({ generatedAt: new Date().toISOString(), snapshots: merged }, null, 2))`。ブランチ内 catch は警告ログ + OUT_TECHNICALS_PATH に空 `{generatedAt, snapshots:[]}` を書く（テクニカルのみ縮退、ニュースは巻き込まない）。

**ニュースブランチ**（独立 try/catch でラップ）: tmp/news.json を `readFile` → `JSON.parse` → `NewsArticleWithId[]`。読込/パース失敗時は `articles = []` にフォールバックし警告ログ（D-20、テクニカルブランチは既に完走済みで巻き込まない）。`const holdings = toPortfolioHoldingShape(activeEntries)`、`const newsMap = buildHoldingNewsMap(articles, holdings)`（無改変流用 D-13）。buildHoldingNewsMap は全 holding.symbol をキーに持つため D-18 を自動享受。1銘柄あたりの記事上限は holding-news.ts の既存 MAX_ARTICLES_PER_HOLDING（5件）をそのまま継承する（D-16: ウォッチリスト専用の別上限は設けない — buildHoldingNewsMap 無改変流用の帰結）。`writeFile(OUT_NEWS_PATH, JSON.stringify(newsMap, null, 2))`。

2ブランチは順次実行するが**それぞれ独立の try/catch で囲む**（Pitfall 4）— ニュースブランチの失敗がテクニカルブランチの出力に影響しないよう、テクニカルブランチを先に完了・書込してからニュースブランチを実行する。両ブランチ完了後に `console.error("[STEP:watchlist-data:OK]")`。Step 3.0/3-P の rm -rf 対象には**追加しない**（Pitfall 5: 両ファイルはルート直下 tmp/*.json で毎回 writeFile 上書き、明示 rm 不要）。

テストを追記する（vi.mock chart メソッド）: (a) 全銘柄キャッシュヒット → chart が呼ばれず全 snapshot がコピーされる、(b) 一部キャッシュヒット + 一部新規 → 欠落分のみ chart 呼び出し、(c) fail-soft: 1銘柄で chart が reject/null → その銘柄が omit され他銘柄 snapshot は保持（`-t "fail-soft"`、Success Criteria 3）、(d) news: news.json 読込成功 → 全アクティブ銘柄のキーが newsMap に存在（`-t "news"`）、(e) news.json 欠損 → newsMap は全銘柄空配列だが watchlist-technicals.json は正常に snapshot を持つ（2ブランチ独立, D-20）。キャッシュ破損 → 全銘柄新規取得フォールバック（D-12）。

コミット: `feat(29-02): technical and news branches with independent fail-soft`。
  </action>
  <verify>
    <automated>npx vitest run src/scripts/collect-watchlist-data.test.ts -t "fail-soft"</automated>
    <automated>npx vitest run src/scripts/collect-watchlist-data.test.ts -t "news"</automated>
    <automated>npx vitest run src/scripts/collect-watchlist-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/scripts/collect-watchlist-data.test.ts` が全ケース pass（Task 1 + Task 2）
    - `npx vitest run src/scripts/collect-watchlist-data.test.ts -t "fail-soft"` が pass（1銘柄 reject/null でも他銘柄 snapshot 保持, TRAC-03/Success Criteria 3）
    - `npx vitest run src/scripts/collect-watchlist-data.test.ts -t "news"` が pass（全アクティブ銘柄キーが newsMap に存在, D-18）
    - source assertion: buildHoldingNewsMap を toPortfolioHoldingShape 経由で呼ぶ — `grep -c 'buildHoldingNewsMap' src/scripts/collect-watchlist-data.ts` が 1 以上、`grep -c 'toPortfolioHoldingShape' src/scripts/collect-watchlist-data.ts` が 1 以上
    - source assertion: fetchChunked に fetchTechnicalSnapshot 単数形を渡す — `grep -c 'fetchTechnicalSnapshot\b' src/scripts/collect-watchlist-data.ts` が 1 以上、`grep -c 'fetchTechnicalSnapshots' src/scripts/collect-watchlist-data.ts` が 0
    - source assertion: 取得失敗ログ様式 — `grep -c '⚠ 取得失敗' src/scripts/collect-watchlist-data.ts` が 1 以上（D-10）
    - source assertion: 出力2ファイルへの writeFile — `grep -c 'watchlist-technicals.json' src/scripts/collect-watchlist-data.ts` が 1 以上、`grep -c 'watchlist-news.json' src/scripts/collect-watchlist-data.ts` が 1 以上
    - behavior assertion: news.json 欠損テストで watchlist-technicals.json が非空 snapshot を持つ（2ブランチ独立, D-20、テストで検証）
    - `npx tsc --noEmit` が型エラーゼロ
  </acceptance_criteria>
  <done>テクニカル・ニュース両ブランチが独立 fail-soft で実装され、同日キャッシュ再利用・チャンク取得・銘柄別ニュースマップ生成が動作する。1銘柄失敗が全体を止めないこと、片系失敗が他系を道連れにしないことがテストで確認できる。全単体テスト GREEN・tsc クリーン。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI ← data/watchlist.json / tmp/technicals.json / tmp/news.json | パイプライン内部の tmp/*.json・data/*.json ファイル境界（TS↔TS ハンドオフ、既に信頼済み境界）。手編集・前 run 破損の可能性はあるため防御的読込が必要 |
| CLI → Yahoo Finance (chart) | 外部 API（非公式スクレイパ、SLA なし、レート制限あり）。信頼できないレスポンス・429 が発生しうる |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-03 | Denial of Service | 破損 data/watchlist.json が CLI を crash させ [PIPELINE:FAIL] に波及 | high | mitigate | loadWatchlistDefensive の ENOENT-vs-corrupted 二段フェイル（write-watchlist.ts から verbatim）。破損は crash せず [STEP:watchlist-data:FAIL:corrupted] + 有効な空JSON出力に縮退。Task 1 の corrupted テストで検証 |
| T-29-04 | Denial of Service | Yahoo Finance chart() の未処理例外・429 がラン全体を停止 | high | mitigate | fetchTechnicalSnapshot 既存 per-ticker try/catch（null 返却, 無改変 D-08）+ fetchChunked のチャンク分割/待機（D-09）+ 同日キャッシュ再利用（D-11 呼び出し量削減）。Task 2 fail-soft テストで1銘柄失敗が全体を止めないことを検証（TRAC-03） |
| T-29-05 | Denial of Service | ニュースプール読込失敗がテクニカル収集を道連れ | medium | mitigate | 2ブランチ独立 try/catch（Pitfall 4/D-20）。news.json 欠損時も watchlist-technicals.json は正常出力。Task 2 の news 欠損テストで検証 |
| T-29-06 | Tampering | tmp/news.json 内の幻覚/不明記事ID | low | accept | 本フェーズは ID 参照を**書く**のみで**解決しない**（D-06）。不明ID drop ガード（resolvePortfolioHoldingNews の console.warn）は Phase 30 の解決時点で発火する下流エンフォースメント。本フェーズに新規脅威面なし |
| V5 Input Validation | Spoofing/Tampering | data/watchlist.json の JSON 形状 | high | mitigate | 非オブジェクト/null/配列を corrupted 扱い（write-watchlist.ts loadExistingWatchlist と同一）。外部ユーザー入力はこの CLI に到達しない（全入力はパイプライン内部ファイル or Yahoo Finance レスポンス） |
| T-29-SC | Tampering | npm/pip/cargo installs | high | accept | 新規パッケージのインストールなし（29-RESEARCH.md Package Legitimacy Audit: not applicable — 既存 yahoo-finance2 + Node built-ins のみ。[ASSUMED]/[SUS] パッケージゼロ、Package Legitimacy Gate 非該当） |
</threat_model>

<verification>
- `npx vitest run src/scripts/collect-watchlist-data.test.ts` が全ケース GREEN
- `npx vitest run`（全スイート）で既存テストへの回帰ゼロ（technicals.ts / holding-news.ts / watchlist.ts は無改変のため既存テストも green のまま）
- `npx tsc --noEmit` が型エラーゼロ
- source grep: [PIPELINE:FAIL] 未出力、fetchTechnicalSnapshots 複数形不使用、buildHoldingNewsMap 無改変流用（holding-news.ts に diff なし）、出力2ファイル書込
- CLI 実行スモーク（任意, ネットワーク到達時）: `npx tsx src/scripts/collect-watchlist-data.ts` 実行後 tmp/watchlist-technicals.json / tmp/watchlist-news.json が有効JSONで生成される
</verification>

<success_criteria>
- TRAC-01: アクティブ銘柄の株価・テクニカルが tmp/watchlist-technicals.json に供給される（同日キャッシュ + チャンク取得）
- TRAC-02: アクティブ銘柄の関連ニュースが tmp/watchlist-news.json に供給される（buildHoldingNewsMap 無改変流用、全銘柄キー保証）
- TRAC-03: 1銘柄の取得失敗が他銘柄・全体を止めないことがテストで確認できる
- OPS-06: [STEP:watchlist-data:OK]/[STEP:watchlist-data:FAIL:<reason>] を自己出力、[PIPELINE:FAIL] は絶対に出さない
- D-04/D-19/D-20 の全異常系で両出力が有効JSON
- holding-news.ts / technicals.ts / watchlist.ts への diff がゼロ（無改変流用）
</success_criteria>

<output>
Create `.planning/phases/29-daily-tracking-data-supply/29-02-SUMMARY.md` when done
</output>
