---
phase: 29-daily-tracking-data-supply
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/portfolio/watchlist-data.ts
  - src/portfolio/watchlist-data.test.ts
autonomous: true
requirements: [TRAC-01, TRAC-02, TRAC-03]
tags: [watchlist, technicals, news-matching, chunking, cache]

must_haves:
  truths:
    - "WatchlistEntry[] を PortfolioHolding[] 形状に決定論的にマップでき、buildHoldingNewsMap へ無改変で渡せる（D-13/D-14）"
    - "アクティブ銘柄のうち同日キャッシュ（tmp/technicals.json）に存在する銘柄は再取得対象から外れ、欠落銘柄のみが取得対象になる（D-11）"
    - "取得対象ティッカーが固定サイズのチャンクに分割され、チャンク単位で並列取得しチャンク間に待機を挟む（D-09）"
    - "1銘柄の取得が null/reject を返しても他銘柄のスナップショットが失われない（TRAC-03 の純関数レベル保証, D-10）"
  artifacts:
    - "src/portfolio/watchlist-data.ts — toPortfolioHoldingShape / mergeWithCache / chunk / fetchChunked + named constants"
    - "src/portfolio/watchlist-data.test.ts — 上記純関数の単体テスト（RED→GREEN）"
  key_links:
    - "toPortfolioHoldingShape の出力 symbol/name/nameJa/sector が buildHoldingNewsMap の PortfolioHolding 消費契約と一致する"
    - "fetchChunked の atomic unit は fetchTechnicalSnapshot（単数）で、fetchTechnicalSnapshots（複数）には一切依存しない（Pitfall 1）"
---

<objective>
Phase 29 の全ロジックの純関数基盤を1新モジュール `src/portfolio/watchlist-data.ts` に構築する。ネットワーク I/O・fs I/O・STEP マーカーは一切持たず（それらは Plan 02 の CLI が担当）、以下の4純関数を TDD で実装する:

1. `toPortfolioHoldingShape`（D-14, TRAC-02）— `WatchlistEntry[]` → `PortfolioHolding[]` 形状マップ。`buildHoldingNewsMap` を無改変流用するための唯一のアダプタ。
2. `mergeWithCache`（D-11/D-12, TRAC-01）— アクティブ ticker と同日キャッシュ snapshots を突き合わせ、キャッシュヒット集合と新規取得が必要な欠落 ticker を返す純関数。
3. `chunk` + `fetchChunked`（D-09, TRAC-01/TRAC-03）— 固定サイズチャンク分割 + チャンク単位並列取得 + チャンク間待機。atomic unit は呼び出し側から注入される `fetchOne`（Plan 02 で `fetchTechnicalSnapshot` を渡す）。

Purpose: 「難しい部分」（指標計算・ニュースマッチ・正規化）は既存モジュールで解決済みのため、本フェーズの唯一の新規ロジックであるチャンク化・キャッシュマージ・形状マップだけを、独立して単体テスト可能な純関数として切り出す（D-21: 純関数部分を単体テストする方針）。matchAliases の人手キュレーションは行わず ticker 完全一致 + 社名一致のみで開始する（D-15: toPortfolioHoldingShape は matchAliases を省略）。urgency-history.ts / watchlist.ts の「純モジュール + fail-soft CLI」分割を踏襲する。

Output: `src/portfolio/watchlist-data.ts`（純モジュール）と `src/portfolio/watchlist-data.test.ts`（単体テスト）。
</objective>

<artifacts_this_phase_produces>
本フェーズ（Phase 29）が新規に作成するシンボル。plan-review-convergence の source-grounding パスはこのリストを新規シンボルとして扱い、ドリフト検証から除外する:

**このプラン（29-01）で新規作成:**
- 新ファイル: `src/portfolio/watchlist-data.ts`
- 新ファイル: `src/portfolio/watchlist-data.test.ts`
- 関数 `toPortfolioHoldingShape(entries: ReadonlyArray<WatchlistEntry>): ReadonlyArray<PortfolioHolding>`
- 関数 `mergeWithCache(activeTickers: ReadonlyArray<string>, cached: ReadonlyArray<TechnicalSnapshot>): { cachedTickers: ReadonlySet<string>; missingTickers: ReadonlyArray<string> }`
- 関数 `chunk<T>(items: ReadonlyArray<T>, size: number): ReadonlyArray<ReadonlyArray<T>>`
- 関数 `fetchChunked(tickers, fetchOne, options?): Promise<ReadonlyArray<TechnicalSnapshot>>`
- 名前付き定数 `CHUNK_SIZE`（数値, D-09 目安 4〜5）
- 名前付き定数 `CHUNK_DELAY_MS`（数値, D-09 目安 200〜500ms）
- 型 `TechnicalsCacheFile`（`{ generatedAt: string; snapshots: ReadonlyArray<TechnicalSnapshot> }`, tmp/technicals.json 形状）

**後続プラン（29-02）で新規作成予定（参考）:** `src/scripts/collect-watchlist-data.ts`、`loadSameDayCache`、`writeEmptyOutputs`、`main`、`[STEP:watchlist-data:*]` マーカー、`tmp/watchlist-technicals.json` / `tmp/watchlist-news.json`。
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-daily-tracking-data-supply/29-CONTEXT.md
@.planning/phases/29-daily-tracking-data-supply/29-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: toPortfolioHoldingShape と mergeWithCache を TDD で実装する</name>
  <files>src/portfolio/watchlist-data.ts, src/portfolio/watchlist-data.test.ts</files>
  <read_first>
    - src/portfolio/watchlist-data.ts（未作成なら新規、既存なら現状を確認）
    - src/portfolio/watchlist.ts（WatchlistEntry / WatchlistFile / getActiveWatchlistEntries の確定シグネチャ。本モジュールが消費する Phase 28 成果物、変更しない）
    - src/portfolio/holdings.ts（PortfolioHolding 型 = 目標形状。symbol/name/nameJa/sector/matchAliases?）
    - src/portfolio/holding-news.ts（normalizeHoldingSymbol の単一情報源。matchAliases undefined を resolveNameMatchType が graceful に処理することを確認）
    - src/data/technicals.ts（TechnicalSnapshot 型 — symbol/asOf/... の形状。mergeWithCache のキャッシュ要素型）
    - src/portfolio/urgency-history.ts（純モジュールの構造アナログ: 型 + 純関数、I/O なし、doc コメントで `// D-NN:` を引用する規約）
    - src/scripts/write-watchlist.test.ts（vitest テスト規約: describe/it/expect、プレーン Error モック規約。ただし本タスクの純関数テストは fs/yahoo モック不要）
  </read_first>
  <behavior>
    - toPortfolioHoldingShape: WatchlistEntry[] を入力に、各エントリを { symbol: entry.ticker, name: entry.name ?? entry.ticker, nameJa: entry.nameJa ?? "", sector: "" } にマップした ReadonlyArray<PortfolioHolding> を返す（D-14）。
      - Test: name を持つエントリは name をそのまま採用する
      - Test: name が undefined のエントリは ticker を name にフォールバックする
      - Test: nameJa が undefined のエントリは nameJa="" になる
      - Test: sector は常に "" 固定（D-14: マッチングロジック未使用）
      - Test: matchAliases はマップ結果に含めない（undefined のまま。resolveNameMatchType が graceful 処理）
      - Test: 空配列入力は空配列を返す（throw しない）
      - Test: 入力 entries を mutate しない
    - mergeWithCache: activeTickers（正規化済み）と cached snapshots を入力に、{ cachedTickers: ReadonlySet<string>, missingTickers: ReadonlyArray<string> } を返す（D-11）。
      - Test: キャッシュに存在する ticker は missingTickers に含まれない
      - Test: キャッシュに存在しない ticker のみ missingTickers に含まれる
      - Test: 全 ticker がキャッシュヒットなら missingTickers は空配列
      - Test: キャッシュが空配列なら全 ticker が missingTickers になる
      - Test: cachedTickers は cached snapshots の symbol 集合と一致する
  </behavior>
  <action>
`src/portfolio/watchlist-data.ts` に純モジュールとして実装する。imports は `import { normalizeHoldingSymbol } from "./holding-news.js"`（キャッシュ側 symbol 比較用。将来の外部入力正規化に備える）、`import type { WatchlistEntry } from "./watchlist.js"`、`import type { PortfolioHolding } from "./holdings.js"`、`import type { TechnicalSnapshot } from "../data/technicals.js"`。

`toPortfolioHoldingShape(entries: ReadonlyArray<WatchlistEntry>): ReadonlyArray<PortfolioHolding>` を実装する。`entries.map` で `{ symbol: entry.ticker, name: entry.name ?? entry.ticker, nameJa: entry.nameJa ?? "", sector: "" }` を返す。`entry.ticker` は Phase 28 が normalizeHoldingSymbol 済みで保存している前提のため再正規化しない（D-14 コメントで明記）。`matchAliases` はキーごと省略する（undefined, D-15: 人手キュレーション不採用）。doc コメントは `// D-14:` を引用する。

`mergeWithCache(activeTickers: ReadonlyArray<string>, cached: ReadonlyArray<TechnicalSnapshot>): { readonly cachedTickers: ReadonlySet<string>; readonly missingTickers: ReadonlyArray<string> }` を実装する。`cachedTickers = new Set(cached.map((s) => s.symbol))`、`missingTickers = activeTickers.filter((t) => !cachedTickers.has(t))`。doc コメントは `// D-11:` を引用する。両関数とも throw-free・I/O-free・入力を mutate しない。イミュータブル構築（spread/map/filter のみ、破壊的操作禁止）。日本語コメント規約に従う。

TDD 手順: 先に `watchlist-data.test.ts` に上記 behavior の各 Test を書き（RED: モジュール未実装で失敗）、次に最小実装で GREEN にする。テストは fs/yahoo-finance2 のモック不要（純関数のため）。RED コミット `test(29-01): add failing tests for shape map and cache merge`、GREEN コミット `feat(29-01): toPortfolioHoldingShape and mergeWithCache pure functions`。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/watchlist-data.test.ts -t "toPortfolioHoldingShape"</automated>
    <automated>npx vitest run src/portfolio/watchlist-data.test.ts -t "mergeWithCache"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/portfolio/watchlist-data.test.ts -t "toPortfolioHoldingShape"` が全て pass する
    - `npx vitest run src/portfolio/watchlist-data.test.ts -t "mergeWithCache"` が全て pass する
    - `grep -c 'export function toPortfolioHoldingShape' src/portfolio/watchlist-data.ts` が 1 を返す
    - `grep -c 'export function mergeWithCache' src/portfolio/watchlist-data.ts` が 1 を返す
    - source assertion: watchlist-data.ts の toPortfolioHoldingShape が `sector: ""` を含む（D-14 固定値）— `grep -c 'sector: ""' src/portfolio/watchlist-data.ts` が 1 以上
    - source assertion: watchlist-data.ts は `fetchTechnicalSnapshots`（複数形）を import していない — `grep -v '^//' src/portfolio/watchlist-data.ts | grep -c 'fetchTechnicalSnapshots'` が 0
    - behavior assertion: name 未指定エントリのマップ結果 name が ticker と等しい（テストで検証）
  </acceptance_criteria>
  <done>toPortfolioHoldingShape と mergeWithCache が実装され、両者の単体テストが GREEN。純関数のため fs/yahoo モック不要。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: chunk と fetchChunked を TDD で実装する（並列度制御 + per-ticker fail-soft 分離）</name>
  <files>src/portfolio/watchlist-data.ts, src/portfolio/watchlist-data.test.ts</files>
  <read_first>
    - src/portfolio/watchlist-data.ts（Task 1 で作成済み。同ファイルに追記）
    - src/data/technicals.ts（fetchTechnicalSnapshot 単数形 = fetchChunked の atomic unit として注入される関数の型 `(symbol: string) => Promise<TechnicalSnapshot | null>`。既に export 済み・変更しない, D-08）
    - src/scripts/collect-technicals.ts（fetchTechnicalSnapshots 複数形が Promise.all 無制限並列であることを確認 — これを流用してはならない Pitfall 1 の根拠）
    - .planning/phases/29-daily-tracking-data-supply/29-RESEARCH.md（Pattern 2: chunk/fetchChunked の完全なコード例と CHUNK_SIZE/CHUNK_DELAY_MS 定数規約。Pitfall 1: fetchTechnicalSnapshots 複数形への依存禁止）
  </read_first>
  <behavior>
    - chunk<T>(items, size): 配列を固定サイズのチャンク配列に分割する純関数。
      - Test: 12要素 size=5 → 3チャンク（5, 5, 2）
      - Test: 空配列 → 空チャンク配列
      - Test: size 以下の要素数 → 単一チャンク
      - Test: 入力配列を mutate しない
    - fetchChunked(tickers, fetchOne): tickers をチャンク分割し、チャンク単位で Promise.all 並列取得、チャンク間に待機、null 結果を除外した TechnicalSnapshot[] を返す。
      - Test: fetchOne が全 ticker で snapshot を返す → 全 snapshot が結果に含まれる
      - Test: 一部 ticker で fetchOne が null を返す → その ticker は omit され、他の snapshot は保持される（TRAC-03/D-10/D-17: null 埋めしない）
      - Test: 一部 ticker で fetchOne が reject する場合でも他 ticker の snapshot が失われない（fetchOne 自身が per-ticker try/catch を持つ前提だが、fetchChunked がそれを壊さないことをテストで確認 — Success Criteria 3 の必須ケース）
      - Test: 空 tickers → 空配列（fetchOne を呼ばない）
      - Test: 待機は fake timers（vi.useFakeTimers）で検証可能、または delay=0 を注入して待機ロジックの存在のみ確認
  </behavior>
  <action>
`src/portfolio/watchlist-data.ts` に追記する。名前付き定数を1箇所で定義する: `CHUNK_SIZE`（値 5、D-09 目安 4〜5 の範囲内）と `CHUNK_DELAY_MS`（値 300、D-09 目安 200〜500ms の範囲内）。マジックナンバー分散禁止（Phase 28 D-09 準拠）— これらの定数はファイル内でリテラル重複させない。

`chunk<T>(items: ReadonlyArray<T>, size: number): ReadonlyArray<ReadonlyArray<T>>` を実装する（29-RESEARCH.md Pattern 2 のコード例に従う。for ループで `items.slice(i, i + size)` を push）。

`fetchChunked(tickers: ReadonlyArray<string>, fetchOne: (symbol: string) => Promise<TechnicalSnapshot | null>, delayMs = CHUNK_DELAY_MS): Promise<ReadonlyArray<TechnicalSnapshot>>` を実装する。テスト容易性のため delayMs を注入可能な引数にする（デフォルト CHUNK_DELAY_MS）。各チャンクを `await Promise.all(batch.map(fetchOne))` で取得し、`.filter((r): r is TechnicalSnapshot => r !== null)` で null を除外して results に push する。最終チャンク以外の後に `await sleep(delayMs)` を挟む（内部 `sleep(ms)` ヘルパー = `new Promise((resolve) => setTimeout(resolve, ms))`）。atomic unit は必ず引数 `fetchOne`（Plan 02 で fetchTechnicalSnapshot 単数形を渡す）— fetchTechnicalSnapshots 複数形は import も呼び出しもしない（Pitfall 1）。doc コメントは `// D-09:` を引用する。

fail-soft の意味論を明確化するコメント: fetchOne が null を返した ticker は結果配列から omit する（D-17: null 埋め・プレースホルダ禁止、既存 technicals.json 契約と同一）。

TDD 手順: 先に test を追記（RED）、最小実装で GREEN。reject 分離テストは fetchOne モックの一部を rejected Promise にして、他 ticker の snapshot が返ることを検証する。fake timers または delayMs=0 注入で待機ロジックを扱う。RED コミット `test(29-01): add failing tests for chunk and fetchChunked fail-soft isolation`、GREEN コミット `feat(29-01): chunk and fetchChunked bounded-concurrency helpers`。
  </action>
  <verify>
    <automated>npx vitest run src/portfolio/watchlist-data.test.ts -t "chunk"</automated>
    <automated>npx vitest run src/portfolio/watchlist-data.test.ts -t "fetchChunked"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/portfolio/watchlist-data.test.ts` が全て pass する（Task 1 + Task 2 の全ケース）
    - `grep -c 'export function chunk' src/portfolio/watchlist-data.ts` が 1 を返す
    - `grep -c 'fetchChunked' src/portfolio/watchlist-data.ts` が 1 以上（定義 + export）
    - source assertion: CHUNK_SIZE と CHUNK_DELAY_MS が名前付き定数として1箇所定義されている — `grep -c 'const CHUNK_SIZE' src/portfolio/watchlist-data.ts` が 1、`grep -c 'const CHUNK_DELAY_MS' src/portfolio/watchlist-data.ts` が 1
    - source assertion: fetchTechnicalSnapshots（複数形）への依存がない — `grep -v '^//' src/portfolio/watchlist-data.ts | grep -c 'fetchTechnicalSnapshots'` が 0（Pitfall 1）
    - behavior assertion: 一部 fetchOne が null/reject でも他 ticker の snapshot が結果に含まれる（TRAC-03 の純関数レベル保証、テストで検証）
  </acceptance_criteria>
  <done>chunk と fetchChunked が実装され、1銘柄の取得失敗（null/reject）が他銘柄の snapshot を失わせないことがテストで確認できる。CHUNK_SIZE/CHUNK_DELAY_MS が名前付き定数として単一定義されている。fetchTechnicalSnapshots 複数形への依存ゼロ。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| なし（このプランは純関数のみ） | 本モジュールは I/O・ネットワーク・外部入力を一切扱わない。信頼境界の越境は Plan 02 の CLI（fs 読込・Yahoo Finance 呼び出し）で発生する |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Denial of Service | fetchChunked の per-ticker 分離が壊れ1銘柄失敗が全体を巻き込む | medium | mitigate | fetchChunked は fetchOne の null/reject を per-chunk で吸収し他 ticker の snapshot を保持する。Task 2 の「一部 null/reject でも他 ticker snapshot が失われない」テストで構造的に検証（TRAC-03） |
| T-29-02 | Tampering | 未正規化 ticker キーによるキャッシュ照合ミスマッチ | low | accept | WatchlistEntry.ticker は Phase 28 が normalizeHoldingSymbol 済み。mergeWithCache は正規化済みキー同士を比較する前提。外部（キャッシュファイル）由来 symbol の正規化は Plan 02 CLI 側の責務（本純関数は与えられた値をそのまま比較） |
| T-29-SC | Tampering | npm/pip/cargo installs | high | accept | 新規パッケージのインストールなし（29-RESEARCH.md Package Legitimacy Audit: not applicable — 既存 yahoo-finance2 + Node built-ins のみ、チャンク化はハンドロール。[ASSUMED]/[SUS] パッケージゼロ） |
</threat_model>

<verification>
- `npx vitest run src/portfolio/watchlist-data.test.ts` が全ケース GREEN
- `npx tsc --noEmit`（型エラーゼロ — TechnicalSnapshot / WatchlistEntry / PortfolioHolding の型整合）
- source grep: fetchTechnicalSnapshots 複数形への依存ゼロ、CHUNK_SIZE/CHUNK_DELAY_MS 単一定義、sector:"" 固定
</verification>

<success_criteria>
- 4純関数（toPortfolioHoldingShape / mergeWithCache / chunk / fetchChunked）が実装され export されている
- 全単体テストが GREEN（RED→GREEN のコミット履歴あり）
- TRAC-03 の「1銘柄失敗が他銘柄を止めない」が純関数レベルでテスト確認済み
- fetchTechnicalSnapshots 複数形への依存ゼロ（Pitfall 1 回避）
- 名前付き定数によるマジックナンバー集約（D-09）
</success_criteria>

<output>
Create `.planning/phases/29-daily-tracking-data-supply/29-01-SUMMARY.md` when done
</output>
